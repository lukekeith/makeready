# Progress Since Last TestFlight Archive — 2026-06-01

**Last archive:** MakeReady **v1.1.2 (build 277)** — May 14, 2026
**Next build:** **v1.1.3** (build ≈ 29, auto-set from git commit count)
**Window:** May 14 → Jun 1, 2026 · **29 commits**

> ⚠️ The iPhone app was merged into the monorepo on **May 20** (commit `fc69ddb`).
> Any changes made in the standalone iPhone repo between the May 14 archive and
> May 20 are folded into that migration commit and aren't itemized individually
> below.

---

## 📱 iPhone app — user-facing (QA should test)

### Lesson authoring / activity editing (leaders & admins)
- Removed the **help icon** and the **"always-visible help"** toggle from activity editing. (`beb32d0`)
- Replaced the activity **help modal** with **inline, collapsible context** on user-input activities. (`3cd57b2`)
- Added a **custom placeholder** field for user-input activities. (`fded371`)
- **Read-activity preview** updates in the day editor. (`5d5f7bf`)
- **QA:** Edit a day's activities (esp. user-input) — verify help context shows inline & collapses, custom placeholder appears in the input, no leftover help icon. Preview a read activity.

### Profile
- **Profile page** updates. (`baec5a4`, `fded371`)
- **QA:** View/edit profile fields and save.

### Login / auth
- Fixed **production login** by forwarding `X-Forwarded-Proto` to the API. (`bfaa612`)
- **QA:** Sign in on a Release/TestFlight build (production server) and confirm OAuth completes end-to-end.

### Home / calendar / groups
- **Main home** and **calendar** view tweaks. (`fded371`)
- **Group invite** and **enrollment schedule** page updates. (`fded371`)
- **QA:** Home + calendar render correctly; create/share a group invite; set an enrollment schedule.

---

## 🔌 Backend / API — affects in-app behavior
- **A2P SMS campaign system** — template rendering, rate limiting, audit logging. (`d77e969`) → member SMS invites/notifications.
- **Persistent read completion** + activity state updates. (`3340075`) → lesson read steps stay marked complete after navigating away.
- **Interactive preview system** + auto-save input. (`5d5f7bf`)
- **QA:** Trigger an SMS invite/notification; complete a read step and confirm it stays complete; auto-save behavior on user-input activities.

---

## 🛠️ Infrastructure / dev only (no QA impact)
- Monorepo consolidation (client/server/iphone/capture) + GitHub Actions CI. (`fc69ddb`, `306262a`)
- CI hardening: single combined workflow, flaky-test fix (server tests now run serially), Railway promote workflow, production login proxy fix.
- Local dev environment + local server discovery (dev builds only).
- **Web (Laravel/Vue) study-home redesign** — interactive parallax study home, lesson navigation, calendar, completion states. **This is the web member experience only; it is NOT in the iPhone app.** (`a714af4`, `4f53738`)

---

## 🔖 Version
- Bumped marketing version **1.1.2 → 1.1.3**. (`fdefa05`)
- Build number is auto-set at build time from the git commit count (`git rev-list HEAD --count`) → next archive ≈ build **29**. Because 1.1.3 is a new marketing version, this is accepted by TestFlight even though it's below the old build 277.
