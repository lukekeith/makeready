# iPhone Progress — 2026-07-28

**Window:** since last TestFlight archive **1.1.3 (320)**, archived 2026-07-23 21:42 → now
**Commits:** 24 (2026-07-25 → 2026-07-28)
**Next build:** 1.1.3 (**344**) — build number auto-set from `git rev-list HEAD --count`
**Uncommitted:** only the auto build-number bump in Info.plist (no QA impact)

---

## 📱 iPhone app — user-facing

### Study editors (Program → lesson activities)

- **Exegesis: overlapping highlights now merge** — selecting text that overlaps an existing highlight absorbs it into one continuous highlight (union of both ranges); notes from absorbed highlights are concatenated. Previously the save silently failed with "Couldn't save the highlight."
  **QA:** In an Exegesis activity, highlight a passage, then make a second highlight that partially overlaps it → the two become one highlight spanning both; reopen the editor and confirm it persisted. If both had notes, the merged highlight carries both.
- **READ editor: Preview includes unsaved text** — tapping Preview flushes the in-progress edits first (monday#12628511666).
  **QA:** Type into a READ block, immediately tap Preview → the new text appears.
- **Editor formatting toolbar pinned above keyboard** (monday#12297336134).
  **QA:** In the markdown/READ editor, focus a text field → the heading/formatting toolbar sits directly above the keyboard, not hidden behind it.
- **Exegesis preview survives backgrounding** — the lesson player re-measures on background→resume (monday#12628642531).
  **QA:** Open an exegesis preview, background the app, return → layout intact, no blank/broken preview.

### Enrollment day editor (Group → enrollment schedule)

- **YouTube + Exegesis rows open working editors** (monday#12628603097).
  **QA:** In an enrollment day, tap a YouTube activity and an Exegesis activity → real editors open (previously broken).
- **Added/reordered activities appear immediately** (monday#12628423737).
  **QA:** Add an activity or drag-reorder within a day → the list updates without leaving/reopening the screen.
- **"+" add-lesson schedules the study's next unscheduled lesson** (monday#12628394490).
  **QA:** Tap "+" on an enrollment → the next unscheduled lesson from the study is added (not a duplicate/empty one).

### Form components

- **Tag field keeps keyboard focus after committing a tag** (monday#12629830200).
  **QA:** In program settings tags, type a tag and hit return → keyboard stays up, ready for the next tag.
- **Cover-image well hint reads "Tap to select cover image"** (monday#12629957974).
  **QA:** New program/group with no cover → hint text matches, and tapping opens the photo picker.

### Login / environments (dev builds only)

- **Login falls back to Production when the Local server is unreachable** — the env switch is persisted and visible in the user-menu switcher; an explicit Profile-page switch **to** Local still surfaces the unreachable error instead of silently bouncing.
  **QA (dev build only):** With Local selected and the dev server off, tap sign-in → you land on Production via Google OAuth instead of a dead-end error.
- Default dev LAN IP updated to 192.168.68.84 (network change; migrates automatically on launch).

## 🔌 Backend / API — affects in-app behavior

- **Program deletion is creator-only** (monday#12622983805) — org leaders can no longer delete studies they didn't create; their delete now reads as not-found and the study stays. Editing others' org studies is unchanged.
  **QA:** As a leader who didn't create a study, attempt delete → refused; the creator can still delete (soft-delete).
- **Exegesis-highlight create endpoint merges overlaps** (both program and scheduled/enrollment variants) — powers the iPhone merge behavior above; returns the merged highlight + absorbed ids.
- Enrollment editor provider compile fixes (@MainActor closures, preview URL builder) — supports the enrollment editor fixes above.

## 🛠️ Infrastructure / dev only (no QA impact)

- **Web-only (NOT in the iPhone app):** leader-app parity work — member-profile modal, member-requests page, managed-page overlay chrome, leader-member store, groups-view wiring; exegesis pane now allows overlap-merge like iOS.
- Capture/compare fixtures + screenshots (member-profile, member-requests, editors, group-home, study-programs), parity manifest updates, capture command docs.
- CI test alignment for the creator-only delete carve-out; force-unwrap lint fixes; monday dossier docs.

## 🔖 Version

- Last archive: **1.1.3 (320)** — 2026-07-23
- Next archive: **1.1.3 (344)** (auto from commit count; bump marketing version with `/version-increment` if desired)
