# 05 — Client

**Not affected** — the web app (`/client`) is untouched. The component browser shows iPhone
renders only (01 §Out of scope); no Blade view, Vue island, store, proxy entry, or
`component-capture` registry change is required. The `/compare` web-twin flow continues to
exist unchanged and independently.
