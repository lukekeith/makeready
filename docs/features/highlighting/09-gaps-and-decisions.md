# Gaps & decisions ledger

> Seeded by `/build-spec-draft`, **owned by** `/build-spec-audit`. OPEN rows block the plan step.
> Row types: **G** gap in the spec · **D** open design decision · **O** open question for the user ·
> **C** convention/pattern violation · **X** cross-app risk.

## Integrity check — 2026-08-04

**Verdict: SOUND** — 3 defects, all corrected in place; none changes scope, cost or a decision.

| # | Check | Result |
|---|---|---|
| 1 | Suite completeness | ✅ README + 01–09 present; every app has an owner doc with content (no ⬜ apps in this feature) |
| 2 | Citations (31 checked, all opened) | ✅ after correction — 4 had drifted and were fixed during the draft's self-review (`ReadActivityActionProvider` :14 not :20 · `ExegesisActivityActionProvider` :22 not :21 · the `activityType` gate is at **four** sites 2968/3020/3105/3151, not two · `canManageOrgContent` at 2964/3016/3101/3147). All `programs.ts`, `StudyModels.swift`, `schema.yaml`, `ViewRegistry.swift` and capture-fixture paths re-opened and confirmed |
| 3 | Counts re-run | ❌→✅ **"SwipeableCard shared by 14 screens"** was both stale and silently scoped — 13 files reference it, of which 10 are production screens, 2 demo pages and 1 a layout wrapper. Corrected in 06. Verified correct: 2,165 lines (`BibleReaderOverlay`), 8 Action methods, ~10 scroll-lock state vars (10 of 22 lifecycle vars), 3 `ExegesisVerseView` consumers, 2 `SelectableLockedBlockView` consumers, **zero** callers of `snapToWordBoundaries`, 5 compare fixtures |
| 4 | Commands exist & invoke correctly | ✅ all 11 verified by existence, not execution: `ios:build-check` (root), client `build`/`guard`/`vendor/bin/phpunit`, server `lint`/`test:run`/`schema:validate`/`schema:diff`/`migrate:status`, `swiftlint` on PATH (must run from `iphone/`), `capture/runners/compare/diff.mjs` |
| 5 | Internal consistency | ❌→✅ all 9 intra-suite links and 3 ticket links resolve; 05/06 invent no fields. **`blockIds[]` was defined in 03 with no consumer doc explaining who reads it** — 06 now states the Read editor is the multi-block consumer and must not read the deprecated singular `readBlockId`. `D-a` was listed OPEN while 03 presented it as decided; now marked provisionally-decided-pending-ratification |
| 6 | Ledger ↔ artifacts ↔ code | ✅ ledger step statuses match disk; README snapshot matches the ledger; progress recomputes to 10% (recon 1 + design 3 + 10 files × 0.6) exactly as recorded; OPEN rows are consistent with "nothing planned, nothing built" |
| 7 | Unverifiable claims | ✅ no `(claimed — unverified)` markers. The one hedged claim — that `lesson-content-hash.ts` *may* include `selections` — is correctly hedged and already carried as `X-c` |

**Not verified (flagged, not silently skipped):** no command was executed, per the skill's read-only
rule — existence and invocation directory were established by reading. Whether the content hash
actually includes `selections` needs the audit or a running stack (`X-c`).

## Verify verdict — 2026-08-05: ✅ READY, and signed off by Luke the same day

**All 7 phase docs carry a signed VERIFIED block.** Zero OPEN `X#` rows (the class that blocks a
verdict). No `(claimed — unverified)` markers survive anywhere in the suite.

**Gates, all four apps, run fresh 2026-08-05:** server `tsc --noEmit` exit 0 · server **476 tests /
40 files** · client `build` exit 0 · client **33** vitest · client **235** phpunit · tokenization
guard **853 — exactly the pre-existing baseline, so this feature's delta is ZERO** · iPhone
**BUILD SUCCEEDED** including the baseline-gated SwiftLint build phase (**zero new violations**) ·
capture server up. `npm run lint` is red and **impossible repo-wide** — no ESLint config exists
anywhere (§G-i, ACKNOWLEDGED); it is not this feature's debt and is recorded as BLOCKED-with-evidence
rather than failed.

**Consumer parity holds, proven with data** (§X-e): on a 3-block READ activity the spans the iPhone
reads (`highlights[]` by `readBlockId`) are identical, per block, to the projection the web player
and shipped builds read. Both match `03`'s frozen table.

**The feature's central claim is measured, not asserted:** brand purple `#6C47FF` → lime
`#F4FF76 @0.35`, a one-for-one 23,319-px swap confined to the highlighted span, with every other web
shot byte-identical. The live `@0.55` value has its first visual test anywhere, at distance 0 from
the contract.

### What READY does NOT cover — read this before trusting the verdict

1. **No pre-feature binary was ever pointed at the current server** (§G-ad). Build 374 cannot be
   reconstructed: no git tags exist, `CFBundleVersion` was committed once, and release builds are
   hardcoded to production so a TestFlight copy cannot reach a local server. The *data* an old build
   reads is proven correct and the *shape* it decodes is proven unchanged, so the residual is
   decode-only — **accepted, not measured.** If an old-build tester reports missing Read highlights,
   start here.
2. **The human walk was a blanket affirmative**, not a per-step report — "everything works, I tested
   it". The Bible reader in particular is left `[~]` in phase 4 rather than ticked, because no
   specific observation of that surface was narrated.
3. **Open, non-blocking:** §G-ab (a chrome flip that runs in both directions — evidence says capture
   harness state, not an app bug), §G-ac (the web half of the live-colour test), §G-j (scheduled
   -activity routes still have no automated tests, owed since the server phase), §G-g/§G-m/§G-x/
   §G-y/§G-z (pre-existing, explicitly not this feature's to fix).
4. **§D-d** — injectable per-surface highlight colour — is decided and **deferred to its own spec**.

## Audit pass log

| Date | Pass | Findings | Notes |
|---|---|---|---|
| 2026-08-04 | 3 — **gap hunt + client component coverage → AUDIT COMPLETE** | 3 gaps (G-a, G-b), 1 closed (G-c) | The adversarial pass. **G-a**: the server work is 8 routes across **2 files** (`enrollments.ts:4267-4456` was hidden behind "and its scheduled-activity counterpart"), and the two contexts gate on differently-named fields (`activityType` vs `type`). **G-b**: `style: "bold"` was in the contract with no rendering rule — now defined as weight-only, no wash. **G-c** closed: the readBlock FK is `onDelete: Cascade`, so a passage change already destroys highlights today and Read's `selections` die with the block identically — convergence adds no new loss. **Client component coverage verified**: `read-step.vue:160/193-239` already renders spans (with offset remapping at `:217`), so 05's "no new components" holds. |
| 2026-08-04 | 2 — delta + owed sweeps | 0 new findings | Delta over the six decisions. **D2/X-c feasibility confirmed in code**: `ReadBlockSelection` encodes exactly `start`/`end`/`style` (`id` is computed, not encoded) — identical to the projection's output — and `mergeSelection` returns `kept + [new]`, i.e. insertion order. So assigning `orderNumber` = array index at backfill makes the projection byte-identical. Owed per-app sweeps run: iPhone `clearInMemory` + `EntityStore` conventions present (`AppState.swift`); all four client stores named in 05 exist; server route conventions unchanged (generalising preserves the zod + try/catch + `{success}` envelope). **Still not run:** adversarial gap hunt and client component-coverage-by-view. |
| 2026-08-04 | 1 — **PARTIAL** | 1 confirmed risk (X-c), 1 closed (X-d) | **Scoped to the cross-app contract and the two unresolved X-rows**, which were the highest-value unknowns. **Not yet done:** per-app pattern compliance sweeps (server route/service conventions, client island/store conventions, iPhone AppState/Actions conventions), component-coverage verification for the client, and the adversarial gap hunt. A full pass is still required before the plan step. |

## Build findings

| Date | Phase | Finding |
|---|---|---|
| 2026-08-04 | 1 | **The human merge-with-notes check PASSED** (Luke, 2026-08-04): two highlights with different note text, merged, and both notes were present in the merged highlight's note editor. This is the observation Phase 1 exists to produce — the reported symptom of monday#12708759849 sub-issue A is gone. **Caveat recorded rather than glossed:** the local database shows no merged row from this walkthrough (activity `f93cc7f1` still reads back its recorded 4 highlights; `53ce58da` its 1), and the simulator app's saved preferences contain no `selectedEnvironment` key, which per `Configuration.swift:126` resolves to **production**. So the walkthrough most likely exercised `api.makeready.org`, not the local stack. That does not weaken the result — production is the stronger surface — but it must be stated, and it means highlight rows were merged in real data. See the follow-up row below. |
| 2026-08-04 | 1 | **Task 1.2 answered with evidence: the merge does NOT lose notes.** API-level repro on the local stack — a highlight spanning two noted highlights produced a merged row containing **all four** notes concatenated in document order. Production-synced data on another block independently shows the same concatenation signature (a note split mid-word across `\n\n` joins). **monday#12708759849 sub-issue A is therefore a client-side defect**, and the fix is 1.3a: re-key the note dictionary by highlight id instead of by range. The server path stays untouched, and Phase 3's backfill is cleared to write into it once 1.3a lands. |

## Audit pass — 2026-08-05 (DELTA, over the 2026-08-04 material)

**Scope:** exactly what 2026-08-04 added — the three dropped-call regressions (§X-p/p2/p3), the
frozen-contract amendment (§X-q), the web port (§X-r), phase 5's §G-v/§G-w/§G-x, the method finding
§C-d, and the client's new test runner. Not a full re-sweep; §3b's delta rule.

**Integrity check first (2026-08-05):** every README link resolves; every gate command in
`08-testing.md` exists as a real npm script in the right workspace; all nine `file:line` citations
written on 2026-08-04 open to what they claim — including the four historical ones, re-verified
through `git show HEAD:` rather than trusted.

**Findings — 4, all from the ONE amendment, which is the case for delta-auditing at all:**

1. **`01-architecture.md` D4 recorded a decision that §X-q reversed**, and it was still stated as
   current with Luke's name on it. A cold session reading the decisions table would have
   implemented verse-tap. Struck through with the reversal and his words, not deleted — the
   history is the useful part.
2. **`01-architecture.md`'s comparison table** listed the Read editor's target granularity as
   "whole verses". Annotated: the row is accurate as *before* state, and the target changed.
3. **`06-iphone.md`'s surface table** still assigned the Read editor `.verse` and promised it would
   "keep its verse-tap UX" — the exact opposite of what shipped. Corrected.
4. **`13-phase-4`'s amendment note** said §X-r "is open and blocks verify". It was resolved the same
   day. Corrected.

**Two things the delta audit went looking for and found, beyond doc drift:**

- **Is verse-tap still reachable anywhere?** It is not (§G-s updated). The last candidate,
  `ExegesisHighlightModal`, turns out to be **unreachable** — new row §G-y. So "consistent
  highlighting" is genuinely achieved rather than merely intended.
- **Is the modal our doing?** No — `git show HEAD:` shows a byte-identical call site. Checked
  because §C-d's pattern had already caught three of ours, and the honest answer mattered more
  than a fourth scalp.

**No new material was created by this pass** (doc corrections only), so it does not itself require
a further delta.

## G — gaps

| # | Status | Gap | Where |
|---|---|---|---|
| G-ad | **CORRECTED 2026-08-05 (found while generating release notes): a build-374 binary DOES exist — `~/Library/Developer/Xcode/Archives/2026-08-03/MakeReady 8-3-26, 12.52 AM.xcarchive`, confirmed `CFBundleShortVersionString` 1.1.3 / `CFBundleVersion` 374.** My original claim that no build-374 binary could be obtained was **wrong as stated** — it was true of the *repository* (no tags, build number committed once) but I did not check the local archive directory. **The conclusion is unchanged, for the reason that was always the stronger one:** the archive is a **Release** build, so `DEV_MODE` is off and the base URL is hardcoded to `https://api.makeready.org` — it cannot be aimed at the local server. It is also a device build, not a simulator one, so "point a build-374 *simulator* build at the same server" remains impossible. Pointing it at **production** would prove nothing about this feature, because the migration has only ever been applied to **local** (phase 3: "Production untouched"). So 7.7 stays **accepted, not executed**, and the residual decode-only risk stands as recorded. Original finding follows | **OPEN — task 7.7 cannot be performed as written. Both mechanisms it exists to test are now PROVEN at the contract level; only the binary check is missing. Needs Luke's call: substitute or accept. Found 2026-08-05.** | **There is no way to obtain a build-374 binary from this repository.** `git tag --list` is **empty** — the repo has no tags at all; `CFBundleVersion` appears in exactly one historical commit (`fc69ddb`, the monorepo consolidation), so build numbers were never committed per-build; `HEAD` carries **373** and the working tree **397**. There is therefore no revision to check out that *is* build 374. **A TestFlight 374 does not substitute:** `Release.xcconfig` builds with `DEV_MODE` off and a hardcoded `https://api.makeready.org`, so a shipped binary **cannot be pointed at the local server** — it would exercise production. **What IS proven, 2026-08-05, over real HTTP:** 02 §Backward compatibility names exactly two mechanisms, and both now hold. **(1) The `selections` projection** — 7.5 showed it regenerating correctly after a merge, 7.8 showed it byte-equal to the API rows per block across a 3-block READ activity; that column is what a shipped build reads for Read highlights. **(2) The legacy alias** — `GET /api/activities/:id/exegesis-highlights` returns the **old shape verbatim** (`{success, readBlockId, highlights[]}`, **no `blockIds`**) with every field an old decoder expects (`id/start/end/noteMarkdown/orderNumber/readBlockId/createdAt/updatedAt`, plus the additive `style` that Swift `Codable` ignores), and it still refuses a READ activity with the **original** error string `"Activity is not an EXEGESIS activity"` — pre-feature behaviour preserved exactly. **Residual risk is now only "does an old binary decode this", not "is the data right".** Faithful substitute if wanted: stash the uncommitted iPhone work, build, point at local — the binary's *code* then predates the feature, which is what actually matters; the version string is cosmetic. | 7.7 · 02 §Backward compatibility · `Release.xcconfig` · X-a |
| G-ac | **RESOLVED FOR IPHONE 2026-08-05; the WEB half stays OPEN.** The recommended fix shipped: an additive, defaulted `initialLiveSelection: NSRange? = nil` on `ExegesisVerseView`, consumed through a fallback binding so no custom `init` was needed — nil in production, so the binding is identical to `$liveSelection` and **no shipped behaviour changes**. The `LiveSelection` variant now measures `rgb(140,147,77)` × 29,146 = `#F4FF76 @0.55`, **distance 0 from the predicted contract value**, beside the saved `rgb(93,99,58)`. **Still open:** the web twin cannot express a seeded live span (its live wash is CSS `::selection` gated on `interactive`, which captures never pass), so the variant is iPhone-only and the contract value has no web-side visual test. Closing that needs an additive `liveSelection` prop on `selectable-locked-block-view.vue` / `exegesis-verse-view.vue` — a client change, therefore a follow-up. Original finding follows | **The live-selection colour (`#F4FF76 @0.55`) cannot be captured at all, and the harness change alone does not fix it.** 6.8 assumed a fixture could seed a live range; the harness half was built (a `liveSelection` field on `CaptureFixture.swift`'s component state + wiring at `ViewRegistry.swift:2077`) and it **provably does not paint**: the seeded shot contains the saved wash `rgb(93,99,58)` at 45,936 px and **no pixel anywhere near** the live value `rgb(140,147,77)`, with only the `selections` span (chars 2–31) washed and the seeded live span (37–57) bare. **Root cause, exactly:** `HighlightableTextView.swift:138` paints the binding only as `mode == .verseTap && isSelectionEnabled ? liveSelection : nil`. `SelectableLockedBlockView` hardcodes `mode: .nativeDrag` (since §X-q), whose preview is painted from the live `UITextView` selection *during a real gesture* (`:439`) — nothing a static snapshot can produce. **The one surface that would work is `ExegesisVerseView`:** its registry case omits `usesNativeTextSelection`, so it defaults to `false` → `mode: .verseTap` → `:138` *would* paint a seeded range. It cannot be seeded only because its live span is `@State private var liveSelection` (`ExegesisVerseView.swift:49`). **Recommended fix:** add one additive, defaulted `initialLiveSelection: NSRange? = nil` to `ExegesisVerseView` seeding that `@State`, and pass it from the registry — same shape as the wrapper's existing capture-facing `scrollSelectedHighlightIntoView`. It touches a production file in a phase whose app is `capture`, and phase 4 is signed VERIFIED, which is why it is not done unilaterally. **Consequence if declined:** a normative contract value in 03 §5 ships with no visual regression test on either platform — the web twin cannot express it either (its live wash is CSS `::selection` under `interactive`, which captures never pass). | 6.8 · `HighlightableTextView.swift:138,439` · `ExegesisVerseView.swift:49` · `ViewRegistry.swift:2077` |
| G-ab | **OPEN — but REFRAMED 2026-08-05 by 6.6, and the new evidence points AWAY from a product regression and toward order-dependent state in the capture harness.** The decisive observation: **the same `canEdit` flip occurs in BOTH DIRECTIONS.** In `edit-read-activity/configured` the current code renders read-only where the baseline was editable; in `create-read-activity/03-styled-blocks` (iphone-se) the current code renders the **full** "Cancel · Edit Activity · Done" editor where the **committed baseline** is read-only "‹ Activity" — the exact inverse. A product regression in the permission path cannot flip one fixture each way. Both are *deterministic within a session* (each reproduced byte-identically across two identical-code runs), so this is not randomness but **order/state dependence** — consistent with `canEdit` reading `AppState.programs[programId]` that a previously-rendered fixture in the same test process has or has not populated. Note also that only **one of three device sizes** changed for `create-read-activity`, which a code-level permission change would not explain. **Still needs the stash-and-rebuild to close**, but the prior suspicion of the Aug 2–3 commits is now weak, and the likeliest fix is in the harness's per-fixture state reset, not in the app. | **The iPhone Read editor renders its read-only branch for a fixture that used to render the full editor.** Found by 6.4. `edit-read-activity` / `configured` on iPhone now shows the `canEdit == false` chrome — header "‹ Activity" instead of "Cancel · Edit Activity · Save", and *Add Bible verse* / *Add custom text* / *Edit Themes* absent (`EditReadActivityPage.swift:399` gates the header, `:486` the three rows; `canEdit` is `programs[programId]?.isEditable(by:)` at `:110-113`). On 2026-07-28 the same fixture rendered the full editor. **Ruled out — dimming:** brightening the header band 3× shows no hidden "Cancel"/"Save" underneath, so this is the read-only branch, not highlight mode's `.opacity(0.3)` (`:426`). **Ruled out — capture noise:** the `default` variant re-captured byte-identical to its 2026-07-28 original, and the changed shot reproduced **byte-identically across a single-variant run and a full-batch run**. **Ruled out — this feature's diff:** it touches no `canEdit`, `isEditable`, `creatorId` or `StudyProgram` (the only `isEditable` hits in the diff are UIKit's `UITextView.isEditable`). **Ruled out — harness/fixture drift:** `edit-read-activity.json`, its adapter, and the program-seeding in `CaptureEnvironment.swift:99-113` / `ViewRegistry.swift:284` are unchanged in git since before the old shots; the only harness change since (`9ff035f`) adds `liveSelection: .constant(nil)`. **What remains in the window** between 2026-07-28 and now: three *committed* Read/Exegesis highlighter fixes (`0cd956c`, `9ff035f`, `82ddba8` — the monday tickets that motivated this spec) and this feature's uncommitted work. **Also unexplained: why only this variant** — `default` renders `canEdit == true` today with the same `programId`, user and seeding path, so a blanket permission failure does not fit. Two candidate shapes remain: a genuine product regression in the permission path, or a harness-state issue where the program is absent from `AppState` when this variant renders (the seeding code contains a precedent — `CaptureEnvironment.swift:224` upserts `capture-prog-0` with **no** `creatorId`, though that block needs `homeStats`, which this fixture has not got). **Settling it = stash the uncommitted iPhone work, rebuild, re-capture the one variant.** Not done: iPhone builds need explicit permission, and attribution may well land outside this feature. | 6.4 · `EditReadActivityPage.swift:110,399,486` · `CaptureEnvironment.swift:99,224` |
| G-aa | **RESOLVED (Luke, 2026-08-05): keep white `@0.85` on every surface.** Ratified as the converged value, so the changed screenshots are the correct baseline and 6.6 may accept this diff class. Found by 6.3, confirmed on a second surface by 6.4. | **Convergence silently changed the Exegesis editor's verse TEXT colour — and nothing in the spec ever decided it.** Task 6.3 expected an unchanged `ExegesisVerseView` screenshot (the highlight was already lime `@0.35`). The highlight *is* unchanged — the lime wash measures `rgb(93,99,58)` in the old and new iPhone shots alike (61,554 → 61,431 px), which is `#F4FF76 @ 0.35` composited over `#0d101a`. But the shots still differ by 2.08–3.40%, and the delta is a **pure text-colour substitution with the glyph geometry untouched**: pure white `rgb(255,255,255)` → `rgb(219,219,221)`, at an *identical* pixel count on `PreviewHighlightStyle` (7,714 → 7,714). Cause is exact, not inferred: the two surfaces disagreed, and the shared service adopted the **Read** surface's value. Pre-refactor `ExegesisVerseView.swift:570` (at HEAD) passed `foregroundColor: UIColor.white`; pre-refactor `SelectableLockedBlockView.swift:127` (at HEAD) used `UIColor.white.withAlphaComponent(0.85)`; the shared `HighlightableTextView.swift:148` now uses `0.85` for both. White `@0.85` over `#0d101a` computes to exactly `(219,219,221)` — the observed value to the digit. **Why it is a gap and not a bug:** the spec converged highlight colour, granularity and gesture lifecycle (01 §Decisions, 03 §5) and never mentions base text colour, so no rule was broken — but a visible change nobody chose is exactly what this row exists to stop shipping silently. **Evidence bearing on the answer:** Luke exercised this very surface on a device after the refactor (phase 4 human confirmation) and raised no complaint about text legibility; and reverting to pure white would either re-introduce a per-surface divergence (the thing the feature exists to remove) or change the **Read** surface instead, whose shots 6.2 already accepted. **The web is unaffected** — all three client variants diffed **0.00%, byte-identical**, so this is an iPhone-only delta and creates no consumer divergence. | 6.3 · `HighlightableTextView.swift:148` · HEAD `ExegesisVerseView.swift:570` · HEAD `SelectableLockedBlockView.swift:127` |
| G-a | **Fixed in the spec (2026-08-04)** — affected effort, not design; marker cleared 2026-08-04 after re-confirming both corrections landed | **04 under-counts the server work.** It says the routes live in `programs.ts` "and its scheduled-activity counterpart" without naming it. The counterpart is a **different file** — `server/src/routes/enrollments.ts:4267` (GET), `:4312` (POST), `:4403` (PATCH), `:4456` (DELETE). The server phase is therefore **8 routes across 2 files**, plus 8 legacy aliases, not 4 in 1. Corrected in 04; recorded because it roughly doubles the route work an implementer would have budgeted. **Second asymmetry found in the same sweep:** the two contexts gate on *differently named fields* — `activity.activityType` (programs) vs **`activity.type`** (`enrollments.ts:4277`) — so relaxing the gate is not a copy-paste. Both corrected in 03 §2 and 04. | 04 §Route work · 03 §2 |
| G-b | **Fixed in the spec (2026-08-04)** — rule added at `03-data-and-api.md:207` (`bold` = font weight only, no background wash, on every surface); marker cleared 2026-08-04 after verifying the rule is actually present | **`bold` has no defined rendering.** `style` is `highlight \| bold` in 03 §1.1/§2.2, and `read-step.vue:63` already documents both values — but 03 §5's normative rules define colours only for highlight/live/active/used. Nothing says how `bold` renders. Two consumers implementing it independently is precisely the divergence this feature exists to end. Needs a rule in 03 §5 before the consumer phases. | 03 §5 |
| G-j | **OPEN — owed; phase 2's automated tests cover one of the two contexts** | **The scheduled-activity highlight routes have no automated coverage.** All 18 tests in `content-highlights.test.ts` drive the **program** context (`/api/activities/…`). The **scheduled** twin (`/api/scheduled-activities/…`, `enrollments.ts`) was verified only by curl against real local data: the general route returned both locked blocks with a correct `blockIds[]` and the deprecated first-block `readBlockId`, and the legacy alias refused a READ activity with the original EXEGESIS-only error string — mirroring the program context exactly. Structurally the two are parallel handler factories written in the same pass, so divergence risk is low but not zero, and "low risk" is not coverage. Automated tests need an enrollment fixture (group → enrollment → lessonSchedule → scheduledActivity → locked block), which is why they are owed rather than written. **Phase 2's checklist item "both context files behave identically" is satisfied by live evidence, not by tests — that distinction is the finding.** | 11 §2.11 · `src/routes/__tests__/content-highlights.test.ts` |
| G-i | **ACKNOWLEDGED (Luke, 2026-08-04): correct the gate list, don't make this feature pay repo-wide debt.** Recorded as BLOCKED-with-evidence in `08-testing.md` and `REFERENCE.md §7`. Standing constraint — cannot be "resolved" by this feature, so it is not left OPEN to stall the gate forever | **`cd server && npm run lint` cannot pass — there is no ESLint configuration in the repository.** The script is `eslint . --ext ts --report-unused-disable-directives --max-warnings 0`, and ESLint 8.40 exits with "couldn't find a configuration file": there is no `.eslintrc*`, no `eslint.config.*` and no `eslintConfig` key in `package.json`, at `server/` or at the repo root. It also walks into `server/dist/` — a git-ignored build artifact last written **2026-04-12**, four months stale — which is where it reports looking. **Verified pre-existing, not caused by this feature.** This matters beyond `highlighting`: `REFERENCE.md` §7 lists this command as a standing **server quality gate**, `11 §Phase gates` inherits it, and `/build-spec-verify` re-runs every gate fresh — so this feature cannot reach READY while the gate is fictional, and neither can any other server feature. Someone has to either add a config or correct the gate list; it is not this feature's call to silently drop a gate. **Interim:** phase 2 records `tsc --noEmit` + `vitest` as its real, passing evidence and marks lint BLOCKED with this row. | 11 §Phase gates · REFERENCE.md §7 |
| G-h | **RESOLVED 2026-08-04 — migration rewritten, verified by rollback-and-re-apply** | **A hand-authored rename must be replay-safe, not just apply-safe.** The first correction of the M1 migration (see X-h) hardcoded the constraint and index names from the **live** database — Prisma-era identifiers like `exegesis_highlights_readBlockId_fkey`. It applied cleanly, and looked finished. But `atlas migrate diff` replays the entire migration directory into a scratch database, and there the table is created by `20260510032114.sql` with **Atlas's** naming (`fk_exegesis_highlights_readBlock`, `idx_exegesis_highlights_readBlockId`), so every rename failed and `schema:diff` broke outright. **Fix:** each rename is now a conditional `DO $$` block that accepts either ancestor name and no-ops when already applied; `ADD COLUMN` uses `IF NOT EXISTS`. **Proof, not assertion:** the revision was rolled back with `atlas migrate set` and the migration re-applied against the already-migrated database — clean both times, row fingerprint unchanged. **Generalisable lesson: "it applied without error" is not evidence a migration is correct.** Two more defects surfaced only from insisting `schema:diff` end silent — the `style` column was `text` where the YAML implies varchar, and the YAML's `selections` description still said "Highlight rows" after the rename. | the M1 migration · 11 §2.2 |
| G-g | **OPEN — pre-existing, deliberately NOT fixed by this feature. Needs a decision eventually, not now** | **The live table's column types do not match the generated schema, and that drift pre-dates this feature.** `exegesis_highlights` stores `id` and `readBlockId` as **`text`** and its timestamps as **`timestamp(3)`** (Prisma-era columns), while the YAML/HCL describe `uuid` and `timestamp` — which is why Atlas's generated `CREATE TABLE` used `uuid`/`gen_random_uuid()`. The hand-authored M1 preserves the **actual** types, because converting them would rewrite every row for no functional gain and put real data at risk mid-feature. **My predicted consequence was WRONG and the truth is worse, corrected 2026-08-04:** I expected `schema:diff` to keep proposing type-alignment changes. It proposes nothing — it reports "synced with the desired state". That is because **`atlas migrate diff` computes current state by replaying the migration directory into a scratch database, not by inspecting the live one.** The directory's own baseline (`20260510032114.sql`) creates the table with `uuid`/`timestamp`, so Atlas believes that is reality. **The live databases genuinely have `text`/`timestamp(3)` and Atlas cannot see it.** So a from-scratch environment gets different column types than production, and no tooling will warn anyone. That is a latent infrastructure issue well beyond this feature — almost certainly affecting every Prisma-era table, not just this one — and it deserves its own investigation. Recorded here because this feature is where it became visible, not because this feature should fix it. | 03 §1.3 · the M1 migration header |
| G-f | **Fixed in the spec (2026-08-04)** — 11 §2.3's file list corrected | **Task 2.3 under-counts the rename, the same way G-a under-counted the routes.** It says "rename `prisma.exegesisHighlight` … files: `programs.ts`, `enrollments.ts`, any service". The rename is bigger in two ways the doc does not say. **(1)** It is not just the model accessor — the back-relation `ActivityReadBlock.exegesisHighlights` is renamed too, and that name is a Prisma `include` key, so every query that eager-loads highlights breaks at compile time until updated. **(2)** Two files that use it are named nowhere in the suite: `src/routes/study-preview.ts` (`:427`, `:540` — inside long inline `include` chains, easy to miss) and `src/services/lesson-copy.ts` (`:27`, `:29`, `:88` — where it is also a *typed field name* in the offset-annotation shape, not just a query key). Full site list: `enrollments.ts:166,4709,4872` · `study-preview.ts:427,540` · `programs.ts:1710,1974` · `lesson-copy.ts:27,29,88`. Found by grepping the relation name after `schema:validate` rejected the dangling reference — worth noting the validator caught the schema half for free, but not the call sites. | 11 §2.3 |
| G-e | **WITHDRAWN 2026-08-04 — the finding was wrong; corrected same session** | I recorded that `/schema` "does not exist anywhere" after grepping `.claude/skills/` and `server/.claude/skills/`. **It does exist** — as a legacy slash *command*, `server/.claude/commands/schema.md`, and it is documented in `server/.claude/CLAUDE.md`. Skills and commands are separate registries and I checked only one. The suite was right; I was wrong. **What survives as a real improvement:** 11 §Companion skills and 03 §1 now spell out the underlying npm sequence (`schema:validate` → `schema:generate` → `schema:diff` → **review** → `migrate:apply` → `migrate:status`) rather than only naming the wrapper — which matters here specifically, because `/schema` applies the migration as its step 4, and **this feature's M1 must be read and hand-corrected before anything is applied** (see X-h). Keeping the explicit sequence is right; the "does not exist" claim is retracted. | 11 §Companion skills · 03 §1 |
| G-d | **CLOSED 2026-08-04 — the guard exists AND covers the shipping path.** `HighlightDraftStoreTests` (15 tests, incl. "a note survives a merge that changes the span" and the unsaved-typing case a plain re-key still loses) was proven able to fail by re-introducing the phase-1 drop-unsaved behaviour; then `EditExegesisActivityPage` adopted `HighlightDraftStore`, so the tested seam IS the code path the app runs. The phase-1 stop-gap is deleted. Original finding follows | **The regression guard promised by task 1.3a did not ship.** 1.3a's task row names its test as "a note survives a merge that changes the highlight's span", and Phase 1's verification checklist carries the same line — but commit `6e349b5` touches only `EditExegesisActivityPage.swift` and three docs. **There is no automated test; the behaviour is guarded by one human walkthrough only.** This is not merely an omission: the succession logic lives inline in a SwiftUI page's create path, with no seam a unit test could call. The repo *does* have a working test target (`iphone/MakeReadyCaptureTests/`, e.g. `EntityStoreTests.swift`, `EnrollmentEditModelTests.swift`), so pure-logic tests are feasible here — what is missing is the extraction. The `applyMerge` seam scheduled for **Phase 4 task 4.8b** is exactly that extraction, so the test lands there. Recorded so the checklist tick reads "verified by human walkthrough", never "covered by tests". | 10 §1.3a · 13 §4.8b |
| G-c | **CLOSED 2026-08-04 — no gap, recorded because it looks like one** | **"Does changing the passage destroy highlights?" Yes — and it already does.** The `readBlock` FK carries `onDelete: Cascade` (`schema.yaml`, Highlight relation), so highlights die with their block today; Read's `selections` live *on* the block and die with it identically. Reset (`programs.ts:2589`) and delete-source-reference (`:2864`) both drop blocks and rely on that cascade. Convergence changes nothing here. | 04, X-d |

## D — open design decisions

| # | Status | Decision needed | Context |
|---|---|---|---|
| D-d | **DECIDED (Luke, 2026-08-05) — DEFERRED TO A FOLLOW-UP SPEC, and therefore NOT blocking this feature.** The directive: *"the highlighting service should allow a highlight color to be specified by the page/component using the service. On exegesis, it's that yellowish color, elsewhere it might be something else."* Settled scope: **(1)** no surface needs a different colour today — build the seam with **every surface defaulting to today's lime**, so zero pixels move and no consumer divergence is created; **(2)** land it **after this feature ships**, as its own spec, because phases 4 (iPhone) and 5 (client) are both signed VERIFIED and doing it here would reopen both. **Nothing in this suite changes as a result**, so no delta audit is owed under REFERENCE §3b — the decision adds no buildable material *here*; it creates a successor. **Carry into the follow-up:** 03 §5's colour rows become per-surface parameters with lime defaults, which is a dated `03` amendment + an `X#` row (§5 froze 2026-08-04); the server is untouched (it stores `style`, never colour); and the seam already has precedent — §5's granularity row reads "injected per surface", and the Bible reader's "used" reference is already `#6c47ff @0.2`. | **Highlight colour becomes a per-surface injected policy instead of a fixed contract value.** Architecturally this is the shape the service already has: 03 §5's granularity row *already* reads "injected per surface", and colour already varies once — the Bible reader's "used" reference is `#6c47ff @ 0.2`. So this extends an existing seam rather than inventing one. **What makes it non-trivial:** 03 §5 lists `#F4FF76 @0.35` (saved) / `@0.55` (live) / white `@0.25` (active) as **normative cross-app rules binding on BOTH consumers**, and §5 is **FROZEN** (2026-08-04, at phase 2 VERIFIED). Making colour a parameter is therefore a dated `03` amendment plus an `X#` row per REFERENCE §3 rule 3 — though the server stores no colour (only `style`), so no server code or migration is implicated; the freeze cost here is consumer agreement, not schema. **Open: (1)** does any surface need a *different* colour today, or is this future-proofing? If the latter, the safe form is to add the parameter with every surface defaulting to today's lime — the seam exists, zero pixels move, and no consumer divergence is created. **(2)** in this feature or as a follow-up? Phase 4 (iPhone) and 5 (client) are both signed VERIFIED, so landing it here reopens both. | 03 §5 (frozen) · `HighlightRenderer.swift:65` · 01 §Governing rule 4 |
| D-a | **DECIDED (Luke, 2026-08-04): incoming style wins** — ratifies 03 §2.2 as written | **Style precedence on merge.** 03 commits to an answer so consumers have something to code against; nobody has ratified it. Alternatives: highest-priority style wins, or merge refuses across differing styles. | 03 §2.2 |
| D-b | **DECIDED (Luke, 2026-08-04): carry the scroll-lock machinery into the shared controller** | **Does the shared controller carry `ExegesisVerseView`'s scroll-lock machinery, or drop it?** ~10 state vars exist to stop the enclosing ScrollView jumping during native selection. Carrying it makes the controller heavier; dropping it risks reintroducing scroll jump that is not currently reported. | 06 §Selection lifecycle |
| D-c | **DECIDED (Luke, 2026-08-04): wrap first, replace later** | **Wrapper or replacement** for `SelectableLockedBlockView` / `ExegesisVerseView`. Wrapping keeps the capture ViewRegistry cases resolving; replacing is cleaner but touches the harness in the same task. | 06, 07 |

## O — open questions for the user

| # | Status | Question | Why it matters |
|---|---|---|---|
| O-a | **DECIDED (Luke, 2026-08-04): ship the colour change silently** — no staged rollout, no announcement | The lime colour change is visible to **members on already-published lessons**. Is a coordinated announcement or a staged rollout wanted, or is deploying it silently acceptable? | 05, 08 §human script |
| O-b | **DECIDED (Luke, 2026-08-04): revisit one release after ship** — the cleanup gets an owner then | How long do the `…/exegesis-highlights` aliases and the `selections` projection stay? 03 says "at least one release" and defers the drop to a separate gated change — that gate has no owner or trigger yet. | 03 §2.5, §3 |

## C — convention / pattern violations

| # | Status | Violation | Fix |
|---|---|---|---|
| C-a | **RESOLVED by this spec** (D9 folds all three snappers into one; the build phase deletes the duplicates) | `ExegesisVerseView.snappedToWordBoundaries` (added by `c8a0311`, 2026-08-03) is a third copy of word snapping alongside the dead `VerseSelectionLogic.snapToWordBoundaries` and the inline copy in `BibleReaderOverlay`. This spec's own D9 resolves it; recorded so the cleanup is not lost if the feature is descoped. | 06 §Snapping |

| C-b | **RETIRED 2026-08-04 — the stop-gap is deleted; 4.8b shipped the entity-keyed design and `applyMerge`.** Previously: ACCEPTED (Luke, 2026-08-04) — deliberate stop-gap, owned by Phase 4 task 4.8b. Not a blocking row; the literal `OPEN` marker was cleared 2026-08-04 because the decision that resolves it is recorded in this same cell | Phase 1 fixes the note loss by re-keying the three range-keyed dictionaries *in step with the merge*, at the one call site that learns a merge happened. This treats the symptom: the keys are still derived from mutable geometry, so the next thing that shifts an offset re-breaks it. **DECIDED (Luke, 2026-08-04): ship the narrow fix now, do the entity-keyed design in Phase 4.** The good version needs the `EntityStore<Highlight>` that Phase 4 builds, so it genuinely cannot be done properly in Phase 1. Recorded here so that if Phase 4 slips or is descoped, this stays visibly a patch instead of quietly becoming the design. | 4.8b |

## X — cross-app risks

| # | Status | Risk | Mitigation |
|---|---|---|---|
| X-a | **ACKNOWLEDGED** — standing constraint, mitigated by 03 §2.5/§3 and proven by 08 E2E step 7 | **Build 374 is in testers' hands now** and reads `selections[]` + `…/exegesis-highlights`. Any slip in the projection or the aliases silently empties their Read highlights. | 03 §2.5/§3, tested by 08 E2E step 7 |
| X-b | **ACKNOWLEDGED** — gated: D8 makes the fix a prerequisite; 04 §Prerequisite blocks M3 until verified | **The backfill writes into the merge path that has an open data-loss report** (monday#12708759849 sub-issue A). | D8 makes the fix a prerequisite; 04 §Prerequisite gates M3 |
| X-c | **DECIDED (Luke, 2026-08-04): normalise the projection to reproduce existing bytes** (option A — hash-neutral). CONFIRMED 2026-08-04 | `lesson-content-hash.ts:180` **does** hash `selections` (`selections: block.selections ?? null`), and `enrollment-sync.ts:322` compares `schedule.currentVersion?.sourceContentHash === lesson.contentHash` to decide whether an enrolled group's scheduled lesson is stale. So the projection is *already* the mechanism by which a highlight edit invalidates a lesson version — and any change to how it serialises re-hashes **every** block that has highlights. Narrowed by inspection: style **values** should not change (exegesis rows are all `'highlight'`; Read's spans already carry real styles), so the exposure is **ordering and JSON shape** — the projection sorts by `orderNumber` and emits exactly `{start,end,style}`, whereas today's Read `selections` is whatever the client wrote. | The backfill must either reproduce each block's existing serialisation byte-for-byte, or be accompanied by a deliberate, announced hash re-baseline. **04 gains a required pre-flight**: diff old vs regenerated `selections` JSON across all blocks in the dry run and report every block whose hash would move. Consumers: `enrollment-sync`, `enrollment-edit`, `enrollment-sync-changes`, `study-program-publish`, `routes/enrollments.ts` |
| X-d | **RESOLVED 2026-08-04 — no new risk** | `programs.ts:2740-2748` scopes the delete to one activity's own blocks (`lessonActivityId: id`) and deletes those blocks on the next line, so the highlights were going away with their blocks regardless. After convergence it also removes Read highlights — but those live on the same doomed blocks, and today's `selections` column dies with them identically. Behaviour is equivalent; no data becomes newly reachable or newly lost. | none — closed with evidence |
| X-e | **RESOLVED 2026-08-04** — 03 §2.1 adds `blockIds[]` and deprecates the singular; 06 names the Read editor as the multi-block consumer | The exegesis endpoints assume **one locked block per activity** (`findFirst`). READ activities have many. Any consumer still reading the singular `readBlockId` will silently address only the first block. | 03 §2.1 introduces `blockIds[]`; consumers must migrate off `readBlockId` |
| X-k | **DECIDED (Luke, 2026-08-04): re-stamp the stored baselines in the same run**, only where the stored hash still equals the pre-backfill value. **Refined on investigation — see the ⚠️ below; the re-stamp must cover TWO tables, not one** | **The backfill cannot be hash-neutral, and the decision that said it could was reasoning about the wrong half of the hash.** D2 / X-c settled on "normalise so M3 is hash-neutral", and 04 §How normalisation is achieved proves the *`selections` JSON* can be reproduced byte-for-byte by assigning `orderNumber` = the span's array index. That analysis is correct — and insufficient. **`canonicalLessonContent` hashes the highlight ROWS as well as `selections`** (`lesson-content-hash.ts`, the `exegesisHighlights` array). A Read block today has spans in `selections` and **zero** rows; after the backfill it has the same `selections` and **N** rows. The hashed array goes from `[]` to N entries, so the hash moves however perfect the `selections` normalisation is. **Measured, not argued:** simulating the backfill in memory over 400 lessons — creating one row per existing span with `orderNumber` = index, exactly as 3.3 specifies — **13 of 13 lessons with un-backfilled Read spans have their content hash move. Not one is neutral.** Consequence: `enrollment-sync.ts` would mark every affected enrolled group's scheduled lesson stale for content nobody edited — precisely the outcome X-c was raised to prevent and that Luke rejected re-baselining for. ⚠️ **Two corrections found while designing the fix, both of which change what the script must do — and one of which softens my own earlier alarm:**

**(1) The staleness is DEFERRED, not immediate.** Drift is `schedule.currentVersion.sourceContentHash === lesson.contentHash` (`enrollment-sync.ts:322`) where `lesson.contentHash` comes from the **stored** `StudyProgramVersion.lessonHashes` map — **both sides are stored values, neither is recomputed live.** So changing the data or the hash function marks nothing stale at the moment it happens. It bites on the next *recompute*, of which there are two: **publishing updates**, which rewrites `lessonHashes` from live content and would then report every backfilled lesson as changed — cutting a version and an AI change summary for every enrolled group, for content nobody edited; and **new enrollments** (`enrollments.ts:286`, `:4900`), which stamp `sourceContentHash` from live content and would immediately mismatch the older snapshot. My earlier wording — that enrolled groups were "told their lessons were out of date" — overstated it for X-j, whose damage was likewise latent until one of those two recomputes fired. The exposure is real; the timing is not what I first said.

**(2) The re-stamp must cover BOTH stored sides.** Updating `LessonScheduleVersion.sourceContentHash` alone is not enough — `StudyProgramVersion.lessonHashes` holds the other half of the comparison, so leaving it stale just relocates the churn to the next publish. The script must update both, and in both cases only where the stored value still equals the pre-backfill hash. A stored value that already differed means that lesson was **genuinely** drifted before the backfill, and must be left alone or the backfill would silently mark real edits as synced. | 03 §4 · 04 §Content-hash pre-flight · 12 §3.2-3.3 |
| X-l | **DECIDED (Luke, 2026-08-04): re-stamp the snapshot too — `contentHash` AND `content` together — with a rollback manifest.** Phase 3 unblocked | **The re-stamp is under-scoped: the hash is stored in FOUR places, not two, and one of them is an audit record.** X-k's decision says re-stamp `LessonScheduleVersion.sourceContentHash` and `StudyProgramVersion.lessonHashes`. Auditing that design against the code found: **(a)** the drift comparison at `enrollment-sync.ts:322` does **not** read `lessonHashes` — it reads `lesson.contentHash` from `snapshotLessons`, i.e. **`StudyProgramVersion.snapshot.lessons[].contentHash`** (`:238`). So the design as decided would have missed the field the main comparison uses. **Corrected by the delta audit of the fix itself (2026-08-04): `lessonHashes` is NOT ignored** — `hasPendingLessons` (`enrollment-sync.ts:171-188`) reads it as its post-apply "is anything still pending?" check. Both are live, by different functions, which is exactly why the settled design updates all four locations rather than choosing between them. My first phrasing here overstated the case against `lessonHashes`. **(b)** `SnapshotLesson` also carries **`content`** — the *entire* canonical lesson object at publish time (`study-program-publish.ts:285-296`), which embeds the highlight arrays. After the backfill, a snapshot's stored `content` no longer matches live content, and `enrollment-sync-changes` diffs `content` to build the human-facing change summary. Re-stamping `contentHash` without `content` leaves the snapshot **internally inconsistent** — claiming a hash its own stored content does not produce. **(c)** That surfaces a question nobody has answered: `snapshot` is an **audit record of what was published at version N**. Rewriting it is rewriting history, which may be the right call for a no-op data migration and may be exactly the wrong one. **This is not a coding detail — it is a decision.** **Resolved:** the run re-stamps all four stored locations, keeping each snapshot internally consistent (its `contentHash` and its `content` move together), and writes a manifest of every `(table, id, field, oldValue, newValue)` it touches so the whole thing is reversible. Rewriting a published-version audit record is accepted deliberately here because nothing a reader would recognise as content changed — and the alternative merely defers the same churn to the next publish. Design written up at 12 §3.7.

**MEASURED 2026-08-04** by the read-only pre-flight, so the decision is not abstract: 49 blocks / 67 spans to migrate; 13 lessons' hashes move; and the re-stamp surface is **16 schedule versions, 11 snapshot entries and 11 `lessonHashes` entries re-stampable, with exactly 1 already-drifted in each category** that must be left alone. So the audit-record question is about **11 snapshot entries**, not thousands — small enough to inspect by hand if that helps the call. | `enrollment-sync.ts:238,322` · `study-program-publish.ts:285` · 12 §3.7 · X-k |
| G-m | **OPEN — a repo-wide gate hole, found by tripping over it** | **`npx tsc --noEmit` does not cover `server/scripts/`.** `tsconfig.json` sets `include: ["src"]`, so anything in the repo-root `scripts/` directory is invisible to the phase gate. I wrote the backfill there and it typechecked clean — because it was not being checked at all. Moving it to `src/scripts/` (which is the existing convention: `backfill-study-sync.ts` already lives there) immediately surfaced **four real errors**, including `lessonById` used but never declared, which would have thrown at runtime mid-transaction on customer data. **Fixed for this feature** by moving the file. **Not fixed repo-wide:** `server/scripts/` still holds ~28 files — `migrate-organizations.ts`, `repair-enrollment-date-ranges.ts`, `sync-linked-member-avatars.ts` and other data-touching scripts — none of which any gate typechecks. Either fold that directory into the tsconfig or move the TypeScript ones under `src/`. Out of scope here; recorded because the next person to write a migration script will land in the same hole. | `server/tsconfig.json` · 12 §3.1 |
| G-l | **CLOSED 2026-08-04 — a spec worry that turned out not to exist, and a bug it caused in my first pre-flight** | **"Key order" cannot differ, because Postgres normalises it.** 03 §4 and 12 §3.2 ask the pre-flight to report blocks differing "by value, key order or array order", and 04 argues at length that the keys already match. Both are moot: `selections` is a **`jsonb`** column, and `jsonb` normalises key order on write — `{"start":1,"end":2,"style":"x"}` is stored as `{"end":2,"start":1,"style":"x"}`. Key order is not representable, so the backfill cannot change or preserve it, and neither can the client. This is a *stronger* guarantee than 04's argument, which reasoned from the Swift encoder. **It also caused a real bug:** the first pre-flight compared `JSON.stringify` of the stored array against the projection and reported **all 49 blocks dirty — a false STOP**. Comparing element-wise on `(start, end, style)` in array order, which is what is actually ours to preserve, reports **CLEAN**. Worth keeping because the same mistake would recur in any future comparison of a `jsonb` column. | `backfill-highlights.ts` · 03 §4 · 04 · 12 §3.2 |
| G-k | **CLOSED 2026-08-04 — guard written, and proven to catch the exact regression it exists for** | **Nothing tested content-hash stability, which is why X-j survived a green typecheck and 435 passing tests.** Now guarded by `src/services/__tests__/lesson-content-hash-stability.test.ts` (5 tests, no database): a **golden hash** pinned against a fixture exercising every branch of the canonical shape; an explicit assertion that the read-block highlight array is still emitted under its ORIGINAL key name; ordering-insensitivity; a not-vacuous check that real edits do move the hash; and one asserting that **adding a highlight row moves the hash** — the fact X-k turns on, pinned so it cannot be forgotten. **Proven, not assumed:** the X-j regression was deliberately re-introduced and both the golden-hash and key-name tests failed, then it was reverted. The file header tells whoever hits a failure that a changed canonical shape is a *data migration*, not a refactor, and explicitly forbids pasting in the new hash to go green — which is the obvious wrong move. | `lesson-content-hash-stability.test.ts` · X-j · X-k |
| X-j | **REGRESSION I INTRODUCED IN PHASE 2 — FOUND AND FIXED 2026-08-04, same session, before it left the local machine** | **Task 2.3's rename moved the content hash of every lesson containing a read block.** `canonicalLessonContent` builds an object that `stableStringify` JSON-encodes, so **its KEY NAMES are a stored wire format, not variable names.** Renaming the Prisma relation `exegesisHighlights` → `contentHighlights` (2.3) renamed the key in that object too, changing the canonical JSON for every read block. **Measured: 183 of 300 sampled lessons — i.e. every single one with a read block — changed hash purely from the key name.** Against hashes stored in `LessonScheduleVersion.sourceContentHash` before this feature began, 66 of 116 schedule versions had gone stale. This is exactly the failure X-c exists to prevent, introduced by the very phase that cited X-c, and the phase's gates (`tsc`, 453 tests) were all green while it was live — **no test covers hash stability across a refactor**, which is the deeper gap. **Fix:** the canonical object's key is pinned back to `exegesisHighlights` while the relation stays `contentHighlights`, with a comment forbidding the "tidy-up". **Verified after the fix:** 0 of 300 lessons change from the key name, and stored-vs-recomputed goes from 50/116 matching to **113/116**. The 3 still moving have read blocks last edited in July, before this work — genuine content drift, which is what a moved hash is supposed to mean. | `lesson-content-hash.ts` · 11 §2.3 |
| X-i | **FOUND + GUARDED 2026-08-04. Guard KEPT — DECIDED (Luke, 2026-08-04)**, having been flagged as spec-additional code. Retires itself when phase 3's backfill lands | **Between phase 2 and phase 3's backfill, the first write to any un-backfilled block would destroy its highlights.** `syncSelectionsForBlock` regenerates `ActivityReadBlock.selections` *from the rows*. Read highlights do not become rows until phase 3. So a leader highlighting a Read passage in that window would produce one row, the projection would be rebuilt from that single row, and **every other span on the block would be gone**. Measured on local production-synced data: **49 blocks holding 67 spans** currently have selections but zero rows. Directly violates governing rule 1. *Not reachable in production today* — no shipped consumer calls the new routes — but the ordering makes it inevitable if phase 3 slips or a consumer ships first, and "unreachable" is not a property worth betting a leader's work on. **Guard shipped:** POST to a block with non-empty `selections` and zero rows returns **409** with an explanatory message instead of writing. Proven against a real at-risk block (`07ec505c…`, 2 spans): 409 returned, both spans intact afterwards. The check retires itself — after the backfill no block matches it. **For Luke:** this is defensive code the spec did not ask for. The alternative is pure sequencing discipline (never deploy phase 2 without phase 3). Flagging rather than assuming. | 11 §2.4–2.9 · 12 (phase 3) |
| X-h | **CAUGHT + NEUTRALISED 2026-08-04 — the migration file is now hand-authored. Verify before applying.** | **Atlas generated a migration that would have deleted every existing highlight.** `npm run schema:diff` does not detect table renames: for `exegesis_highlights` → `content_highlights` it emitted `CREATE TABLE "content_highlights" (…)` followed by **`DROP TABLE "exegesis_highlights";`**. Applying it would have destroyed every leader-authored highlight and note in the database — a direct violation of governing rule 1, and precisely the hazard 03 §1.3 was written to warn about. It was caught by reading the generated SQL, which is the only reason 03 and 11 §2.2 instruct that it be read. **Resolution:** `server/atlas/migrations/20260804070717.sql` was replaced by hand with in-place `ALTER TABLE … RENAME TO`, six constraint/index renames (Postgres keeps the old identifiers after a table rename), and `ADD COLUMN "style" text NOT NULL DEFAULT 'highlight'` — no row is created, copied or dropped. `atlas migrate hash` re-run so `atlas.sum` matches. The file carries a header explaining why it deviates from generated output. **⚠️ Not yet applied** — `migrate:apply` has NOT been run, so this is verified by reading, not by execution. **Standing consequence for the rest of this feature: never run `/schema` unattended on this suite**, since its step 4 applies whatever was generated. The same trap awaits any future rename. | 03 §1.3 · 11 §2.2 |
| X-g | **DECIDED (Luke, 2026-08-04): `ContentHighlight` / `content_highlights`** — "generic, so this is best". Back-relation `ActivityReadBlock.exegesisHighlights` → `contentHighlights` in the same edit. **Destination verified free before applying** (the omission that caused this row): no `ContentHighlight`/`contentHighlight`/`content_highlights` anywhere in server, client, iphone or capture. `schema:validate` ✅ green. **The API surface is deliberately unchanged** — routes stay activity-scoped at `…/highlights` and the response key stays `highlights`; only the Prisma model, the table and the relation field are renamed. Applied 2026-08-04 | **The name the spec renames to is already taken.** 03 §1.1 and 11 §2.1 say: rename `ExegesisHighlight` → `Highlight`, `table_name: exegesis_highlights` → `highlights`. **A `Highlight` model with `table_name: highlights` already exists** (`server/schema/schema.yaml:1435`) — it is the Bible reader's **per-user personal verse highlights**: `userId`, `translationId`, `bookNumber`, `chapter`, `verseStart`/`verseEnd`, `color` (enum `HighlightColor`), unique on the whole verse tuple, cascading from `User` and `Translation`. Same word, unrelated domain: members' private Bible marks vs. leader-authored spans on lesson content. **Blast radius of the incumbent, measured:** the model is used in exactly one server file — `src/routes/bible.ts`, 7 call sites (`:368`, `:1021`, `:1174`, `:1303`, `:1315`, `:1405`, `:1417`) — plus the iPhone Bible reader that consumes those routes. `prisma.highlight` is likewise already bound to it, so 11 §2.3's "rename `prisma.exegesisHighlight` → `prisma.highlight`" would silently retarget the wrong model if the collision were not caught. **Why three audit passes missed it:** every pass verified the *source* symbol (`ExegesisHighlight`, its routes, its consumers) and none grepped the *destination* name for an existing occupant. Cheap lesson — a rename's target is as much a citation as its source. **The API path is NOT affected either way**: the routes are activity-scoped (`/api/activities/:id/highlights`) and never collide with the Bible reader's `/api/bible/...`. Only the model + table name is in question. | 03 §1.1 · 11 §2.1, §2.3 |
| X-f | **ACKNOWLEDGED** — mitigation specified in 06 §Disk cache (version bump or optional `style` with a default) | iPhone disk cache holds the old entity shape; an in-place upgrade must not fail the decode. | 06 §Disk cache |
| G-n | **Fixed in the spec (2026-08-04)** — 06 §The service corrected in the same session; no `03` amendment needed | **The renderer contract enumerates four appearances; six ship today.** 06 §The service and 13 §4.3 describe `HighlightRenderer` as painting `.live` / `.saved` / `.active` / `.used` — 03 §5's four *cross-app normative* values. Both adopting surfaces additionally paint two appearances that 03 never mentions and that have no web counterpart: **the highlight currently open in the editor** (opaque `UIColor.white` + black text — `ExegesisVerseView.swift:586`, `SelectableLockedBlockView.swift:150`) and **the read-only preview wash** (`white @ 0.9` + black text — `ExegesisVerseView.swift:591`, `SelectableLockedBlockView.swift:153`, driven by `usePreviewHighlightStyle`, whose live caller is `ExegesisNoteEditorPage.swift:271`). Building the renderer to the four-case list would have silently dropped both when 4.11/4.12 wrap the surfaces. **Why this is NOT a contract amendment:** 03 §5 governs values both apps must agree on; these two are iOS editor chrome the web player has no state for. `03` is untouched and stays frozen. Implemented as `.editing` and `.preview` in `HighlightAppearance`, documented as iOS-only. **Second correction in the same read:** 03 §5's row is labelled "Active/being-edited: white @ 0.25", but white @ 0.25 is today's *transient tap-selection* wash (`ExegesisVerseView.swift:113`, comment "matches Figma spec"), NOT the being-edited highlight, which is opaque white. `.active` is implemented as the transient wash, so no shipped appearance changes value. | 06 §The service · 13 §4.3 · `HighlightRenderer.swift` |
| G-o | **OPEN — expected, in scope, and the checklist already asks for it; recorded so nobody "fixes" it back** | **On the Read editor, saved highlights are opaque brand purple today — lime is a visible change there, not a no-op.** `SelectableLockedBlockView.swift:139` defines `editMarker` as `#6c47ff` at **alpha 1.0** and paints every stored span with it (`:156`). The Exegesis editor already paints `#F4FF76 @ 0.35` (`ExegesisVerseView.swift:572`), so only one of the two native surfaces moves. Phase 4's checklist line "saved highlights render lime @0.35 … on every surface" is therefore a real behaviour change on the Read editor, covered by the decisions-gate answer *ship the colour change silently*. Note the value collision this removes: `#6c47ff` at 0.2 remains legal as the Bible reader's **used-passage** marker (03 §5), which is a different meaning — the same hue at full opacity meaning "highlight" is exactly the cross-surface disagreement this feature exists to end. | 03 §5 · 13 §4.11 · `SelectableLockedBlockView.swift:139` |
| C-c | **DECIDED (Luke, 2026-08-04): exempt the one file in `.swiftlint.yml`** — applied same session | **The carried scroll-lock machinery violates the `async_after_choreography` gate, which is a build-phase error.** Carrying `ExegesisVerseView`'s scroll lock (DECIDED 2026-08-04, 06 §Selection lifecycle) means carrying its retry ladders — `[0, 0.05, 0.2, 0.5]` for the preserve path and `[0, 0.05, 0.2, 0.5, 0.75]` for the selection anchor — which are wall-clock waits on UIKit's own asynchronous scroll settling. UIKit exposes no completion callback for it, so there is nothing to await; the delays were tuned against a real device and cannot be re-derived. The rule exists to ban *animation choreography* by timer, which this is not. **Mitigation applied before asking:** all three original call sites were funnelled through a single `dispatch(_:after:)` helper, so the exemption surface is **one line in one file**, not three scattered sites. **Chosen over baselining** because `.swiftlint.yml` shows the exemption to anyone reading the config, next to the three rules that already carry file exemptions, whereas the baseline is a ~1,000-entry JSON and regenerating it would also silence any other new violation. **Chosen over restructuring** because the display-link freeze only covers the frozen path, the timed restores also serve the non-frozen preserve path, and scroll-jump behaviour cannot be verified without a human on a device — rewriting it is the option that risks silently reintroducing the bug the machinery prevents. Retires if `ScrollLockCoordinator` is ever replaced with a UIKit-observation approach. | `iphone/.swiftlint.yml` · `ScrollLockCoordinator.swift` · 13 §4.4 |
| G-p | **CLOSED 2026-08-04 — caught by the build in the same session, but the sweep should have caught it** | **A rename's call-site sweep undercounted, for the third time in this suite** (cf. G-a on the routes, G-f on the Prisma accessor). Renaming `ExegesisHighlight` → `ContentHighlight` (task 4.7), I grepped the symbol and filtered the results with `grep -v "Modal\|Row"` — intending to exclude the *unrelated* component names `ExegesisHighlightModal` / `ExegesisHighlightRow` from the count. That filter drops whole matching LINES, so it also hid the six genuine references to the bare type **inside** `ExegesisHighlightModal.swift:17,20,21,25,113,156`. The reported "5 files" was really 6, and `npm run ios:build-check` failed with six `cannot find type` errors on the file I had convinced myself was excluded. **The generalisable rule:** when counting a symbol's call sites, exclude by SYMBOL (a negative lookahead in the pattern — `\bExegesisHighlight\b(?!Modal\|Row)`), never by filtering output lines, because a file whose *name* contains the excluded token can still use the symbol you are counting. The `perl` rename itself used the correct lookahead form and was right; only the counting grep was wrong. | 13 §4.7 · `ExegesisHighlightModal.swift` |
| G-q | **CLOSED 2026-08-04 — removed while wiring 4.8b** | **A view was passed three state bindings it never read.** `HighlightActionMenuContent` (`EditExegesisActivityPage.swift`) declared, stored and threaded `noteDrafts`, `attributedNoteDrafts` and `savedNoteMarkdownByHighlight` — the last of which carried an eight-line comment explaining why it had to be a `Binding` rather than `@State`, referencing monday#12668543338. **Not one of the three was read anywhere in the struct's body.** The "Add note" vs "Edit note" label it was supposedly for is computed by the PAGE and passed in as `hasNote`, which the comment two fields above says explicitly. So the fix for that ticket landed twice: once correctly, once as plumbing nobody removed. Deleted with 4.8b. **Worth generalising: a `Binding` that is stored but never read is invisible to the type checker and to lint, and reads as load-bearing precisely because someone wrote a careful comment about it.** | `EditExegesisActivityPage.swift` §HighlightActionMenuContent · 13 §4.8b |
| G-r | **CLOSED 2026-08-04 — removed while collapsing the Actions (4.9)** | **The client kept its own copy of the server's merge rule.** After creating a highlight, `EditExegesisActivityPage` computed which existing highlights had been absorbed by running an overlap test locally — `$0.id != created.id && $0.start < created.end && $0.end > created.start` — and removed those. That is the server's own merge semantics (03 §2.2) reimplemented in a consumer, which 03 §5 forbids in as many words: *"Overlap — resolved server-side by §2.2 merge. Consumers never merge locally and then post a union."* It was also unnecessary: `POST …/highlights` returns `absorbedIds` precisely so the client does not have to guess. The two agreed today, which is why nothing was visibly broken — but they could drift the moment the server's rule changes (e.g. if adjacency ever counts as overlap), and the client's copy would then delete the wrong rows locally while the server kept them. Now uses the returned `absorbedIds`. **Worth generalising: a client-side re-derivation that happens to agree with the server is not a duplicate to leave alone — it is a divergence waiting for the next contract change.** | `EditExegesisActivityPage.swift` §applyStyle · 03 §2.2, §5 · 13 §4.9 |
| X-m | **ACKNOWLEDGED 2026-08-04 — deliberate partial adoption, not an omission** | **The Bible reader cannot adopt the selection controller without changing its UX.** 13 §4.13 says the reader adopts "`HighlightRange`, snapping, renderer and the controller". The first three landed. The controller did not, because `TextSelectionController` exists to commit a highlight **on genuine finger release** (03 §5), and the reader does not work that way: a selection persists after release and becomes a passage reference only when the user taps **Select**. Adopting release-commit here would either fire a passage confirmation the user did not ask for, or require the controller to grow a "commit is deferred to an external trigger" mode — which is a second lifecycle wearing the first one's name. The reader's own commit path is unchanged and correct per D5 ("its output: a passage reference, not a Highlight row"). **What the reader DID adopt is the part that was actually broken:** one snapping engine (its inline copy split "Lord's"; the editors did not) and the contract's colours (its selection tint was 0.5 where the editors used 0.55). The divergence this feature exists to end is closed on this surface; the gesture-lifecycle unification is not, and should not be forced. Revisit only if the reader ever grows a commit-on-release affordance. | 13 §4.13 · 06 §Adopting surfaces · `BibleReaderOverlay.swift` |
| G-s | **UPDATED 2026-08-05 by the §X-q delta audit: `.verse` now has NO reachable production caller at all.** After the Read editor moved to `.word`, verse-tap survives only in `ExegesisHighlightModal` (unreachable — see §G-y), `ExegesisNoteEditorPage` (`isSelectionEnabled: false`, so nothing selects) and the capture registry (selection off by default). So the injection is not merely vestigial for `.verse`, it is unexercised — which is the good news for "consistent highlighting": no surface a user can reach does anything but word-drag. Original finding follows. OPEN — design observation, no behaviour bug; decide at the next touch of this code** | **Verse granularity has TWO implementations after all — the injection is only exercised for `.word`.** 13's checklist expects `snapToWordBoundaries` to end with "one definition, three callers". It has one definition and **two** callers (`TextSelectionController.commit`, `BibleReaderOverlay.textViewDidChangeSelection`), because the third surface never asks for word snapping: the Read editor runs in `.verseTap` mode, whose selection comes from `VerseTapPolicy` — which computes whole-verse ranges itself via `VerseSelectionLogic.rangeForVerses`. The consequence is that **`HighlightSnapping.snapToVerseBoundaries` has no production caller** (it is exercised only by its own tests), and "granularity is injected per surface" is true for `.word` and vestigial for `.verse`. **This is not a behaviour bug** — the Read editor does produce whole-verse ranges, which is what 03 §5 requires — but it is a second path to the same guarantee, which is the shape this feature exists to remove. Two defensible resolutions: route `.verseTap` commits through `HighlightSnapping.snap(granularity:)` as a final widening step (cheap, makes the injection real, and would also catch a malformed verse map), or delete `snapToVerseBoundaries` and let `VerseTapPolicy` own verse granularity outright (honest, but leaves `HighlightGranularity.verse` meaning nothing in the snapper). Recorded rather than resolved because both are defensible and neither is urgent. | 13 §Verification checklist · `HighlightSnapping.swift` · `TextSelectionController.swift` §VerseTapPolicy |
| X-n | **REGRESSION THIS FEATURE INTRODUCED — FOUND AND FIXED 2026-08-04, in phase 5, having shipped undetected through phases 2–4** | **Phase 2's Prisma rename silently emptied the web member player's exegesis notes.** The back-relation `ActivityReadBlock.exegesisHighlights` was renamed to `contentHighlights` (03 §1.1, 09 §X-g), and every server include/select moved with it — `programs.ts:1710,1974`, `enrollments.ts:166,4810,4973`, `study-preview.ts:427,540`. **The web client was never updated.** `exegesis-step.vue:91` read `block?.exegesisHighlights`, which is now always `undefined`, so it fell through to its own fallback path — and that path builds highlights from `selections[]`, which sets **`noteMarkdown: ""`**. Net member-facing effect: the highlights still painted, every note rendered **blank**, and nothing errored. **Why nothing caught it:** the server's own gate is green (its tests assert the hash's canonical key, which is deliberately still spelled `exegesisHighlights` — X-j — so even a grep for the old name in `server/` looks reassuring); phase 2's checklist verified the API responses, not the consumers of the nested payload; and the client has no test that opens an exegesis step with a note. **Fixed here:** the step reads `contentHighlights` first and keeps `exegesisHighlights` as a fallback for payloads produced before the rename (the same dual-read window as the legacy route aliases, 03 §2.5). `use-lesson-state.ts:38` carries both names too. **Generalisable: a rename is only complete when every CONSUMER of the renamed key is grepped, not every producer.** 09 §G-f caught the server-side call sites of this same rename and stopped there; the client was never swept. | `exegesis-step.vue:91` · `use-lesson-state.ts:38` · 11 §2.3 · 09 §G-f, §X-g |
| G-t | **Fixed in the spec (2026-08-04)** — 14 §5.1–5.3 corrected in place | **Phase 5's plan assumed a payload shape the server does not send, and implementing it as written would have BROKEN bold rendering.** Task 5.1 says "consume `highlights[]` from the activity payload; keep decoding `selections[]` when `highlights` is absent". There is no `highlights[]` on the activity payload: the server nests `contentHighlights` **per read block**, and every one of those selects lists `{ id, orderNumber, start, end, noteMarkdown }` — **no `style`** (`programs.ts:1712-1718`, `study-preview.ts:427`, `enrollments.ts:166`). Rendering from that array would therefore paint every span as a highlight, including `style: "bold"` ones, contradicting 03 §5. **The correct source for RENDERING is `selections[]`, and always was** — it is the server-maintained derived projection (03 §1.2, §3), regenerated after every mutation, and since phase 2 it carries **real** style values instead of the hardcoded one. So the dual-read the task describes buys nothing for rendering and would be a regression. The nested `contentHighlights` array is the right source for **notes** (it is the only one carrying `noteMarkdown`), which is exactly how `exegesis-step.vue` uses it — see X-n. | 14 §5.1–5.3 · `read-step.vue:193-239` · `ThemePlayer.scss:158` |
| G-u | **ACKNOWLEDGED (Luke, 2026-08-04), same decision as §G-i** — gate list corrected in `08-testing.md` + `REFERENCE.md §7`; this feature's contribution is measured at 856 -> 853 rather than argued | **`cd client && npm run guard` cannot pass — 856 pre-existing violations.** The tokenization guard scans `resources/css/components` and reports raw px/hex across ~40 component stylesheets that predate the design-system migration (`action-card-menu.scss`, `add-menu.scss`, `age-range-input.scss`, …). It exits 1, so the standing client gate in REFERENCE §7 and 14 §Phase gates is red before this feature touches anything. **Verified not caused by this feature by construction:** phase 5's only style edits are `resources/css/styles/_semantic.scss` and `resources/js/themes/ThemePlayer.scss`, and the guard's `SCAN_DIR` is `resources/css/components` (`tokenization-guard.mjs:25`) — neither file is scanned. Exactly parallel to §G-i on the server: a gate the pipeline is required to run fresh, which has never been green. **Interim:** phase 5 records `npm run build` + `phpunit` as its real passing evidence and marks guard BLOCKED with this row. | `client/scripts/tokenization-guard.mjs:25` · 14 §Phase gates · REFERENCE.md §7 |
| G-v | **Fixed in the spec (2026-08-04)** — 05 §LeaderApp, 14 §5.6 and the stale code comment corrected | **The suite's premise that the web has no highlight authoring is FALSE — it has had it since the parity project.** D7, `05 §Client` and `14 §Phase 5`'s header all say the web is a *consumer only* and that authoring "stays deferred"; `05` even cites the read pane's own header comment (`edit-read-activity-pane.vue:25`, `DEFERRED (explicit): highlighter/selection styling (.stylePicker route)`) as evidence. That comment is stale and the claim is wrong on both panes: the **read** pane ships highlight mode (`toggleHighlight`), the `.stylePicker` menu route and `applyStyle` (`edit-read-activity-pane.vue:137-188`), and the **exegesis** pane creates highlights from a native selection, edits their notes and deletes them (`edit-exegesis-activity-pane.vue:179-300`). **Why it matters, and it is not bookkeeping:** `03 §5` scopes its normative rules to the web with the words *"D7: web implements these when its authoring UI is built"* — so the contract believes the web is exempt today. It is not exempt; it is authoring highlights right now, against rules it was never checked against. That check is what found **X-o**. Corrected in place rather than raised as a decision, because nothing is being *added* — the docs are being made to describe what already shipped. | 05 §LeaderApp · 14 §5.6 · 03 §5 |
| G-w | **RESOLVED 2026-08-04 (phase 5.6)** — twin honours `style`; both stores carry it | **`style` was dropped on the floor twice on the web, so a stored `bold` span rendered as a highlight wash.** (1) The `SelectableLockedBlockView` twin's `selectionStyle()` took **no argument** and returned `'highlight'` for every selection — the read editor therefore painted a `bold` span with the saved-highlight background, contradicting `03 §5` ("font weight only — no background wash, on every surface"). Its sibling twin `exegesis-verse-view.vue` had honoured `bold` all along, so the two web surfaces disagreed with each other as well as with iOS. (2) Both LeaderApp stores' `fetchExegesisHighlights`/`create…` mappers listed the fields explicitly and **omitted `style`**, so even a correct renderer could not have seen it — the same shape as **X-n** (a contract field silently lost in a hand-written mapper) found two tasks earlier in this phase. Fixed: `selectionStyle(sel)` mirrors `HighlightRenderer.paintHighlight`'s precedence, a `--seg--bold` rule ships weight-only, and both stores carry `style` with a `?? 'highlight'` default for older payloads. | 14 §5.6 · `selectable-locked-block-view.vue` · `leader-program.store.ts` · `leader-enrollment-schedule.store.ts` |
| X-o | **RESOLVED 2026-08-04 (phase 5.6)** — one snapper, in `utils/verse-selection.ts`, verified against the contract's named cases | **The web was already authoring highlights with a word-snapper that disagreed with the frozen contract on both counts `03 §5` pins down.** `exegesis-verse-view.vue` carried a private `snapToWordBoundaries` whose `isWordChar` was `!/[\s\p{P}]/u` — and `'`, `’` and `-` are all `\p{P}`, so **the three marks 03 §5 explicitly names as intra-word were treated as boundaries**: selecting inside `"Lord’s"` snapped to `"Lord"` on the web and to `"Lord’s"` on iOS, which is the *exact* case `08 §iPhone` calls "the case today's three copies disagree on". Second divergence, found in the same read: it grew the start backwards while a word character merely *preceded* the boundary, without checking the character *at* it — so a selection beginning on a space swallowed the whole previous word (`" world"` → `"hello world"`), where `HighlightSnapping` requires word characters on **both** sides and leaves it alone. **This is the feature's own thesis failing on the one surface the contract had written off as not-yet-authoring (see G-v):** a fourth copy of the snapper, disagreeing in a fourth way, discovered only because G-v forced the web authoring path to be checked at all. Fixed by moving the function to `resources/js/utils/verse-selection.ts` as a line-for-line port of `HighlightSnapping.snapToWordBoundaries` + `isWordCharacter`, leaving ONE definition per platform. Behaviour re-checked against the contract's cases before and after: `"Lord’s"`/`"Lord's"` and `well-being` now snap whole, a selection starting on a space no longer grows backwards, a verse selection ending on the verse-terminating newline does not walk into the next verse, and snapping remains grow-only. | 03 §5 · `resources/js/utils/verse-selection.ts` · `exegesis-verse-view.vue` · G-v |
| G-x | **OPEN — latent, not live; found by the phase-5 contract-parity trace. NOT a `03` amendment** | **The activity payload's nested `contentHighlights` omits `style`, so a consumer reading the primary path cannot implement 03 §5's `bold` rule.** `programs.ts:1710` and `:1974` both `select` exactly `{id, orderNumber, start, end, noteMarkdown}` — no `style`, no `readBlockId`. The dedicated endpoint (`03 §2.1`) returns both, and the LeaderApp stores now read it (§G-w), but the **member** exegesis step only ever sees the nested array, so it paints every row as a wash regardless of style. **Why this is not an amendment:** `03` specifies the `/highlights` endpoints and never specified the nested activity payload at all, so the contract is silent rather than wrong — this is a gap in the spec, and the freeze is not implicated. **Why it is latent rather than live:** nothing authors a `bold` exegesis highlight on either platform (the style picker belongs to the READ editor, whose member rendering goes through `selections[]`, which *does* carry style and which `exegesis-step.vue` correctly filters). So no member can currently reach the bad rendering. **Why it is still worth a row:** it is the same shape as §X-n and §G-w — a field that exists in the contract, is dropped by a hand-written select, and produces plausible output instead of an error. The next feature that adds bold to an exegesis surface gets a silent divergence. Fixing it is a **server** edit and therefore not phase 5's to make (REFERENCE §3 rule 5); carried to phase 7's cross-app walk as owed. | `programs.ts:1710,1974` · 03 §2.1 · 14 §Verification |
| X-p | **RESOLVED — fixed 2026-08-04 and CONFIRMED BY LUKE ON A REAL DEVICE the same day** ("it's working without jumping right now"): the create-highlight flow works, so it is no longer committing under the finger. Regression this feature had introduced; reopened phase 4 | **The Exegesis editor committed a highlight under the user's finger on the first long press — the exact bug (monday#12708759849) this feature exists to end, re-created by the refactor that was supposed to end it.** Reported symptom: "the first time I tap and hold it forms a highlight around the word I'm tap and holding, but it creates the highlight **before I release my finger**. I have to tap and hold a second time for it to work." **Root cause is one dropped line of wiring, not a logic error.** The pre-phase-4 `ExegesisVerseView.swift:200` set `observer.delegate = self`. Phase 4.4 moved `TouchObserverGestureRecognizer` into `TextSelectionController.attach()` and did **not** carry that line — while faithfully carrying the delegate *method* into the new Coordinator (`HighlightableTextView.swift:278-298`), where it still explicitly handles `TouchObserverGestureRecognizer`. So the code read as complete, and the accessor added alongside it (`touchObserver`, documented "so a host can allow it to run simultaneously") had **zero callers**. **Mechanism:** a recognizer with no simultaneity permission is sent `touchesCancelled` the instant another recognizer recognizes; for the observer that is UIKit's text-selection interaction claiming the long press — i.e. the exact moment the word gets selected. The observer subtracted the touch, reported a count of **0**, and `SelectionCommitPolicy` read that as a release with a live selection, scheduled the hop, and committed. **Why every gate stayed green:** `SelectionCommitPolicyTests` (14 tests, including "does not commit while a finger is down at any elapsed time") drives the policy *directly* and never involves a gesture — the policy was correct throughout; what broke was which events reached it. `SelectionCommitPolicyTests:70` even reads "one of two fingers lifts **or is cancelled** — still not a release", conflating the two at the one point where the difference decides everything. **Fix, in two parts:** (1) `TouchObserverGestureRecognizer` now owns its delegate (`AlwaysSimultaneousDelegate`, retained because `UIGestureRecognizer.delegate` is weak), so no host can drop the wire again, and the dead `touchObserver` accessor is deleted rather than left as an invitation to re-create the dependency; (2) the observer reports a `Cause`, and the policy **refuses to arm a commit on a cancellation**, so 03 §5's "never on a cancelled touch" is now true of the state machine by construction instead of true only while the wiring holds. 4 new tests. **Unverified until built and used:** part 1's effect is only observable in a real gesture — see the note at the foot of `SelectionCommitPolicyTests` for why that case is not unit-tested and why its absence is precisely how this reached a human. ⚠️ **If part 1 somehow does not hold on device, part 2 converts the failure from "commits too early" into "never commits"** — more visible, and it does not write unwanted data. | `TextSelectionController.swift` · `HighlightableTextView.swift:278` · 13 §4.4 · 06 §Selection lifecycle |
| X-p2 | **RESOLVED — fixed 2026-08-04 and CONFIRMED BY LUKE ON A REAL DEVICE the same day: "it's working without jumping right now."** This is the row his words settle most directly, the reported symptom being the jump itself | **The page jumps backwards when you start your SECOND highlight, because the first highlight's scroll anchor is never cleared.** `ScrollLockCoordinator.selectionAnchor` records where the view sat when a selection began, so the text interaction cannot scroll it away mid-drag. The old view cleared it on every successful commit (`ExegesisVerseView.swift:849`, `clearNativeSelectionScrollAnchor(reason: "selectionCommit complete")`). **Phase 4.4's move dropped that call**, and on the successful path nothing else makes it: `releaseAll` is reached only from the three *failure* exits, and the "selection emptied" route that would otherwise fire is deliberately suppressed during a commit by `ignoresEmptySelectionUntilRelease`. So the anchor outlives the highlight. On the next long press `freeze()` resolves its offset as `frozenOffset ?? selectionAnchor ?? contentOffset` — the stale anchor wins over the view's real position — and immediately `setContentOffset`s the scroll view back to wherever the *previous* highlight was made, then `captureSelectionAnchorIfNeeded` declines to re-capture (`guard selectionAnchor == nil`) and the guard timers re-assert it at 0/0.05/0.2/0.5/0.75s. **A second step was dropped in the same place:** the old commit wrapped its whole teardown in `preservingEnclosingScrollPosition(targetOffset: anchor)` — `resignFirstResponder()` makes UIKit scroll, and unpinned it scrolls unopposed — and called `onRangeSelected` *inside* that window. **Detected the same way as §X-p** (diff the new service against `git show HEAD:…ExegesisVerseView.swift`), and the tell was the same: **three of `ScrollLockCoordinator`'s methods had NO callers anywhere** — `preservingScrollPosition`, `clearSelectionAnchor`, `textInteractionAnchor` — i.e. the refactor kept the machinery and lost the calls. **Fix:** the commit path restores both steps, **and** `releaseFreeze` now clears the anchor itself, so an anchor can never outlive its freeze regardless of what a caller remembers — the structural version, per §X-p's lesson. | `ScrollLockCoordinator.swift` · `HighlightableTextView.swift` §commit · 13 §4.4 |
| X-p3 | **FIXED 2026-08-04, build + 200 tests green — STILL UNCONFIRMED BY A HUMAN.** Luke's device check covered making highlights and the absence of the jump; it did not exercise this one, which needs the specific sequence *highlight → delete → highlight the same words again*. Found by sweeping for the §X-p pattern, before any human hit it | **Re-highlighting a span you have already highlighted once did nothing, silently, for the life of the view.** `TextSelectionController.resetCommitDedupe()` — "Forget the last committed range so an identical selection commits again — call after the surface consumes or discards a highlight" — was **called from nowhere**. It is the replacement for three resets the old view performed (`ExegesisVerseView.swift:501` when native selection was disabled, `:751` in `setSelection`, `:759` in `clearSelection`), none of which survived the move. With `lastEmittedRange` never cleared, `commit()` fails its `!NSEqualRanges(snapped, lastEmittedRange)` guard and exits through `releaseAll(reason: "commit duplicate or empty")` — no highlight, no error, no feedback. Reachable by: highlight a phrase → delete it → try to highlight the same phrase again. **Found by a systematic sweep** of every member in `Services/Highlighting/` for definition-only symbols, run precisely because §X-p and §X-p2 were the same defect twice and waiting for a human to hit the third was not a plan. It was the only remaining one. **Fix:** reset on a new touch sequence (unambiguously the user starting something new, and it cannot race the duplicate-hop protection, which lives inside a single release), plus on selection being disabled, mirroring the old site. | `TextSelectionController.swift` §resetCommitDedupe · `HighlightableTextView.swift` §attach |
| C-d | **OPEN — a method for the pipeline, not a bug** | **Three regressions in one refactor shared one signature, and it is cheaply detectable: a moved behaviour whose *code* arrived and whose *call* did not.** §X-p (`observer.delegate = self`), §X-p2 (`clearSelectionAnchor()` + the `preservingScrollPosition` wrapper) and §X-p3 (`resetCommitDedupe()`) are the same defect three times. In every case the destination file contained the right machinery, correctly written and often carefully commented, with nothing calling it — and in two of the three the *host-side* half was carried over faithfully, so the code read as complete. **No gate in phases 4 or 5 could see any of it**: 196 iPhone tests and 29 client tests all drive units directly, and a unit test cannot observe that nothing calls the unit. **The two checks that did work, in minutes each:** (1) `git show HEAD:<old file>` diffed against the new service for behaviours whose call sites vanished; (2) a sweep for symbols that appear exactly once in the codebase — their own definition. **Proposal: both become standard for any "extract a service from an existing view" phase**, run at the end of the phase rather than after a user reports something. Recorded as a convention finding because the cost of not doing it here was three regressions in shipped-quality code, two of which a human had to find by using the app. | 13 §4.4 · REFERENCE §7 |
| X-q | **CONTRACT AMENDMENT (03 §5, dated 2026-08-04) — requested by Luke, implemented same session. RATIFIED by Luke: "I dont' want the verse tapping, I want consistent highlighting. so that's okay" — the removal of whole-verse tap-to-select is deliberate and approved, not a side effect anyone has to live with.** Not a regression: it changes a rule the spec had settled | **The Read editor now selects by tap-and-hold at WORD granularity, like the Exegesis editor. It used to select whole verses by tapping.** Luke, using the app: *"text highlighting on a read block of a read activity is not working… I should be able to tap + hold to highlight just like exegesis… All I can do is tap a verse to highlight the entire verse, this should be using the same highlight service."* **It was already using the service** — 4.11 made `SelectableLockedBlockView` a thin wrapper over `HighlightableTextView` — but with `mode: .verseTap, granularity: .verse`, which is exactly what `03 §5`'s granularity table specified and what the **pre-refactor** view did (`git show HEAD:…SelectableLockedBlockView.swift:50` — `isSelectable = false // We handle selection ourselves via taps`). So nothing had broken; the settled design was wrong for the user's need, and a phrase inside a verse was simply not expressible on that surface. **Changed to `mode: .nativeDrag, granularity: .word`.** `03 §5` amended in place with a dated note; the freeze is not violated in the sense that matters (no endpoint, field or payload moves — this is an interaction rule), but it is recorded as an amendment because §5 is normative for **both** consumers. **Two consequences, neither incidental:** (1) **tapping a verse no longer selects it** — that capability is removed rather than retained alongside, since two long-press-adjacent selection models on one surface is what this feature exists to stop; a tap now only opens an existing highlight. (2) **the scroll lock starts running on this surface for the first time** — `.verseTap` never became first responder, so `TextSelectionController` was never even constructed for it, and native drag makes the text view first responder inside a SwipeableCard inside a ScrollView. **Checked before changing, not after:** the parent already sets `canDrag = highlightingBlockId == nil`, so drag-to-reorder is off during highlight mode and the long press is uncontested; `isSelectionEnabled` is true only in highlight mode, so no stray handles appear elsewhere (monday#12668695071); the public shape is untouched, so the capture `ViewRegistry`'s memberwise init still resolves and captures (which never enable selection) render identically. **→ Opens X-r: the WEB read pane is now divergent.** | `SelectableLockedBlockView.swift` · 03 §5 · 06 §Adopting surfaces · X-r |
| X-r | **RESOLVED 2026-08-04 — ported the same session; gates green, UNSEEN in a browser.** DECIDED (Luke): option (a) — PORT IT. His stated principle, "I want consistent highlighting", is exactly the criterion this row needed, and it is the feature's whole thesis; a permanent per-platform split would contradict it. Reopens phase 5 (client) | **The web Read editor still selects whole verses by tapping; the iPhone one now selects words by dragging.** `03 §5` is normative for **both** consumers, and REFERENCE §3 rule 4 is explicit that a divergence must be a decision and never an accident — so this row exists to make it one. The web pane (`edit-read-activity-pane.vue` + the `selectable-locked-block-view` twin) implements the *old* rule via `applyVerseTap`, and it is a faithful port of what iOS did until today. **The pieces for parity already exist on the web side:** `exegesis-verse-view.vue` does native-selection drag with pointer-release commit, and `snapToWordBoundaries` was moved to `resources/js/utils/verse-selection.ts` in phase 5.6 precisely so it could be shared. So this is a port of a known-good pattern between two web components, not new design. **Options:** (a) port it, reopening phase 5 — keeps the two platforms honest, and is the only option consistent with "one behaviour everywhere"; (b) accept the divergence with a dated rationale in `02-app-impact.md`; (c) revert §X-q. **Not decided here** — Luke asked specifically for the iPhone Read activity, and quietly rewriting the web editor's interaction model on the back of that request would be exactly the kind of scope drift this pipeline exists to prevent. **Done:** the `selectable-locked-block-view` twin now selects by native drag with a pointer-release commit and the shared `snapToWordBoundaries`, mirroring `exegesis-verse-view.vue` line for line; a click still reopens an existing span (`openSelection`). **The pane needed no change at all** — it already listened for `@confirm` / `@open-selection`, and both are still what the twin emits, so the style-picker flow is untouched. **The old styling actively forbade the new interaction:** `--seg--tappable` set `user-select: none`; it is replaced by `--seg--interactive` (`cursor: text` + a `::selection` wash at the contract's live colour). **Capture safety is asserted, not assumed** (REFERENCE §3 rule 6): every part of the new interaction is gated on `interactive`, which captures never pass, and 4 new tests pin that the non-interactive DOM has no `data-start` and no interactive class, that the styled spans and text are identical either way, and that clicks do nothing when not interactive. Gates: build ✅, vitest ✅ **33**, phpunit ✅ 235, guard ❌ blocked pre-existing but unchanged at 853 (a `#fff` I introduced was tokenized rather than left). | 05 §LeaderApp · `edit-read-activity-pane.vue` · X-q |
| G-y | **OPEN — pre-existing, NOT caused by this feature; found by the §X-q delta audit** | **`ExegesisHighlightModal` is a dead surface: a registered route with no presenter.** `Route.swift:57` declares `case exegesisHighlightModal` and `:119` gives it an id, the file is in the build (4 `project.pbxproj` references), and **nothing anywhere calls `overlayManager.present(.exegesisHighlightModal, …)`** — the only reference to the type outside its own file is a doc comment. It also contradicts itself: its header says *"Uses system text selection (becomeFirstResponder) … matching the Bible reader pattern"*, but it never passes `usesNativeTextSelection: true`, so it resolves to the verse-tap model instead. **Verified NOT ours:** `git show HEAD:…ExegesisHighlightModal.swift` has a byte-identical call site and the old `ExegesisVerseView` defaulted the flag to `false` too, so this predates the feature. Recorded rather than fixed because deleting a routed screen is a product decision, not a refactor — but it is exactly the §C-d shape (the code exists, the call does not), which is why the sweep found it. **Its practical effect on this feature is nil and its effect on §G-s is large:** it is the last thing that looked like a live verse-tap surface, and it isn't one. | `ExegesisHighlightModal.swift` · `Route.swift:57,119` · G-s · C-d |
| G-z | **OPEN — pre-existing twin-geometry gap, MEASURED 2026-08-05 by phase 6's first diff. Not caused by this feature** | **The `SelectableLockedBlockView` web twin's text block is displaced from the iPhone's — further left and higher — so line breaks diverge.** Measured at `pro-max`: `NoSelections` 5.59%, `PreviewHighlightStyle` 14.01%, `WithHighlight` 18.92%. **The spread is the finding, not the peak.** The displacement is variant-independent, so with no wash only thin glyph outlines disagree; add a large solid highlight wash and the same displacement produces solid mismatched bands. iPhone breaks line 1 after "…and the Word", the web fits "…was" as well. **Confirmed NOT ours:** the only rules this feature changed on the twin are a background colour, a `font-weight`, and replacing a `cursor`/`user-select` rule — none is a geometry property; and the colour itself is confirmed identical on both platforms by eye. **Why it went unnoticed until now:** before the colour change this surface's captured highlight was solid brand purple on iPhone and solid brand purple on web — equally displaced, equally wrong, and nobody had reason to diff it. The feature did not create the gap; it created the first occasion to look. Out of scope to fix (twin geometry is reverse-engineered pixel work, and `compare-twin` memories warn against casual edits), so it is recorded with numbers so the next person starts from evidence. | `selectable-locked-block-view.scss` · 15 §6.2 · 07 |
