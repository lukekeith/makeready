# Triage refresh — 2026-07-19

Re-review of all 22 open items on **Ongoing Tasks Tracking** (`18413909869`) against `main` @ `ec028ba`, run with `/monday-review`. Verdicts per ticket with code/commit evidence. Builds on the 2026-07-05 work organization ([README](README.md), [code-fixes.md](code-fixes.md), [ui-ux.md](ui-ux.md)) — work-item IDs (CF-x/UX-x) refer to those docs.

**Counts: 0 verified fixed · 4 likely fixed · 12 outstanding · 5 need clarification · 1 answered question.**
No tickets were closed. The 2026-05-20 squash-merge (`fc69ddb`) erased pre-monorepo commit history, so several fixes present on main can't be tied to a fix commit; the LIKELY FIXED items need a device/browser check before closing. Housekeeping: moved already-Done item 12101610091 "Stronger scripture adding" from Backlog to the Completed group.

## What changed since the 2026-07-05 analysis

- **Study-sync landed** (PR #1, commits `0e27364`→`cf3eb5d`, merged `718e606`, spec `docs/plans/study-sync-2026-07-06.md`). Enrollments are now self-owned versioned lesson copies; curriculum edits propagate only via explicit "Publish updates" + per-enrollment opt-in sync (`syncMode` default OFF). This addresses the snapshot-drift cluster (CF-3…CF-7 territory) but makes "unpublish should kick groups" intentionally *not* the model.
- **Org-role authorization shipped earlier** (`8efbb7c` 2026-06-20, `3afaa00` 2026-06-21): `canManageOrgContent` + filters in `server/src/services/permission.ts:277-347`, wired into ~31 handlers. Fixed the unenroll lockout; did NOT add per-role granularity (CF-1 still open as a policy question).
- **Leader preview exists** (`server/src/routes/study-preview.ts`, `client/routes/web.php:93-99`, `ProgramHomePage.swift:994`) — but the "Open Lesson" button still routes leaders through the member `/join/study` phone-auth flow (UX-1 half-done).

## Per-ticket verdicts

### Likely fixed — manually verify, then close with evidence comment

| Ticket | Verdict evidence |
|---|---|
| ~~12268478769 add bible verse unclear~~ | **RESOLVED-PENDING-VERIFY** (2026-07-19): deep dive overturned the LIKELY FIXED verdict (source menu predated the complaint — squash `fc69ddb`); fixed in `84be9f8` — `+` replaced by two labeled buttons, source menu removed. Runtime check pending; see dossier resolution log |
| 12268576962 calendar breaks when unpublishing | `POST /enrollments/:id/schedules` creates versioned schedules members render — `server/src/routes/enrollments.ts:4174-4333`, study-sync `0e27364`…`b0e17cd` |
| 12268578241 covering text | Bottom button removed → top nav pill (`3529dba`); READ reserves live header height (`read-step.vue:113`, `--member-lesson-header` var) |
| 12270302158 rescheduling | Published badge re-opens publish/draft dialog; enrollment sync pane added — `ProgramHomePage.swift:543-570`, `program-home-modal.vue:291-303`. (Days/frequency editing still missing — see 12268648378) |

Sub-reports likely fixed inside mixed tickets: exegesis last-verse highlight crash (range clamping, `ExegesisVerseView.swift:495-656`); phantom-text/autosave (markdown editor rework, `MarkdownEditor.swift:15-21`, `EditReadActivityPage.swift:1161-1167`); heading picker now a native Menu (`MarkdownEditor.swift:610-646`); exegesis highlight edit/delete via `exegesisHighlightActionMenu` (`EditExegesisActivityPage.swift:347-382`); unenroll lockout (`3afaa00`, `enrollmentManageFilter` at `enrollments.ts:1938` — the 6/23 and 6/27 "can't unenroll" reports were 2–6 days after the fix, likely stale deploy); keyboard covering input (`interactive-widget=resizes-content`, `fded371`); SMS-consent blocking join (`471b06c`, `eef13d4`); "empty lessons" from rearrange-then-enroll (study-sync `04022a3`, `b0e17cd`, `9b0efb5`).

### Outstanding

**Root cause spanning 4 tickets** (12415667946 member-video, 12415662995 member-reading, 12297336134 "stuck" URL-bar report, 12344966853 bottom-story report): member lesson player has **no `env(safe-area-inset-top)` anywhere** despite `viewport-fit=cover` (`ce6bf4e`); header uses flat `padding: 8px` (`member-lesson-header.scss:1-5`). Exegesis step uses a **static 120px top inset** instead of READ's dynamic `var(--member-lesson-header)` (`exegesis-step.vue`, matches CF-10). Fix: safe-area padding on the header + dynamic inset for exegesis.

| Ticket | Still broken |
|---|---|
| 12268464531 publishing toggle error | Unpublish never warns/kicks enrollments (`programs.ts:905-991`; iPhone `ProgramHomePage.swift:559-561`; web `program-home-modal.vue:251,299`) — now *by design* under study-sync. Product decision (CF-6 question in README): warn, or reply explaining the model. Lesson-drift half likely fixed by study-sync (opt-in only). |
| 12268648378 no way to change enrollment calendar | `PATCH /enrollments/:id` accepts only `requireResponse/smsTime/timezone/syncMode` (`enrollments.ts:1586-1667`); `enabledDays` written only at creation (`:254`), no reschedule endpoint. = UX-10 |
| 12297338039 error log | Long read title: server zod caps 200 chars (`programs.ts:3931,2045,3765`), no client-side guard; now surfaces friendly error (`EditReadActivityPage.swift:1186-1194`) but still can't save. Other sub-reports likely fixed or need clarification ("transfer stuck" — no transfer flow exists in code). |
| 12325580210 UX change needed | Group Leader can still delete an org-mate's study: `mutationFilter` = creator OR any-org-role (`programs.ts:1402-1434`, `:63-71`; `permission.ts:277-311`). = CF-1, now intentional org-scoping with no role granularity — policy decision. Highlight-editing half likely fixed. |
| 12344861586 UX update please | Group can enroll into ANY published study system-wide — program lookup only `{id, isActive}` (`enrollments.ts:143-148`), no sharing/org-consumption flag in schema (`StudyProgram`, schema.prisma:847). = CF-2/UX-8. "Open Lesson" still member phone-auth flow (`GroupHomePage.swift:745` → `enrollments.ts:2480` → `/join/study/...`) despite preview feature existing. = UX-1. Member note-review surface still absent. |
| 12344966853 UI update please | Full birthday still required, no min-age (`profile-form.blade.php:59-64`); reading themes still shipping (theme-typography redesign at Phase 0); bottom-story coverage = safe-area gap. |
| 12344966891 UI not updating | Server enrollment delete cascades cleanly (`enrollments.ts:1929-1956`, schema cascades); lingering deleted enrollment = client stale-cache, no targeted fix found. = CF-7. Note: 2026-07-05 analysis (CF-4) also flagged missing `isActive` filters on program soft-delete in list endpoints — reconcile when fixing. |
| 12415690223 member - error | Same stale-cache class + CF-4 program-soft-delete filter question. |
| 12386101354 error member experience | **DEEP-DIVED 2026-07-20 — MIXED.** Sub-1 "nav covering verse" = the exegesis overlap **already fixed** by `fbcd848` (ticket 12415662995). Sub-2 "empty lessons" = a blank member **READ render** (NOT a zero-lesson study — corrects the CF-19 guess); screen confirmed (`read-step.vue` + `ActivityPreviewPlayer`), root cause provisional. Recommended: client empty-state/auto-advance guard when `payload.blocks` is empty. Turnkey repro: /preview/lesson/f244a81d.../1 (Test Day 1). Dossier has full pick-up notes. |
| 12415667946 member - video | **✅ CLOSED — Done (03e1367 + 1e3f035, 2026-07-19).** Deep dive corrected the review guess: "strange message" = literal **"No video URL configured"** = `youtube-step.vue` empty-URL fallback (NOT header status text); step is a YouTube activity. Fix: header safe-area-inset-top + empty state → warning icon + "Video never specified." note (empty activity is pre-rule leftover data; no Continue button — header step-nav advances). User verified in-browser on real data (King Saul → Genesis 1:1-5, step 6). Follow-up idea: confirm the publish/enroll "no incomplete activities" guardrail in the leader editor. |
| 12415662995 member - reading issue | **✅ CLOSED — Done (fbcd848, 2026-07-19).** Exegesis step, first verse clipped under the expanded header. Fix: `exegesis-step.vue` static 120px top inset + mask → `calc(var(--member-lesson-header, 200px) + 16px)` (mirrors read-step; also absorbs the safe-area header height from 03e1367). User verified on the real member route (Young Professionals, Genesis 1:6-10, 6 highlights). Thread 2 (no passage reference rendered) left as a separate enhancement. |
| 12297336134 stuck | URL-bar/safe-area sub-report outstanding; rest likely fixed. |

### Needs clarification

| Ticket | Ask |
|---|---|
| 12081607589 confused with notes in group | No detail; nothing maps in code. Which screen? |
| 12101572041 inspirational notes | No detail. (FR-3-adjacent per README.) |
| 12268645785 validation error → **SMS verification fails to send** | Deep-dived 2026-07-19: screenshots show "Failed to send verification code" = Twilio Verify send failure (not the E.164/CF-9 guess). **Split** the unrelated Jun-20 exegesis-save report to new ticket **12572733385**. Applied a diagnostic fix (join controllers now surface the real Twilio `error`); root cause still pending the Twilio server log (ops). |
| 12572733385 iPhone exegesis editor — "Couldn't save changes" | **RESOLVED-PENDING-VERIFY** — root-caused from code (client exact-match create-vs-update vs server non-overlap), fixed in `e8e9718` (merge overlapping notes into the existing highlight). Monday status "Verify"; runtime sim check pending. Secondary stale-ID-after-passage-change trigger left as follow-up. |
| ~~12268465402 should say lessons~~ | **CLOSED (Done)** 2026-07-19 — deep dive pinned it to the Program Home tab slider; renamed "Studies"→"Lessons" on iPhone + web twin + fixture in `fdb1b31`. Answers CF-13. |
| 12297345805 suggestion (combine read/exegesis) | Product call, not implemented; read editor's add-menu already uses suggested wording ("Bible verse"/"Custom text"). = UX-7 |

### Answered question

**~~12271625826 image optimize~~ — CLOSED (Done) 2026-07-19** — images are NOT stored full-res: every upload path runs sharp → max 1200px JPEG q85 "original" + 400px/150px variants → R2 (`media.ts:1206-1215`, `programs.ts:3658-3663`, avatars 512px `members.ts:1357`, writer `storage.ts:194`). Answered on the ticket (optimized, but not ideal — JPEG-only + coarse ladder; the hero cover even loads the 400px `-md` and looks soft). Follow-up Feature **12572712291** opened for WebP/AVIF + right-sized delivery. = CF-18 done.

## Next actions

1. **Safe-area fix** (member-lesson-header + exegesis inset) — one small CSS change clears the biggest complaint cluster (4 tickets).
2. **Device/browser check** of the 4 LIKELY FIXED tickets → close with evidence comments via `/monday-review --apply` or manually.
3. ~~Post the image-optimization answer on 12271625826 and close.~~ ✅ Done 2026-07-19 — closed; follow-up Feature 12572712291 opened.
4. **Product decisions:** unpublish warning (CF-6), delete/enroll role policy (CF-1/CF-2), Open Lesson → preview routing (UX-1).
5. **Clarifying questions** on the 5 NEEDS CLARIFICATION tickets.
