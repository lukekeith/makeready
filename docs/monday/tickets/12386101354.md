---
ticket: 12386101354
title: error member experience
board: 18413909869
url: https://scotts-team283817.monday.com/boards/18413909869/pulses/12386101354
created: 2026-06-27
reporter: Scott Stickane
status: open
verdict: OUTSTANDING
last_review: 2026-07-19
deep_dive: none
affected_areas: provisional
---

# error member experience

## Problem
Two member-web reports: (1) **"Empty lessons"** — a study renders with no lesson content (same reorder-then-enroll snapshot class as 12297336134's sub-report; = CF-19, likely fixed by study-sync); (2) **"Navigation covering the verse"** — in the exegesis step the top of the verse and the Bible address are hidden under the lesson header (= CF-10, **outstanding**: the exegesis step still uses a static 120px top inset instead of the dynamic header variable the read step consumes). "Fixed" observably: enrolled studies always show content (or an explicit empty-state), and the full verse including its reference is visible below the header.

## Reports (verbatim)
- (2026-06-27) "Empty lessons." (screenshot)
- (2026-06-27) "Navigation covering the verse."

## Screenshots
Not yet viewed (attachments exist on the monday item — run /monday-ticket; the updates reference at least one screenshot).

## Affected areas
> `provisional` — inferred from report text + code analysis; screenshots not yet viewed. /monday-ticket must confirm before /monday-resolve will act.

### In scope
- **Platform:** web-member (both sub-issues).

Sub-issue 2 (nav covering verse, CF-10):
- **Screen:** exegesis step — `client/resources/js/components/domain/lesson-island/steps/exegesis-step.vue` (hardcoded `padding: 120px 16px 24px` at :607; 120px fade mask :611-625)
- **Component(s):** header height publisher — `client/resources/js/components/domain/lesson-island/member-lesson-header.vue` (:10-17 publishes `--member-lesson-header`); reference implementation — `read-step.vue:113` consumes the variable

Sub-issue 1 (empty lessons, CF-19):
- **Logic:** enroll-time snapshot copy — `server/src/routes/enrollments.ts:282-503`; client silently drops empty-id lessons — `client/resources/views/pages/group-home.blade.php:42-55`; no empty-state — `client/resources/js/components/domain/member-studies-island/member-studies-island.vue:51-96`
- **Endpoints:** enrollment read endpoints (render path); none change for the CSS fix

### Explicitly out of scope
- Sibling lesson-player steps — `read-step.vue` (already migrated to the header variable — it is the *model*, don't touch), `video-step.vue`, `input-step.vue`, `complete-step.vue` under `client/resources/js/components/domain/lesson-island/steps/` — separate files with their own tickets (12415667946 video, 12297336134 input).
- iPhone exegesis rendering (`ExegesisVerseView.swift`) — leader-side, different platform, not this report.
- Leader web exegesis editor pane (`client/resources/js/islands/leader-app/components/edit-exegesis-activity-pane.vue`) — authoring surface, not the member player.
- The safe-area-*bottom* nav fix (CF-11) — same cluster, tracked under 12297336134/12344966853.

### Cross-app impact
None per the root CLAUDE.md table for the CSS fix (web-member only). Sub-issue 1 was server-side; study-sync landed — both member consumers render the corrected schedules.

## Root cause
- **CF-10:** `exegesis-step.vue:607` hardcodes a 120px top inset (plus a 120px fade mask at :611-625) while the header is dynamic and publishes its real height to `--member-lesson-header` (`member-lesson-header.vue:10-17`); the read step already consumes the variable (`read-step.vue:113`) — exegesis was never migrated and ignores `safe-area-inset-top`. Fix: replace the hardcoded inset/mask with `calc(var(--member-lesson-header, 200px) + 16px)` + safe-area, mirroring read.
- **CF-19 (likely fixed):** reorder-then-enroll could snapshot an empty/broken lesson set; study-sync commits `04022a3`, `b0e17cd`, `9b0efb5` reworked snapshot creation. Client guardrail (explicit empty-state) still recommended.

## Verification
- **Sub 2 (outstanding):** code-level — `exegesis-step.vue:607` static 120px vs. the dynamic var; repro: member exegesis lesson on a phone, expand/scroll the header → verse top and Bible reference hidden.
- **Sub 1:** repro reorder→enroll on main; expect content. If still empty, capture the enrollment API response.

## Triage history
- 2026-07-05 analysis: mapped to CF-10, CF-19 — exegesis static inset + empty snapshot lessons.
- 2026-07-19 /monday-review: OUTSTANDING — exegesis nav-coverage outstanding (static 120px inset, matches CF-10); "empty lessons" likely fixed by study-sync.

## Resolution log
(empty)
