---
ticket: 12268648378
title: no way to change enrollment calendar
board: 18413909869
url: https://scotts-team283817.monday.com/boards/18413909869/pulses/12268648378
created: 2026-06-13
reporter: Scott Stickane
status: closed
verdict: RESOLVED (iPhone — edit-enrollment flow landed; web parity gap tracked separately)
last_review: 2026-07-23
deep_dive: none
affected_areas: provisional
type: none
app: none
nature: none
---

# no way to change enrollment calendar

## Problem
Once a group is enrolled in a study, the leader cannot change the enrollment's cadence — days of the week, time, or frequency. The server accepts no such edits and neither client has a screen for it. "Fixed" looks like: an enrollment-schedule management surface (edit cadence, reschedule) backed by a server capability to rewrite the enrollment's schedule. This is the UX-10 cluster — the largest UX item — and overlaps the "living studies" feature requests (12270300418/12354285433/12270300519).

## Reports (verbatim)
- (2026-06-13) "I want to change the days and time and frequency of the study but can't."

## Screenshots
Not yet viewed (attachments exist on the monday item — run /monday-ticket).

## Affected areas
> `provisional` — inferred from report text + code analysis; screenshots not yet viewed. /monday-ticket must confirm before /monday-resolve will act.

### In scope
- **Platform:** multiple — server capability gap + a new leader screen on iPhone and web
- **Logic (server):** `PATCH /api/enrollments/:id` — `server/src/routes/enrollments.ts:1586-1667` accepts only `requireResponse/smsTime/timezone/syncMode`; `enabledDays` written only at creation (`:254`); no reschedule endpoint. Existing per-lesson schedule-add: `POST /enrollments/:id/schedules` (`enrollments.ts:4062`, versioned form :4174-4333)
- **Screen (iPhone leader):** enrollment schedule — `iphone/MakeReady/Pages/Manage/Group/Enrollment/EnrollmentSchedulePage.swift` (the natural host for cadence editing)
- **Screen (web leader):** LeaderApp program-home/enrollment panes — `client/resources/js/islands/leader-app/components/program-home-modal.vue`
- **Endpoints:** new/extended `PATCH /api/enrollments/:id` (cadence fields) or a dedicated reschedule endpoint — to be designed

### Explicitly out of scope
- Study-level day/lesson editing (`programs.ts` lessons) — the study's curriculum, not the enrollment's calendar.
- 12270302158 (navigating back to the publish/enrollment screen) — sibling UX-10 ticket with its own dossier; design them together, track separately.
- Feature requests 12270300418/12354285433/12270300519 ("living studies", skip/insert lesson) — overlapping roadmap items, not this bug's minimum scope.
- Member-facing calendar rendering (member group home, lesson player) — consumers of the schedule, unchanged unless the schema changes.

### Cross-app impact
Per the root CLAUDE.md table: new API capability + schema-adjacent change → both consumers (iPhone and web-leader) need the new screen/controls; member surfaces re-render the resulting schedules but need no code change unless response shapes change.

## Root cause
Capability gap, not a defect: cadence fields are creation-only (`enrollments.ts:254`), the PATCH whitelist excludes them (`:1586-1667`), and no client screen exists to host editing. = UX-10 in [ui-ux.md](../ui-ux.md).

## Verification
- **Code-level:** `enrollments.ts:1586-1667` — confirm the PATCH schema still lacks days/time/frequency fields.
- **Manual repro:** iPhone or web LeaderApp → group → enrolled study → look for any affordance to change days/time/frequency (none exists).

## Triage history
- 2026-07-05 analysis: mapped to UX-10 — enrollment schedule management (edit cadence, reschedule, re-open publish screen); design once for the whole cluster.
- 2026-07-19 /monday-review: OUTSTANDING — `PATCH /enrollments/:id` accepts only `requireResponse/smsTime/timezone/syncMode` (`enrollments.ts:1586-1667`); `enabledDays` written only at creation (`:254`); no reschedule endpoint.

## Resolution log
- 2026-07-23 — **Verified resolved (code-level) + closed on monday (Done, Completed group).**
  Capability gap is closed on iPhone: leaders can edit an enrollment's days, time, and
  frequency via the **Edit enrollment** flow.
  - **Server:** `PATCH /api/enrollments/:id` (`server/src/routes/enrollments.ts:1577`) now
    accepts `startDate`, `enabledDays`, `smsTime` (+ `groupId`/`studyProgramId`); structural
    edits route through `applyEnrollmentEdit`/`applyStudySwap`, rebuilding the schedule.
    Was previously `requireResponse/smsTime/timezone/syncMode` only (the gap this ticket named).
  - **iPhone:** `EditEnrollmentFlowModal` → `SelectEnrollDatePage`/`DayOfWeekPicker`
    (S·M·T·W·T·F·S `toggleDay` → `enabledDays`, ≥1-day guard, start date + per-date overrides)
    + `ConfirmEnrollmentPage` time picker (`smsTime`, `.hourAndMinute`). Dry-run
    `POST /enrollments/:id/edit/preview` warns on lesson-impacting changes; commit via
    `patchBody()` (changed-fields only). Action-menu entry gated by `canManage`
    (managed-groups fallback pre-deploy, `f4fbf33`).
  - **Commits:** `9742424` (enrollment action menu) + `c836ad3` (edit-enrollment flow),
    landed under sibling `12270302158`.
  - **Verification:** code-level only (not built/on-device). "Frequency" = the enabled-days
    set (no separate interval field). Deploy dep: server `canManage` flag.
  - **Out-of-scope carryover:** web LeaderApp has **no** edit-enrollment UI
    (`group-home-modal.vue:225` omits `onEditEnrollment`) — parity gap, tracked separately.
  - **Monday:** evidence comment (update `5400542287`); status → **Done**; moved to
    **Completed** group.
