---
ticket: 12268578241
title: covering text
board: 18413909869
url: https://scotts-team283817.monday.com/boards/18413909869/pulses/12268578241
created: 2026-06-13
reporter: Scott Stickane
status: open
verdict: LIKELY FIXED
last_review: 2026-07-19
deep_dive: none
affected_areas: provisional
---

# covering text

## Problem
In the member web lesson player, on an **old (legacy-content) study**, lesson text is hidden behind a button/control band — a "holdover error" on legacy content after the general overlap fix. At analysis time, modern themed blocks reserved footer space via `--member-lesson-footer` but the legacy `readContent` path did not honor the same inset. "Fixed" looks like: legacy-content read lessons scroll fully clear of any overlaid controls. Likely fixed by the bottom-button removal (`3529dba`) — verify against one of the reporter's old studies before closing.

## Reports (verbatim)
- (2026-06-13) "I can't see the text because of the button. You mentioned you fixed this, but this is an old study and I think it's a holdover error."

## Screenshots
Not yet viewed (attachments exist on the monday item — run /monday-ticket).

## Affected areas
> `provisional` — inferred from report text + code analysis; screenshots not yet viewed. /monday-ticket must confirm before /monday-resolve will act.

### In scope
- **Platform:** web-member (lesson player)
- **Screen:** READ step, legacy-content path — `client/resources/js/components/domain/lesson-island/steps/read-step.vue` (legacy `readContent` renders through the synthetic `legacy-read` / `themeSlug: 'none'` path at :174-186; dynamic header inset consumed at :113)
- **Component(s):** footer-inset handling in `client/resources/js/preview/ActivityPreviewPlayer.vue` (`--member-lesson-footer` reservation, :690-702)
- **Endpoints:** none (UI/CSS-only)

### Explicitly out of scope
- Sibling lesson-player steps — `client/resources/js/components/domain/lesson-island/steps/exegesis-step.vue`, `video-step.vue`, `input-step.vue`, `complete-step.vue` — separate step files with their own inset issues tracked under other tickets (12415662995 exegesis, 12415667946 video).
- Modern themed READ blocks — already reserve footer space; only the legacy/no-theme path is in question.
- iPhone lesson rendering — the report is the web member player.
- Header-side coverage (`member-lesson-header.scss` safe-area gap) — that is the 4-ticket safe-area cluster, not this bottom-band issue.

### Cross-app impact
None per the root CLAUDE.md table — CSS-only change in the web member player.

## Root cause
Per CF-12 ([code-fixes.md](../code-fixes.md)): modern themed blocks reserve footer space via `--member-lesson-footer` (`ActivityPreviewPlayer.vue:690-702`), but legacy `readContent` renders through the synthetic `legacy-read` / `themeSlug: 'none'` path (`read-step.vue:174-186`) which doesn't honor the same bottom inset. Since then the bottom button was removed in favor of a top nav pill (`3529dba`), and READ reserves live header height (`read-step.vue:113`, `--member-lesson-header` var) — which likely obsoletes the overlap.

## Verification
- **Code-level:** `3529dba` (bottom button → top nav pill) on main; `read-step.vue:113` consumes the dynamic header var.
- **Manual check (required before closing):** open one of the reporter's *old* studies as a member in a mobile browser → READ lesson → scroll to the end → confirm no control band covers the text.

## Triage history
- 2026-07-05 analysis: mapped to CF-12 — legacy read path missing the `--member-lesson-footer` inset modern blocks get.
- 2026-07-19 /monday-review: LIKELY FIXED — bottom button removed → top nav pill (`3529dba`); READ reserves live header height (`read-step.vue:113`, `--member-lesson-header` var). Verify on a legacy study before closing.

## Resolution log
(empty)
