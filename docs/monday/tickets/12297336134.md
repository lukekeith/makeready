---
ticket: 12297336134
title: stuck
board: 18413909869
url: https://scotts-team283817.monday.com/boards/18413909869/pulses/12297336134
created: 2026-06-17
reporter: Scott Stickane
status: open
verdict: MIXED (sub-5 FIXED pending device-verify `0581087` · sub-4 ALREADY FIXED · sub-1/2 iPhone-editor UX · sub-3 = 12386101354 blank-read)
last_review: 2026-07-24
deep_dive: 2026-07-24
affected_areas: confirmed
type: UI/UX
app: Multiple
nature: Bug
---

# stuck

## Problem
Grab-bag "stuck" ticket bundling **five** reports (all screenshots viewed 2026-07-21).
**Two platform corrections vs. the prior provisional dossier: sub-1 and sub-2 are the
iPhone editor, NOT web-member.**

1. **Stuck after highlighting in the iPhone read/exegesis editor.** *(CORRECTED — iPhone
   editor, not web-member.)* After entering highlight mode the screen dims to an overlay
   with only an X — no obvious way back. iPhone UX. Needs on-device repro.
2. **No "done" affordance in the iPhone exegesis note editor.** *(CORRECTED — iPhone
   editor, not the web input step.)* The note-input overlay (PREV/NEXT, keyboard up) has
   no clear complete/submit button; escaped only by dismissing the keyboard. iPhone UX.
3. **Member study renders blank after rearranging lessons then enrolling.** web-member
   (+ server snapshot). **This is the same blank-read render as ticket 12386101354** — and
   it supplies the REPRO that ticket lacked: *rearrange the lessons, then enroll*. = CF-19
   enroll-time snapshot. Root cause still unpinned (the study, "Struggle Well", is deleted).
4. **Safari URL bar covers the lesson-player header/nav.** web-member. **ALREADY FIXED by
   `03e1367`** — `member-lesson-header.scss:10` now insets `env(safe-area-inset-top)`.
5. **iPhone read editor ("read activity"): can't reach the H1/H2/H3 heading picker.**
   *(User confirmed 2026-07-24: reporter's "Reid lesson" = a **read activity**.)* Selecting
   text in a custom-text block with the keyboard up pushes the block's formatting toolbar
   out of view. Screenshot 5 shows the selection with **no toolbar visible at all**, whereas
   the exegesis note overlay (screenshot 2) keeps its toolbar pinned. Root cause is now
   pinned by code (see below) — the toolbar is inline-at-top-of-block, not a keyboard
   accessory. iPhone UX.

## Reports (verbatim)
- (2026-06-17) "Read lesson. Highlighted. Now stuck."
- (2026-06-20) "I'm looking for a button to complete my Input. I get stuck here until I accidentally swiped down on the keyboard."
- (2026-06-23) "As a member the study is totally empty. I caused this error by rearranging the lessons in the study and then enrolling. If you look there should be content."
- (2026-06-23) "As a user I have moved to a different app and the UrL bar is covering the nav text. I don't know what to do now. The button at the top takes me to the next section."
- (2026-06-27) "It is impossible for me to create a Reid lesson and then go back to the top of that custom text that I've added in select it and then change the heading type. The UI scrolls down to the point where I can't get to the H1H2H3 heading selection."

## Screenshots (viewed 2026-07-21)
1. iPhone **Edit Activity** (read/exegesis editor) — scripture block "Matthew 9:4-7" in highlight mode (purple border + pencil); whole screen **dimmed with an X** bottom-right → stuck in a highlight overlay. → iPhone editor.
2. iPhone **Exegesis** note editor overlay — PREV / "1 / 1" / NEXT, rich-text toolbar, note "God used David…", keyboard up, only an X. → iPhone editor; no "done" affordance.
3. web-member (mobile Safari, app.makeready.org) — "Struggle Well / Day 1 / Continue reading", **blank content**, "Swipe to continue", ~3 block-markers. Identical to 12386101354's blank-read.
4. web-member — "Struggle Well / Day 1 / All highlights reviewed", Genesis 37 with blue highlights; the lesson-player header (the "nav text" the URL bar covered).
5. iPhone **Edit Activity** (read editor) — "This is an H1 title" selected at top; keyboard up; the H▾ heading toolbar is scrolled out of reach.

## Affected areas
> `confirmed` — all 5 attachments viewed; paths verified on `main`. Platform mapping
> corrected (sub-1/2 = iPhone). Per-sub-issue readiness differs (see below).

### In scope, by sub-issue
- **Sub-1 (iPhone editor, stuck-after-highlight):** `iphone/MakeReady/Components/Content/ExegesisVerseView.swift` + `iphone/MakeReady/Pages/Manage/Program/EditExegesisActivityPage.swift` / `EditReadActivityPage.swift` (highlight-mode overlay + its dismiss). Needs repro to pin the stuck overlay. Endpoints: none.
- **Sub-2 (iPhone exegesis note editor, no done affordance):** the note-input overlay in `EditExegesisActivityPage.swift` (PREV/NEXT highlight nav + note text) — add a clear done/save affordance reachable with the keyboard up. iPhone UX. Endpoints: none.
- **Sub-3 (blank read after reorder→enroll):** enroll-time snapshot copy — `server/src/routes/enrollments.ts` (readBlock copy ~:312, includes ~:575-578); member render `client/resources/js/components/domain/lesson-island/steps/read-step.vue` + `ActivityPreviewPlayer.vue`. **Same surface + symptom as ticket 12386101354** — reconcile there. Repro: reorder lessons → enroll → blank content.
- **Sub-4 (header safe-area):** `client/resources/js/components/domain/lesson-island/member-lesson-header.scss:10` — **already has** the top inset (`03e1367`). No work.
- **Sub-5 (iPhone read editor heading toolbar):** `iphone/MakeReady/Components/Input/RichTextInput.swift` — the `RichTextToolbar` is rendered **inline at the TOP of each editable block** ("Toolbar — rigid 44pt, never compresses" :28-29), with the heading `Menu` at :315 and applyHeading H1/H2 at :323/:329. Because it's laid out above the block's text (not attached as a keyboard `inputAccessoryView`), raising the keyboard on a block scrolls its toolbar above the visible area → the H▾ picker becomes unreachable (matches screenshot 5). **Proposed fix:** pin the active block's formatting toolbar as a keyboard accessory (or a sticky bar above the keyboard) so it stays reachable while editing. Shared component — affects every RichTextInput consumer (read + exegesis editors). iPhone UX; on-device confirmation still advised but the mechanism is code-visible.

### Explicitly out of scope
- The bottom group-home nav safe-area-inset-*bottom* (`navigation.scss` still lacks it) — a different surface than sub-4's lesson-player header; the "last card covered" bottom-nav report was 12344966853 sub-6 (which resolved to iPhone). Don't conflate.
- Web LeaderApp twin editor panes (`edit-read-activity-pane.vue`, `edit-exegesis-activity-pane.vue`) — leader web, not in these reports.
- Sibling lesson-player steps not implicated (video's affordance = 12415667946; exegesis verse inset = 12415662995, fixed `fbcd848`).
- `MarkdownEditor.swift` / `RichTextInput.swift` are shared across editors — a change affects every consumer (callout).

### Cross-app impact
Per root CLAUDE.md: sub-3 is server snapshot + both member consumers (reconcile with 12386101354). Subs 1/2/5 are iPhone-only UI. Sub-4 is web-member CSS (done).

## Root cause
- **Sub-1/2:** iPhone editor overlays (highlight mode; exegesis note input) lack an obvious exit/done affordance with the keyboard up — the user gets stuck. UX; needs repro to pin exact dismiss gap.
- **Sub-3:** reorder-then-enroll produces a member schedule whose read renders blank — the same case-B blank render as 12386101354 (blocks present but not visible), now with a concrete repro. Cause unpinned (study deleted; not reproducible locally without the data).
- **Sub-4:** member lesson header had no `env(safe-area-inset-top)`; **fixed `03e1367`**.
- **Sub-5:** the per-block rich-text toolbar (with the H▾ heading Menu) is rendered inline at the top of each block rather than as a keyboard accessory, so raising the keyboard scrolls it above the visible area and the H1/H2/H3 picker can't be reached (`RichTextInput.swift:28-29` toolbar placement; Menu :315).

## Verification
- **Sub-4:** code-level — `member-lesson-header.scss:10` has `padding-top: calc(8px + env(safe-area-inset-top, 0px))` (`03e1367`, post-dates the report). Close via /monday-review.
- **Sub-1/2:** on-device iPhone repro (highlight-mode exit; note-editor done button).
- **Sub-5:** code-level — `RichTextInput.swift:28-29` places `RichTextToolbar` inline above the block (not a keyboard accessory), so it scrolls off when the keyboard is up (matches screenshot 5); on-device confirmation optional.
- **Sub-3:** reorder lessons → enroll → open as member → blank content. Same as 12386101354; pin there.

## Triage history
- 2026-07-05 analysis: mapped to CF-11, CF-19, UX-2, UX-5 — safe-area nav, empty snapshot, missing input affordance, unreachable heading toolbar.
- 2026-07-19 /monday-review: OUTSTANDING — URL-bar/safe-area outstanding; rest likely fixed (study-sync; native Menu heading picker). (Provisional; screenshots not viewed.)
- 2026-07-21 /monday-ticket: MIXED — deep dive, all 5 screenshots viewed. **Corrections: sub-1 + sub-2 are the iPhone editor (not web-member); sub-4 ALREADY FIXED by `03e1367` (member-lesson-header top safe-area inset); sub-3 = the 12386101354 blank-read render and supplies its missing repro (reorder lessons → enroll).** Subs 1/2/5 = iPhone editor UX needing on-device repro. affected_areas → confirmed. Classified UI/UX · Multiple · Bug (sub-3 leans Data & Lifecycle).
- 2026-07-24 /monday-ticket: re-confirmed — all 5 screenshots re-viewed; every in-scope path re-verified on `main` (sub-4 fix `member-lesson-header.scss:10` still present; no commits touched these surfaces since 2026-07-21). **User clarified "Reid lesson" = read activity (sub-5).** Sub-5 root cause pinned in code: `RichTextToolbar` is inline-at-top-of-block (`RichTextInput.swift:28-29`), not a keyboard accessory, so it scrolls out of reach with the keyboard up — downgraded from "needs repro" to code-level. Classification unchanged (UI/UX · Multiple · Bug).

## Resolution log
- 2026-07-25 /monday-resolve — **Sub-issue 5** (read-activity heading toolbar unreachable).
  Resolution (user): "In RichTextInput.swift, pin the block's RichTextToolbar as a keyboard
  inputAccessoryView (sticky above the keyboard) so the H1/H2/H3 heading picker stays
  reachable while editing a read-activity text block."
  - **SCOPE CORRECTION (discovered during resolve):** the dossier's sub-5 mapping to
    `RichTextInput.swift` was **wrong** — that component is **dead code (zero consumers)**.
    Both the read-activity editor (`EditReadActivityPage.swift:733`) and the exegesis note
    editor (`EditExegesisActivityPage.swift:1019`) use **`MarkdownEditor.swift`**. The
    stale `EditReadActivityPage.swift:7` comment ("Uses RichTextInput…") caused the misread.
  - **Approved scope expansion (user, AskUserQuestion):** fix the shared `MarkdownEditor.swift`,
    accepting that it changes **both** the read-activity editor (sub-5) and the exegesis note
    editor (also improves sub-2's toolbar reachability).
  - **Implementation:** both `RichTextInput` and `MarkdownEditor` are pure SwiftUI
    (`TextEditor`), so there is no UIKit `inputAccessoryView`; the SwiftUI equivalent was used.
    In `MarkdownEditor.swift`, `MarkdownEditorToolbar` moved from inline-above-the-editor to
    a keyboard accessory: `.toolbar { ToolbarItemGroup(placement: .keyboard) { MarkdownEditorToolbar(...) } }`
    on the `TextEditor`; removed the now-unused inline `toolbar`/`divider`. Toolbar button
    logic unchanged.
  - **Files changed:** `iphone/MakeReady/Components/Input/MarkdownEditor.swift` only.
    `git diff --name-only` (this fix) = that one file.
  - **Verification:** **pending device/simulator.** NOT built by the assistant (iPhone build
    needs user action; user deployed without building). `transition-review`/`present-overlay`
    N/A (no animation/modal/transition triggers). On-device check required: (1) editing a
    read-activity custom-text block with the keyboard up keeps the formatting toolbar pinned
    above the keyboard and the H1/H2/H3 picker tappable; (2) same in the exegesis note editor;
    (3) confirm the keyboard toolbar surfaces inside `EditReadActivityPage`'s `DragulaView`
    (`MarkdownEditor` is rendered in a `UIHostingController` there — keyboard accessories can
    fail in nested hosting controllers).
  - **Commit:** see push. **Monday:** evidence comment posted; status → **Verify**
    (pending-verify, not Done). Subs 1/2/3/4 untouched by this change (sub-4 already fixed;
    sub-3 → 12386101354; subs 1/2 need on-device repro).
  - **Follow-ups (out of scope, flagged):** delete dead `RichTextInput.swift`; fix the stale
    `EditReadActivityPage.swift:7` comment.
