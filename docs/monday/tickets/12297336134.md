---
ticket: 12297336134
title: stuck
board: 18413909869
url: https://scotts-team283817.monday.com/boards/18413909869/pulses/12297336134
created: 2026-06-17
reporter: Scott Stickane
status: open
verdict: OUTSTANDING
last_review: 2026-07-19
deep_dive: none
affected_areas: provisional
---

# stuck

## Problem
Grab-bag ticket bundling five distinct "I'm stuck" reports:

1. **Read lesson, highlighted, now stuck** (web-member) — after highlighting in a read lesson there is no visible forward control (same single-affordance root as #2).
2. **Input step traps the member behind the keyboard** (web-member) — no button to complete input; the only forward control is the header chevron, hidden by the keyboard, escaped only by accidentally dismissing it. = UX-2.
3. **Study totally empty for the member after rearranging lessons then enrolling** (web-member/server) — = CF-19; likely fixed by study-sync.
4. **Mobile URL bar covers the nav text** (web-member) — bottom nav ignores the browser chrome/safe area. = CF-11; part of the 4-ticket safe-area cluster. **Outstanding.**
5. **iPhone read editor: can't scroll back up to the H1/H2/H3 heading selection** (iPhone leader) — keyboard auto-scroll keeps pulling the view to the caret. = UX-5; heading picker is now a native Menu, likely fixed.

Rollup verdict OUTSTANDING because sub-issue 4 (and the UX-2 affordance gap) remain unfixed on main.

## Reports (verbatim)
- (2026-06-17) "Read lesson. Highlighted. Now stuck."
- (2026-06-17) "I'm looking for a button to complete my input. I get stuck here until I accidentally swiped down on the keyboard."
- (2026-06-17) "As a member the study is totally empty. I caused this error by rearranging the lessons in the study and then enrolling. There should be content."
- (2026-06-17) "As a user I have moved to a different app and the URL bar is covering the nav text. I don't know what to do now. The button at the top takes me to the next section."
- (2026-06-17) "It is impossible for me to create a read lesson and then go back to the top of custom text I've added, select it, and change the heading type. The UI scrolls down to the point where I can't get to the H1/H2/H3 heading selection."

## Screenshots
Not yet viewed (attachments exist on the monday item — run /monday-ticket).

## Affected areas
> `provisional` — inferred from report text + code analysis; screenshots not yet viewed. /monday-ticket must confirm before /monday-resolve will act.

### In scope
- **Platform:** multiple — sub-issues 1–4 are web-member; sub-issue 5 is iPhone leader.

Sub-issues 1–2 (no forward affordance, UX-2):
- **Screen:** input step — `client/resources/js/components/domain/lesson-island/steps/input-step.vue` (debounced auto-save :71-93, no button); header chevron — `client/resources/js/components/domain/lesson-island/member-lesson-header.vue` (:106-115)

Sub-issue 3 (empty study, CF-19):
- **Logic:** enroll-time snapshot copy — `server/src/routes/enrollments.ts:282-503`; client silently drops empty-id lessons — `client/resources/views/pages/group-home.blade.php:42-55`; no empty-state — `client/resources/js/components/domain/member-studies-island/member-studies-island.vue:51-96`

Sub-issue 4 (URL bar / safe area, CF-11):
- **Styles:** `.GroupHome__navigation` fixed to bottom — `client/resources/css/components/domain/group-home.scss:69-76` (page uses `min-height: 100vh` at :4); `.Navigation` has no `env(safe-area-inset-bottom)` — `client/resources/css/components/domain/navigation.scss:1-10`; viewport meta already enables safe-area — `client/resources/views/layouts/home.blade.php:7`; member lesson header uses flat `padding: 8px` with no `safe-area-inset-top` — `client/resources/js/components/domain/lesson-island/member-lesson-header.scss:1-5`

Sub-issue 5 (heading picker, UX-5):
- **Screen:** iPhone read editor — `iphone/MakeReady/Components/Input/RichTextInput.swift` (inline toolbar :314-369) / `iphone/MakeReady/Components/Input/MarkdownEditor.swift` (heading picker now a native Menu, :610-646)
- **Endpoints:** none for 1, 2, 4, 5 (UI/CSS-only); sub-issue 3 is server snapshot logic

### Explicitly out of scope
- Sibling lesson-player steps not implicated per sub-issue: `read-step.vue`, `exegesis-step.vue`, `video-step.vue`, `complete-step.vue` (each is a separate file under `client/resources/js/components/domain/lesson-island/steps/`) — video's own affordance gap is ticket 12415667946; exegesis inset is 12415662995.
- iPhone editors other than the read editor's formatting toolbar (`EditExegesisActivityPage.swift`, Write/YouTube editors) — sub-issue 5 is specifically the read editor.
- Web LeaderApp twin editor panes (`client/resources/js/islands/leader-app/components/edit-read-activity-pane.vue`) — leader web, not in any of these reports.
- `MarkdownEditor.swift` is shared across block types — any further change there affects every consumer; callout per PIPELINE rules.

### Cross-app impact
None per the root CLAUDE.md table for 1, 2, 4, 5 (single-platform UI). Sub-issue 3 was server-side; study-sync already landed — both member consumers render the corrected schedules.

## Root cause
- **1–2:** the input step auto-saves but renders no button; the only forward control is the header chevron, hidden by the keyboard (UX-2, [ui-ux.md](../ui-ux.md)).
- **3:** enroll-time snapshot could produce an empty lesson set on reorder-then-enroll (CF-19); study-sync commits `04022a3`, `b0e17cd`, `9b0efb5` likely fixed it.
- **4:** member web has **no `env(safe-area-inset-top)` anywhere** despite `viewport-fit=cover` (`ce6bf4e`); bottom nav lacks safe-area padding and the shell uses `100vh` not `dvh` (CF-11; 2026-07-19 triage, root cause shared with 12415667946/12415662995/12344966853).
- **5:** inline per-block toolbar unreachable under keyboard auto-scroll (UX-5); heading picker since moved to a native Menu (`MarkdownEditor.swift:610-646`).

## Verification
- **Sub 4 (outstanding):** grep confirms no `safe-area-inset-bottom` in `navigation.scss`/`group-home.scss`; repro: mobile browser, member group home, watch the nav sink under the URL bar.
- **Sub 2 (outstanding):** `input-step.vue` has no Done/Next control; repro: member input step, keyboard up, look for a way forward.
- **Sub 3:** re-run reorder→enroll on main; expect content (study-sync).
- **Sub 5:** on-device — read editor, add custom text, try changing heading type via the Menu.

## Triage history
- 2026-07-05 analysis: mapped to CF-11, CF-19, UX-2, UX-5 — safe-area nav, empty snapshot, missing input affordance, unreachable heading toolbar.
- 2026-07-19 /monday-review: OUTSTANDING — URL-bar/safe-area sub-report outstanding (no `safe-area-inset-top` anywhere; `member-lesson-header.scss:1-5`); rest likely fixed (study-sync for empty lessons; native Menu heading picker `MarkdownEditor.swift:610-646`).

## Resolution log
(empty)
