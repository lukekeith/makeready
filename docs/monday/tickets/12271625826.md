---
ticket: 12271625826
title: image optimize
board: 18413909869
url: https://scotts-team283817.monday.com/boards/18413909869/pulses/12271625826
created: 2026-06-14
reporter: Scott Stickane
status: open
verdict: ANSWERED
last_review: 2026-07-19
deep_dive: none
affected_areas: provisional
---

# image optimize

## Problem
A question, not a bug: the reporter feeds the app full-resolution images and asks what size they are stored at on the server and whether they are optimized. The answer (from code): they are **not** stored full-res — every active upload path resizes via sharp before storing to R2. "Resolved" looks like: post the answer as a ticket comment and close (CF-18); optionally close two small legacy gaps found during the audit.

## Reports (verbatim)
- (2026-06-14) "Question — I'm feeding this app full-res images. What is the size of them on the server? Are they optimized?"

## Screenshots
Not yet viewed (attachments exist on the monday item — run /monday-ticket).

## Affected areas
> `provisional` — the answer is code-verified; only the optional gap-closing work needs scoping confirmation.

### In scope
- **Platform:** server (upload/ingest paths only)
- **Logic:** sharp resize on every active ingest path — `server/src/routes/media.ts:1208-1215` (media upload: "original" capped at 1200px JPEG q85 + 400px/150px variants), `server/src/routes/programs.ts:3658-3663` (program covers), `server/src/routes/events.ts:1362-1371`, `server/src/routes/groups.ts:1208-1219`, avatars 512px `server/src/routes/members.ts:1357`, writer `server/src/services/storage.ts:194-208`
- **Optional gap-closing:** legacy `uploadMemberAvatar` stores originals with no resize (`storage.ts:118-169`, likely dead code — confirm and remove); URL-register endpoints accept client-supplied URLs with no processing (`media.ts:601-667`, `:1367-1443`)
- **Endpoints:** the upload endpoints above; no changes needed for the answer itself

### Explicitly out of scope
- Image *delivery* (no Cloudflare Images transform is used in delivery — factual note, not a work item here).
- Client and iPhone upload UIs — they send what they send; sizing is enforced server-side.
- Video storage (Cloudflare Stream) — different pipeline entirely.

### Cross-app impact
None per the root CLAUDE.md table for the answer. If the legacy paths are deleted/guarded, confirm nothing in client or iPhone still calls them before removal.

## Root cause
n/a — question. Findings per CF-18 ([code-fixes.md](../code-fixes.md)) and the 2026-07-19 triage: all active ingest resizes via sharp; two closable legacy gaps noted above.

## Verification
- **Code-level:** the sharp pipelines at the cited lines (`media.ts:1208-1215`, `programs.ts:3658-3663`, `events.ts:1362-1371`, `groups.ts:1208-1219`, `members.ts:1357`, `storage.ts:194-208`).
- **Empirical:** upload a large image via the app, fetch the stored "original" from R2, confirm ≤1200px JPEG.

## Triage history
- 2026-07-05 analysis: mapped to CF-18 — answer & close; two legacy no-resize paths flagged for cleanup.
- 2026-07-19 /monday-review: ANSWERED — images are NOT stored full-res; every upload path runs sharp → max 1200px JPEG q85 "original" + 400px/150px variants → R2. Next action: reply with this on the ticket and close.

## Resolution log
(empty)
