---
ticket: 12297345805
title: suggestion
board: 18413909869
url: https://scotts-team283817.monday.com/boards/18413909869/pulses/12297345805
created: 2026-06-17
reporter: Scott Stickane
status: open
verdict: NEEDS CLARIFICATION
last_review: 2026-07-19
deep_dive: none
affected_areas: none
---

# suggestion

## Problem
A product proposal, not a bug: merge the "Read (bible)" and "Exegesis" activity types, renaming *Exegesis* → **"Add bible verse"** and *Read* → **"Custom text"**. Combining two activity types is a product/UX decision touching both editors on both platforms (plus how members render each type); nothing is actionable until that call is made. Note the read editor's add-content menu already uses exactly the suggested wording ("Bible verse" / "Custom text") for its block sources — partially answering the naming half.

## Reports (verbatim)
- (2026-06-17) "We don't need a 'read (bible)' and an 'exegesis'. Should combine. Exegesis should be renamed 'add bible verse'. 'Read' should be 'custom text'."

## Screenshots
Not yet viewed (attachments exist on the monday item — run /monday-ticket).

## Affected areas
> `none` — no defect to map; the change is a product decision (merge two activity types) whose scope depends entirely on the decision. Candidate surfaces listed for orientation only.

### In scope
- **Platform:** would be multiple (iPhone + web-leader editors; member rendering on both; server activity-type model)
- **Screen:** none committed. The activity types are authored in `iphone/MakeReady/Pages/Manage/Program/EditReadActivityPage.swift` and `iphone/MakeReady/Pages/Manage/Program/EditExegesisActivityPage.swift` (+ web LeaderApp twins `client/resources/js/islands/leader-app/components/edit-read-activity-pane.vue`, `edit-exegesis-activity-pane.vue`) and rendered for members by separate step files `client/resources/js/components/domain/lesson-island/steps/read-step.vue` and `exegesis-step.vue`
- **Endpoints:** activity-type model server-side — unscoped

### Explicitly out of scope
- The `+` add-block clarity fix inside the read editor — that is ticket 12268478769 (also UX-7) and already reflects the proposed wording.
- Write and Video/YouTube activity types — not part of the proposal.

### Cross-app impact
If the types merge, per the root CLAUDE.md table this is a study/lesson content-model change: server → client lesson player + iPhone lesson views all change. That breadth is exactly why this needs a product decision first.

## Root cause
n/a — product suggestion. = UX-7 in [ui-ux.md](../ui-ux.md): pure renaming is a copy change, but combining the two activity types and reworking the add-content entry point is a product/UX call touching both editors.

## Verification
- n/a until the product decision. Current-state check: the read editor's `+` source menu already says "Bible verse" / "Custom text" (see 12268478769 dossier, `EditReadActivityPage.swift:512-535`).

## Triage history
- 2026-07-05 analysis: mapped to UX-7 — merge/rename proposal grouped with the add-bible-verse clarity ticket.
- 2026-07-19 /monday-review: NEEDS CLARIFICATION — product call, not implemented; read editor's add-menu already uses the suggested wording ("Bible verse"/"Custom text").

## Resolution log
(empty)
