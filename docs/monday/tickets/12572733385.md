---
ticket: 12572733385
title: iPhone exegesis editor — "Couldn't save changes"
board: 18413909869
url: https://scotts-team283817.monday.com/boards/18413909869/pulses/12572733385
created: 2026-07-19
reporter: Scott Stickane (reported 2026-06-20; split from 12268645785)
status: open
verdict: STILL RELEVANT
last_review: 2026-07-19
deep_dive: 2026-07-19
affected_areas: provisional
type: Data & Lifecycle
app: iPhone
nature: Bug
---

# iPhone exegesis editor — "Couldn't save changes"

> **Split 2026-07-19** from "validation error" ([12268645785](12268645785.md)), which had bundled this unrelated iPhone bug with a member web SMS-verification issue.

## Problem
On the iPhone native **Edit Exegesis Activity** editor, after heavy editing the save fails with a red **"Couldn't save changes"** banner. Reported after "a bunch of work," suggesting the failure correlates with accumulated edits (notes/highlights) rather than a fresh activity. "Fixed" = exegesis edits save reliably. Root cause not yet determined — needs the underlying server response.

## Reports (verbatim)
- (2026-06-20, on parent 12268645785) "Did a bunch of work here and now the changes won't save"

## Screenshots
- `img1` (Jun 20) — iPhone native "Edit Activity" page: Activity title "Exegesis", Passage "Acts 13:36", font-size (Aa) picker, a highlighted verse ("after he had served God's purpose"), red banner **"Couldn't save changes"** at the top. Confirms the screen is the exegesis editor and the failure is on save.

## Affected areas
> `provisional` — the screen is confirmed (screenshot), but the failing call and its cause are NOT known without the server response, so this is not resolve-ready. `/monday-ticket` should re-confirm once the error is captured.

### In scope
- **Platform:** iPhone (+ server, depending on cause).
- **Screen:** `iphone/MakeReady/Pages/Manage/Program/EditExegesisActivityPage.swift` — `saveTitle()` (`:760-780`) calls `ProgramActions().updateActivityContent(title:)` then `savePendingNotes()`; either throwing surfaces "Couldn't save changes" (`:779`).
- **Likely server counterpart:** the exegesis activity/notes/highlights update endpoints (per memory `parity-edit-exegesis-activity`: server REPLACES blocks on passage change — a candidate for save conflicts after heavy edits). Confirm against the actual failing request.

### Explicitly out of scope
- The other three activity editors (Read/Write/YouTube) — they share the "Couldn't save changes" string but are separate pages; the screenshot is exegesis-specific.
- `MarkdownEditor.swift` and other shared components — not implicated without evidence.
- The member web SMS-verification issue — parent ticket 12268645785.

### Cross-app impact
iPhone → server persistence. If the cause is a server-side rejection (validation / block-replacement), the fix may be server-side and would also affect the web exegesis editor twin — check once the cause is known.

## Root cause
Not yet diagnosed — needs the server response behind the failing `updateActivityContent`/`savePendingNotes` call (4xx/5xx body).

## Verification
- On iPhone, edit an exegesis activity heavily (add/adjust highlights + notes, maybe change the passage) and Save; capture the server response and the `recordError` context that triggers the banner (`EditExegesisActivityPage.swift:779`).
- Cross-check the exegesis update endpoints for a validation/replacement rejection.

## Triage history
- 2026-07-19 /monday-ticket: **STILL RELEVANT (new, split from 12268645785).** Screenshot confirms the exegesis editor save failure; root cause pending the server error. Type=Data & Lifecycle · App=iPhone · Nature=Bug. `affected_areas: provisional`.

## Resolution log
(empty)
