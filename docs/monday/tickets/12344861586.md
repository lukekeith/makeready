---
ticket: 12344861586
title: UX update please
board: 18413909869
url: https://scotts-team283817.monday.com/boards/18413909869/pulses/12344861586
created: 2026-06-23
reporter: Scott Stickane
status: open
verdict: OUTSTANDING
last_review: 2026-07-19
deep_dive: none
affected_areas: provisional
---

# UX update please

## Problem
The widest grab-bag ticket — eleven updates bundling five clusters:

1. **Enrollment permission gap** (server): a leader can enroll their group in *anyone's* published study, system-wide; the reporter wants a sharing/permission model. = CF-2 (code-only scoping) + UX-8 (sharing flag). **Outstanding.**
2. **Can't unenroll a group** (server auth): = CF-8 territory; likely fixed by the org-authorization work (`3afaa00`).
3. **"Open Lesson" routes the leader into the member phone-auth join funnel** ("a fancy way to preview"). = UX-1. **Outstanding** despite the preview feature existing.
4. **Member lesson player affordance/notes cluster** (web-member): can't exit after writing notes; stuck without a next/save button (= UX-2); rich-text toolbar buttons don't work / should be removed; text selection nearly impossible; keyboard covers the note being written (likely fixed via `interactive-widget=resizes-content`); **highlight-overlap deleted an existing note — data loss** (= UX-4 note); **no way to read or edit an existing note** (= UX-4, dead Notes tab). Notes destination **outstanding**.

"Fixed" observably: enrollment restricted to own/org-consumable studies; Open Lesson gives a leader preview; the member player has a save/next affordance and a real notes destination; no note data loss on overlapping highlights.

## Reports (verbatim)
- (2026-06-23) "I can enroll my group in someone else's study. This shouldn't be possible." (The GL should have to make the study their own / get permission / creator marks it for org consumption.)
- (2026-06-23) "I can't unenroll a group from a study."
- (2026-06-23) "'Open lesson' here doesn't make sense — it makes me validate as a member and throws a validation error. This is a fancy way to preview."
- (2026-06-23) "I have written my notes as a member and now I don't know how to get out of the study."
- (2026-06-23) "Member — I think people will get stuck here without a next or save button."
- (2026-06-23) "It is nearly impossible for me to select what I want." (member text selection)
- (2026-06-23) "These don't work. Also unconvinced about this bar in general — should go away for v1." (rich-text toolbar buttons)
- (2026-06-23) "Get rid of the text edit bar."
- (2026-06-23) "I made a long note, then selected over top of it with another highlight and it deleted my other note. I can't get it back." (data loss)
- (2026-06-23) "I'm writing a note and the keyboard is covering what I am trying to edit."
- (2026-06-23) "I have no way to edit my existing note, or even read it again."

## Screenshots
Not yet viewed (attachments exist on the monday item — run /monday-ticket).

## Affected areas
> `provisional` — inferred from report text + code analysis; screenshots not yet viewed. /monday-ticket must confirm before /monday-resolve will act.

### In scope
- **Platform:** multiple — cluster 1–2 server; cluster 3 iPhone + server routing; cluster 4 web-member.

Cluster 1 (enroll-anyone's-study, CF-2/UX-8):
- **Logic:** `POST /api/enrollments` — `server/src/routes/enrollments.ts` (study lookup only `{id, isActive}` at :143-148 per 2026-07-19 triage; 2026-07-05 cited :161-165; `groupManageFilter` on the group :150); no sharing flag on `StudyProgram` — `server/prisma/schema.prisma:847` (:831-859 range)

Cluster 2 (can't unenroll, CF-8):
- **Logic:** `DELETE /enrollments/:id` + `enrollmentManageFilter` — `server/src/routes/enrollments.ts` (filter at :1938 post-fix), `server/src/services/permission.ts:337-347`

Cluster 3 (Open Lesson, UX-1):
- **Screen:** `iphone/MakeReady/Pages/Manage/Group/GroupHomePage.swift:745` → `server/src/routes/enrollments.ts:2480` → member `/join/study/...` URL; the unused leader preview: `server/src/routes/study-preview.ts`, `client/routes/web.php:93-99`

Cluster 4 (member player notes/affordances, UX-2/UX-4):
- **Screens:** input step — `client/resources/js/components/domain/lesson-island/steps/input-step.vue` (auto-save :71-93, no button); header chevron — `client/resources/js/components/domain/lesson-island/member-lesson-header.vue:106-115`; highlight note editor sheet — `client/resources/js/components/card/exegesis-highlight-menu/exegesis-highlight-menu.vue` (:121-148, no keyboard avoidance); dead Notes tab — `client/resources/js/components/domain/navigation-island/navigation-island.vue:152` (`href="#"`)
- **Endpoints:** enrollment create/delete above; notes endpoints unscoped until UX-4 is designed

### Explicitly out of scope
- Study *deletion* authorization — sibling permission issue, ticket 12325580210 (CF-1).
- The member join-flow validation error itself — ticket 12268645785 (CF-9); this ticket only carries the *routing* half (UX-1).
- Web LeaderApp and legacy admin enrollment UIs — enrollment scoping is fixed server-side.
- Leader-side editors (iPhone `EditExegesisActivityPage.swift`, web `client/resources/js/islands/leader-app/components/edit-exegesis-activity-pane.vue`) — cluster 4 is the *member* player; note the overlap-guard evidence cites the leader web pane (`edit-exegesis-activity-pane.vue:173-174`) as the guarded comparison only.
- Other lesson-player steps (`read-step.vue`, `exegesis-step.vue`, `video-step.vue`, `complete-step.vue` under `client/resources/js/components/domain/lesson-island/steps/`) beyond the input step — video affordance is 12415667946; exegesis inset is 12415662995.

### Cross-app impact
Per the root CLAUDE.md table: CF-2/UX-8 is schema + endpoint → both consumers' enrollment UIs must handle newly-rejected enrollments; UX-1 touches iPhone + server (and web-leader if it gets the same button); cluster 4 is web-member UI only. UX-8's sharing flag is a schema change — name both consumers in any resolve plan.

## Root cause
- **CF-2:** the enrollment endpoint checks the *group* but looks the *study* up with only `{ id, isActive: true }` — the code comments this as intentional; only gates are `isPublished` and has-lessons. No creator/org/sharing check; no sharing flag exists in the schema.
- **CF-8 (likely fixed):** unenroll required `enrollmentManageFilter` while the affordance had no filter; org-authorization `3afaa00` fixed the lockout (re-reports were 2–6 days later, likely stale deploy).
- **UX-1:** Open Lesson resolves to the member join URL instead of the existing token-based leader preview.
- **UX-2:** input step auto-saves with no visible button; only forward control is the keyboard-hidden header chevron.
- **UX-4:** Notes tab is a dead link (`navigation-island.vue:152`); notes only reachable transiently inside a lesson. Data-loss note: overlap refusal is guarded on web (`edit-exegesis-activity-pane.vue:173-174`); if it reproduces, it's the iPhone path — check before designing.
- **Keyboard-over-note (likely fixed):** `interactive-widget=resizes-content` (`fded371`); the highlight-note sheet still lacks its own keyboard avoidance (`exegesis-highlight-menu.vue:121-148`).

## Verification
- **CF-2 (outstanding):** code-level — `enrollments.ts:143-148`; repro: enroll your group in a stranger's published study by id → succeeds (bug).
- **UX-1 (outstanding):** `GroupHomePage.swift:745` still opens the join URL; repro: leader taps Open Lesson → phone verification appears.
- **UX-2/UX-4 (outstanding):** `input-step.vue` has no button; `navigation-island.vue:152` Notes href is `#`.
- **CF-8:** repro as non-creator org leader — unenroll should now succeed.
- **Data loss:** attempt overlapping highlight over a noted highlight on iPhone; expect refusal, not deletion.

## Triage history
- 2026-07-05 analysis: mapped to CF-2, UX-1, UX-2, UX-4, UX-8 — enrollment permission gap, Open Lesson routing, missing save/next affordance, notes destination, sharing model.
- 2026-07-19 /monday-review: OUTSTANDING — group can enroll into ANY published study system-wide (`enrollments.ts:143-148`, no sharing flag in `schema.prisma:847`); "Open Lesson" still the member phone-auth flow (`GroupHomePage.swift:745` → `enrollments.ts:2480`) despite preview existing; member note-review surface still absent.

## Resolution log
(empty)
