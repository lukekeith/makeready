---
ticket: 12344966853
title: UI update please
board: 18413909869
url: https://scotts-team283817.monday.com/boards/18413909869/pulses/12344966853
created: 2026-06-23
reporter: Scott Stickane
status: open
verdict: OUTSTANDING
last_review: 2026-07-19
deep_dive: none
affected_areas: provisional
---

# UI update please

## Problem
Member web polish grab-bag — six sub-issues:

1. **Post-unenrollment icon reads like an error state.** = UX-11 polish.
2. **Join form asks for full birthday; no minimum-age enforcement** — should be birth year only, with min-age tied to the group age range (or drop the age range). **Outstanding.**
3. **Reading surface color** — should look like paper (white pages, black text, yellow/blue/pink highlights); current blue highlight on black with serif "looks like an error". = UX-11.
4. **A screen should be vertically centered.** = UX-11 nit (screen unidentified — needs the screenshot).
5. **Reading themes are too much for v1** — consider hiding the picker. **Outstanding** (themes still ship; theme-typography redesign is at Phase 0).
6. **Bottom-most story always half covered by the UI.** = CF-11 safe-area gap. **Outstanding.**

"Fixed" observably: join form takes birth year with age-range enforcement (or no age range); themes hidden or paper-look default; bottom content fully reachable above the nav.

## Reports (verbatim)
- (2026-06-23) "This icon is incorrect — makes me feel like there was an error in the unenrollment."
- (2026-06-23) "Not sure we need the birthday — maybe just birth year. Also there's no minimum-age requirement; should be based on the group age range, otherwise remove the age range."
- (2026-06-23) "The blue color here isn't right. I'd like this to look like a piece of paper — white pages, black text; highlights yellow/blue/pink. On black the serif font looks like an error."
- (2026-06-23) "I feel like this should be vertically centered."
- (2026-06-23) "The themes for reading are too much for v1."
- (2026-06-23) "I can't get to the bottom-most story easily — always half covered by the UI."

## Screenshots
Not yet viewed (attachments exist on the monday item — run /monday-ticket). Several sub-issues (icon, vertical centering) cannot be pinned to a screen without them.

## Affected areas
> `provisional` — inferred from report text + code analysis; screenshots not yet viewed. /monday-ticket must confirm (especially sub-issues 1 and 4) before /monday-resolve will act.

### In scope
- **Platform:** web-member (all sub-issues).

Sub-issue 2 (birthday/min-age):
- **Screen:** member join profile form — `client/resources/views/components/domain/profile-form.blade.php` (:59-64 full birthday required); min-age enforcement itself would be server logic (join/membership endpoints — unscoped)

Sub-issues 3, 5 (reading surface/themes, UX-11):
- **Screens:** member lesson player reading surfaces — `client/resources/js/components/domain/lesson-island/steps/read-step.vue`, `exegesis-step.vue`; theme system per the theme-typography redesign (Phase 0 schema done — see memory `theme-typography-redesign`); shared theme tokens are a shared-component callout

Sub-issue 6 (bottom coverage, CF-11):
- **Styles:** `.GroupHome__navigation` fixed bottom — `client/resources/css/components/domain/group-home.scss:69-76` (`min-height: 100vh` at :4); `.Navigation` no `env(safe-area-inset-bottom)` — `client/resources/css/components/domain/navigation.scss:1-10`; viewport meta already safe-area-capable — `client/resources/views/layouts/home.blade.php:7`

Sub-issues 1, 4: unmapped until screenshots are viewed.
- **Endpoints:** none (UI/CSS) except sub-issue 2's enforcement half

### Explicitly out of scope
- iPhone reading/highlight surfaces (leader `ExegesisVerseView.swift` etc.) — reporter is testing member on mobile web here.
- The member lesson-player *header* safe-area fix — same root-cause family (CF-11 cluster) but its reports live in 12297336134/12415667946/12415662995; coordinate, don't duplicate.
- Leader join/invite surfaces and web LeaderApp — the join profile form is the member flow.
- Highlight *color* tokens shared with other themes — shared-component callout: changing theme tokens affects every themed screen; confirm scope with the user first.

### Cross-app impact
Per the root CLAUDE.md table: mostly none (single-platform CSS/UI). Sub-issue 2's min-age enforcement is server logic → both join consumers (web member join; iPhone has no member join) — verify only web consumes it. Theme changes touch the shared theme system used by both member player and preview player.

## Root cause
- **Sub 2:** `profile-form.blade.php:59-64` requires a full birthday; no minimum-age check exists anywhere; group age range is unenforced.
- **Sub 6 (CF-11):** bottom nav is `position: fixed; bottom: 0` with no `safe-area-inset-bottom` padding and a `100vh` (not `dvh`) shell — the viewport meta enables safe-area but nothing uses it.
- **Subs 1, 3, 4, 5:** design opinions grouped as UX-11 in [ui-ux.md](../ui-ux.md); not defects.

## Verification
- **Sub 6:** code-level — grep `safe-area-inset-bottom` in `navigation.scss`/`group-home.scss` (absent); repro: mobile browser, member group home, scroll to the last story card → half covered.
- **Sub 2:** view `profile-form.blade.php:59-64`; repro: join a group as a member → full birthday demanded, no age gate.
- **Sub 5:** member lesson player still offers reading themes (theme redesign at Phase 0).
- **Subs 1, 4:** blocked on screenshots.

## Triage history
- 2026-07-05 analysis: mapped to CF-11, UX-11 — safe-area bottom coverage + member reading-experience polish batch (paper look, themes, icon, centering, birth year).
- 2026-07-19 /monday-review: OUTSTANDING — full birthday still required, no min-age (`profile-form.blade.php:59-64`); reading themes still shipping (theme-typography redesign at Phase 0); bottom-story coverage = safe-area gap.

## Resolution log
(empty)
