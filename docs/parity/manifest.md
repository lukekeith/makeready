# iPhone → Web Leader-App Parity Manifest

The master tracker for porting every iPhone leader screen to the mobile-web
LeaderApp (`/admin`, `client/resources/js/islands/leader-app/`) with pixel-level
parity. **This file is the source of truth for what's done** — update it at the
end of every screen's work so a fresh session can pick up exactly where the last
one stopped.

**How to work a screen:** run `/parity-screen` (no argument — takes the first
item in the **Build queue** below that is neither `[x]` done nor `[~]` ready;
the order encodes dependencies) or `/parity-screen <screen-id>` to target one
directly. The skill encodes the proven pipeline (analyze iPhone → build shared
twin → register compare → capture both → diff → wire production → mark ready →
update this manifest). Verification is batched: built screens accumulate at
`ready`/`[~]`, and `/parity-screen verify` walks them all with the user in one
sitting.

**Status legend** (a screen advances through these in order):

| Status | Meaning |
|---|---|
| `—` | not started |
| `spec` | iPhone source analyzed, layout/data/transition spec captured in a memory or notes |
| `twin` | shared Vue twin built + registered (component-capture + app.scss) |
| `compare` | compare fixture + adapter registered, web + iPhone shots captured, diff inspected |
| `wired` | production route/store/overlay wiring done, build + tests green |
| `ready` | wired + diff clean + test script delivered — awaiting live verification. Marked `[~]` in the Build queue; satisfies prerequisites for dependent items, so building continues while verification batches up. `/parity-screen verify` signs off all `ready` screens at once |
| ✅ `verified` | user verified live + compare comments resolved |

## Foundation (done — reuse, don't rebuild)

- ✅ **Overlay system** — `islands/leader-app/overlay/`: `overlay-routes.ts`
  (Route registry, priorities 100/200/300), `overlay.store.ts` (priority-sorted
  stack, two-phase dismiss, `dismissThen`), `managed-modal.vue` (bottom sheet,
  springs, drag-dismiss), `overlay-host.vue` (z = 100+index), `slide-stack.vue`
  (300ms ease-in-out, mount-then-slide, completion-tied unmount),
  `managed-menu.vue` (content-sized bottom card #111215/stroke #242937,
  tertiaryLabel grabber as first layout child, scrim tap ALWAYS dismisses,
  grabber-only drag-dismiss; see `parity-managed-menu` memory).
  `managed-page.vue` (horizontal push chrome — iOS ManagedPageView: easeOut
  300ms in / easeIn 250ms out, derived black@0.3 non-tappable scrim, 40px
  edge-swipe back w/ 80px commit; built 2026-07-28 for `.memberRequests`, its
  only iOS consumer — see `parity-member-requests` memory). ALL FOUR chromes
  now exist (modal / menu / page / raw).
- ✅ **Animated not-ready border** — `styles/_animated-border.scss` mixin
  (rotating conic brand ring, frozen in `.capture-page`/`.capture-wrap`).
- ✅ **Swipe + drag-reorder** — `swipeable-card` twin (iOS mechanics: progressive
  reveal, velocity snap, `bare` production mode, `action`/`tap` emits) +
  `dragula-list` twin (long-press lift, drop indicator, FLIP make-way,
  `reorder` emit). Wrap any card twin; see `parity-swipeable-drag-reorder`
  memory for the gating/endpoint patterns.
- ✅ **Confirm-dialog service** — `overlay/confirm-dialog.store.ts`
  (`useConfirmDialog().confirm({title,message,buttons}) → Promise<index|null>`;
  `present({sticky:true})` returns an update/close handle for in-flight button
  states like "Adding...") + `overlay/confirm-dialog-host.vue` mounted once in
  leader-app.vue (fixed full-screen blurred scrim, z-500, DialogOverlay
  scale-in — the /library/programs chrome). EVERY iOS-style alert/confirm must
  present through this — no local dialog scrims. All 11 prior call sites
  migrated 2026-07-03 (library, edit-day, program-home, create-program,
  edit-read-activity).
- ✅ **ConfirmationOverlay route host** — `components/confirmation-overlay-modal.vue`
  presents the ConfirmationOverlay twin through the raw `.confirmationOverlay`
  overlay route (blurred scrim, content-appear motion, live getter props);
  reuse for any processing→success confirmation.
- ✅ **Interactive twin inputs** — text-input / multiline-text-input / tag-input
  / toggle-control / search-field have additive `interactive` modes; PageHeader
  / PageTitle / TabSlider / NavBar / AddMenu / ActionCardMenu / DialogOverlay
  have additive emits. **Twins must only ever change additively** (compare
  snapshots bind no listeners / pass no new props).
- Key memories: `leader-app-study-management`, `leader-app-library-page`,
  `compare-twins-index` (+ per-twin traps), `admin-mobile-web-rebuild`.

## Web-first / study-sync surfaces (built OUTSIDE this pipeline — do not rebuild)

Screens added by the study-sync project (phase 6, 2026-07) that live in the
LeaderApp but were NOT produced by /parity-screen — they have no compare
fixtures and their iOS-parity status is unassessed unless noted:

- **Notifications modal** — `components/notifications-modal.vue` +
  `leader-notifications` store, `.notifications` overlay route (web-first, no
  iOS Route twin). Overlaps the "Notification feed" row below: the iOS
  NotificationFeedPage PORT is still unstarted; this modal is the study-sync
  feed opened from the dashboard banner.
- **Enrollment sync pane** — `components/enrollment-sync-pane.vue` +
  `leader-enrollment-sync` store; SlideStack detail from Program Home
  Enrollments tab + notification actions.
- **Review changes pane** — `components/review-changes-pane.vue` (header says
  "web twin of iPhone ReviewChangesPage" — parity built during study-sync,
  not compare-verified through this pipeline).

When a /parity-screen run touches an area these occupy, treat them as existing
production surfaces (extend additively), not as blank slate.

## Tab roots

| Screen | iPhone source | Compare id | Status | Notes |
|---|---|---|---|---|
| Home dashboard | `Pages/Main/MainHome.swift` | `home-dashboard` | ✅ verified | `views/dashboard-view.vue`; Activity sub-tab still static |
| Groups (3 tabs) | `Pages/Manage/Member/MemberHomePage.swift` | `groups` / `group-members` / `groups-enrolled` | ✅ verified | `views/groups-view.vue`; header paperplane/plus no-ops |
| Library Programs | `Pages/Main/MainLibrary.swift` (tab 0) | `study-programs` | ✅ verified | `views/library-view.vue`; filters + sort wired (see library-filters-sort queue item) |
| Library Media | `Pages/Main/MainLibrary.swift` (tab 1) | `media` | ✅ verified | media detail overlay on tap not built |
| Calendar | `Pages/Main/MainCalendar.swift` + `Components/Calendar/*` | `calendar` (variants default / with-events / expanded — the last two WEB-ONLY) | ready | SPEC 2026-07-30 (`parity-calendar` memory). ⚠️ FRAMING CORRECTION: iOS is NOT a stub — it ships a full calendar (~2,600 lines); only the WEB was `ComingSoonView`. SplitMonthCalendar = continuous-scroll UIKit grid (section per month, cellWidth (W−32)/7 × 56, today circle = FULL cell width so it overflows the 56pt row, 1px white@10 week separators except a month's last row, no padding days) + curtain-split expanded event list (0.6s spring slices, 62 day sections, CardLesson rows). Data fans out groups→enrollments→details with NO date/timezone params, buckets LOCAL tz, today+future only. Event tap → `.lessonActionMenu` (Edit Activities → `.editEnrollmentDay`, Edit Enrollment → `.enrollmentSchedule`, Open/Share/Delete). DEAD, do not port: YearSelector/MonthSelector/CalendarGrid in MainCalendar.swift, CalendarEventListContent/CalendarEventRow, UpcomingLessonCard, WeekdayIndicator (all zero production call sites). **READY 2026-07-30**: new `SplitMonthCalendar` page twin (composes CalendarWeekdayHeader/CalendarDayCell/CalendarBottomBar/PageTitle/CardLesson; month grid via explicit grid-column/grid-row placement so there are no padding cells; capture-only `statusBar` + `homeIndicator` props) + `leader-calendar` store (groups→enrollments→details fan-out, local-tz keys, today+future, startTime→title sort) + `calendar-view.vue` replacing ComingSoonView at `/admin/calendar`; event tap → `.lessonActionMenu` (Edit Activities → `.enrollmentSchedule` seeded with the day via the new additive `initialScheduleId` prop, Edit Enrollment → `.enrollmentSchedule`, Open/Share via the lesson invite, Delete w/ iOS-exact "Delete Lesson?" strings). Diff 1.04% → **0.45%**, structurally clean. Post-diff parity fixes: SF font stack on the twin root, SF line boxes for the weekday row (12pt→14.3px, which alone had pushed the whole grid ~2px high) and the Today label (14pt→16.7px), and the page-context capsule fill — the standalone CalendarBottomBar twin draws NO capsule because its isolated iOS material renders invisible, but over the grid the reference samples rgb(39,41,46) ≈ white@10%. Residual = SF glyph side-bearings (pill icon/label gap) + status bar. NOT ported: the UIKit curtain-split snapshot animation (web swaps grid↔list directly) |
| Global search | `Pages/Search/GlobalSearchPage.swift` + `GlobalSearchEngine.swift` | `search` (variants recent-results / with-results two-sided; recents-populated / results-populated / results-filtered / no-results WEB-ONLY) | ready | SPEC 2026-07-30 (`parity-global-search` memory). ⚠️ iOS is a REAL tab; only the WEB was `ComingSoonView`. NO page header (SearchField is the first element, "Search everything"); 4 body branches (recents / loading / empty / results); recents come from `GET /api/activities?action=CREATED,UPDATED,PUBLISHED&limit=50` (NOT /api/search/recent) deduped + capped 3/category + hydrated from AppState (uncached entities silently dropped); search = `GET /api/search?q=&limit=5&links=VIDEO`, min 2 chars, 300ms debounce, cap 5/section; ONE row type (CardSearchResult) with the first query match recolored brand and `showChevron:false`; filter-badge rail collapses to a single category (Motion.micro). Server scopes to the OWNED org and 400s otherwise (videos scoped to the caller) — no led-group filtering. DEAD, do not port: "Show all N results" (empty action + impossible condition), `.notification` case, events/posts in recents, `Route.globalSearch`, and the stale file-header comments. **READY 2026-07-30**: new `GlobalSearch` page twin (composes SearchField + CardSearchResult; owns the category→label/glyph/sortOrder map, the filter rail, section headers/dividers and both empty states) + `leader-search` store (both endpoints, 300ms debounce w/ in-flight supersede, min-2, per-entity title/subtitle mapping, iOS `relativeTime`) + `search-view.vue` replacing ComingSoonView at `/admin/search`. Result taps present the existing hosts: program → `.programHome`, group → `.groupHome`, member → `.memberProfile` (seeded name), enrollment → `.enrollmentSchedule`; lesson/video/post/event are INERT pending the `search-detail-pages` item (they are iOS's string-keyed `searchLesson`/`searchVideo`/`searchPost`/`searchEvent` modals), and template is a no-op on iOS too. Diffs **0.30%** (recents) / **0.16%** (loading), structurally clean. Post-diff parity fixes, all scoped to the page: the SearchField twin types its text at `--text-md` **16px** though iOS is s15 (visible as a wider placeholder on a full-width field), its 18px icon box pushed the text ~4px right of the reference, and its material fill is transparent by design (invisible in isolation, but the page reference samples rgb(37,39,44) ≈ white@10%) — field height now matches exactly (137px) and the placeholder lands within 0.3px. Empty-state glyphs are drawn INK-TIGHT to their 36px box because SF Symbols at s36 nearly fill the nominal size (a normal 24-viewBox icon inked ~6pt small) |
| Profile | `Pages/Profile/ProfilePage.swift` | — | — | ComingSoonView stub |
| Notification feed | `Pages/Notifications/NotificationFeedPage.swift` | — | — | PageHeader bell not wired. NOTE: a web-first `.notifications` modal + `leader-notifications` store already exist (study-sync — see the Web-first surfaces section); the iOS-feed PORT should extend/reconcile them, not add a parallel surface |

## Study program management

| Screen | iPhone source | Compare id | Status | Notes |
|---|---|---|---|---|
| Program Home | `Pages/Manage/Program/ProgramHomePage.swift` | `program-home` | ✅ verified | shared `ProgramHome` twin; Enrollments tab wired 2026-07-03 (see queue item); publish-badge dialogs + export flow wired; eye/preview icon still no-op. ✅ PARITY GAP CLOSED 2026-07-30: the **Analytics tab** is ported (see `program-analytics-tab` queue item, status ready) — twin renders KPI grid / Top Groups / Recent Activity / heatmap / freshness footer with verbatim iOS hide rules; production loads via store `loadProgramAnalytics` on tab select. Compare: `analytics` + `analytics-sparse` are TWO-SIDED (diffed structurally clean 2026-07-30); full-page iPhone shots via fixture `captureHeight`, tab seeding via `ProgramHomePage(initialTab:)` ← `shared.selectedTab` |
| Edit Program (settings pane) | `ProgramHomePage.editProgramContent` | — (no iPhone ViewRegistry case yet) | wired | `edit-program-pane.vue`; add `pages.edit-program` to ViewRegistry for a composed compare |
| EditDay (lesson editor) | `Pages/Manage/Program/EditDay.swift` | `edit-day` (variants: mixed/empty) | wired | `edit-day-pane.vue`; compare = CAPTURE-ONLY `EditDay` twin (adapter mirrors studyActivityCard mapping) + new `pages.edit-day` ViewRegistry case; parity fixes must land in the pane too |
| Add-activity menu | `Components/Navigation/AddActivityMenu.swift` | `AddActivityMenu` | ready | `add-activity-menu.vue` (raw chrome) — the island component IS the twin (registered in ComponentCapture; capture-only `statusBar` prop). BOTH platforms captured + diffed structurally tight 2026-07-04 (0.82%, remaining = font/glyph artifacts). Production parity fixes found by the diff: VIDEO label was white-on-white (CardActivityType gained additive `labelColor`), tiles were fixed 120px vs iOS fluid aspectRatio(1) columns, missing 32px header spacer, title line-height pinned to 29px (web font line box pushed grid 4pt low). iPhone = `pages.add-activity-menu` ViewRegistry case with `.transaction { disablesAnimations }` (view fades from opacity 0 on .onAppear). Fixture is type PAGE (absolute-inset overlay ⇒ component-type live preview collapses to 0 height). Awaiting live verify |
| Write activity editor | `EditUserInputActivityPage.swift` | `edit-user-input-activity` (variants: default/configured/help-enabled) | wired | `EditUserInputActivity` twin (pane is now a thin wrapper); iOS-exact chrome ("Edit Activity", "Activity title", long help-toggle copy, Preview button) |
| YouTube activity editor | `EditYouTubeActivityPage.swift` | `edit-youtube-activity` (variants: default/configured) | wired | `EditYouTubeActivity` twin (pane is now a thin wrapper); iOS field order (title→URL), thumbnail+play preview well, Preview button |
| Read activity editor | `EditReadActivityPage.swift` | `edit-read-activity` (variants: default/configured/highlighting) | ✅ verified | full spec in `parity-edit-read-activity` memory (tri-state Save, locked/editable block duality, auto-save split); `EditReadActivity` twin (Screen 1) built + adapter toClient added, BOTH variants diffed structurally tight (accepted: floating-label ghost, line-wrap font metrics, paintbrush glyph approximation); Screen 1 WIRED (EditDay READ tap → edit-read-activity-pane: tri-state save, add custom block via iOS source menu, delete dialog, drag-reorder, swipe-delete, expand/collapse; store readBlocks CRUD + themes; MarkdownEditor additive interactive textarea). Pane = EditDay precedent (capture-only twin, shared scss). Screen 2 style interactivity WIRED (BlockStyleEditor additive interactive: theme native-select, font tiles → fontSize null==m, color well → 24-swatch picker + opacity slider menu on dynamic id, image well → backgroundSourceMenu → MediaLibraryPicker modal → backgroundImageUrl). ALL flows wired 2026-07-03: (a) highlighter/.stylePicker — SelectableLockedBlockView additive verse-tap selection (tap=select, tap-again-inside=confirm→StylePickerMenu on stylePicker_id menu route, replace-overlapping PATCH selections optimistic) + highlight-mode dims/border/disables + multi-verse gutter-number rendering; (b) FULL Bible passage picker — BiblePassagePicker twin + bible-passage-picker-host (books/chapters/verses grids, Charter reader with verse-tap selection, smart search + recents, version menu) + leader-bible store, confirm→addSourceReference→iOS appendNewBlocksToEnd quirk→Set-titles modal (exact strings); (c) Photos/Take-Photo — file inputs (capture attr) → canvas ≤2000 JPEG 0.85 → org media upload → backgroundImageUrl, uploading scrim. COMPARE: `highlighting` variant (web-forward) + new `bible-passage-picker` comparison (books/reader/search, WEB-ONLY — UIKit overlay has no ViewRegistry case). Confirm dialogs via the shared confirm-dialog service; toggles/expand-collapse animated. Spec memories: `parity-edit-read-activity`, `parity-bible-passage-picker`. ✅ verified 2026-07-04. ⚠️ PARITY GAP 2026-07-28: iOS commit 84be9f8 (monday#12268478769) replaced the '+' with 'Add Bible verse' / 'Add custom text' BoxButtons and removed the source menu — the web twin + edit-read-activity-pane still render the OLD layout (recapture sweep confirmed the structural diff; see _diff/default.png). Follow-up: port the two-button layout to the twin + pane, then recapture |
| Exegesis activity editor | `EditExegesisActivityPage.swift` | `edit-exegesis-activity` (variants: default/configured) | ready | full spec in `parity-edit-exegesis-activity` memory. WIRED 2026-07-04: `EditExegesisActivity` twin (passage chip row, inline BlockStyleEditor w/o themes, #1A1D28 preview + ExegesisVerseView) + adapter toClient — BOTH variants diffed structurally tight vs the existing iPhone refs on first capture; `edit-exegesis-activity-pane` in EditDay (native DOM selection → word-snapped auto-highlight POST, tap-highlight → `exegesisHighlightActionMenu` menu (new `ExegesisHighlightMenu` twin + WEB-ONLY compare `exegesis-highlight-menu`: actions/note-editor) with PREV/count/NEXT + deferred note drafts saved on Save, delete; passage via shared Bible picker gated by the 'Change passage?' confirm (iOS ships it unreachable — ported by intent), style snapshot re-applied after the server replace; Cancel reverts unsaved style). ExegesisVerseView gained additive interactive + selectedRange + multi-verse gutter rendering + font-size-scaled line pitch. Store: exegesis-highlights CRUD. Build + 234 tests green. Awaiting live verify |
| Video activity picker | `Pages/Video/…` + `VideoActivityPicker` | `video-activity-picker` (default variant, iPhone-only) | ready | FRAMING (see `parity-video-activity-picker` memory): iOS picker = DEVICE Photos picker + camera; web maps the iOS media-library-item path — picker twin (library-panel layout: album header + 4-col 9:16 grid + duration badges) lists uploaded videos (GET /api/videos/me, ready only) via managed-modal from the EditDay VIDEO card tap; select → PATCH {videoId,videoUrl,status COMPLETE} with card spinner (non-optimistic, iOS); configured tap/eye-swipe → playback modal (HTML5 video + Remove → PATCH nulls/PENDING); video card shows thumbnail/duration/Select-video. COMPARE: web side now previewable (2026-07-03) — default (empty library panel) + 'library' variant (seeded grid); iPhone pane shows the recorder (harness can't seed showingLibrary) — see fixture note. Upload/record = excluded hardware |
| Create Program | `Pages/Manage/Program/CreateProgramPage.swift` | `create-program` | ✅ verified | `CreateProgram` twin + `create-program-modal.vue` (SlideStack → ProgramHomeModal `preloaded` on success); Library plus-menu entry live (via `.libraryAddMenu` managed-menu); verified 2026-07-03 |
| Exegesis highlight modal | `ExegesisHighlightModal.swift` | — | — | topLevel overlay |
| Read activity preview | `ReadActivityPreviewModal.swift` | — | — | web preview may substitute |

## Group management

| Screen | iPhone source | Compare id | Status | Notes |
|---|---|---|---|---|
| Group Home | `Pages/Manage/Group/GroupHomePage.swift` | `group-home` (variants: default/posts) | ready | WIRED 2026-07-04: `.groupHome` + `.lessonActionMenu` overlay routes (Route.swift-exact), `group-home-modal.vue` host + `leader-group-home` store (group/posts+cursor pagination via twin scroll `loadMore`/join-request badge/next-lesson across active enrollments), Groups-tab CardGroup tap presents; lesson menu rows iOS-exact — Open Lesson LIVE (invite URL → new tab), Edit Activities/Share/Add/Delete dismiss-only until their queue items (enrollment-schedule, StudyInvitePage); toolbar icons + Invite/Enroll inert (edit-group/members/invite/enrollment-flow items); chips inert = TRUE parity (iOS stubs). Build + 234 tests green. COMPARE 2026-07-04: GroupHomeLeader extended additively (statusBar, cover, badge, NEXT LESSON CardLesson, posts wall w/ GroupPostCard + SkeletonPostCard + pagination spinner); default variant diffed pixel-aligned (1.29%, residual = s22 title Display-vs-Text width artifact); `posts` variant web-forward (iPhone can't seed posts/nextLesson — live API); SlideStack gained additive `detailEdge: leading`. SPEC captured 2026-07-04 (`parity-group-home` memory). CORRECTIONS: page = `.groupHome` MODAL overlay (dismissOnTapOutside true), settings form is INLINE (`editGroupContent`) as the outer SlideStack's `.leading` detail (NOT EditGroupPage.swift), members/enrollments/invite = inner TRAILING SlideStack panes; iOS post-create chips + calendar icon are stubs; iOS addScheduledLesson bug (empty body vs required lessonId). Production wiring: CardGroup tap in Groups tab |
| Create Group | `Pages/Manage/Group/CreateGroupPage.swift` | — | — | entry: Groups header plus + AddMenu item |
| Edit Group | INLINE `editGroupContent` in `GroupHomePage.swift:381-529` (NOT EditGroupPage.swift) | `edit-group` (WEB-ONLY — iOS form is private @State-gated) | ready | WIRED 2026-07-04: `EditGroup` twin (composes CoverImagePicker .display / TextInput floating / MultilineTextInput / 3× bare ToggleControl in one group card / AgeRangeInput / MenuInput wheel — iOS-exact strings) + web-only compare captured; ToggleControl gained additive `bare`, AgeRangeInput additive `interactive` (invisible native selects, iOS wheel ranges); group-home-modal hosts it as the leading SlideStack detail (gearshape → seeded drafts — WEB DECISION: drafts not iOS live-bindings; Done = optimistic slide-back + fire-and-forget PATCH; cover = downscale 1200px JPEG → base64 JSON on Done; maxMembers null for Unlimited — iOS omits, can't clear). **SERVER FIX: memberDirectory added to PATCH+create zod → memberDirectoryEnabled column, exposed in all 4 group responses (was silently stripped for BOTH platforms; iOS toggle never persisted).** Build + 234 tests green. SPEC 2026-07-04 (`parity-edit-group` memory): exact strings for all 6 field groups; iOS quirks = LIVE group bindings (no draft) + optimistic Done (fire-and-forget PATCH, no spinner); cover = base64 JSON like program covers. ⚠️ SERVER BUG: `memberDirectory` missing from PATCH zod — silently dropped (fix required for the toggle to work). Outer SlideStack `.leading` detail of the .groupHome modal |
| Group Invite | `Pages/Manage/Group/GroupInvitePage.swift` | `group-invite` (WEB-ONLY, variants: default/toast/error) | ready | WIRED 2026-07-04: `GroupInvite` twin (code card w/ brand@0.2 stroke + brand@0.1 row, 264px white QR card rendering the REAL server bitmap, 3 BoxButtons, Copied toast — iOS-exact strings; fixture embeds a pre-generated deterministic QR for code ABC123; NEW `--color-brand-10` token); group-home-modal gained the INNER trailing SlideStack (rightScreen), paperplane + Invite button both open it (iOS handleInvite); store prefetches GET /api/groups/:id/invite during loadGroupHome (iOS :887); copy code/link → clipboard + 2s toast; Share Invite/Invite friends = navigator.share (iOS payload shapes) w/ copy fallback; join link opens origin+/join/group (env-aware, like iOS openJoinPage). Build + 234 tests green. SPEC 2026-07-04 (`parity-group-invite` memory). CORRECTION: does NOT use ShareInviteSheet/InviteQRCodeView — renders the plain 512px no-logo server QR from `GET /api/groups/:id/invite`; group URL `app.makeready.org/join/group/{code}` (≠ org-wide `www…/join/{code}`); shares TEXT+URL not the QR image. Trailing SlideStack pane (paperplane + Invite button) |
| Group members | `Pages/Manage/Group/Member/GroupMembersPage.swift` | `group-members-page` (WEB-ONLY, variants: default/requests/empty — NOT `group-members`, the TAB comparison) | ready | WIRED 2026-07-05: `GroupMembersPage` twin (floating SearchField + 60px pad + 52px mask-image fade, Requests section w/ Respond pills, "Members" header only when requests exist, CardMember rows w/ Joined/Requested "MMM d, yyyy", empty/error/no-results states — iOS-exact strings); CardMember gained additive `inviteLabel` ("Respond"); host: person.2 → `openMembers` (members fetched on pane open like iOS .task; requests warm from the badge prefetch — store keeps full rows now), search live client-side; member/request taps + Respond intentionally unbound (member-profile / member-request-respond items). Build + 234 tests green. SPEC 2026-07-05 (`parity-group-members` memory). CORRECTIONS: composes CardMember (NOT MemberListItem — no scrubber/sections), flat name-sorted list, floating SearchField over 60pt pad + 52pt gradient mask, Requests section ("Respond" purple ActionButton) with "Members" header only when requests exist. Member/request taps + Respond modal = separate items |
| Member profile | `Pages/Manage/Member/MemberProfilePage.swift` | `member-profile` (WEB-forward: variants default/hero/error; `pages.member-profile` ViewRegistry case ADDED but needs xcodebuild — profile loads over network so iPhone can only ever render the `hero` seed state) | ready | WIRED 2026-07-28: `MemberProfile` twin (full-bleed hero photo/initials + ALWAYS-on gradient, name @45% modal height, 3 circleBlur actions, keyValue+data InfoPanels — both data values PURPLE per iOS, CardGroup rows w/ wrapper-based removed state: 50% dim + UNDIMMED #df1439 1.5px border) + `.memberProfile` overlay route (Route.swift-exact: 100/modal/tapOutside-true) + `member-profile-modal.vue` host + `leader-member` store (GET /api/members/:id/profile; verbatim ports of formatPhoneNumber/relativeDuration/age/earliestJoinDate). Entry: group-home Members pane memberTap → present w/ seed name+avatar (iOS seed choreography). Text/Call/Email → buttons-only confirm dialogs (iOS-exact strings) → sms:/tel:/mailto:. INERT by design: person.badge.plus (iOS CNContact — no web equiv), group-card tap (changeMembership = later item). InfoPanel gained additive per-item `tappable` + `rowTap`. Spec memory: `parity-member-profile`. Build + 234 tests green |
| Member requests | `Pages/Manage/Member/MemberRequestsPage.swift` | `member-requests` (variants default/empty; iPhone side fully seedable via new `pages.member-requests` ViewRegistry case + `CaptureState.joinRequests` — needs user-approved xcodebuild for first capture) | ready | READY 2026-07-28: **managed-page.vue chrome BUILT** (iOS ManagedPageView: translateX push easeOut 300ms in / easeIn 250ms out, derived black@0.3 scrim pointer-events:none — tap-outside NEVER dismisses, 40px edge-swipe zone / 15px min-distance / 80px commit w/ drag carried into exit, completion-tied unmount; `page` chrome added to overlay-routes + overlay-host). `MemberRequestsPage` twin (PageTitle 'Member Requests' + CardMember rows w/ [Group]/[Requested] chips — Group chip omitted when group unknown — + Respond pill; person.badge.clock empty state; TransitionGroup scale+opacity row removal ≈ iOS Motion.standard). Store: LeaderRequest enriched to full rows (SINGLE source of truth — badge count derives from the same array, iOS's desync-prone presenter snapshot deliberately not ported) + approveRequest/rejectRequest (splice-on-success, unused until respond modal lands). Entry: Groups tab 'Member requests' card → present. Row tap + Respond INERT (memberRequestProfile/memberRequestRespond = next item). Spec memory: `parity-member-requests`. Build + 234 tests green |
| Member request respond | `MemberRequestRespondModal.swift` | `member-request-respond` (web captured; iPhone case added, needs approved xcodebuild) | ready | topLevel RAW (own opaque #0d101a wash, dismiss-STRICTLY-before-act, tap-outside swallowed). Spec memory `parity-respond-change-membership`. TWIN 2026-07-30: `MemberRequestRespond` (card/member-request-respond) — full-screen wash + centered name/sentence/3-button stack, approve/reject/cancel emits; registered, build green |
| Change membership | `ChangeMembershipModal.swift` | `change-membership` (5 variants web captured — confirm/transfer panels WEB-ONLY, iOS panel is private @State; iPhone case added for main variants, needs approved xcodebuild) | ready | topLevel RAW; 3-panel state machine. Spec memory `parity-respond-change-membership`. TWIN 2026-07-30: `ChangeMembership` (card/change-membership) — owns panel state machine (initialPanel/initialConfirmKind/initialTransferName capture props), directional slide+fade 300ms, chevron drag-back w/ underlay + commit-inside-completion, CardGroup transfer rows, capture-frozen pulse; remove/rejoin/transfer(id)/close emits; registered, build green |
| Member overview | `MemberOverview.swift` | — | — | |

## Enrollment flows (dismissOnTapOutside: false modals)

| Screen | iPhone source | Compare id | Status | Notes |
|---|---|---|---|---|
| Enrollment flow modal | `Group/Enrollment/EnrollmentFlowModal.swift` | `enrollment-flow` (variants: select-group/select-program/select-dates/confirm — select-dates + confirm WEB-ONLY, step is private @State) | ready | SPEC 2026-07-30 (`parity-enrollment-flow` memory): 3-MOUNTED-PANEL wizard (HStack offset, Motion.standard) — panel 0 = SelectGroupPage (program entry, `.programEnrollmentFlow`) OR SelectStudyProgramPage (group entry, `.enrollmentFlow`), panel 1 = SelectEnrollDatePage (custom UIKit 12-month calendar + ultraThinMaterial range panel + DayOfWeekPicker, defaults Mon–Fri), panel 2 = ConfirmEnrollmentPage(.create) (overview/stats/schedule cards + Send invites 7:30 AM + Require response ON + Sync to study OFF + segmented Updates). dismissOnTapOutside FALSE both routes. Exactly 2 entries: GroupHome Enroll → confirmationOverlay on success; ProgramHome Enrollments + (draft-gated) → silent refresh. POST /api/enrollments (server computes schedule/endDate). NO ViewRegistry case yet. EditEnrollmentFlowModal = separate later item |
| Enrollment schedule | `EnrollmentSchedulePage.swift` | `enrollment-schedule` (variants default/empty, WEB-ONLY — ViewRegistry case + enrollmentDetailsById seeding deferred to the xcodebuild batch) | ready | SPEC 2026-07-30 (`parity-enrollment-schedule` memory). CORRECTIONS: rows are CardLesson (ScheduledLessonCard = dead code); addScheduledLesson bug FIXED (POST schedules {lessonId}); `.editEnrollmentDay` route vestigial (inner SlideStack); swipe `calendar` reschedule = iOS TODO stub. Entries: row tap presents `.enrollmentActionMenu` first (Edit lessons/Edit enrollment/Preview; canManage gate). Editors for scheduled activities = follow-up item |
| Edit enrollment day | `EditEnrollmentDay.swift` | — (inner SlideStack pane of enrollment-schedule; `.editEnrollmentDay` route is VESTIGIAL on iOS) | ready | CORE wired 2026-07-30 inside `enrollment-schedule-modal.vue`: Day-N header (Cancel/Save|Saving... when title dirty, xmark/Done clean), Lesson-title field (PATCH schedules {title}), activity rows (CardLessonActivity small, iOS pending labels/displayName), swipe clear/delete w/ exact alert strings, Dragula reorder (failure → refetch), AddActivityMenu add. Per-activity CONTENT editors LANDED 2026-07-30 — see the `Enrollment activity editors` row (nested SlideStack, activity tap → READ/WRITE/YOUTUBE/EXEGESIS editors, VIDEO picker/playback, eye swipe) |
| Enrollment activity editors | `EditEnrollmentDay.swift` editor branches (:466-525) + shared `Edit{Read,YouTube,Exegesis}ActivityPage` `.enrollment` action providers + private `EditScheduledUserInputView` (:947-1064) | `edit-scheduled-user-input` (WEB-ONLY — iOS view is private; variants default/help-enabled) + `edit-exegesis-activity` `scheduled` variant (WEB-ONLY — ViewRegistry case builds the PROGRAM page) | ready | READY 2026-07-30 (spec memory `parity-enrollment-activity-editors`). SEAM: new `stores/activity-editor-actions.ts` (`ActivityEditorActions` = web port of the iOS action-provider structs; `programActivityActions` default) — READ/EXEGESIS panes gained an additive optional `actions` prop; enrollment store provides `scheduledActivityActions` (supportsBlockStyling FALSE → exegesis style row hidden via additive twin `showStyleEditor`; `updateLessonTitle` ABSENT → set-titles Lesson toggle no-ops like iOS). STORE: rows gained `detail: LeaderActivity` (exported `mapActivity`, `type`→`activityType` normalized) + full scheduled mutation set (read-blocks CRUD/reorder optimistic-first, source-references, exegesis-highlights w/ server-merge absorb, video PATCH {videoId,videoUrl} NO status) + **iOS-port BUG FIX: scheduled EXEGESIS isConfigured misread Swift (`locked` = the locked BLOCK, not a field) — rows always showed not-ready**. HOST: day pane owns a nested SlideStack → editors (READ/EXEGESIS panes w/ scheduled actions, YOUTUBE pane → PATCH {title,youtubeUrl}, new `EditScheduledUserInput` twin → ONE PATCH incl isHelpEnabled + null-cleared help fields — iOS uses 2 calls + can't clear); VIDEO tap → picker/playback modals (busy spinner, non-optimistic), eye swipe (VIDEO&&configured) → playback modal (iOS opens the web activity preview which 404s for scheduled ids); silent loadDetails refresh on editor close. **SERVER FIX: scheduled read-block PATCH zod gained backgroundImageUrl/backgroundColor/backgroundOverlayOpacity/fontSize (mirrors programs.ts; was silently stripping every style write — why iOS gates exegesis styling; READ Edit Themes now persists on web)**. Preview buttons OMITTED in scheduled editors (iOS builds /preview/activity/{scheduledActivityId} → 404s there too). Inherits the edit-read-activity two-button-layout gap (shared pane). Build + 234 tests green; server tsc clean + container restarted. Awaiting live verify |
| Enrollments list | `EnrollmentsListPage.swift` | — | — | |
| Study invite | `StudyInvitePage.swift` | — | — | |
| Unenroll options | `UnenrollOptionsModal.swift` | `unenroll-options` (4 variants options-clean/options-blocked/confirm-remove/confirm-cancel, WEB-ONLY) | ready | WIRED 2026-07-30: `UnenrollOptions` twin (4-phase machine, exact strings incl the disabled full-removal block) + `.unenrollOptions` route (tap-outside TRUE) + `unenroll-options-modal.vue` host (unenroll-info → options → confirm → DELETE /enrollments/:id or POST /cancel-future → ConfirmationOverlay "Processing unenrollment" w/ iOS success messages). Entry: Groups-Enrolled row swipe-trash (iOS MemberHomePage :678) |

## Other

| Screen | iPhone source | Compare id | Status | Notes |
|---|---|---|---|---|
| Org Home | `Pages/Manage/Org/OrgHomePage.swift` | — | — | |
| Invite contacts | `InviteContactsPage.swift` (Pages/…) | — | — | AddMenu → Invite member → Invite contacts |
| Share invite (QR) | `Components/Display/ShareInviteSheet.swift` | — | ✅ verified | `share-invite-sheet.vue` (predates overlay manager — migrate onto it) |
| Global AddMenu | `Components/Navigation/AddMenu.swift` | `AddMenu` (component) | ✅ verified | `add-menu-sheet.vue` (predates overlay manager — migrate) |
| Bible reader/page | `Pages/Bible/…` | `bible-passage-picker` (web-only) | — | large; decide scope separately. The PICKER overlay (BibleReaderOverlay: grids + reader + smart search + version menu) is already built as the `BiblePassagePicker` twin + `bible-passage-picker-host` + `leader-bible` store for edit-read-activity — a standalone reader page can reuse all of it |
| Video recording | `Pages/Video/…` | — | **excluded** | hardware capture — not portable |
| Lesson/Post/Event detail (search results) | `Pages/Search/*.swift` | — | — | after global search |

## Build queue (dependency-ordered)

`/parity-screen` with no argument takes the **first item that is neither `[x]`
nor `[~]`** here. Each item lists what it *needs* and what it *unlocks* — a
prerequisite is satisfied when it is `[x]` verified OR `[~]` ready, so pending
verification never blocks downstream builds. Items become `[~]` (ready) when
their screen reaches `ready`, and `[x]` when the user verifies it live —
`/parity-screen verify` batch-processes every `[~]` item. Some items are
foundation/work items rather than literal screens — they still run through the
same pipeline.

- [x] **program-home** + **edit-program** + **edit-day** + **edit-user-input-activity** + **edit-youtube-activity** — the study-management core (foundation: overlay system, SlideStack, animated border)
- [x] **swipeable-card + drag-reorder** — ✅ verified 2026-07-02. SwipeableCard twin has full iOS mechanics (progressive 24→48 reveal, velocity snap, tap-to-close, `action`/`tap` emits, `bare` mode); new `dragula-list` twin (long-press lift, white@6% 48px indicator, FLIP reorder). Wired: lesson delete+reorder (Program Home), activity reset/clear/delete+reorder (EditDay, iOS gating incl. rawStatus COMPLETE), program delete (Library, creator-gated via island `memberId`), exact iOS confirm strings (DialogOverlay + additive `destructive` style). Spec memory: `parity-swipeable-drag-reorder`. Unlocked: group/enrollment swipe actions later.
- [x] **program-publish-export** — ✅ verified 2026-07-02. Publish badge → Publish/Unpublish DialogOverlay (exact strings) with the zero-activity "Cannot Publish" gate; share icon → GET export-preview → new `export-confirm-overlay` twin (Kpi iconValue grid) → POST export (binary via proxy passthrough) → ConfirmationOverlay (additive secondary button) via the `.confirmationOverlay` route (`confirmation-overlay-modal.vue` host), Save = download `.makeready` / Discard. No compare fixture (ExportConfirmOverlay is private on iOS). Spec memory: `parity-publish-export`. Program Home chrome complete except the eye/preview icon.
- [x] **create-program** — `CreateProgramPage.swift` (ViewRegistry `pages.create-program` exists). Needs: nothing (reuses Edit Program form pieces). Unlocks: Library plus-menu "Study Program" item. **Status: wired, diff clean** — `create-program` twin (empty-form defaults = the iPhone ViewRegistry render; capture-only statusBar; additive MenuInput `interactive` native-select mode), compare fixture+adapter registered, BOTH shots captured, all compare pins resolved (last: `.ToggleControl` BEM collision with the legacy form variant — renamed to `.ToggleRow`; the toggle-description wrap now matches iOS exactly), store createProgram/loadTemplates/addTags (cover + tags synced AFTER create like iOS), `.createProgram` overlay route, dismiss-then-present from the Library create menu, post-create in-place slide to Program Home (`preloaded`). Build+tests green. Spec memory: `parity-create-program`. ✅ verified 2026-07-03.
- [x] **managed-menu chrome** — `managed-menu.vue` (iOS ManagedMenuView: content-sized bottom card, #111215 fill, #242937 stroke, shadow, same springs) wired into overlay-host. Needs: nothing. Unlocks: every `.menu`-priority route (filter/style/highlight/user menus). **Status: wired** — chrome built + `menu` branch in overlay-host; first consumer = `.libraryAddMenu` (Library "+" create menu migrated off LeaderMenuOverlay onto the overlay manager with real `dismissThen` → `.createProgram`; the setTimeout hack is gone). SPEC CORRECTIONS (see `parity-managed-menu` memory): EditDay does NOT present `.lessonActionMenu` (GroupHome/EnrollmentSchedule/Calendar/MainHome do — wire when those port); the Library sort menu is a native SwiftUI `Menu` popover, NOT ManagedMenuView (`.librarySortMenu` route is registered-but-unused on iOS — library-filters-sort item must pick a web equivalent). No compare fixture: same precedent as managed-modal (chrome verified live through consumers; ActionCardMenu content already compare-verified; ManagedMenuView's deferred entrance would snapshot offscreen). Build + host tests green. ✅ verified 2026-07-03.
- [x] **library-filters-sort** — ✅ verified 2026-07-03. Library filter chip dropdowns + Browse-all sort menus. Needs: managed-menu chrome (✓). **COMPARE (policy 2026-07-03): filter states previewable** — LibraryPrograms/LibraryMedia twins gained additive chips/sortLabel/openPanel props; study-programs variants default/filters-active/tags-panel-open + media filters-active captured (web-forward — iPhone panes show the default tab, see fixture notes). Sort MENU itself still has no fixture (managed-menu chrome precedent). **Status: wired** — FilterChipDropdown twin gained ADDITIVE `interactive` mode (live search, toggle/clearAll emits; captured rendering unchanged, recaptured to confirm); library-view chips are live triggers (iOS labels incl. "My content"/"N tags", active = white capsule) with the instant no-animation panel overlay + black@0.5 scrim under the sticky top bar; tags/leaders/type filters REFETCH server-side (?tag/?leaders/?tags/?type via proxy), media time + both sorts client-side; FilterState persistence to /api/preferences/filters.library.{programs,media} (1500ms debounce, iOS-shape JSON incl. enum display-strings, "My content" leaders default when no explicit pref); sort menus present via managed-menu on `.librarySortMenu` (iOS uses a native Menu popover — no web idiom; checkmark rows). Spec memory: `parity-library-filters-sort`.
- [x] **edit-read-activity** — ✅ verified 2026-07-04. `EditReadActivityPage.swift` (ViewRegistry exists). The deepest editor: read blocks (RichTextInput ✓ + DragulaView), third-level SlideStack to BlockStyleEditor (StylePickerMenu ✓, BackgroundSwatch ✓, CoverImagePicker ✓), MediaLibraryPicker pane. Needs: swipeable-card + drag-reorder, managed-menu chrome. **FULLY wired 2026-07-03, verified 2026-07-04** (see row). Also produced the shared confirm-dialog service + animated toggles/expand-collapse (foundation).
- [~] **edit-exegesis-activity** — `EditExegesisActivityPage.swift` (ViewRegistry exists). Needs: edit-read-activity (✓) + managed-menu chrome (✓). **Status: WIRED 2026-07-04** (see row). Awaiting live verify.
- [~] **video-activity-picker** — **Status: wired** (see row). PICKING from uploaded library only — device pick/recording/upload are hardware scope, excluded. Needs: nothing. Awaiting live verify.
- [~] **program-enrollments-tab** — **Status: wired.** ProgramHome twin tab 1 extended ADDITIVELY (enrollments/enrollmentsLoading props + addEnrollment/selectEnrollment emits): 3× SkeletonCardGroup loading, CardGroup rows (new additive `subtitle` prop = creator name; clock metadata = iOS dateRangeString "MMM D - MMM D" uppercased local-tz, start-only when no end; NO member count — iOS omits it) + plus BoxButton; empty state corrected to iOS strings ("Groups enrolled in this program will appear here") + add button. Store loadProgramEnrollments (GET /api/programs/:id/enrollments); modal loads once on first tab select (iOS .task). Row tap + add INERT (enrollment-schedule/flow are later items). COMPARE: 'enrollments' variant added (2026-07-03, web-forward). UPDATE 2026-07-30: iPhone tab seeding now EXISTS (`ProgramHomePage(initialTab:)` ← `shared.selectedTab`); the remaining iPhone-capture blocker is the fixture's enrollment shape — `CaptureState` decode fails on `CaptureEnrollment` (requires `groupId`; also lacks the group-name/date display fields this tab renders). Extend CaptureEnrollment + the fixture seed to unblock. Needs: nothing. Unlocks: enrollment-flow entry points. Awaiting live verify.
- [~] **group-home** — `GroupHomePage.swift`. **Status: WIRED 2026-07-04** (see row). SlideStack `detailEdge: leading` foundation BUILT (consumed by edit-group next). Unlocks: the whole group chain. Awaiting live verify.
- [~] **edit-group** — the INLINE `editGroupContent` form in `GroupHomePage.swift:381-529`. **Status: WIRED 2026-07-04** (see row) — first consumer of SlideStack `detailEdge: leading`; server memberDirectory zod fix included. Awaiting live verify (with group-home).
- [~] **group-invite** — `GroupInvitePage.swift`. **Status: WIRED 2026-07-04** (see row) — inner trailing SlideStack added to the group-home host (members/enrollments panes plug into it next). Groups-header paperplane still TODO (needs a group picker or per-group entry — decide when wiring). Awaiting live verify (with group-home).
- [~] **group-members** — `GroupMembersPage.swift`. **Status: WIRED 2026-07-05** (see row; SPEC CORRECTION: uses CardMember, NOT MemberListItem). Awaiting live verify (with the group chain).
- [~] **member-profile** — `MemberProfilePage.swift`. Needs: group-members ([~] satisfied). READY 2026-07-28 (see row); built out-of-order under the new ready/[~] queue rules.
- [~] **managed-page chrome + member-requests** — READY 2026-07-28 (see row): managed-page.vue chrome + twin + wiring done. Needs: group-members ([~] satisfied). Unlocks: member-request-respond + change-membership.
- [~] **member-request-respond + change-membership** — READY 2026-07-30. Twins `MemberRequestRespond` + `ChangeMembership` (own opaque #0d101a wash; panel machine w/ capture-only initialPanel props; CardGroup transfer rows); `.memberRequestRespond`/`.changeMembership` topLevel RAW routes + hosts (synced wash-opacity/content-scale springs, dismiss-STRICTLY-before-act); wired at ALL 3 iOS surfaces — member-requests page Respond (splice-on-success, fallbacks 'This member'/'the group'), group-home Members pane Respond (FULL refetch via new store respondToRequest), member-profile group-card tap (mode from removed state, candidates = org groups minus active, remove/rejoin/transfer w/ ADD-then-REMOVE composite; card flips via store, 'this member' lowercase fallback). Failures = shared confirm dialog w/ iOS strings (NO web error-banner foundation yet — future item; iOS uses the global banner). Web candidates' activeStudies currently 0 (no enrollment-count source in leader-groups store — chip omitted; refine when enrollments carry groupId). Compare: web 1/1 + 5/5 variants captured; iPhone ViewRegistry cases pages.member-request-respond/change-membership + CaptureRespondSeed/CaptureChangeMembershipSeed added — FIRST iPHONE CAPTURE + diff.mjs NEED a user-approved xcodebuild. Spec memory parity-respond-change-membership.
- [~] **program-analytics-tab (web port)** — **Status: ready 2026-07-30** (memory `parity-program-analytics-tab`). TWIN: ProgramHome analytics tab (replaces "Coming soon") — KPI 2×2 116px rows (`Kpi` additive `expand`), Top Groups capsule table, Week/Month/Year `VerticalBarChart` (additive `xAxisValues` thinning i>0&&i%7==0 + `yAxisWidth 'auto'` hugging col), `HeatMapChart` additive `showXLabels` bottom band, verbatim hide rules, RelativeDateTimeFormatter-mirror footer (capture-only `analyticsNow` pins "11 hours ago"). ⚠️ Swift beats the spec doc: NO video KPI row / funnel / donut / top-members on iOS (Phase C) — omitted. COMPARE: analytics 2.18% / analytics-sparse 1.72%, structurally clean (residuals = pre-existing cover/tabs +8px offset also on the verified default variant + font AA + captureHeight tail). Swift-Charts finds: heatmap y-domain = [0,23] when a non-zero cell reaches the top band else [0,24] (twin `yUnits` + minmax(0,0fr) top row); y labels anchor at cell BOTTOM edges; day labels LEADING-aligned at marks. WIRED: store `loadProgramAnalytics` (timezone IANA param) + modal tab-2 watch (cache-first, refresh per select, error only w/o cache). Build + 234 tests green. Grows again when analytics Phase C lands — port what exists then. Awaiting live verify. (2026-07-30, memory `parity-program-analytics-tab`; ⚠️ Swift beats the spec doc: NO video KPI row / funnel / donut / top-members on iOS — omit for parity. Twin: ProgramHome analytics tab built — KPI 2×2 `Kpi expand` rows, Top Groups capsule table, Week/Month/Year `VerticalBarChart xAxisValues` thinning, `HeatMapChart showXLabels` bottom day band, verbatim hide rules, `analyticsNow` capture-only footer determinism) — port the now-real iOS Program Home **Analytics tab** to the shared `ProgramHome` twin + production LeaderApp (replaces the twin's "Coming soon" stub). Spec is NOT the Swift-only source this time: `docs/features/analytics/program-analytics-tab.md` + STATUS.md carry the owner-approved design (2×2 uniform KPI grid, Top Groups table w/ completion % + capsule fills, Week/Month/Year VerticalBarChart w/ thinned month axis, zero-filled 7×24 heatmap, per-section hide-when-empty, freshness footer) — mirror `ProgramHomePage.swift` analytics sections for pixel truth. Data: `GET /api/programs/:id/analytics` via the `/admin/api` proxy (wrapper already live; response contract in the spec § Data contract). Web twins for the charts exist (`compare-kpi-twin`, `compare-verticalbarchart-model`, `compare-heatmapchart-empty-iphone` memories) but need the additive params the iOS components gained (`Kpi expand`, `VerticalBarChart xAxisValues`) + the zero-fill/hide logic. Compare: reuse the `analytics`/`analytics-sparse` variants — they become two-sided once the twin renders. Needs: program-home (✓). Grows again when analytics Phase C lands (funnel/content mix/top members) — port what exists at build time and note the rest.
- [~] **enrollment-flow** — READY 2026-07-30 (spec memory `parity-enrollment-flow`; CORRECTION: GroupSelectorSheet/DatePickerField/BoxButton NOT used by iOS — the flow composes PageTitle/SearchField/CardGroup/CardStudySelectable/skeletons/FieldGroup/ToggleControl/MenuInput + a custom UIKit calendar). TWINS: new `EnrollmentFlow` (3-mounted-panel track, controlled props) + new `EnrollCalendar` (12-month geometry, #3D2C8C week-range bars r22.5, 45px terminals, 36px override rings green/pink, 40px ghosts, Sun-first weekday header w/ disabled dimming) + additive PageTitle `rightLinkDisabled`, CardStudySelectable `disabled`/`enrolledUntil`. COMPARE: `enrollment-flow` 4 web variants captured + reviewed (webBuilt 4/4); iPhone side = new `pages.enrollment-flow` ViewRegistry case + CaptureFixture `enrollmentFlowEntry` (panel 0 only — step/calendar/confirm are private @State → select-dates/confirm WEB-ONLY) — FIRST iPHONE CAPTURE NEEDS user-approved xcodebuild. WIRED: `.enrollmentFlow`/`.programEnrollmentFlow` routes (dismissOnTapOutside FALSE) + `enrollment-flow-modal.vue` host (EnrollmentDateState mirror: ≥today gating, override toggles via long-press in-range + tap-dialog Remove Override/Set as Start Date, overlap alert vs existing lesson dates w/ Schedule anyway, toggleDay floor 1, endDate walk maxIter×10; POST /api/enrollments w/ dayMap+timezone; iOS defaults 7:30 AM / requireResponse ON / sync OFF) + new `leader-enrollment-flow` store (groups/programs pickers, group context incl per-enrollment lesson dates, createEnrollment); entries: group-home Enroll → `.enrollmentFlow` + success ConfirmationOverlay ("Processing enrollment" → "**{group}** has been successfully enrolled…") + posts/nextLesson refresh; program-home Enrollments "+" → draft-gated `.programEnrollmentFlow` + silent enrollments refresh (loadProgramEnrollments now carries groupId). Build + 234 tests green. NOT ported (refinements): blackout drag-PAINT (single-day long-press toggle only), calendar highlight travel/bounce animations. Awaiting live verify. Unlocks: enrollment-schedule.
- [~] **enrollment-schedule** — READY 2026-07-30 (spec memory `parity-enrollment-schedule`; CORRECTIONS: rows = CardLesson NOT the dead ScheduledLessonCard; add-lesson bug already fixed on iOS; `.editEnrollmentDay` route vestigial; swipe `calendar` reschedule = iOS TODO stub → web inert = TRUE parity). TWINS: `EnrollmentSchedule` (page: iconTitleIcon header w/ sync icon, CardLesson rows w/ additive `fill` percent-complete boxes, skeletons ×10, iOS empty/error) + `EnrollmentActionMenu` (manage rows / FYI readonly card) + `UnenrollOptions` (4-phase machine); CardLesson gained additive per-activity `fill`. COMPARE: 3 comparisons all WEB-ONLY (enrollment-schedule 2, enrollment-action-menu 2, unenroll-options 4 variants; ViewRegistry cases + enrollmentDetailsById/unenroll-info seeding plumbing deferred to the approved-xcodebuild batch — fixtures note it). WIRED: `.enrollmentSchedule` (no tap-outside) / `.enrollmentActionMenu` (menu) / `.unenrollOptions` routes; `leader-enrollment-schedule` store (details+completion-stats→fills, add next-unscheduled lesson w/ "All lessons…scheduled" info, delete/title/scheduled-activity CRUD w/ verbatim `isConfigured`, lesson invite, unenroll trio); `enrollment-schedule-modal.vue` host (row tap → lessonActionMenu Edit Activities/Open/Share/Add/Delete; sync icon → EnrollmentSyncPane pane; EditEnrollmentDay CORE as inner SlideStack detail); entries: Groups-Enrolled row tap→actionMenu + swipe-trash→unenroll; ProgramHome Enrollments row tap→actionMenu (sync pane stays reachable via the schedule's sync icon). Edit-enrollment + Preview-study menu rows dismiss-only (later items). Build + 234 tests green. Awaiting live verify.
- [~] **enrollment-activity-editors** — READY 2026-07-30 (spec memory `parity-enrollment-activity-editors`). The iOS action-provider seam ported: `ActivityEditorActions` interface + program/scheduled factories; READ/EXEGESIS panes take an additive `actions` prop; enrollment store keeps full `detail: LeaderActivity` per row + the whole scheduled mutation set; day pane hosts a nested SlideStack to the 4 editors + VIDEO picker/playback modals + eye swipe; new `EditScheduledUserInput` twin (WEB-ONLY compare, 2 variants) + exegesis `scheduled` variant (style row hidden, WEB-ONLY). Server fix: scheduled read-block PATCH now accepts the 4 style fields (was silently stripping). iOS-port bug fixed: scheduled EXEGESIS isConfigured. Needs: enrollment-schedule ([~] satisfied). Awaiting live verify.
- [~] **calendar** — READY 2026-07-30 (spec memory `parity-calendar`). ⚠️ SPEC CORRECTION: iOS was never a stub — only the web was; and the queue item's assumption was wrong on components: `UpcomingLessonCard` + `WeekdayIndicator` have ZERO production call sites and are NOT part of this screen (the grid is a UIKit collection view; the event list is CardLesson). Built the `SplitMonthCalendar` page twin + `leader-calendar` store + `calendar-view.vue` (replaces ComingSoonView), wired the lessonActionMenu row set (no "Add Lesson" here) into the already-built `.enrollmentSchedule` modal via a new additive `initialScheduleId`. Compare `calendar` is now two-sided on `default` (0.45%) with `with-events` + `expanded` web-only. Awaiting live verify.
- [~] **global-search** — READY 2026-07-30 (spec memory `parity-global-search`). ⚠️ QUEUE-ITEM CORRECTIONS: `SearchableList` is NOT used by this page (it hand-rolls its own ScrollView + section headers; SearchableList belongs to the contacts pages), and the "string-keyed overlay ids" note is only half right — 5 of 9 destinations are typed Route cases, 4 are ad-hoc ids. Built the `GlobalSearch` twin + `leader-search` store + `search-view.vue` (replaces ComingSoonView); program/group/member/enrollment taps wired to existing hosts, the four detail-page taps left inert for the next item. Compare now two-sided on both original variants (0.30% / 0.16%) plus 4 web-only variants covering every wired state. Awaiting live verify.
- [ ] **search-detail-pages** — `LessonDetailPage` / `PostDetailPage` / `EventDetailPage`. Needs: global-search.
- [ ] **profile** — `ProfilePage.swift` (UserMenu twin ✓). Needs: nothing.
- [ ] **notification-feed** — `NotificationFeedPage.swift` + PageHeader bell wiring. Needs: nothing (deep links richer as screens land).
- [ ] **org-home** — `OrgHomePage.swift`. Needs: nothing.
- [ ] **invite-contacts** — `InviteContactsPage.swift` (AddMenu → Invite member → Invite contacts). Needs: nothing.
- [ ] **overlay-migration cleanup** — move the pre-overlay-manager surfaces (dashboard AddMenuSheet, ShareInviteSheet, Library create-menu / LeaderMenuOverlay) onto the overlay manager + managed-menu chrome. Needs: managed-menu chrome. Do last-ish; pure refactor.
