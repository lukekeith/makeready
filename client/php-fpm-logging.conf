[www]
; Send PHP worker output (error_log, stderr) to the FPM master process
; so supervisord can capture it and forward to container stdout/stderr.
catch_workers_output = yes
decorate_workers_output = no

[global]
; Send FPM error log to stderr (captured by supervisord → container stderr)
error_log = /proc/self/fd/2
