---
phase: 01-foundation-compliance
plan: 04
type: execute
wave: 3
depends_on: ["01-01", "01-02", "01-03"]
files_modified:
  - .github/workflows/ci.yml
autonomous: false
requirements:
  - INFR-01

must_haves:
  truths:
    - "GitHub Actions CI workflow runs composer install, npm run build, and php artisan test on push"
    - "CI workflow builds the Docker image and runs a smoke test"
    - "Pushing a commit triggers the workflow and it passes"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "CI/CD pipeline for Laravel"
      contains: "php artisan test"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "Dockerfile"
      via: "docker build step"
      pattern: "docker build"
---

<objective>
Create the GitHub Actions CI/CD workflow for the Laravel app and verify the full pipeline works end-to-end with a push.

Purpose: Without CI, there is no automated safety net for deployments. This also validates the Dockerfile builds correctly in a clean environment.
Output: Working CI pipeline that builds, tests, and Docker-builds on every push.
</objective>

<execution_context>
@/Users/lukekeith/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lukekeith/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-compliance/01-CONTEXT.md
@.planning/phases/01-foundation-compliance/01-RESEARCH.md
@.planning/phases/01-foundation-compliance/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GitHub Actions CI workflow for Laravel</name>
  <files>.github/workflows/ci.yml</files>
  <action>
    Replace the existing `.github/workflows/ci.yml` (which was for the React app) with the Laravel CI workflow from RESEARCH.md Pattern 8.

    The workflow must have two jobs:

    **Job 1: build-and-test**
    - Trigger on push to main and develop, and PRs to main and develop
    - Setup PHP 8.3 with extensions: mbstring, pdo, zip, gd, bcmath
    - Setup Node.js 20
    - `composer install --no-dev --no-interaction --optimize-autoloader`
    - `npm ci && npm run build`
    - `cp .env.example .env`
    - `php artisan key:generate`
    - `php artisan test`

    **Job 2: docker-build** (needs build-and-test)
    - `docker build -t makeready-client:test .`
    - Run container with test env vars (APP_KEY, APP_ENV=testing, API_URL)
    - Sleep 5 seconds for startup
    - `curl -sf http://localhost:8080/up || exit 1` — Laravel's built-in health route
    - Stop container

    Ensure the workflow file is valid YAML. Use `actions/checkout@v4`, `shivammathur/setup-php@v2`, `actions/setup-node@v4`.
  </action>
  <verify>
    <automated>cd /Users/lukekeith/www/makeready/client && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))" && echo "YAML valid"</automated>
  </verify>
  <done>
    - .github/workflows/ci.yml exists with valid YAML
    - Workflow has build-and-test job (PHP + Node + tests)
    - Workflow has docker-build job (builds image, smoke tests)
    - Triggers on push to main/develop and PRs
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Phase 1 end-to-end</name>
  <files>None — verification only</files>
  <action>
    Present the full Phase 1 deliverables to the user for visual and functional verification. Start the Laravel dev server with `php artisan serve` before presenting.
  </action>
  <verify>
    <automated>cd /Users/lukekeith/www/makeready/client && php artisan test</automated>
  </verify>
  <done>User approves Phase 1 deliverables</done>
  <what-built>
    Complete Phase 1 foundation: Laravel project, API service with cookie proxy, auth middleware, three compliance pages (/privacy, /terms, /sms-opt-in), Dockerfile, and CI pipeline. All automated tests pass locally.
  </what-built>
  <how-to-verify>
    1. Run `php artisan test` — all tests should pass
    2. Run `npm run build` — Vite should compile without error
    3. Visit http://localhost:8000/privacy — should show privacy policy with compliance language
    4. Visit http://localhost:8000/terms — should show terms with STOP/HELP in bold
    5. Visit http://localhost:8000/sms-opt-in — should show unchecked checkbox demo
    6. Run `curl -s http://localhost:8000/privacy | head -20` — should see real HTML, not blank div
    7. Run `docker build -t makeready-test .` — should build successfully
    8. (Optional) Push to GitHub and verify CI workflow runs green
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 1, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
1. `.github/workflows/ci.yml` is valid YAML with correct job structure
2. Full test suite passes: `php artisan test`
3. Docker build succeeds
4. Human verifies compliance pages look correct
</verification>

<success_criteria>
- CI workflow file created and valid
- All Phase 1 tests pass locally
- Docker image builds
- Human confirms compliance pages render correctly
- Phase 1 is complete and ready for Railway deployment
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-compliance/01-04-SUMMARY.md`
</output>
