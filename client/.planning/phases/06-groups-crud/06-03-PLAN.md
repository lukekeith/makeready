---
phase: 06-groups-crud
plan: "03"
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - resources/js/components/admin/admin-image-upload/admin-image-upload.vue
  - resources/css/components/admin/admin-image-upload.scss
  - resources/css/components/admin/admin-tabs.scss
  - resources/css/app.scss
  - resources/js/islands/admin-island/stores/ui/group-detail.ui.ts
  - resources/js/islands/admin-island/sections/groups-section.vue
autonomous: true
requirements: [GRP-03, GRP-04, GRP-05, GRP-07]

must_haves:
  truths:
    - "Leader sees group detail with name, cover image, and tab strip"
    - "Tab strip has Members, Enrollments, Posts, and Settings tabs that switch content"
    - "Settings tab shows an editable form with metadata and settings fields"
    - "Leader can upload a cover image and see a preview before it saves"
    - "Members, Enrollments, and Posts tabs show placeholder content"
  artifacts:
    - path: "resources/js/components/admin/admin-image-upload/admin-image-upload.vue"
      provides: "Reusable image upload component with preview"
    - path: "resources/js/islands/admin-island/stores/ui/group-detail.ui.ts"
      provides: "Group detail UI store with tab state and settings form values"
      exports: ["useGroupDetailUI"]
    - path: "resources/css/components/admin/admin-tabs.scss"
      provides: "Tab strip styling for reka-ui Tabs"
  key_links:
    - from: "groups-section.vue"
      to: "group-detail.ui.ts"
      via: "useGroupDetailUI() Pinia store"
      pattern: "useGroupDetailUI"
    - from: "groups-section.vue"
      to: "reka-ui Tabs"
      via: "TabsRoot/TabsList/TabsTrigger/TabsContent imports"
      pattern: "TabsRoot"
    - from: "groups-section.vue"
      to: "groups.domain.ts uploadCoverImage"
      via: "domain store method call"
      pattern: "domain\\.uploadCoverImage"
---

<objective>
Build the group detail view with tabbed interface, settings form, and cover image upload.

Purpose: Completes the Groups CRUD feature by adding the detail/settings view. The tab chrome pattern established here will be reused for Program detail in Phase 7. Cover image upload is the first multipart upload in the admin surface.
Output: Group detail view with 4 tabs (3 stubs + working settings), cover image upload, AdminImageUpload reusable component
</objective>

<execution_context>
@/Users/lukekeith/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lukekeith/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-groups-crud/06-RESEARCH.md
@.planning/phases/06-groups-crud/06-02-SUMMARY.md

<interfaces>
<!-- From Plan 02 — stores and components created -->

From groups.domain.ts:
```typescript
export interface Group {
  id: string; name: string; description?: string; coverImageUrl?: string;
  isPrivate: boolean; allowInvites: boolean; memberDirectory: boolean;
  welcomeMessage?: string; ageRange?: { min?: number; max?: number };
  maxMembers?: number; memberCount?: number;
}
export const useGroupsDomain = defineStore('groups-domain', () => {
  // ...
  async function getGroup(id: string): Promise<Group>
  async function updateGroup(id: string, payload: UpdateGroupPayload): Promise<Group>
  async function uploadCoverImage(id: string, file: File): Promise<string>
  // ...
})
```

From groups-list.ui.ts:
```typescript
export const useGroupsListUI = defineStore('groups-list-ui', () => {
  // ... list-specific state — detail view uses a SEPARATE store
})
```

From admin-form.vue props:
```typescript
interface Props {
  open: boolean; title: string;
  fields: { key: string, label: string, type: 'text'|'textarea'|'toggle'|'number'|'select', required?: boolean, placeholder?: string, options?: { value: any, label: string }[] }[];
  values: Record<string, any>; error?: string; saving?: boolean;
}
// Emits: save(payload), cancel()
```

reka-ui Tabs imports:
```typescript
import { TabsRoot, TabsList, TabsTrigger, TabsContent } from 'reka-ui'
```

groups-section.vue (from Plan 02):
- Already has list view with v-if="!route.params.id"
- Detail placeholder at v-else — to be replaced by this plan
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AdminImageUpload component, group-detail.ui.ts store, and AdminTabs SCSS</name>
  <files>resources/js/components/admin/admin-image-upload/admin-image-upload.vue, resources/css/components/admin/admin-image-upload.scss, resources/css/components/admin/admin-tabs.scss, resources/css/app.scss, resources/js/islands/admin-island/stores/ui/group-detail.ui.ts</files>
  <action>
**admin-image-upload.vue** — Pure display component, NO store imports:
- Props: `currentUrl?: string`, `uploading?: boolean`, `label?: string` (default "Cover Image"), `accept?: string` (default "image/*"), `maxSizeMb?: number` (default 5)
- Emits: `upload(file: File)`
- Internal state: `previewUrl: ref(props.currentUrl ?? null)`, track `objectUrl` for cleanup
- Template: BEM root `.AdminImageUpload`
  - `.AdminImageUpload__label` — label text
  - `.AdminImageUpload__preview` — if previewUrl, show `<img>` (200px wide, aspect ratio auto, border-radius 8px). If no previewUrl, show `.AdminImageUpload__placeholder` with camera icon (from lucide-vue-next: `Camera` or `ImagePlus`)
  - `.AdminImageUpload__input-wrapper` — styled file input:
    - Hidden `<input type="file" :accept="accept">` with `@change="handleFileChange"`
    - Visible button label "Choose Image" or "Change Image" (based on whether previewUrl exists)
  - `.AdminImageUpload__uploading` — if `uploading`, show "Uploading..." text
  - Client-side size validation: if `file.size > maxSizeMb * 1024 * 1024`, show error text, do not emit
- `handleFileChange(event)`:
  - Get file from input.files[0]
  - Revoke previous objectUrl if exists: `URL.revokeObjectURL(objectUrl)`
  - Create new: `objectUrl = URL.createObjectURL(file)`
  - Set `previewUrl.value = objectUrl`
  - Emit `upload(file)`
- `onBeforeUnmount`: revoke objectUrl

**admin-image-upload.scss** — BEM:
- `.AdminImageUpload`: display flex, flex-direction column, gap 12px
- `.AdminImageUpload__label`: font-size 13px, font-weight 500, color rgba(255,255,255,0.7)
- `.AdminImageUpload__preview img`: max-width 200px, border-radius 8px, object-fit cover
- `.AdminImageUpload__placeholder`: width 200px, height 120px, border-radius 8px, background rgba(255,255,255,0.06), border 1px dashed rgba(255,255,255,0.2), display flex, align-items center, justify-content center, color rgba(255,255,255,0.3)
- `.AdminImageUpload__input-wrapper`: display flex, align-items center, gap 8px
- `.AdminImageUpload__btn`: background rgba(255,255,255,0.1), color white, padding 8px 14px, border-radius 6px, border none, font-size 13px, cursor pointer, hover background rgba(255,255,255,0.15)
- `.AdminImageUpload__error`: color #ef4444, font-size 12px
- `.AdminImageUpload__uploading`: color rgba(255,255,255,0.5), font-size 13px

**admin-tabs.scss** — BEM for reka-ui Tabs chrome:
- `.AdminTabs__list`: display flex, gap 0, border-bottom 1px solid rgba(255,255,255,0.1), margin-bottom 24px
- `.AdminTabs__trigger`: padding 10px 20px, font-size 14px, font-weight 500, color rgba(255,255,255,0.5), background none, border none, border-bottom 2px solid transparent, cursor pointer, transition color 150ms / border-color 150ms. `&[data-state="active"]`: color white, border-bottom-color #7c3aed. Hover: color rgba(255,255,255,0.8)
- `.AdminTabs__content`: min-height 200px

**Update app.scss** — add under `// Admin components`:
```scss
@use 'components/admin/admin-image-upload' as admin-image-upload;
@use 'components/admin/admin-tabs' as admin-tabs;
```

**group-detail.ui.ts** — Pinia composition store:
- Store ID: `'group-detail-ui'`
- Import `useGroupsDomain`, `useRoute` from vue-router
- Refs: `activeTab: ref('settings')` (default to settings since other tabs are stubs), `isSavingSettings: ref(false)`, `isUploadingCover: ref(false)`, `settingsError: ref<string | null>(null)`
- Computeds:
  - `currentGroup`: find group from domain.groups by `route.params.id`
  - `settingsFormValues`: derive from currentGroup — `{ name, description, welcomeMessage, isPrivate, allowInvites, memberDirectory, maxMembers }`
  - `settingsFields`: returns array of FormField definitions:
    - `name` (text, required), `description` (textarea), `welcomeMessage` (textarea, placeholder "Message shown to new members"),
    - `isPrivate` (toggle), `allowInvites` (toggle), `memberDirectory` (toggle),
    - `maxMembers` (number, placeholder "Leave empty for unlimited")
  - `pageTitle`: `currentGroup?.name ?? 'Group Detail'`
- Return all refs and computeds
  </action>
  <verify>
    <automated>cd /Users/lukekeith/www/makeready/client && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>AdminImageUpload component exists with file preview and upload emit. AdminTabs SCSS styles reka-ui tab triggers with active state. group-detail.ui.ts exports useGroupDetailUI with settings form computeds. All SCSS compiles. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Extend groups-section.vue with detail view, tabs, settings form, and cover image upload</name>
  <files>resources/js/islands/admin-island/sections/groups-section.vue</files>
  <action>
Extend the existing groups-section.vue (from Plan 02) to add the group detail view when `route.params.id` is present.

Add imports:
- `import { useGroupDetailUI } from '../stores/ui/group-detail.ui'`
- `import { TabsRoot, TabsList, TabsTrigger, TabsContent } from 'reka-ui'`
- `import AdminImageUpload from '../../../components/admin/admin-image-upload/admin-image-upload.vue'`
- `import AdminForm from '../../../components/admin/admin-form/admin-form.vue'` (already imported from Plan 02)
- `import { ArrowLeft } from 'lucide-vue-next'` (for back button)

Add to script setup:
- `const detailUI = useGroupDetailUI()`
- Update `onMounted`: if `route.params.id`, call `domain.getGroup(route.params.id as string)` instead of `loadGroups()`

Add handler functions:
- `handleSettingsSave(payload)`: set `detailUI.isSavingSettings = true`, call `domain.updateGroup(route.params.id, payload)`, catch sets `detailUI.settingsError`, finally resets isSaving
- `handleCoverUpload(file: File)`: set `detailUI.isUploadingCover = true`, call `domain.uploadCoverImage(route.params.id, file)`, finally resets isUploadingCover

Replace the detail placeholder (`v-else` branch) with:
```html
<div class="AdminSection" v-else>
  <header class="AdminSection__header">
    <button class="AdminSection__back-btn" @click="$router.push('/admin/groups')">
      <ArrowLeft :size="18" /> Back to Groups
    </button>
    <h1 class="AdminSection__title">{{ detailUI.pageTitle }}</h1>
  </header>

  <div class="AdminSection__body">
    <!-- Cover Image Upload -->
    <AdminImageUpload
      :current-url="detailUI.currentGroup?.coverImageUrl"
      :uploading="detailUI.isUploadingCover"
      label="Group Cover Image"
      @upload="handleCoverUpload"
    />

    <!-- Tabs -->
    <TabsRoot v-model="detailUI.activeTab" class="AdminTabs" style="margin-top: 24px">
      <TabsList class="AdminTabs__list">
        <TabsTrigger value="members" class="AdminTabs__trigger">Members</TabsTrigger>
        <TabsTrigger value="enrollments" class="AdminTabs__trigger">Enrollments</TabsTrigger>
        <TabsTrigger value="posts" class="AdminTabs__trigger">Posts</TabsTrigger>
        <TabsTrigger value="settings" class="AdminTabs__trigger">Settings</TabsTrigger>
      </TabsList>
      <TabsContent value="members" class="AdminTabs__content">
        <p style="color: rgba(255,255,255,0.4); padding: 20px 0;">Members management — coming in Phase 9</p>
      </TabsContent>
      <TabsContent value="enrollments" class="AdminTabs__content">
        <p style="color: rgba(255,255,255,0.4); padding: 20px 0;">Enrollments management — coming in Phase 9</p>
      </TabsContent>
      <TabsContent value="posts" class="AdminTabs__content">
        <p style="color: rgba(255,255,255,0.4); padding: 20px 0;">Posts management — coming in Phase 9</p>
      </TabsContent>
      <TabsContent value="settings" class="AdminTabs__content">
        <AdminForm
          :key="'settings-' + (detailUI.currentGroup?.id ?? 'none')"
          :open="true"
          title="Group Settings"
          :fields="detailUI.settingsFields"
          :values="detailUI.settingsFormValues"
          :error="detailUI.settingsError"
          :saving="detailUI.isSavingSettings"
          @save="handleSettingsSave"
          @cancel="() => {}"
        />
      </TabsContent>
    </TabsRoot>
  </div>
</div>
```

IMPORTANT: The settings form uses AdminForm but NOT as a Dialog modal — it's rendered inline in the Settings tab content. Since AdminForm wraps content in DialogRoot, either:
- Option A: Create AdminForm with a prop `inline?: boolean` that skips the Dialog wrapper and renders the form directly. This is cleaner.
- Option B: Create a separate inline form. This duplicates code.

Go with Option A: Add an `inline` boolean prop to AdminForm. When `inline=true`, render form fields directly without DialogRoot/DialogPortal/DialogOverlay/DialogContent wrappers. The form content (title, fields, actions) stays the same. Update admin-form.vue from Plan 02 to support this. In groups-section.vue detail view, use `<AdminForm :inline="true" ...>`.

When inline, the Cancel button can be hidden (no cancel action for settings — just don't save). Add `hideCancelButton?: boolean` prop to AdminForm as well.

Add `.AdminSection__back-btn` styling: display flex, align-items center, gap 6px, background none, border none, color rgba(255,255,255,0.5), font-size 13px, cursor pointer, hover color white, margin-bottom 8px.

Watch for route changes: use `watch(() => route.params.id, (newId) => { if (newId) domain.getGroup(newId as string) })` to reload group data when navigating between detail views.
  </action>
  <verify>
    <automated>cd /Users/lukekeith/www/makeready/client && npm run build 2>&1 | tail -5 && php artisan test tests/Feature/GroupsAdminTest.php 2>&1 | tail -10</automated>
  </verify>
  <done>Group detail view renders with cover image upload, 4-tab strip (reka-ui Tabs), settings form inline in Settings tab, and placeholder content in Members/Enrollments/Posts tabs. Back button navigates to /admin/groups. Cover image upload shows preview and calls domain store. Settings save calls updateGroup. Build passes. All tests pass.</done>
</task>

</tasks>

<verification>
npm run build — passes (all Vue + SCSS compiles)
php artisan test tests/Feature/GroupsAdminTest.php — all 7 pass
php artisan test — full suite green
Manual: navigate to /admin/groups/:id — see cover image, tabs, settings form
</verification>

<success_criteria>
- Group detail page shows group name as title with back button
- Cover image upload shows preview and saves via domain store
- 4 tabs render: Members, Enrollments, Posts (stubs), Settings (working form)
- Settings form saves metadata and settings fields via PATCH
- AdminImageUpload is reusable (no store imports)
- Tab switching works without page reload
- reka-ui Tabs handle keyboard navigation and ARIA
</success_criteria>

<output>
After completion, create `.planning/phases/06-groups-crud/06-03-SUMMARY.md`
</output>
