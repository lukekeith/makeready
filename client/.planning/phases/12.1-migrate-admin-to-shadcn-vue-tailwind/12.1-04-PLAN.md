---
phase: 12.1-migrate-admin-to-shadcn-vue-tailwind
plan: 04
type: execute
wave: 2
depends_on: [12.1-01]
files_modified:
  - resources/js/components/admin/admin-virtual-table/admin-virtual-table.vue
  - resources/js/components/admin/member-filter-bar/member-filter-bar.vue
  - resources/js/components/admin/member-profile-drawer/member-profile-drawer.vue
autonomous: true
requirements: [COMP-VTABLE, COMP-FILTER, COMP-DRAWER]

must_haves:
  truths:
    - "AdminVirtualTable renders with shadcn Table styling while keeping TanStack Virtual scrolling"
    - "MemberFilterBar uses shadcn Combobox for filter input and Badge for active filter chips"
    - "MemberProfileDrawer renders as a shadcn Sheet sliding from the right"
    - "Virtual table spacer rows do not show hover/border artifacts"
  artifacts:
    - path: "resources/js/components/admin/admin-virtual-table/admin-virtual-table.vue"
      provides: "Hybrid TanStack Virtual + shadcn Table"
      contains: "TableRow"
    - path: "resources/js/components/admin/member-filter-bar/member-filter-bar.vue"
      provides: "Filter bar with shadcn Combobox + Badge"
      contains: "Badge"
    - path: "resources/js/components/admin/member-profile-drawer/member-profile-drawer.vue"
      provides: "Profile drawer using shadcn Sheet"
      contains: "SheetContent"
  key_links:
    - from: "admin-virtual-table.vue"
      to: "@tanstack/vue-virtual"
      via: "useVirtualizer composable preserved"
      pattern: "useVirtualizer"
    - from: "member-profile-drawer.vue"
      to: "@/components/ui/sheet"
      via: "Sheet with controlled open state from store"
      pattern: "Sheet.*:open"
---

<objective>
Rewrite AdminVirtualTable, MemberFilterBar, and MemberProfileDrawer to use shadcn-vue components while preserving TanStack Virtual integration and existing store bindings.

Purpose: These three components are specific to the /admin/members view. The virtual table is the most technically complex migration (hybrid TanStack Virtual + shadcn Table styling). The drawer is a direct replacement with shadcn Sheet.
Output: Three rewritten members components using shadcn Table, Combobox, Badge, and Sheet.
</objective>

<execution_context>
@/Users/lukekeith/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lukekeith/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12.1-migrate-admin-to-shadcn-vue-tailwind/12.1-CONTEXT.md
@.planning/phases/12.1-migrate-admin-to-shadcn-vue-tailwind/12.1-RESEARCH.md
@.planning/phases/12.1-migrate-admin-to-shadcn-vue-tailwind/12.1-01-SUMMARY.md
@resources/js/components/admin/admin-virtual-table/admin-virtual-table.vue
@resources/js/components/admin/member-filter-bar/member-filter-bar.vue
@resources/js/components/admin/member-profile-drawer/member-profile-drawer.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite AdminVirtualTable with hybrid shadcn Table + TanStack Virtual</name>
  <files>resources/js/components/admin/admin-virtual-table/admin-virtual-table.vue</files>
  <action>
1. Read current admin-virtual-table.vue (148 lines) to preserve:
   - Props: table (TanStack useVueTable instance), virtualizer (useVirtualizer instance), columns, loading, emptyMessage
   - Emits: row-click (with userId or row data)
   - The spacer-row pattern (top spacer + virtual rows + bottom spacer) for scroll positioning
   - The FlexRender usage for column header and cell rendering
   - Warning banner slot or section for failed group loads

2. Rewrite using shadcn Table components following RESEARCH.md Pattern 7:
   - `<Table>` wrapper with `<TableHeader>` and `<TableBody>`
   - `<TableHeader>`: iterate `table.getHeaderGroups()` > `<TableRow>` > `<TableHead>` with `<FlexRender>`
   - `<TableBody>`: spacer-row pattern with virtual rows between top and bottom spacers
   - **CRITICAL per RESEARCH.md Pitfall 5**: Spacer rows must use bare `<tr>` elements (NOT `<TableRow>`) to avoid hover/border artifacts. OR use `<TableRow>` with class override: `class="border-0 hover:bg-transparent p-0 pointer-events-none"`.
   - Virtual rows: `<TableRow>` with `@click="emit('row-click', ...)"`, `class="cursor-pointer hover:bg-muted/50"`
   - Each cell: `<TableCell>` with `<FlexRender>`

3. Loading state: when loading is true, render skeleton rows (5-10 `<TableRow>` with `<Skeleton>` in each `<TableCell>`).

4. Empty state: when rows array is empty and not loading, render a single `<TableRow>` with `<TableCell :colspan="columns.length" class="text-center text-muted-foreground py-8">` containing emptyMessage.

5. Warning banner (for failed group loads): preserve existing warning banner rendering. Style with Tailwind: `class="bg-yellow-900/20 border border-yellow-700/50 text-yellow-200 px-4 py-2 rounded-md text-sm mb-2"`.

6. The scrollable container ref that `useVirtualizer` attaches to must be preserved. Wrap `<Table>` in a `<div ref="tableContainerRef" class="overflow-auto max-h-[600px]">` (or whatever the current scroll container pattern is).

7. Import Table/TableBody/TableCell/TableHead/TableHeader/TableRow from '@/components/ui/table', Skeleton from '@/components/ui/skeleton'.
8. Keep FlexRender import from '@tanstack/vue-table'.
9. PRESERVE exact props/emits interface.
10. Remove any `<style>` block.
  </action>
  <verify>
    <automated>cd /Users/lukekeith/www/makeready/client && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>AdminVirtualTable renders TanStack Virtual rows inside shadcn Table components. Spacer rows do not show hover/border artifacts. Loading skeleton and empty state work. Props/emits preserved. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite MemberFilterBar and MemberProfileDrawer</name>
  <files>resources/js/components/admin/member-filter-bar/member-filter-bar.vue, resources/js/components/admin/member-profile-drawer/member-profile-drawer.vue</files>
  <action>
**MemberFilterBar (181 lines):**
1. Read current member-filter-bar.vue to preserve:
   - Props: filters/activeFilters (array of filter chip objects with type, value, label), filterOptions per category (groups, statuses, types), searchQuery
   - Emits: add-filter, remove-filter, clear-all, search
   - The combobox behavior: type to search, select adds a chip, input clears after selection
   - Per Phase 12 decision: reka-ui ComboboxRoot uses controlled model-value with local ref reset

2. Rewrite using shadcn Combobox pattern (Popover + Command):
   - `<Popover>` > `<PopoverTrigger>` (styled as input) > `<PopoverContent>` > `<Command>` > `<CommandInput>` + `<CommandList>` > `<CommandGroup>` > `<CommandItem>`
   - Group command items by filter category (Name, Group, Status, Type)
   - On CommandItem select: emit 'add-filter' with the selected filter, close popover, clear input
   - Active filter chips: `<Badge variant="secondary">` with X button for removal
   - Clear all: `<Button variant="ghost" size="sm">Clear all</Button>`
   - Search input behavior preserved: pressing enter or selecting "Name: {query}" adds a name filter chip

3. Layout: `class="flex flex-wrap items-center gap-2 mb-4"` for chips row, search input as the combobox trigger.

4. Import Popover/PopoverContent/PopoverTrigger from '@/components/ui/popover', Command/CommandInput/CommandList/CommandGroup/CommandItem/CommandEmpty from '@/components/ui/command', Badge from '@/components/ui/badge', Button from '@/components/ui/button'.

5. PRESERVE exact props/emits interface. The members-list.ui store drives this component.
6. Remove any `<style>` block.

**MemberProfileDrawer (216 lines):**
1. Read current member-profile-drawer.vue to preserve:
   - Props: isOpen (boolean), member data (name, phone, email, avatar, groups, enrollments), or it may read directly from a Pinia store
   - Emits: close, add-to-group, remove-from-group (or these may call store methods directly)
   - Content sections: contact info, group membership chips, enrollment progress bars, action buttons

2. Rewrite using shadcn Sheet following RESEARCH.md Pattern 5:
   - `<Sheet :open="isOpen" @update:open="(val) => !val && closeDrawer()">` — controlled by store
   - `<SheetContent side="right" class="w-[400px] sm:w-[540px]">`
   - `<SheetHeader>` > `<SheetTitle>` with member name
   - Content sections with Tailwind layout:
     - Contact info: Avatar + name/phone/email in a card-like layout using `class="space-y-3"`
     - Group chips: `<Badge>` for each group, with X button for remove-from-group
     - "Add to group" button: `<Button variant="outline" size="sm">`
     - Enrollment progress: progress bars with percentage text, `<div class="bg-primary/20 rounded-full h-2">` with inner fill div
   - No SheetTrigger needed — open is controlled programmatically by the store

3. Per Phase 13 decision: closeDrawer resets state with 200ms setTimeout delay — this lives in the store, NOT in the component. The component just calls the store's closeDrawer.

4. Import Sheet/SheetContent/SheetHeader/SheetTitle from '@/components/ui/sheet', Avatar/AvatarImage/AvatarFallback from '@/components/ui/avatar', Badge from '@/components/ui/badge', Button from '@/components/ui/button', Separator from '@/components/ui/separator'.

5. PRESERVE exact props/emits interface (or store binding pattern).
6. Remove any `<style>` block.
  </action>
  <verify>
    <automated>cd /Users/lukekeith/www/makeready/client && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>MemberFilterBar uses shadcn Combobox (Popover+Command) with Badge chips. MemberProfileDrawer uses shadcn Sheet with right slide-over. Props/emits preserved for both. Build passes.</done>
</task>

</tasks>

<verification>
- `npm run build` passes
- admin-virtual-table.vue uses shadcn Table with TanStack Virtual spacer-row pattern
- member-filter-bar.vue uses shadcn Popover+Command for combobox and Badge for chips
- member-profile-drawer.vue uses shadcn Sheet with side="right"
- All three components preserve their existing props/emits interfaces
</verification>

<success_criteria>
The virtual table renders with shadcn styling while maintaining TanStack Virtual scroll performance. Filter bar uses shadcn combobox with badge chips for active filters. Profile drawer slides from the right using shadcn Sheet with proper animation and backdrop. All components maintain backward-compatible interfaces.
</success_criteria>

<output>
After completion, create `.planning/phases/12.1-migrate-admin-to-shadcn-vue-tailwind/12.1-04-SUMMARY.md`
</output>
