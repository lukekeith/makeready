---
phase: 12.1-migrate-admin-to-shadcn-vue-tailwind
plan: 02
type: execute
wave: 2
depends_on: [12.1-01]
files_modified:
  - resources/js/islands/admin-island/admin-island.vue
  - resources/js/islands/admin-island/components/admin-sidebar.vue
  - resources/js/components/admin/admin-confirm-dialog/admin-confirm-dialog.vue
  - resources/js/components/admin/admin-table/admin-table.vue
autonomous: true
requirements: [SHELL-SIDEBAR, SHELL-LAYOUT, COMP-TABLE, COMP-DIALOG]

must_haves:
  truths:
    - "Admin panel renders with shadcn Sidebar that collapses to icons"
    - "Admin island uses forced dark mode via .dark class on SidebarProvider"
    - "Sidebar navigation items link to correct Vue Router paths with active state"
    - "Avatar menu with logout and Member Experience links appears in sidebar footer"
    - "AdminTable renders data rows with shadcn Table styling"
    - "AdminConfirmDialog renders as shadcn AlertDialog with cancel/confirm"
  artifacts:
    - path: "resources/js/islands/admin-island/admin-island.vue"
      provides: "Root admin layout with SidebarProvider"
      contains: "SidebarProvider"
    - path: "resources/js/islands/admin-island/components/admin-sidebar.vue"
      provides: "shadcn Sidebar with navigation"
      contains: "SidebarMenuButton"
    - path: "resources/js/components/admin/admin-table/admin-table.vue"
      provides: "Reusable data table with shadcn Table"
      contains: "TableRow"
    - path: "resources/js/components/admin/admin-confirm-dialog/admin-confirm-dialog.vue"
      provides: "Confirm dialog with AlertDialog"
      contains: "AlertDialog"
  key_links:
    - from: "admin-island.vue"
      to: "admin-sidebar.vue"
      via: "SidebarProvider wrapping AdminSidebar + SidebarInset"
      pattern: "SidebarProvider"
    - from: "admin-sidebar.vue"
      to: "vue-router"
      via: "SidebarMenuButton as-child with RouterLink"
      pattern: "RouterLink"
---

<objective>
Rewrite the admin shell (admin-island.vue + admin-sidebar.vue) to use shadcn SidebarProvider and Sidebar, and migrate AdminTable and AdminConfirmDialog to shadcn equivalents.

Purpose: The shell wraps every admin page, so it must be migrated first. AdminTable and AdminConfirmDialog are used by multiple section views — migrating them here unblocks all section view updates.
Output: Working admin shell with shadcn Sidebar (collapsible to icons, dark theme), plus reusable Table and AlertDialog components.
</objective>

<execution_context>
@/Users/lukekeith/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lukekeith/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12.1-migrate-admin-to-shadcn-vue-tailwind/12.1-CONTEXT.md
@.planning/phases/12.1-migrate-admin-to-shadcn-vue-tailwind/12.1-RESEARCH.md
@.planning/phases/12.1-migrate-admin-to-shadcn-vue-tailwind/12.1-01-SUMMARY.md
@resources/js/islands/admin-island/admin-island.vue
@resources/js/islands/admin-island/components/admin-sidebar.vue
@resources/js/components/admin/admin-table/admin-table.vue
@resources/js/components/admin/admin-confirm-dialog/admin-confirm-dialog.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite admin-island.vue and admin-sidebar.vue with shadcn Sidebar</name>
  <files>resources/js/islands/admin-island/admin-island.vue, resources/js/islands/admin-island/components/admin-sidebar.vue</files>
  <action>
**admin-island.vue:**
1. Read current admin-island.vue to preserve: Props interface (avatarUrl, initials, memberName, googleEmail, logoutUrl, memberId), provide() calls, CSRF axios setup in onMounted.
2. Rewrite template to wrap in SidebarProvider with forced dark mode:
   ```vue
   <SidebarProvider class="dark min-h-screen w-full">
     <AdminSidebar :avatar-url="avatarUrl" :initials="initials" :member-name="memberName" :logout-url="logoutUrl" />
     <SidebarInset>
       <main class="p-6">
         <RouterView />
       </main>
     </SidebarInset>
   </SidebarProvider>
   ```
3. Import SidebarProvider and SidebarInset from '@/components/ui/sidebar'.
4. Remove old BEM classes (AdminLayout, AdminLayout__content, AdminLayout__content-inner).
5. Keep ALL existing script logic (props, provide, onMounted CSRF) unchanged.

**admin-sidebar.vue (complete rewrite):**
1. Read current admin-sidebar.vue to preserve: props (avatarUrl, initials, memberName, logoutUrl), navigation items list, active route detection, avatar menu links (Member Experience, logout).
2. Rewrite using shadcn Sidebar components following RESEARCH.md Pattern 4 (sidebar-07):
   - `<Sidebar collapsible="icon">` as root
   - `<SidebarHeader>` with app logo/title
   - `<SidebarContent>` with `<SidebarMenu>` containing nav items
   - Each nav item: `<SidebarMenuItem>` > `<SidebarMenuButton as-child :is-active="...">` > `<RouterLink>`
   - Icons from lucide-vue-next: LayoutDashboard, Users, ContactRound, BookOpen, UserRound
   - `<SidebarFooter>` with avatar dropdown menu using shadcn DropdownMenu:
     - User avatar + name display
     - "Member Experience" link (href to /member/home, full page nav not Vue Router)
     - "Logout" link (href to logoutUrl, full page nav)
   - `<SidebarRail />` for the resize handle
3. Navigation items: Dashboard (/admin), Groups (/admin/groups), Members (/admin/members), Programs (/admin/programs), Profile (/admin/profile).
4. Active state: `route.path === item.path` for exact match on /admin, `route.path.startsWith(item.path)` for others.
5. Import all sidebar sub-components from '@/components/ui/sidebar', DropdownMenu components from '@/components/ui/dropdown-menu', Avatar from '@/components/ui/avatar'.
6. Do NOT use `@apply` in any `<style>` block — all styles via Tailwind utility classes in template.
  </action>
  <verify>
    <automated>cd /Users/lukekeith/www/makeready/client && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>admin-island.vue wraps content in SidebarProvider with class="dark". admin-sidebar.vue renders shadcn Sidebar with 5 nav items, active state, avatar dropdown with Member Experience and Logout links. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite AdminConfirmDialog with shadcn AlertDialog</name>
  <files>resources/js/components/admin/admin-confirm-dialog/admin-confirm-dialog.vue</files>
  <action>
1. Read current admin-confirm-dialog.vue to preserve its props/emits interface exactly:
   - Props: open (boolean), title (string), message (string), confirmLabel (string, optional), dangerous (boolean, optional)
   - Emits: confirm, cancel
2. Rewrite using shadcn AlertDialog following RESEARCH.md Pattern 6:
   - `<AlertDialog :open="open">`
   - `<AlertDialogContent>` > `<AlertDialogHeader>` > `<AlertDialogTitle>` + `<AlertDialogDescription>`
   - `<AlertDialogFooter>` > `<AlertDialogCancel @click="emit('cancel')">` + `<AlertDialogAction @click="emit('confirm')">`
   - If `dangerous` prop is true, add destructive styling to the action button: `class="bg-destructive text-destructive-foreground hover:bg-destructive/90"`
   - Default confirmLabel to "Delete" if not provided
3. Import all AlertDialog sub-components from '@/components/ui/alert-dialog'.
4. The props/emits contract MUST remain identical so all existing section view usages continue working without changes.
5. Remove any `<style>` block — AlertDialog is styled by shadcn/Tailwind.
  </action>
  <verify>
    <automated>cd /Users/lukekeith/www/makeready/client && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>AdminConfirmDialog renders as shadcn AlertDialog with identical props/emits interface. Dangerous variant uses destructive button styling. Build passes.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite AdminTable with shadcn Table components</name>
  <files>resources/js/components/admin/admin-table/admin-table.vue</files>
  <action>
1. Read current admin-table.vue to preserve its props/emits interface exactly:
   - Props: columns (array of column definitions with key, label, optional width), rows (array of data objects), loading (boolean), emptyMessage (string), rowClickable (boolean)
   - Emits: row-click (with row data)
   - Slots: cell-{columnKey} for custom cell rendering, actions for row action buttons
2. Rewrite template using shadcn Table sub-components:
   - `<Table>` wrapper
   - `<TableHeader>` > `<TableRow>` > `<TableHead>` for each column
   - `<TableBody>` > `<TableRow>` for each data row, with `@click` if rowClickable
   - `<TableCell>` for each column value, using named slots `cell-{key}` or default text rendering
   - Row hover: `class="cursor-pointer hover:bg-muted/50"` when rowClickable
   - Loading state: render `<Skeleton>` components inside table cells when loading=true
   - Empty state: render a single row spanning all columns with emptyMessage text when rows is empty and not loading
3. Import Table sub-components from '@/components/ui/table', Skeleton from '@/components/ui/skeleton'.
4. The props/emits/slots contract MUST remain identical so all existing section view usages continue working.
5. Remove any `<style>` block.
  </action>
  <verify>
    <automated>cd /Users/lukekeith/www/makeready/client && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>AdminTable renders data using shadcn Table components with loading skeleton, empty state, row click, and slot-based custom cells. Props/emits/slots interface preserved. Build passes.</done>
</task>

</tasks>

<verification>
- `npm run build` passes
- admin-island.vue contains SidebarProvider with class="dark"
- admin-sidebar.vue uses shadcn Sidebar components with RouterLink navigation
- admin-confirm-dialog.vue uses AlertDialog with same props/emits
- admin-table.vue uses shadcn Table with same props/emits/slots
</verification>

<success_criteria>
The admin shell renders with a shadcn Sidebar that collapses to icons, has dark mode forced on the admin container only, and includes the avatar dropdown menu. AdminTable and AdminConfirmDialog are rewritten with shadcn equivalents while preserving their public API so section views continue to compile.
</success_criteria>

<output>
After completion, create `.planning/phases/12.1-migrate-admin-to-shadcn-vue-tailwind/12.1-02-SUMMARY.md`
</output>
