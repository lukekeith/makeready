---
phase: 12.1-migrate-admin-to-shadcn-vue-tailwind
plan: 05
type: execute
wave: 3
depends_on: [12.1-02, 12.1-03, 12.1-04]
files_modified:
  - resources/js/islands/admin-island/sections/dashboard-section.vue
  - resources/js/islands/admin-island/sections/groups-section.vue
  - resources/js/islands/admin-island/sections/programs-section.vue
  - resources/js/islands/admin-island/sections/profile-section.vue
  - resources/js/islands/admin-island/sections/members-section.vue
  - resources/css/app.scss
  - resources/css/components/admin/*.scss (deleted)
  - resources/css/layouts/admin-layout.scss (deleted)
  - resources/css/pages/admin.scss (deleted)
autonomous: false
requirements: [SECT-DASH, SECT-GROUPS, SECT-PROGRAMS, SECT-PROFILE, SECT-MEMBERS, SCSS-CLEANUP]

must_haves:
  truths:
    - "All 5 section views render with Tailwind utility classes instead of BEM"
    - "Section views use shadcn Tabs for tab switching"
    - "Section views use shadcn Button variants for all action buttons"
    - "All 16 admin SCSS files are deleted"
    - "app.scss has no admin @use imports"
    - "Member-facing Blade pages still render correctly (not affected by changes)"
    - "npm run build passes with zero SCSS and zero Tailwind errors"
  artifacts:
    - path: "resources/js/islands/admin-island/sections/dashboard-section.vue"
      provides: "Dashboard with Tailwind layout, shadcn cards"
    - path: "resources/js/islands/admin-island/sections/groups-section.vue"
      provides: "Groups CRUD with shadcn Tabs, Table, Form, Dialog"
    - path: "resources/js/islands/admin-island/sections/programs-section.vue"
      provides: "Programs CRUD with shadcn Tabs, activity list"
    - path: "resources/js/islands/admin-island/sections/profile-section.vue"
      provides: "Profile editor with shadcn inputs"
    - path: "resources/js/islands/admin-island/sections/members-section.vue"
      provides: "Members view with virtual table, filter bar, drawer"
  key_links:
    - from: "all section views"
      to: "Pinia stores"
      via: "Store computed props/actions unchanged"
      pattern: "use.*Store"
    - from: "app.scss"
      to: "admin SCSS files"
      via: "All @use admin imports removed"
      pattern: "admin"
---

<objective>
Update all 5 admin section views to use Tailwind utility classes and shadcn components for layout/typography/buttons/tabs, delete all 16 admin SCSS files, remove admin @use imports from app.scss, and verify the complete migration with a human checkpoint.

Purpose: This is the final migration step — section views contain the inline BEM markup that must be replaced with Tailwind classes. After this plan, the admin panel is fully shadcn/Tailwind with zero BEM dependencies.
Output: All section views migrated, all admin SCSS deleted, build passes, human-verified admin panel.
</objective>

<execution_context>
@/Users/lukekeith/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lukekeith/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12.1-migrate-admin-to-shadcn-vue-tailwind/12.1-CONTEXT.md
@.planning/phases/12.1-migrate-admin-to-shadcn-vue-tailwind/12.1-RESEARCH.md
@.planning/phases/12.1-migrate-admin-to-shadcn-vue-tailwind/12.1-02-SUMMARY.md
@.planning/phases/12.1-migrate-admin-to-shadcn-vue-tailwind/12.1-03-SUMMARY.md
@.planning/phases/12.1-migrate-admin-to-shadcn-vue-tailwind/12.1-04-SUMMARY.md
@resources/js/islands/admin-island/sections/dashboard-section.vue
@resources/js/islands/admin-island/sections/groups-section.vue
@resources/js/islands/admin-island/sections/programs-section.vue
@resources/js/islands/admin-island/sections/profile-section.vue
@resources/js/islands/admin-island/sections/members-section.vue
@resources/css/app.scss
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate simple section views (dashboard, profile, members) to Tailwind + shadcn</name>
  <files>resources/js/islands/admin-island/sections/dashboard-section.vue, resources/js/islands/admin-island/sections/profile-section.vue, resources/js/islands/admin-island/sections/members-section.vue</files>
  <action>
For each of the 3 simpler section views, read the current file and apply these changes:

**Common changes across all sections:**
- Replace all BEM class attributes (e.g., `class="AdminDashboard__header"`) with Tailwind utility classes
- Page titles: `class="text-2xl font-bold tracking-tight mb-6"`
- Section headers: `class="text-lg font-semibold mb-4"`
- Page layout wrappers: `class="space-y-6"` for vertical sections
- Card-like containers: `class="rounded-lg border bg-card p-6"`
- All buttons: replace with shadcn `<Button>` component imports (variant="default", "outline", "destructive", "ghost")
- Loading states: replace BEM shimmer with shadcn `<Skeleton>`
- Remove any `<style>` or `<style scoped>` blocks entirely

**dashboard-section.vue (95 lines):**
- KPI cards layout: `class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4"`
- Each KPI card: `class="rounded-lg border bg-card p-4"` with title, value, and change indicator
- ApexCharts containers: keep chart components, replace BEM wrapper divs with Tailwind `class="rounded-lg border bg-card p-4 mt-6"`
- Heatmap and weekly chart: ensure containers have proper dark mode background via card class

**profile-section.vue (53 lines -- smallest):**
- Avatar display: use shadcn `<Avatar>` > `<AvatarImage>` + `<AvatarFallback>`
- Edit form: shadcn `<Input>` for name, `<AdminImageUpload>` for avatar
- Save button: shadcn `<Button>`

**members-section.vue (98 lines):**
- `<MemberFilterBar>` usage stays
- `<AdminVirtualTable>` usage stays
- `<MemberProfileDrawer>` usage stays
- Update any BEM wrapper divs to Tailwind utilities
- Page title and warning banner: Tailwind classes

**Key rules:**
- Do NOT change any Pinia store bindings, computed props, or method calls
- Do NOT change any component props/emits wiring to child components
- ONLY change template markup classes and component imports (add shadcn, remove BEM)
- Do NOT use `@apply` in any `<style>` block -- use Tailwind classes directly in templates
- Import shadcn components as needed: Button, Skeleton, Avatar, etc.
  </action>
  <verify>
    <automated>cd /Users/lukekeith/www/makeready/client && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Dashboard, profile, and members section views use Tailwind utility classes and shadcn components. No BEM class references remain. No `<style>` blocks remain. All Pinia store bindings preserved. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate complex section views (groups, programs) to Tailwind + shadcn</name>
  <files>resources/js/islands/admin-island/sections/groups-section.vue, resources/js/islands/admin-island/sections/programs-section.vue</files>
  <action>
These are the two largest and most complex section views. Read each file carefully before making changes.

**Common changes (same as Task 1):**
- Replace all BEM class attributes with Tailwind utility classes
- Page titles: `class="text-2xl font-bold tracking-tight mb-6"`
- Section headers: `class="text-lg font-semibold mb-4"`
- Page layout wrappers: `class="space-y-6"` for vertical sections
- Card-like containers: `class="rounded-lg border bg-card p-6"`
- All buttons: replace with shadcn `<Button>` component imports
- Tab containers: replace BEM tab markup with shadcn `<Tabs>`, `<TabsList>`, `<TabsTrigger>`, `<TabsContent>`
- Loading states: replace BEM shimmer with shadcn `<Skeleton>`
- Remove any `<style>` or `<style scoped>` blocks entirely

**groups-section.vue (623 lines -- largest section):**
- List view: `<AdminTable>` usage stays (component already migrated), just update any BEM wrapper divs
- Create/Edit form: `<AdminForm>` usage stays, update surrounding layout divs
- Delete confirmation: `<AdminConfirmDialog>` usage stays
- Detail view tabs (Members, Enrollments, Posts, Settings): replace with shadcn `<Tabs defaultValue="members">` > `<TabsList>` > `<TabsTrigger value="members">` etc. > `<TabsContent value="members">`
- Cover image: `<AdminImageUpload>` usage stays
- Members tab, Enrollments tab, Posts tab contents: replace BEM divs with Tailwind grid/flex utilities
- Action buttons: replace all with shadcn `<Button>` imports (Add Group, Edit, Delete, etc.)

**programs-section.vue (425 lines):**
- List view: `<AdminTable>` usage stays
- Create dialog with template selector: update BEM wrapper with Tailwind layout
- Detail view tabs: replace with shadcn `<Tabs>`
- Publish toggle: shadcn `<Button variant="outline">` or switch pattern
- `<AdminActivityList>` usage stays (already migrated)
- Lesson list: update BEM list items with Tailwind `class="flex items-center justify-between py-3 border-b"`

**Key rules:**
- Do NOT change any Pinia store bindings, computed props, or method calls
- Do NOT change any component props/emits wiring to child components
- ONLY change template markup classes and component imports (add shadcn, remove BEM)
- Do NOT use `@apply` in any `<style>` block -- use Tailwind classes directly in templates
- Import shadcn components as needed: Tabs, TabsList, TabsTrigger, TabsContent, Button, Skeleton, etc.
  </action>
  <verify>
    <automated>cd /Users/lukekeith/www/makeready/client && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Groups (623 lines) and programs (425 lines) section views fully migrated to Tailwind utility classes and shadcn components. Detail view tabs use shadcn Tabs. No BEM class references remain. No `<style>` blocks remain. All Pinia store bindings preserved. Build passes.</done>
</task>

<task type="auto">
  <name>Task 3: Delete admin SCSS files and remove app.scss imports</name>
  <files>resources/css/app.scss, resources/css/components/admin/*.scss, resources/css/layouts/admin-layout.scss, resources/css/pages/admin.scss</files>
  <action>
1. Delete all 16 admin SCSS files:
   ```bash
   rm resources/css/components/admin/admin-activity-list.scss
   rm resources/css/components/admin/admin-confirm-dialog.scss
   rm resources/css/components/admin/admin-dashboard.scss
   rm resources/css/components/admin/admin-enrollments-tab.scss
   rm resources/css/components/admin/admin-form.scss
   rm resources/css/components/admin/admin-image-upload.scss
   rm resources/css/components/admin/admin-lesson-list.scss
   rm resources/css/components/admin/admin-members-tab.scss
   rm resources/css/components/admin/admin-posts-tab.scss
   rm resources/css/components/admin/admin-profile.scss
   rm resources/css/components/admin/admin-sidebar.scss
   rm resources/css/components/admin/admin-table.scss
   rm resources/css/components/admin/admin-tabs.scss
   rm resources/css/components/admin/admin-virtual-table.scss
   rm resources/css/components/admin/member-filter-bar.scss
   rm resources/css/components/admin/member-profile-drawer.scss
   ```

2. Delete admin layout and page SCSS if they exist:
   ```bash
   rm resources/css/layouts/admin-layout.scss 2>/dev/null
   rm resources/css/pages/admin.scss 2>/dev/null
   ```

3. Remove the `resources/css/components/admin/` directory if empty after deletions.

4. Edit `resources/css/app.scss` -- remove all admin-related @use imports (lines 68-86 and line 108 per current file):
   - Remove: `@use 'layouts/admin-layout' as layout-admin;`
   - Remove: `@use 'components/admin/admin-sidebar' as admin-sidebar;`
   - Remove: `@use 'components/admin/admin-table' as admin-table;`
   - Remove: `@use 'components/admin/admin-virtual-table' as admin-virtual-table;`
   - Remove: `@use 'components/admin/member-filter-bar' as member-filter-bar;`
   - Remove: `@use 'components/admin/admin-form' as admin-form;`
   - Remove: `@use 'components/admin/admin-confirm-dialog' as admin-confirm-dialog;`
   - Remove: `@use 'components/admin/admin-image-upload' as admin-image-upload;`
   - Remove: `@use 'components/admin/admin-tabs' as admin-tabs;`
   - Remove: `@use 'components/admin/admin-lesson-list' as admin-lesson-list;`
   - Remove: `@use 'components/admin/admin-activity-list' as admin-activity-list;`
   - Remove: `@use 'components/admin/admin-profile' as admin-profile;`
   - Remove: `@use 'components/admin/admin-members-tab' as admin-members-tab;`
   - Remove: `@use 'components/admin/admin-enrollments-tab' as admin-enrollments-tab;`
   - Remove: `@use 'components/admin/admin-posts-tab' as admin-posts-tab;`
   - Remove: `@use 'components/admin/admin-dashboard' as admin-dashboard;`
   - Remove: `@use 'components/admin/member-profile-drawer' as member-profile-drawer;`
   - Remove: `@use 'pages/admin' as page-admin;`
   Keep all other @use imports (member-facing Blade page styles) intact.

5. Run `npm run build` to verify both SCSS and Tailwind compile without errors. The SCSS build should have fewer files to process now. The Tailwind build should still only scan admin island paths.
  </action>
  <verify>
    <automated>cd /Users/lukekeith/www/makeready/client && npm run build 2>&1 | tail -20 && echo "--- SCSS admin check ---" && ls resources/css/components/admin/ 2>&1 && echo "--- app.scss admin refs ---" && grep -c "admin" resources/css/app.scss</automated>
  </verify>
  <done>All 16 admin SCSS files deleted. admin-layout.scss and admin page SCSS deleted. app.scss has zero admin @use imports. `npm run build` passes cleanly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verification of complete admin panel migration</name>
  <files>n/a</files>
  <action>
Present the completed migration for human verification. The admin panel has been fully migrated from BEM/SCSS to shadcn-vue + Tailwind. All 8 admin components rewritten with shadcn equivalents, all 5 section views updated with Tailwind utility classes, sidebar uses shadcn Sidebar with collapse-to-icons, member profile drawer uses shadcn Sheet, all 16 admin SCSS files deleted.

Verification steps for the human:
1. Start the dev server: `cd /Users/lukekeith/www/makeready/client && npm run dev`
2. Open the admin panel in the browser at the local dev URL (/admin)
3. Verify these specific behaviors:
   a. Sidebar: Collapses to icon-only mode (try the collapse toggle or Cmd+B). Expands back. Shows correct nav items with active state highlighting.
   b. Dark theme: Admin panel has dark zinc theme. Navigate to a member-facing page (e.g., /groups) and confirm it still has the light BEM styling -- NOT dark.
   c. Dashboard: KPI cards and charts render correctly with dark theme.
   d. Groups: Table lists groups. Click a group -- detail view with tabs (Members, Enrollments, Posts, Settings) works. Create/Edit/Delete flows work.
   e. Programs: Table lists programs. Program detail shows lessons. Activity list expands/collapses. Activity editor fields work.
   f. Members: Filter bar accepts search/filter chips. Virtual table scrolls smoothly with 500+ rows. Click a member -- Sheet drawer slides from right with profile info.
   g. Profile: Profile section loads with avatar and name editing.
4. Check browser console for any Vue or JavaScript errors.
  </action>
  <verify>Human visual inspection of all 5 admin sections plus member-facing pages for Tailwind isolation.</verify>
  <done>Human approves all admin sections render correctly with shadcn components and dark theme, member-facing pages are unaffected, and no console errors present.</done>
</task>

</tasks>

<verification>
- `npm run build` passes
- No admin SCSS files remain in resources/css/components/admin/
- app.scss has zero references to admin component @use imports
- All 5 section views render with shadcn components and Tailwind classes
- Member-facing Blade pages are NOT affected by Tailwind (no dark mode, no utility class leaking)
</verification>

<success_criteria>
The admin panel is fully migrated to shadcn-vue + Tailwind with dark zinc theme. Zero BEM admin SCSS files remain. All section views use shadcn components. Member-facing pages are unaffected. Human verification confirms the complete migration looks and functions correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/12.1-migrate-admin-to-shadcn-vue-tailwind/12.1-05-SUMMARY.md`
</output>
