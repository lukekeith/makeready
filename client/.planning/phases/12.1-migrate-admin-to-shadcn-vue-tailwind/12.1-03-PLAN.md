---
phase: 12.1-migrate-admin-to-shadcn-vue-tailwind
plan: 03
type: execute
wave: 2
depends_on: [12.1-01]
files_modified:
  - resources/js/components/admin/admin-form/admin-form.vue
  - resources/js/components/admin/admin-image-upload/admin-image-upload.vue
  - resources/js/components/admin/admin-activity-list/admin-activity-list.vue
autonomous: true
requirements: [COMP-FORM, COMP-UPLOAD, COMP-ACTIVITY]

must_haves:
  truths:
    - "AdminForm renders dynamic fields using shadcn Input, Select, Textarea, and Button components"
    - "AdminImageUpload shows avatar preview and file input using shadcn Avatar and Input"
    - "AdminActivityList renders collapsible activity panels with type badges"
  artifacts:
    - path: "resources/js/components/admin/admin-form/admin-form.vue"
      provides: "Dynamic form component with shadcn inputs"
      contains: "Input"
    - path: "resources/js/components/admin/admin-image-upload/admin-image-upload.vue"
      provides: "Image upload with avatar preview"
      contains: "Avatar"
    - path: "resources/js/components/admin/admin-activity-list/admin-activity-list.vue"
      provides: "Activity list with accordion and badges"
      contains: "Accordion"
  key_links:
    - from: "admin-form.vue"
      to: "@/components/ui/input"
      via: "shadcn Input import"
      pattern: "from.*components/ui/input"
    - from: "admin-activity-list.vue"
      to: "@/components/ui/accordion"
      via: "shadcn Accordion import"
      pattern: "from.*components/ui/accordion"
---

<objective>
Rewrite AdminForm, AdminImageUpload, and AdminActivityList to use shadcn-vue components while preserving their existing props/emits/slots interfaces.

Purpose: These three components are used by groups-section, programs-section, and profile-section. Migrating them with preserved APIs lets section views continue working without template changes to the component usage.
Output: Three rewritten admin components using shadcn inputs, avatars, accordions, and badges.
</objective>

<execution_context>
@/Users/lukekeith/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lukekeith/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12.1-migrate-admin-to-shadcn-vue-tailwind/12.1-CONTEXT.md
@.planning/phases/12.1-migrate-admin-to-shadcn-vue-tailwind/12.1-RESEARCH.md
@.planning/phases/12.1-migrate-admin-to-shadcn-vue-tailwind/12.1-01-SUMMARY.md
@resources/js/components/admin/admin-form/admin-form.vue
@resources/js/components/admin/admin-image-upload/admin-image-upload.vue
@resources/js/components/admin/admin-activity-list/admin-activity-list.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite AdminForm with shadcn Input, Select, Textarea, Button</name>
  <files>resources/js/components/admin/admin-form/admin-form.vue</files>
  <action>
1. Read current admin-form.vue thoroughly (282 lines) to understand:
   - The `fields` prop interface (array of field definitions with type, key, label, placeholder, options, etc.)
   - How it renders different field types dynamically (text, textarea, select, number, checkbox, etc.)
   - The submit/cancel emits and form data binding pattern
   - Any slot-based customization for specific fields

2. Rewrite using shadcn visual components WITHOUT the shadcn Form/vee-validate wrapper. Per RESEARCH.md Pitfall 7: the dynamic `fields[]` array pattern conflicts with vee-validate's static field name requirement. Use shadcn Input, Select, Textarea, and Button components for visual consistency only.

3. For each field type in the dynamic fields array:
   - `text` / `email` / `number` / `url`: shadcn `<Input>` with appropriate type attribute
   - `textarea`: shadcn `<Textarea>`
   - `select`: shadcn `<Select>` > `<SelectTrigger>` > `<SelectValue>` + `<SelectContent>` > `<SelectItem>` for each option
   - `checkbox`: shadcn-styled checkbox (or keep native with Tailwind utility classes)
   - Labels: shadcn `<Label>` component wrapping each field

4. Layout: Use Tailwind utility classes for form grid/spacing (e.g., `class="space-y-4"` for field stack, `class="flex gap-2 justify-end"` for button row).

5. Buttons: shadcn `<Button>` for submit (variant="default") and cancel (variant="outline").

6. PRESERVE the exact props/emits interface so section views that use `<AdminForm :fields="..." @submit="..." @cancel="...">` continue working without changes.

7. Remove any `<style>` block — all styling via Tailwind utility classes in template.

8. Import shadcn components: Input from '@/components/ui/input', Textarea from '@/components/ui/textarea', Select/SelectContent/SelectItem/SelectTrigger/SelectValue from '@/components/ui/select', Button from '@/components/ui/button', Label from '@/components/ui/label'.
  </action>
  <verify>
    <automated>cd /Users/lukekeith/www/makeready/client && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>AdminForm renders dynamic fields using shadcn Input, Select, Textarea, Label, and Button. Props/emits interface preserved. No `<style>` block — Tailwind only. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite AdminImageUpload and AdminActivityList</name>
  <files>resources/js/components/admin/admin-image-upload/admin-image-upload.vue, resources/js/components/admin/admin-activity-list/admin-activity-list.vue</files>
  <action>
**AdminImageUpload (83 lines):**
1. Read current admin-image-upload.vue to preserve: props (modelValue/imageUrl for preview, accept, maxSize), emits (update:modelValue/file-selected).
2. Rewrite using shadcn Avatar for image preview and shadcn Input (type="file") for file selection:
   - `<Avatar>` with `<AvatarImage :src="previewUrl">` + `<AvatarFallback>` for placeholder
   - `<Input type="file" :accept="accept" @change="handleFileChange">` for file picker
   - Preview logic: show current imageUrl prop, switch to local blob URL when file selected
3. Layout with Tailwind: `class="flex items-center gap-4"` for horizontal avatar + input layout.
4. Import Avatar/AvatarImage/AvatarFallback from '@/components/ui/avatar', Input from '@/components/ui/input'.
5. PRESERVE exact props/emits interface.
6. Remove any `<style>` block.

**AdminActivityList (559 lines — largest component):**
1. Read current admin-activity-list.vue thoroughly to understand:
   - Props: activities array, lessonId, loading state
   - The activity item structure (type badge, title, content preview, expand/collapse)
   - Action buttons per activity (edit, delete, reorder up/down)
   - Type-specific rendering (READ, VIDEO, SOAP, OIA, DBS, HEAR, USER_INPUT)
   - The inline activity editor (when editing an activity)
   - Emits: add, edit, delete, reorder, save, cancel, and any field update emits
   - Slots for custom content areas

2. Rewrite using shadcn Accordion for expand/collapse and Badge for type labels:
   - `<Accordion type="single" collapsible>` wrapping the activity list
   - Each activity: `<AccordionItem>` > `<AccordionTrigger>` (shows Badge + title) + `<AccordionContent>` (shows details/editor)
   - `<Badge>` with variant based on activity type: READ=default, VIDEO=secondary, SOAP=outline, etc.
   - Action buttons: shadcn `<Button variant="ghost" size="icon">` with lucide icons (Pencil, Trash2, ChevronUp, ChevronDown, Plus)
   - Loading state: `<Skeleton>` components

3. For the inline editor within AccordionContent:
   - Use shadcn `<Input>` for title field
   - Use shadcn `<Textarea>` for content/plain text editing
   - Use shadcn `<Select>` for type selection
   - Use shadcn `<Button>` for save/cancel actions
   - Source references section: use `<Input>` fields with add/remove buttons

4. PRESERVE the exact props/emits interface. This is the largest component — be meticulous about not breaking the data flow to/from programs-section.vue.
5. Remove any `<style>` block.
6. Import Accordion components from '@/components/ui/accordion', Badge from '@/components/ui/badge', and other shadcn components as needed.
  </action>
  <verify>
    <automated>cd /Users/lukekeith/www/makeready/client && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>AdminImageUpload uses shadcn Avatar + Input. AdminActivityList uses shadcn Accordion + Badge + Button with all activity types rendered, inline editor working, and action buttons present. Props/emits preserved for both. Build passes.</done>
</task>

</tasks>

<verification>
- `npm run build` passes
- admin-form.vue uses shadcn Input, Select, Textarea, Button
- admin-image-upload.vue uses shadcn Avatar + Input
- admin-activity-list.vue uses shadcn Accordion + Badge
- All three components preserve their existing props/emits interfaces
</verification>

<success_criteria>
AdminForm dynamically renders fields using shadcn components. AdminImageUpload shows avatar preview with file input. AdminActivityList renders collapsible activity panels with type badges and inline editing. All three maintain backward-compatible interfaces for section views.
</success_criteria>

<output>
After completion, create `.planning/phases/12.1-migrate-admin-to-shadcn-vue-tailwind/12.1-03-SUMMARY.md`
</output>
