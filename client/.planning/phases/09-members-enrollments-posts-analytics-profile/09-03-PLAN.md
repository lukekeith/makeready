---
phase: 09-members-enrollments-posts-analytics-profile
plan: "03"
type: execute
wave: 3
depends_on: ["09-02"]
files_modified:
  - resources/js/islands/admin-island/sections/groups-section.vue
  - resources/js/islands/admin-island/stores/ui/enrollments-tab.ui.ts
  - resources/css/components/admin/admin-enrollments-tab.scss
autonomous: true
requirements: [ENRL-05, SCHD-01, SCHD-02, SCHD-03, SCHD-04]

must_haves:
  truths:
    - "Leader clicks an enrollment and sees the lesson schedule with dates and titles"
    - "Leader can edit a scheduled lesson title inline"
    - "Leader can add a new scheduled lesson to extend the enrollment"
    - "Leader can delete a scheduled lesson"
  artifacts:
    - path: "resources/js/islands/admin-island/sections/groups-section.vue"
      provides: "Enrollment detail expansion panel with schedule list CRUD"
    - path: "resources/js/islands/admin-island/stores/ui/enrollments-tab.ui.ts"
      provides: "Updated UI store with schedule editing state"
  key_links:
    - from: "groups-section.vue"
      to: "enrollments.domain.ts loadEnrollmentDetail"
      via: "store method call on enrollment card click"
      pattern: "domain\\.loadEnrollmentDetail"
    - from: "groups-section.vue"
      to: "/admin/api/enrollments/:id/schedules"
      via: "enrollments domain store methods"
      pattern: "admin/api/enrollments.*schedules"
---

<objective>
Add enrollment detail expansion with scheduled lessons list showing CRUD operations (view, edit title, add, delete) within the Enrollments tab.

Purpose: Completes enrollment management by showing the lesson schedule generated from the enrollment, allowing leaders to customize individual scheduled lesson titles, extend with new lessons, or remove lessons.
Output: Expandable enrollment detail panel with schedule list and inline CRUD
</objective>

<execution_context>
@/Users/lukekeith/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lukekeith/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-members-enrollments-posts-analytics-profile/09-RESEARCH.md
@.planning/phases/09-members-enrollments-posts-analytics-profile/09-02-SUMMARY.md

<interfaces>
From enrollments.domain.ts (created in Plan 02):
```typescript
export interface LessonSchedule {
  id: string
  enrollmentId: string
  scheduledDate: string
  title?: string
  lesson?: { dayNumber: number; title: string }
}
export interface EnrollmentDetails extends Enrollment {
  studyProgram?: { id: string; name: string; coverImageUrl?: string }
  lessonSchedules: LessonSchedule[]
}
// Methods:
async function loadEnrollmentDetail(enrollmentId: string): Promise<void>
// Need to ADD these new methods for schedule CRUD:
// async function updateScheduleTitle(enrollmentId, scheduleId, title)
// async function addSchedule(enrollmentId)
// async function deleteSchedule(enrollmentId, scheduleId)
```

From enrollments-tab.ui.ts (created in Plan 02):
```typescript
// Has: expandedEnrollmentId, enrollmentDetail computed
// Need to ADD: editingScheduleId, editingTitle refs and methods
```

API endpoints (confirmed from EnrollmentActions.swift):
- PATCH /api/enrollments/:id/schedules/:sid body: { title: string }
- POST /api/enrollments/:id/schedules body: {} (empty)
- DELETE /api/enrollments/:id/schedules/:sid
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add schedule CRUD methods to enrollments domain store and UI store</name>
  <files>resources/js/islands/admin-island/stores/domain/enrollments.domain.ts, resources/js/islands/admin-island/stores/ui/enrollments-tab.ui.ts</files>
  <action>
**Extend enrollments.domain.ts** — add three new methods:

- `async function updateScheduleTitle(enrollmentId: string, scheduleId: string, title: string)`: PATCH `/admin/api/enrollments/${enrollmentId}/schedules/${scheduleId}` body `{ title }`. On success, update local state: find schedule in enrollmentDetails[enrollmentId].lessonSchedules by id, update its title directly (API returns only `{ success }`, same pattern as Phase 7 updateLessonTitle).
- `async function addSchedule(enrollmentId: string)`: POST `/admin/api/enrollments/${enrollmentId}/schedules` body `{}`. On success, reload enrollment detail to get the new schedule with server-assigned date: `await loadEnrollmentDetail(enrollmentId)`.
- `async function deleteSchedule(enrollmentId: string, scheduleId: string)`: DELETE `/admin/api/enrollments/${enrollmentId}/schedules/${scheduleId}`. On success, filter schedule from enrollmentDetails[enrollmentId].lessonSchedules locally.

Add these to the store's return object.

**Extend enrollments-tab.ui.ts** — add schedule editing state:

- Add refs: `editingScheduleId: ref<string|null>(null)`, `editingTitle: ref('')`, `confirmDeleteScheduleId: ref<string|null>(null)`
- Add methods:
  - `startEditSchedule(scheduleId: string, currentTitle: string)`: set editingScheduleId, editingTitle
  - `cancelEditSchedule()`: reset editingScheduleId, editingTitle
  - `saveScheduleTitle()`: if !expandedEnrollmentId return; call `domain.updateScheduleTitle(expandedEnrollmentId, editingScheduleId, editingTitle)`. Reset editing state.
  - `addSchedule()`: if !expandedEnrollmentId return; call `domain.addSchedule(expandedEnrollmentId)`
  - `requestDeleteSchedule(scheduleId: string)`: set confirmDeleteScheduleId
  - `cancelDeleteSchedule()`: reset confirmDeleteScheduleId
  - `confirmDeleteSchedule()`: call `domain.deleteSchedule(expandedEnrollmentId, confirmDeleteScheduleId)`. Reset.
- Return all new refs and methods.
  </action>
  <verify>
    <automated>cd /Users/lukekeith/www/makeready/client && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>enrollments.domain.ts has updateScheduleTitle, addSchedule, deleteSchedule methods. enrollments-tab.ui.ts has editing state and schedule CRUD methods. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Wire enrollment detail expansion with schedule list into groups-section.vue</name>
  <files>resources/js/islands/admin-island/sections/groups-section.vue, resources/css/components/admin/admin-enrollments-tab.scss</files>
  <action>
Modify the Enrollments tab in groups-section.vue to show an expandable detail panel when an enrollment is clicked.

**In the enrollment card template**, update the enrollment list to support expand/collapse:
- When `enrollmentsUI.expandedEnrollmentId === enrollment.id`, show the detail panel below the card
- Add a chevron icon (ChevronDown/ChevronUp) to indicate expand state
- Card click calls `enrollmentsUI.expandEnrollment(enrollment.id)` if collapsed, or `enrollmentsUI.collapseEnrollment()` if expanded

**Add the detail expansion panel** below each enrollment card (inside the v-for, render conditionally):
```html
<div v-if="enrollmentsUI.expandedEnrollmentId === enrollment.id && enrollmentsUI.enrollmentDetail" class="EnrollmentsTab__detail">
  <div class="EnrollmentsTab__detail-header">
    <span class="EnrollmentsTab__detail-title">Lesson Schedule ({{ enrollmentsUI.enrollmentDetail.lessonSchedules.length }})</span>
    <button class="EnrollmentsTab__btn" @click.stop="enrollmentsUI.addSchedule()">
      <Plus :size="14" /> Add Lesson
    </button>
  </div>
  <div class="EnrollmentsTab__schedule-list">
    <div v-for="schedule in enrollmentsUI.enrollmentDetail.lessonSchedules" :key="schedule.id" class="EnrollmentsTab__schedule-row">
      <div class="EnrollmentsTab__schedule-date">{{ new Date(schedule.scheduledDate).toLocaleDateString() }}</div>
      <!-- Inline title editing -->
      <div v-if="enrollmentsUI.editingScheduleId === schedule.id" class="EnrollmentsTab__schedule-edit" @click.stop>
        <input v-model="enrollmentsUI.editingTitle" class="EnrollmentsTab__form-input" style="flex:1" @keyup.enter="enrollmentsUI.saveScheduleTitle()" @keyup.escape="enrollmentsUI.cancelEditSchedule()" />
        <button class="EnrollmentsTab__btn" style="background:#7c3aed;color:white" @click="enrollmentsUI.saveScheduleTitle()">Save</button>
        <button class="EnrollmentsTab__btn" @click="enrollmentsUI.cancelEditSchedule()">Cancel</button>
      </div>
      <div v-else class="EnrollmentsTab__schedule-title" @click.stop="enrollmentsUI.startEditSchedule(schedule.id, schedule.title ?? schedule.lesson?.title ?? '')">
        {{ schedule.title ?? schedule.lesson?.title ?? 'Untitled' }}
        <Pencil :size="12" style="opacity:0.4;margin-left:6px" />
      </div>
      <div class="EnrollmentsTab__schedule-day" v-if="schedule.lesson">Day {{ schedule.lesson.dayNumber }}</div>
      <button class="EnrollmentsTab__btn" style="margin-left:auto" @click.stop="enrollmentsUI.requestDeleteSchedule(schedule.id)">
        <Trash2 :size="14" />
      </button>
    </div>
  </div>
</div>
```

Add icon imports if not already present: `Pencil` from lucide-vue-next.

**Add delete schedule confirmation dialog** (after the enrollment delete dialog):
```html
<AdminConfirmDialog
  :open="!!enrollmentsUI.confirmDeleteScheduleId"
  title="Delete Scheduled Lesson"
  message="Are you sure you want to delete this scheduled lesson? Any member data for this lesson will be lost."
  confirm-label="Delete"
  :dangerous="true"
  @confirm="enrollmentsUI.confirmDeleteSchedule()"
  @cancel="enrollmentsUI.cancelDeleteSchedule()"
/>
```

**Extend admin-enrollments-tab.scss** — add:
- `.EnrollmentsTab__detail`: background rgba(255,255,255,0.02), border 1px solid rgba(255,255,255,0.06), border-top none, border-radius 0 0 8px 8px, padding 16px
- `.EnrollmentsTab__detail-header`: display flex, justify-content space-between, align-items center, margin-bottom 12px
- `.EnrollmentsTab__detail-title`: font-size 13px, font-weight 600, color rgba(255,255,255,0.6)
- `.EnrollmentsTab__schedule-list`: display flex, flex-direction column, gap 4px
- `.EnrollmentsTab__schedule-row`: display flex, align-items center, gap 12px, padding 8px 12px, border-radius 6px, hover background rgba(255,255,255,0.04)
- `.EnrollmentsTab__schedule-date`: font-size 13px, color rgba(255,255,255,0.5), width 100px, flex-shrink 0
- `.EnrollmentsTab__schedule-title`: font-size 14px, color white, flex 1, cursor pointer, display flex, align-items center
- `.EnrollmentsTab__schedule-edit`: display flex, gap 6px, flex 1, align-items center
- `.EnrollmentsTab__schedule-day`: font-size 12px, color rgba(255,255,255,0.4), width 50px, text-align right
- Adjust `.EnrollmentsTab__card` when expanded: if parent has expanded state, border-radius 8px 8px 0 0 (use a modifier class `.EnrollmentsTab__card--expanded`)
  </action>
  <verify>
    <automated>cd /Users/lukekeith/www/makeready/client && npm run build 2>&1 | tail -5 && php artisan test tests/Feature/Phase9AdminTest.php --filter "schedule" 2>&1 | tail -10</automated>
  </verify>
  <done>Enrollment cards expand to show lesson schedule list. Schedule rows show date, title (click to inline edit), day number. Add lesson button creates new schedule. Delete button with confirmation. Title edit saves on Enter, cancels on Escape. Build passes. Schedule proxy tests pass.</done>
</task>

</tasks>

<verification>
npm run build — passes
php artisan test tests/Feature/Phase9AdminTest.php — all schedule proxy tests pass
php artisan test — full suite green
</verification>

<success_criteria>
- Clicking an enrollment card expands to show the lesson schedule
- Each schedule row shows date, title, and day number
- Clicking a title enables inline editing with save/cancel
- Add Lesson button creates a new scheduled lesson via POST
- Delete button with confirmation removes a scheduled lesson
- Collapsing and re-expanding preserves loaded detail data
</success_criteria>

<output>
After completion, create `.planning/phases/09-members-enrollments-posts-analytics-profile/09-03-SUMMARY.md`
</output>
