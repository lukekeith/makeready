---
phase: 05-admin-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - routes/web.php
  - app/Http/Controllers/AdminController.php
  - app/Services/ApiService.php
  - resources/views/layouts/admin.blade.php
  - resources/views/pages/admin.blade.php
  - resources/js/app.js
  - tests/Feature/AdminTest.php
  - tests/Feature/AdminShellTest.php
autonomous: true
requirements: [SHELL-01, SHELL-05, SHELL-06]

must_haves:
  truths:
    - "Authenticated leader accessing /admin gets a 200 response with AdminIsland mount point"
    - "Authenticated leader accessing /admin/groups gets the same Blade shell (catch-all works)"
    - "Unauthenticated user accessing /admin is redirected (302)"
    - "Unauthenticated user accessing /admin/groups is redirected (302)"
    - "ApiService has a delete() method for future CRUD operations"
  artifacts:
    - path: "resources/views/layouts/admin.blade.php"
      provides: "Admin shell HTML layout with CSRF meta tag and AdminIsland mount point"
      contains: "data-vue=\"AdminIsland\""
    - path: "resources/views/pages/admin.blade.php"
      provides: "Admin page extending admin layout, passing island props"
    - path: "tests/Feature/AdminShellTest.php"
      provides: "Feature tests for admin shell server-side preconditions"
      min_lines: 40
  key_links:
    - from: "routes/web.php"
      to: "AdminController::show"
      via: "catch-all route /admin/{any?}"
      pattern: "Route::get.*any.*AdminController"
    - from: "resources/views/pages/admin.blade.php"
      to: "resources/views/layouts/admin.blade.php"
      via: "@extends('layouts.admin')"
      pattern: "@extends.*layouts\\.admin"
    - from: "resources/js/app.js"
      to: "AdminIsland component"
      via: "componentRegistry + vue-router app.use"
      pattern: "AdminIsland"
---

<objective>
Set up the server-side infrastructure for the admin shell: Laravel catch-all route, admin Blade layout, AdminController with island props, ApiService.delete(), app.js AdminIsland registration with Vue Router, and test scaffolds.

Purpose: Every subsequent admin phase depends on the catch-all route, Blade layout, and AdminIsland mount point existing. This plan delivers all server-side preconditions and the app.js wiring so Plan 02 can build the Vue components.
Output: Working route, layout, controller, test suite, app.js registration
</objective>

<execution_context>
@/Users/lukekeith/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lukekeith/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-admin-shell/05-CONTEXT.md
@.planning/phases/05-admin-shell/05-RESEARCH.md

@routes/web.php
@app/Http/Controllers/AdminController.php
@app/Services/ApiService.php
@resources/views/layouts/admin.blade.php
@resources/views/pages/admin.blade.php
@resources/js/app.js
@tests/Feature/AdminTest.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install vue-router, create catch-all route, admin layout, controller, and ApiService.delete()</name>
  <files>
    routes/web.php,
    app/Http/Controllers/AdminController.php,
    app/Services/ApiService.php,
    resources/views/layouts/admin.blade.php,
    resources/views/pages/admin.blade.php
  </files>
  <action>
1. Install vue-router pinned to v4: `npm install vue-router@^4.5.0`. Verify package.json shows `"vue-router": "^4.x.x"` (NOT 5.x).

2. In `routes/web.php`, replace the existing admin route group (lines 73-76):
   ```php
   Route::middleware('member.auth')->prefix('admin')->name('admin.')->group(function () {
       // Phase 6+ will add API proxy routes here, before the catch-all
       Route::get('/{any?}', [AdminController::class, 'show'])
           ->where('any', '.*')
           ->name('shell');
   });
   ```
   IMPORTANT: The named route changes from `'admin'` to `'admin.shell'`. This affects AdminTest.php (updated in Task 2).

3. Update `AdminController::show()` to accept any path and pass island props:
   ```php
   public function show(Request $request): \Illuminate\View\View
   {
       $member = $request->attributes->get('member');
       $firstName = $member['firstName'] ?? '';
       $lastName = $member['lastName'] ?? '';
       $initials = '';
       if ($firstName) $initials .= strtoupper(substr($firstName, 0, 1));
       if ($lastName) $initials .= strtoupper(substr($lastName, 0, 1));
       if ($initials === '') $initials = '?';

       $islandProps = [
           'avatarUrl'   => $member['profilePicture'] ?? $member['avatarUrl'] ?? null,
           'initials'    => $initials,
           'memberName'  => trim("$firstName $lastName") ?: 'Member',
           'googleEmail' => $member['googleEmail'] ?? null,
           'logoutUrl'   => route('logout'),
       ];

       return view('pages.admin', compact('member', 'islandProps'));
   }
   ```

4. Create `resources/views/layouts/admin.blade.php` — a minimal HTML shell:
   - DOCTYPE, html lang, head with charset, viewport, csrf-token meta, favicon, title with @yield('title', 'MakeReady Admin')
   - @vite(['resources/css/app.scss', 'resources/js/app.js'])
   - body with class "AdminBody"
   - Single @yield('content') inside body — NO sidebar in Blade (sidebar lives in Vue AdminIsland)
   - NO ModalProvider island — admin uses its own modal pattern inside AdminIsland

5. Rewrite `resources/views/pages/admin.blade.php`:
   - @extends('layouts.admin') instead of 'layouts.home'
   - @section('title', 'MakeReady Admin')
   - @section('content') contains a single div: `<div id="admin-island" data-vue="AdminIsland" data-props="{{ json_encode($islandProps) }}"></div>`
   - Nothing else — all admin UI is rendered by the Vue island

6. Add `delete()` method to `ApiService.php` after the `patch()` method. Mirror the `patch()` method pattern exactly but use `->delete()` instead of `->patch()`, and accept no `$data` parameter (DELETE requests typically have no body). Signature: `public function delete(string $endpoint, Request $request): array`.
  </action>
  <verify>
    <automated>cd /Users/lukekeith/www/makeready/client && php artisan route:list --path=admin 2>&1 | head -20 && grep -c "vue-router" package.json && grep -c "function delete" app/Services/ApiService.php</automated>
  </verify>
  <done>
    - `/admin` and `/admin/{any}` routes registered under member.auth middleware
    - vue-router@4.x in package.json (not 5.x)
    - admin.blade.php layout exists with CSRF meta tag and @yield('content')
    - pages/admin.blade.php mounts AdminIsland with JSON props
    - ApiService.php has delete() method
  </done>
</task>

<task type="auto">
  <name>Task 2: Register AdminIsland in app.js and create test scaffolds</name>
  <files>
    resources/js/app.js,
    tests/Feature/AdminTest.php,
    tests/Feature/AdminShellTest.php
  </files>
  <action>
1. In `resources/js/app.js`:
   - Add import at top: `import AdminIsland from './islands/admin-island/admin-island.vue'`
   - Add import: `import { router as adminRouter } from './islands/admin-island/router'`
   - Add to componentRegistry: `'AdminIsland': AdminIsland`
   - In the `document.querySelectorAll('[data-vue]').forEach(...)` block, after `app.use(createPinia())`, add:
     ```javascript
     if (name === 'AdminIsland') {
       app.use(adminRouter)
     }
     ```
   NOTE: The AdminIsland Vue component and router.ts files do not exist yet — they will be created in Plan 02. app.js must be updated NOW so that Plan 02 does not need to touch it. The Vite build may warn about missing modules until Plan 02 completes — this is expected.

2. Create placeholder files so app.js imports don't fail during build:
   - `resources/js/islands/admin-island/admin-island.vue`: Minimal SFC with `<script setup>` and `<template><div class="AdminLayout"><router-view /></div></template>`. Import `{ RouterView } from 'vue-router'`.
   - `resources/js/islands/admin-island/router.ts`: Export a `router` created with `createRouter({ history: createWebHistory(), routes: [{ path: '/admin/:pathMatch(.*)*', component: { template: '<div>Loading...</div>' } }] })`.

3. Update `tests/Feature/AdminTest.php`:
   - Change `route('admin')` to `route('admin.shell')` in `test_admin_route_is_registered`
   - Update `test_admin_page_renders_for_leader` to assert `$response->assertSee('AdminIsland')` instead of asserting for 'Jane' and 'Smith' (the Blade no longer renders member info directly — it's in the Vue island props)
   - Update `test_admin_page_has_logout_button` — the Blade no longer has a logout button visible in HTML (it's in Vue). Replace with asserting the page renders successfully (200) and contains `data-vue="AdminIsland"`.
   - Remove `test_admin_page_shows_phone` — phone is no longer rendered in Blade HTML
   - Keep `test_admin_page_redirects_unauthenticated` as-is (still redirects to `/`)

4. Create `tests/Feature/AdminShellTest.php` with these tests:
   - `test_admin_page_mounts_admin_island`: GET /admin -> 200, assertSee('data-vue="AdminIsland"')
   - `test_admin_subpaths_served_by_same_blade_template`: GET /admin/groups -> 200, assertSee('data-vue="AdminIsland"')
   - `test_admin_subpath_redirects_unauthenticated`: GET /admin/groups with no session -> 302
   - `test_admin_island_props_contain_member_data`: GET /admin -> 200, assertSee island props JSON containing memberName
   - `test_admin_layout_contains_csrf_meta`: GET /admin -> 200, assertSee('meta name="csrf-token"', false)

   Use same Http::fake() pattern as AdminTest.php. Include `googleEmail` in the fake session to test leader prop passthrough.
  </action>
  <verify>
    <automated>cd /Users/lukekeith/www/makeready/client && php artisan test --filter AdminTest && php artisan test --filter AdminShellTest</automated>
  </verify>
  <done>
    - app.js registers AdminIsland with Vue Router plugin
    - Placeholder admin-island.vue and router.ts exist (prevents build errors)
    - AdminTest.php updated for new Blade structure (no more inline member info)
    - AdminShellTest.php covers catch-all routing, island mount point, CSRF meta, and auth redirect for sub-paths
    - All tests pass
  </done>
</task>

</tasks>

<verification>
- `php artisan test` — full suite passes (no regressions from route rename or Blade changes)
- `npm run build` — Vite build succeeds with AdminIsland placeholder
- GET /admin returns 200 with `data-vue="AdminIsland"` in response body
- GET /admin/groups returns 200 with same `data-vue="AdminIsland"` (catch-all works)
- GET /admin without auth returns 302 redirect
</verification>

<success_criteria>
- Laravel catch-all route serves admin Blade shell for all /admin/* paths
- AdminIsland mount point present in HTML response
- Vue Router installed at v4.x
- ApiService.delete() available for future CRUD phases
- All existing tests pass plus new AdminShellTest assertions
</success_criteria>

<output>
After completion, create `.planning/phases/05-admin-shell/05-01-SUMMARY.md`
</output>
