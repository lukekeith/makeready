---
phase: 05-admin-shell
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - resources/js/islands/admin-island/admin-island.vue
  - resources/js/islands/admin-island/router.ts
  - resources/js/islands/admin-island/components/admin-sidebar.vue
  - resources/js/islands/admin-island/sections/dashboard-section.vue
  - resources/js/islands/admin-island/sections/groups-section.vue
  - resources/js/islands/admin-island/sections/programs-section.vue
  - resources/js/islands/admin-island/sections/profile-section.vue
  - resources/css/layouts/admin-layout.scss
  - resources/css/components/admin/admin-sidebar.scss
  - resources/css/app.scss
autonomous: true
requirements: [SHELL-02, SHELL-03, SHELL-06, SHELL-07]

must_haves:
  truths:
    - "Admin page shows a fixed left sidebar with icon-only nav items that expand labels on hover"
    - "Sidebar has links for Dashboard, Groups, Programs, Profile"
    - "Clicking sidebar links changes the URL and content area without a full page reload"
    - "Avatar at bottom of sidebar opens a dropdown menu with Member Experience link and Logout"
    - "Navigating between sections does not destroy Vue/Pinia state"
    - "CSRF token is configured on axios when AdminIsland mounts"
  artifacts:
    - path: "resources/js/islands/admin-island/admin-island.vue"
      provides: "Root admin Vue component with CSRF config, sidebar, and router-view"
      min_lines: 30
    - path: "resources/js/islands/admin-island/router.ts"
      provides: "Vue Router with history mode and admin route definitions"
      exports: ["router"]
    - path: "resources/js/islands/admin-island/components/admin-sidebar.vue"
      provides: "Sidebar nav with icon-only collapse, hover expand, avatar menu dropdown"
      min_lines: 80
    - path: "resources/css/layouts/admin-layout.scss"
      provides: "Flexbox layout for sidebar + content area"
      contains: ".AdminLayout"
    - path: "resources/css/components/admin/admin-sidebar.scss"
      provides: "Sidebar styles with collapsed/expanded states, hover transition"
      contains: ".AdminSidebar"
  key_links:
    - from: "resources/js/islands/admin-island/admin-island.vue"
      to: "resources/js/islands/admin-island/router.ts"
      via: "Vue Router plugin (app.use in app.js)"
      pattern: "RouterView"
    - from: "resources/js/islands/admin-island/components/admin-sidebar.vue"
      to: "vue-router"
      via: "router-link components for client-side navigation"
      pattern: "router-link"
    - from: "resources/js/islands/admin-island/admin-island.vue"
      to: "axios"
      via: "CSRF configuration in onMounted"
      pattern: "axios\\.defaults\\.headers"
---

<objective>
Build the AdminIsland Vue application: sidebar navigation with icon-only collapse and hover expansion, Vue Router with section placeholder views, avatar dropdown menu with "Member Experience" link and logout, CSRF axios configuration, and all admin SCSS.

Purpose: This is the interactive shell that all future admin CRUD phases render inside. Sidebar navigation, client-side routing, and CSRF configuration must work before any entity management UI is built.
Output: Fully functional admin shell with sidebar, routing, avatar menu, and placeholder section views
</objective>

<execution_context>
@/Users/lukekeith/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lukekeith/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-admin-shell/05-CONTEXT.md
@.planning/phases/05-admin-shell/05-RESEARCH.md
@.planning/phases/05-admin-shell/05-01-SUMMARY.md

@resources/js/app.js
@resources/css/app.scss

<interfaces>
<!-- From Plan 01: AdminIsland is registered in app.js with vue-router plugin -->
<!-- app.js calls app.use(adminRouter) for the AdminIsland component -->
<!-- The Blade template passes these props via data-props JSON: -->

Props interface for AdminIsland:
- avatarUrl?: string (member profile picture URL)
- initials?: string (fallback initials, default '?')
- memberName?: string (display name)
- googleEmail?: string (null if not a leader)
- logoutUrl?: string (route to POST logout form)

Route structure (from CONTEXT.md locked decisions):
- /admin -> DashboardSection
- /admin/groups -> GroupsSection
- /admin/groups/:id -> GroupsSection (detail — Phase 6)
- /admin/programs -> ProgramsSection
- /admin/programs/:id -> ProgramsSection (detail — Phase 7)
- /admin/profile -> ProfileSection
- /admin/:pathMatch(.*)* -> redirect to /admin

Sidebar sections (from CONTEXT.md locked decisions):
- Dashboard (icon: LayoutDashboard from lucide)
- Groups (icon: Users from lucide)
- Programs (icon: BookOpen from lucide)
- Profile (icon: User from lucide)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build AdminIsland root component, Vue Router, section placeholders, and SCSS</name>
  <files>
    resources/js/islands/admin-island/admin-island.vue,
    resources/js/islands/admin-island/router.ts,
    resources/js/islands/admin-island/sections/dashboard-section.vue,
    resources/js/islands/admin-island/sections/groups-section.vue,
    resources/js/islands/admin-island/sections/programs-section.vue,
    resources/js/islands/admin-island/sections/profile-section.vue,
    resources/css/layouts/admin-layout.scss,
    resources/css/app.scss
  </files>
  <action>
1. Replace the placeholder `resources/js/islands/admin-island/router.ts` with the full router:
   - Import `createRouter`, `createWebHistory` from 'vue-router'
   - Import all four section components from './sections/'
   - Routes: /admin -> DashboardSection, /admin/groups -> GroupsSection, /admin/groups/:id -> GroupsSection, /admin/programs -> ProgramsSection, /admin/programs/:id -> ProgramsSection, /admin/profile -> ProfileSection
   - Catch-all: /admin/:pathMatch(.*)* redirects to /admin
   - Export `router`

2. Replace the placeholder `resources/js/islands/admin-island/admin-island.vue` with the full component:
   - `<script setup lang="ts">` with Props interface matching the Blade island props (avatarUrl, initials, memberName, googleEmail, logoutUrl)
   - `onMounted`: Configure CSRF via `axios.defaults.headers.common['X-CSRF-TOKEN']` from `meta[name="csrf-token"]`
   - Template: `<div class="AdminLayout">` containing AdminSidebar component and `<main class="AdminLayout__content"><RouterView /></main>`
   - Pass all relevant props to AdminSidebar (avatarUrl, initials, memberName, logoutUrl)

3. Create four section placeholder components in `resources/js/islands/admin-island/sections/`:
   - Each is a minimal SFC with `<script setup>` and a template containing:
     - `<div class="AdminSection">` wrapper
     - `<header class="AdminSection__header">` with `<h1 class="AdminSection__title">` showing section name (Dashboard, Groups, Programs, Profile)
     - `<div class="AdminSection__body">` with paragraph text: "Groups management coming in Phase 6" (etc.)
   - `groups-section.vue` and `programs-section.vue` should also use `useRoute()` to check for `:id` param and show "Detail view for [id]" when present (demonstrating route params work)

4. Create `resources/css/layouts/admin-layout.scss`:
   - `.AdminLayout`: `display: flex; height: 100vh; background: #1a1d2e;`
   - `.AdminLayout__content`: `flex: 1; overflow-y: auto; padding: 32px 24px;`
   - `.AdminLayout__content-inner`: `max-width: 960px; margin: 0 auto;` (Claude's discretion: 960px content max-width)
   - `.AdminBody`: `background: #1a1d2e;` (override body gradient for admin)
   - `.AdminSection__header`: flex row, justify-content: space-between, align-items: center, margin-bottom: 24px
   - `.AdminSection__title`: font-size: 24px, font-weight: 600, color: white
   - `.AdminSection__body`: color: rgba(255,255,255,0.6), font-size: 14px

5. Add SCSS imports to `resources/css/app.scss`:
   - Add `@use 'layouts/admin-layout' as layout-admin;` in the Layout components section (after `@use 'components/layout/modal-provider'`)
   - Add `@use 'components/admin/admin-sidebar' as admin-sidebar;` — create a new "Admin components" section comment after the Panel components section
  </action>
  <verify>
    <automated>cd /Users/lukekeith/www/makeready/client && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - AdminIsland renders with AdminLayout wrapper and RouterView
    - Vue Router handles /admin, /admin/groups, /admin/programs, /admin/profile paths
    - Section placeholders show section title and "coming in Phase N" message
    - CSRF token configured on axios at mount time
    - SCSS compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Build AdminSidebar with icon nav, hover expand, avatar dropdown menu</name>
  <files>
    resources/js/islands/admin-island/components/admin-sidebar.vue,
    resources/css/components/admin/admin-sidebar.scss
  </files>
  <action>
1. Create `resources/js/islands/admin-island/components/admin-sidebar.vue`:
   - Props: avatarUrl, initials, memberName, logoutUrl (all optional strings)
   - Reactive state: `isExpanded` ref (boolean, default false) for sidebar expansion, `showAvatarMenu` ref (boolean) for dropdown
   - Use `useRoute()` from vue-router to determine which nav item is active (match on path prefix: '/admin/groups' -> groups active, '/admin/programs' -> programs active, '/admin/profile' -> profile active, '/admin' exact -> dashboard active)
   - Sidebar nav items as an array of objects: `{ label, icon, path, key }` for Dashboard, Groups, Programs, Profile
   - Use lucide-vue-next icons: `LayoutDashboard`, `Users`, `BookOpen`, `UserRound` — import from 'lucide-vue-next'. Also import `ChevronRight` for expand indicator and `LogOut` for logout button.
   - Template structure:
     ```
     <aside class="AdminSidebar" :class="{ 'AdminSidebar--expanded': isExpanded }" @mouseenter="isExpanded = true" @mouseleave="isExpanded = false; showAvatarMenu = false">
       <div class="AdminSidebar__logo">
         <img src="/logo-mark.svg" alt="MakeReady" class="AdminSidebar__logo-icon" />
       </div>
       <nav class="AdminSidebar__nav">
         <router-link v-for="item in navItems" :key="item.key" :to="item.path"
           :class="['AdminSidebar__item', isActive(item.key) && 'AdminSidebar__item--active']">
           <component :is="item.icon" :size="20" class="AdminSidebar__item-icon" />
           <span class="AdminSidebar__item-label">{{ item.label }}</span>
         </router-link>
       </nav>
       <div class="AdminSidebar__footer">
         <button type="button" class="AdminSidebar__avatar-button" @click="showAvatarMenu = !showAvatarMenu">
           <span class="Avatar AdminSidebar__avatar">
             <img v-if="avatarUrl" :src="avatarUrl" class="Avatar__image" referrerpolicy="no-referrer" />
             <span v-else class="Avatar__fallback">{{ initials }}</span>
           </span>
           <span class="AdminSidebar__avatar-name">{{ memberName }}</span>
         </button>
         <!-- Avatar dropdown menu -->
         <div v-if="showAvatarMenu" class="AdminSidebar__menu">
           <a href="/member/home" class="AdminSidebar__menu-item">
             Member Experience
           </a>
           <button type="button" class="AdminSidebar__menu-item AdminSidebar__menu-item--danger" @click="handleLogout">
             Logout
           </button>
         </div>
       </div>
     </aside>
     ```
   - handleLogout function: Create a form programmatically (same pattern as navigation-island.vue), POST to logoutUrl with CSRF token from meta tag.
   - "Member Experience" link is a standard `<a href="/member/home">` (full page navigation — intentional, switching experiences is a context switch per CONTEXT.md locked decision)
   - The avatar dropdown is a simple v-if toggle — no Teleport needed (sidebar is full-height fixed, z-index is predictable)

2. Create `resources/css/components/admin/admin-sidebar.scss` with BEM naming:
   - `.AdminSidebar`: width 56px (collapsed), background #252936, flex column, align-items center, padding 12px 0, transition width 0.2s ease, overflow hidden, position relative, flex-shrink 0, z-index 10
   - `.AdminSidebar--expanded`: width 220px, align-items flex-start
   - `.AdminSidebar__logo`: width 100%, display flex, justify-content center, padding 8px 0 16px, border-bottom 1px solid rgba(255,255,255,0.08)
   - `.AdminSidebar__logo-icon`: width 28px, height 28px
   - `.AdminSidebar__nav`: flex 1, display flex, flex-direction column, gap 4px, padding 12px 8px, width 100%
   - `.AdminSidebar__item`: display flex, align-items center, gap 12px, padding 10px 12px, border-radius 8px, color rgba(255,255,255,0.6), text-decoration none, white-space nowrap, transition background 0.15s ease, cursor pointer
   - `.AdminSidebar__item:hover`: background rgba(255,255,255,0.06)
   - `.AdminSidebar__item--active`: background rgba(139,92,246,0.15), color white (purple accent highlight per locked decision)
   - `.AdminSidebar__item-icon`: flex-shrink 0
   - `.AdminSidebar__item-label`: font-size 14px, font-weight 500, opacity 0, transition opacity 0.15s ease. In `.AdminSidebar--expanded`: opacity 1
   - `.AdminSidebar__footer`: width 100%, padding 8px, border-top 1px solid rgba(255,255,255,0.08), position relative
   - `.AdminSidebar__avatar-button`: display flex, align-items center, gap 10px, padding 8px, width 100%, background none, border none, cursor pointer, border-radius 8px, transition background 0.15s. Hover: background rgba(255,255,255,0.06)
   - `.AdminSidebar__avatar`: flex-shrink 0, width 32px, height 32px
   - `.AdminSidebar__avatar-name`: font-size 13px, color rgba(255,255,255,0.8), opacity 0, white-space nowrap, transition opacity 0.15s. In `.AdminSidebar--expanded`: opacity 1
   - `.AdminSidebar__menu`: position absolute, bottom 100%, left 8px, width calc(100% - 16px), background #323748, border-radius 8px, padding 4px, box-shadow 0 -4px 16px rgba(0,0,0,0.3), margin-bottom 4px
   - `.AdminSidebar__menu-item`: display block, width 100%, padding 10px 12px, font-size 13px, color rgba(255,255,255,0.85), background none, border none, text-align left, border-radius 6px, cursor pointer, text-decoration none. Hover: background rgba(255,255,255,0.06)
   - `.AdminSidebar__menu-item--danger`: color #ef4444
  </action>
  <verify>
    <automated>cd /Users/lukekeith/www/makeready/client && npm run build 2>&1 | tail -5 && php artisan test --filter AdminShellTest</automated>
  </verify>
  <done>
    - Sidebar renders with 4 nav items (Dashboard, Groups, Programs, Profile) using lucide icons
    - Sidebar is icon-only (56px) by default, expands to 220px on hover showing labels
    - Active nav item has purple highlight background
    - router-link tags provide client-side navigation (no page reloads)
    - Avatar at sidebar bottom opens dropdown with "Member Experience" link (/member/home) and Logout button
    - All SCSS compiles, all tests pass
  </done>
</task>

</tasks>

<verification>
- `npm run build` passes — all Vue and SCSS compiles
- `php artisan test` — full suite green
- Manual: Visit /admin in browser -> see sidebar with icons, hover to expand with labels
- Manual: Click Groups in sidebar -> URL changes to /admin/groups, content updates, no page reload
- Manual: Click avatar -> dropdown shows "Member Experience" and "Logout"
- Manual: Click "Member Experience" -> navigates to /member/home (full page nav)
</verification>

<success_criteria>
- Admin shell renders with sidebar + content area layout
- Sidebar nav items link to correct Vue Router routes
- Client-side navigation works (no full page reloads between admin sections)
- Avatar menu shows "Member Experience" link at top per locked decision
- CSRF token configured on axios for all future admin API calls
- All existing tests pass, new AdminShellTest assertions pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-admin-shell/05-02-SUMMARY.md`
</output>
