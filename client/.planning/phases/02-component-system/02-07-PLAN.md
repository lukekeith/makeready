---
phase: 02-component-system
plan: 07
type: execute
wave: 3
depends_on: [02-06]
files_modified:
  - resources/js/app.js
  - resources/js/components/index.ts
autonomous: false
requirements: [CSYS-04, CSYS-05, CSYS-06, CSYS-07, CSYS-08]

must_haves:
  truths:
    - "Only ~8 interactive Vue SFCs remain in resources/js/components/ (Digit, Modal, VerifyCode, BulletTextInput, PhoneEntry, VideoPlayer, ModalProvider, Keypad)"
    - "All ~52 presentation-only Vue SFCs and their ~52 story files are deleted"
    - "The barrel export index.ts is deleted"
    - "app.js registers only the ~8 interactive Vue components in componentRegistry"
    - "npm run build succeeds with no missing import errors"
    - "npm run story:build succeeds for the remaining ~8 Vue component stories"
    - "php artisan test passes all tests"
  artifacts:
    - path: "resources/js/app.js"
      provides: "Vue island mounter with only interactive components registered"
      contains: "PhoneEntry"
    - path: "resources/js/components/domain/phone-entry/phone-entry.vue"
      provides: "PhoneEntry Vue SFC (KEPT)"
      contains: "script setup"
    - path: "resources/js/components/primitive/modal/modal.vue"
      provides: "Modal Vue SFC (KEPT)"
      contains: "script setup"
    - path: "resources/js/components/layout/modal-provider/modal-provider.vue"
      provides: "ModalProvider Vue SFC (KEPT)"
      contains: "Teleport"
  key_links:
    - from: "resources/js/app.js"
      to: "resources/js/components/domain/phone-entry/phone-entry.vue"
      via: "import + componentRegistry entry"
      pattern: "import PhoneEntry"
    - from: "resources/js/app.js"
      to: "resources/js/components/layout/modal-provider/modal-provider.vue"
      via: "import + componentRegistry entry"
      pattern: "import ModalProvider"
---

<objective>
Remove all presentation-only Vue SFCs and their story files (replaced by Blade in Plan 06), delete the barrel export, update app.js to register only the ~8 interactive Vue components, and verify the entire build still works.

Purpose: Completes the architecture transition by cleaning up the now-redundant Vue files. After this plan, the codebase has a clean separation: ~52 Blade components for server rendering + ~8 Vue components for client-side interactivity.

Output: Clean resources/js/components/ directory with only interactive Vue SFCs. Updated app.js. All builds passing.
</objective>

<execution_context>
@/Users/lukekeith/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lukekeith/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-component-system/02-RESEARCH.md
@.planning/phases/02-component-system/02-CONTEXT.md
@.planning/phases/02-component-system/02-06-SUMMARY.md

<interfaces>
<!-- Vue components to KEEP (interactive — require client-side JS): -->
<!-- resources/js/components/primitive/digit/digit.vue -->
<!-- resources/js/components/primitive/modal/modal.vue -->
<!-- resources/js/components/primitive/verify-code/verify-code.vue -->
<!-- resources/js/components/primitive/bullet-text-input/bullet-text-input.vue -->
<!-- resources/js/components/domain/phone-entry/phone-entry.vue -->
<!-- resources/js/components/domain/video-player/video-player.vue -->
<!-- resources/js/components/layout/modal-provider/modal-provider.vue -->
<!-- resources/js/components/panel/keypad/keypad.vue -->

<!-- Each of these also has a .scss and .story.vue file — KEEP all three -->

<!-- Supporting files to KEEP: -->
<!-- resources/js/util/cva.ts (used by Vue components) -->
<!-- resources/js/util/classnames.ts (used by Vue components) -->
<!-- resources/js/util/index.ts -->
<!-- resources/js/stores/modal.store.ts -->
<!-- resources/js/modal-registry.ts -->
<!-- histoire.config.ts -->

<!-- Files to DELETE: -->
<!-- resources/js/components/index.ts (barrel export — not needed for Blade) -->
<!-- All .vue and .story.vue files NOT in the keep list above -->
<!-- Story files for kept components stay -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove replaced Vue SFCs/stories, delete barrel export, update app.js</name>
  <files>resources/js/components/ (deletions), resources/js/app.js</files>
  <action>
**Step 1: Delete presentation-only Vue SFCs and their stories.**

Delete the ENTIRE directory for each component that has been replaced by Blade. This removes the .vue, .scss (Vue's copy), and .story.vue files in one go.

**Primitive directories to DELETE (18):**
- resources/js/components/primitive/avatar/
- resources/js/components/primitive/badge/
- resources/js/components/primitive/button/
- resources/js/components/primitive/card/
- resources/js/components/primitive/date-input/
- resources/js/components/primitive/empty-state/
- resources/js/components/primitive/gender-select/
- resources/js/components/primitive/icon/
- resources/js/components/primitive/input/
- resources/js/components/primitive/label/
- resources/js/components/primitive/loading/
- resources/js/components/primitive/mobile-date/
- resources/js/components/primitive/mobile-input/
- resources/js/components/primitive/mobile-select/
- resources/js/components/primitive/qr-code/
- resources/js/components/primitive/social-button/
- resources/js/components/primitive/step-indicator/
- resources/js/components/primitive/toggle/

**Domain directories to DELETE (28):**
- resources/js/components/domain/account-link/
- resources/js/components/domain/account-modal-content/
- resources/js/components/domain/edit-profile-modal-content/
- resources/js/components/domain/event-card/
- resources/js/components/domain/group-card/
- resources/js/components/domain/group-home/
- resources/js/components/domain/group-home-header/
- resources/js/components/domain/group-leader-info/
- resources/js/components/domain/group-leader-note/
- resources/js/components/domain/group-list-card/
- resources/js/components/domain/group-post-card/
- resources/js/components/domain/invite-header/
- resources/js/components/domain/invite-modal/
- resources/js/components/domain/join-code-page/
- resources/js/components/domain/lesson-page-header/
- resources/js/components/domain/member-card/
- resources/js/components/domain/navigation/
- resources/js/components/domain/navigation-menu-content/
- resources/js/components/domain/organization-card/
- resources/js/components/domain/profile-form/
- resources/js/components/domain/question-button/
- resources/js/components/domain/question-modal/
- resources/js/components/domain/read-passage-button/
- resources/js/components/domain/read-verse-modal/
- resources/js/components/domain/scripture-display/
- resources/js/components/domain/study-card/
- resources/js/components/domain/study-launcher/
- resources/js/components/domain/study-schedule-card/

**Layout directories to DELETE (2):**
- resources/js/components/layout/auth/
- resources/js/components/layout/home/

**Panel directories to DELETE (4):**
- resources/js/components/panel/confirmation/
- resources/js/components/panel/group-info-card/
- resources/js/components/panel/page-title/
- resources/js/components/panel/study-info-card/

**Barrel export to DELETE:**
- resources/js/components/index.ts

**KEEP these directories (interactive Vue components):**
- resources/js/components/primitive/digit/ (KEEP)
- resources/js/components/primitive/modal/ (KEEP)
- resources/js/components/primitive/verify-code/ (KEEP)
- resources/js/components/primitive/bullet-text-input/ (KEEP)
- resources/js/components/domain/phone-entry/ (KEEP)
- resources/js/components/domain/video-player/ (KEEP)
- resources/js/components/layout/modal-provider/ (KEEP)
- resources/js/components/panel/keypad/ (KEEP)

**Step 2: Update `resources/js/app.js`** to register only the 8 interactive components:

```javascript
import './bootstrap'
import { createApp } from 'vue'
import { createPinia } from 'pinia'

// Interactive Vue components (require client-side JS)
import PhoneEntry from './components/domain/phone-entry/phone-entry.vue'
import VideoPlayer from './components/domain/video-player/video-player.vue'
import Digit from './components/primitive/digit/digit.vue'
import Modal from './components/primitive/modal/modal.vue'
import VerifyCode from './components/primitive/verify-code/verify-code.vue'
import BulletTextInput from './components/primitive/bullet-text-input/bullet-text-input.vue'
import ModalProvider from './components/layout/modal-provider/modal-provider.vue'
import Keypad from './components/panel/keypad/keypad.vue'

const componentRegistry = {
  'PhoneEntry': PhoneEntry,
  'VideoPlayer': VideoPlayer,
  'Digit': Digit,
  'Modal': Modal,
  'VerifyCode': VerifyCode,
  'BulletTextInput': BulletTextInput,
  'ModalProvider': ModalProvider,
  'Keypad': Keypad,
}

// Vue island auto-mounter
// Usage in Blade: <div data-vue="PhoneEntry" data-props='{"title":"Enter your phone"}'></div>
document.querySelectorAll('[data-vue]').forEach((el) => {
  const name = el.dataset.vue
  const Component = componentRegistry[name]
  if (!Component) {
    console.warn(`[Vue islands] No component registered for "${name}"`)
    return
  }
  const props = el.dataset.props ? JSON.parse(el.dataset.props) : {}
  const app = createApp(Component, props)
  app.use(createPinia())
  app.mount(el)
})
```

**Step 3: Verify cleanup is correct:**
- Run `find resources/js/components -name "*.vue" -not -name "*.story.vue" | wc -l` — should be exactly 8
- Run `find resources/js/components -name "*.story.vue" | wc -l` — should be exactly 8
- Run `npm run build` — should succeed with no missing imports
- Run `npm run story:build` — should succeed building only the 8 interactive component stories
- Run `php artisan test` — should pass all tests

**CRITICAL:** Before deleting, double-check each directory against the keep list. If a Vue component's SCSS is imported by its `.vue` file AND also exists in `resources/css/components/`, that is expected (the css/components copy serves Blade; the js/components copy served Vue — deleting the Vue copy is correct because the component is gone).

However, some of the kept Vue components (Digit, Modal, etc.) import their own SCSS via `import './digit.scss'`. Their SCSS files must NOT be deleted. They live inside the kept directories, so deleting only the listed directories is safe.
  </action>
  <verify>
    <automated>cd /Users/lukekeith/www/makeready/client && find resources/js/components -name "*.vue" -not -name "*.story.vue" | wc -l && npm run build && php artisan test</automated>
  </verify>
  <done>Only 8 interactive Vue SFCs remain. Barrel export deleted. app.js registers exactly 8 components. npm run build and php artisan test both pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of Blade + Vue hybrid component system</name>
  <files>N/A</files>
  <action>
Present the completed Blade + Vue hybrid component system for human visual verification:
- 52 anonymous Blade components at resources/views/components/ (primitive, domain, layout, panel)
- 8 interactive Vue SFCs retained (PhoneEntry, VideoPlayer, Digit, Modal, VerifyCode, BulletTextInput, ModalProvider, Keypad)
- PHP cva() helper for variant-to-BEM class mapping
- All SCSS compiled through Vite via app.scss
- ~52 replaced Vue SFCs and story files removed

Verification steps:
1. Run `php artisan serve` and visit http://localhost:8000 — verify the app loads with styles (CSS compiled correctly)
2. Run `npm run story:dev` — verify Histoire starts and shows stories for the 8 interactive Vue components
3. Check a few Blade components render correctly by visiting any page that uses them
4. In browser DevTools, confirm CSS rules exist for `.Button`, `.Avatar`, `.Navigation`, `.Card` BEM classes
5. Verify `find resources/js/components -name "*.vue" -not -name "*.story.vue" | wc -l` returns 8
6. Verify `find resources/views/components -name "*.blade.php" | wc -l` returns 52
  </action>
  <verify>User confirms visual fidelity of Blade components matches Vue originals</verify>
  <done>User approves the Blade + Vue hybrid component system as visually correct and functionally complete.</done>
</task>

</tasks>

<verification>
- `npm run build` — succeeds (Vite compiles JS + SCSS)
- `npm run story:build` — succeeds (Histoire builds ~8 Vue stories)
- `php artisan test` — all tests pass
- `find resources/views/components -name "*.blade.php" | wc -l` — 52
- `find resources/js/components -name "*.vue" -not -name "*.story.vue" | wc -l` — 8
- `grep -c "componentRegistry" resources/js/app.js` — confirms 8 entries
</verification>

<success_criteria>
Clean separation achieved: 52 Blade components for server rendering, 8 Vue components for client interactivity. All builds pass. No orphaned Vue files. Visual verification by user confirms styles are correct.
</success_criteria>

<output>
After completion, create `.planning/phases/02-component-system/02-07-SUMMARY.md`
</output>
