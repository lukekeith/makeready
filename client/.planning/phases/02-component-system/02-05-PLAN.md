---
phase: 02-component-system
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - app/View/helpers.php
  - composer.json
  - resources/css/app.scss
  - resources/css/components/primitive/button.scss
  - resources/css/components/primitive/avatar.scss
  - resources/css/components/primitive/badge.scss
  - resources/css/components/primitive/card.scss
  - resources/css/components/primitive/icon.scss
  - resources/css/components/primitive/input.scss
  - resources/css/components/primitive/label.scss
  - resources/css/components/primitive/loading.scss
  - resources/css/components/primitive/step-indicator.scss
  - resources/css/components/primitive/toggle.scss
  - resources/css/components/primitive/date-input.scss
  - resources/css/components/primitive/empty-state.scss
  - resources/css/components/primitive/gender-select.scss
  - resources/css/components/primitive/mobile-date.scss
  - resources/css/components/primitive/mobile-input.scss
  - resources/css/components/primitive/mobile-select.scss
  - resources/css/components/primitive/qr-code.scss
  - resources/css/components/primitive/social-button.scss
  - resources/css/components/primitive/digit.scss
  - resources/css/components/primitive/modal.scss
  - resources/css/components/primitive/verify-code.scss
  - resources/css/components/primitive/bullet-text-input.scss
  - resources/css/components/domain/account-link.scss
  - resources/css/components/domain/account-modal-content.scss
  - resources/css/components/domain/edit-profile-modal-content.scss
  - resources/css/components/domain/event-card.scss
  - resources/css/components/domain/group-card.scss
  - resources/css/components/domain/group-home.scss
  - resources/css/components/domain/group-home-header.scss
  - resources/css/components/domain/group-leader-info.scss
  - resources/css/components/domain/group-leader-note.scss
  - resources/css/components/domain/group-list-card.scss
  - resources/css/components/domain/group-post-card.scss
  - resources/css/components/domain/invite-header.scss
  - resources/css/components/domain/invite-modal.scss
  - resources/css/components/domain/join-code-page.scss
  - resources/css/components/domain/lesson-page-header.scss
  - resources/css/components/domain/member-card.scss
  - resources/css/components/domain/navigation.scss
  - resources/css/components/domain/navigation-menu-content.scss
  - resources/css/components/domain/organization-card.scss
  - resources/css/components/domain/phone-entry.scss
  - resources/css/components/domain/profile-form.scss
  - resources/css/components/domain/question-button.scss
  - resources/css/components/domain/question-modal.scss
  - resources/css/components/domain/read-passage-button.scss
  - resources/css/components/domain/read-verse-modal.scss
  - resources/css/components/domain/scripture-display.scss
  - resources/css/components/domain/study-card.scss
  - resources/css/components/domain/study-launcher.scss
  - resources/css/components/domain/study-schedule-card.scss
  - resources/css/components/domain/video-player.scss
  - resources/css/components/layout/auth.scss
  - resources/css/components/layout/home.scss
  - resources/css/components/layout/modal-provider.scss
  - resources/css/components/panel/page-title.scss
  - resources/css/components/panel/confirmation.scss
  - resources/css/components/panel/group-info-card.scss
  - resources/css/components/panel/keypad.scss
  - resources/css/components/panel/study-info-card.scss
  - tests/Feature/CvaHelperTest.php
  - tests/Feature/ComponentSmokeTest.php
autonomous: true
requirements: [CSYS-01, CSYS-02, CSYS-03]

must_haves:
  truths:
    - "PHP cva() function is callable from any Blade template and returns correct BEM class strings"
    - "All ~60 component SCSS files are compiled into the Vite output via app.scss @use imports"
    - "npm run build succeeds with all SCSS loaded"
    - "php artisan test passes with cva() unit tests and a basic Blade component smoke test"
  artifacts:
    - path: "app/View/helpers.php"
      provides: "PHP cva() helper function"
      contains: "function cva"
    - path: "composer.json"
      provides: "Autoload entry for helpers.php"
      contains: "helpers.php"
    - path: "resources/css/app.scss"
      provides: "Central SCSS entry importing all component styles"
      contains: "@use 'components/primitive/button'"
    - path: "resources/css/components/primitive/button.scss"
      provides: "Button SCSS in new location"
      contains: ".Button"
    - path: "tests/Feature/CvaHelperTest.php"
      provides: "Unit tests for cva() helper"
      contains: "cva("
    - path: "tests/Feature/ComponentSmokeTest.php"
      provides: "Smoke test rendering Blade components"
      contains: "assertSee"
  key_links:
    - from: "composer.json"
      to: "app/View/helpers.php"
      via: "autoload.files entry"
      pattern: "helpers\\.php"
    - from: "resources/css/app.scss"
      to: "resources/css/components/"
      via: "@use imports"
      pattern: "@use 'components/"
---

<objective>
Create the PHP cva() helper function, relocate all ~60 component SCSS files from resources/js/components/ to resources/css/components/, wire them into app.scss, and create the test scaffold for Blade component smoke testing.

Purpose: Establishes the foundation that all subsequent Blade components depend on — the variant-to-class mapping function and the SCSS pipeline. Without this, no Blade component can render with correct styles or variant classes.

Output: PHP cva() helper autoloaded via Composer, all SCSS files in resources/css/components/ imported by app.scss, PHPUnit tests for cva() and a basic component smoke test.
</objective>

<execution_context>
@/Users/lukekeith/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lukekeith/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-component-system/02-RESEARCH.md
@.planning/phases/02-component-system/02-CONTEXT.md

<interfaces>
<!-- Existing SCSS resolution config in vite.config.js -->
<!-- loadPaths already includes resources/css — @use 'styles/colors' resolves -->
<!-- Vue SCSS files import via `import './button.scss'` — this mechanism stays for the ~8 kept Vue components -->
<!-- Blade SCSS must flow through app.scss @use — no per-file import possible -->

From resources/css/app.scss (current):
```scss
@use 'compliance';
@use 'card-depth';
// No component SCSS imports yet
```

From composer.json autoload (current):
```json
"autoload": {
    "psr-4": {
        "App\\": "app/"
    }
}
// No "files" array yet — helpers.php needs to be added
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PHP cva() helper + Composer autoload + unit tests</name>
  <files>app/View/helpers.php, composer.json, tests/Feature/CvaHelperTest.php</files>
  <action>
1. Create `app/View/helpers.php` with the PHP `cva()` function exactly as specified in 02-RESEARCH.md Pattern 2:
   - Takes (string $base, array $config, array $selected) and returns space-separated BEM class string
   - Supports `variants` and `defaultVariants` keys in $config
   - Wraps in `if (!function_exists('cva'))` guard
   - No Composer package — pure PHP

2. Update `composer.json` to add `"files": ["app/View/helpers.php"]` inside the `"autoload"` block (alongside existing `"psr-4"`).

3. Run `composer dump-autoload` to regenerate the autoloader.

4. Create `tests/Feature/CvaHelperTest.php` with PHPUnit tests:
   - Test base class returned when no variants selected
   - Test single variant mapping (e.g., variant=Primary -> "Button Button--primary")
   - Test multiple variants combined (variant + size + mode)
   - Test defaultVariants used when no selection provided
   - Test unknown variant value returns base class only (no crash)
   - Test empty config returns base class only

5. Run `php artisan test --filter CvaHelperTest` to confirm all tests pass.
  </action>
  <verify>
    <automated>cd /Users/lukekeith/www/makeready/client && composer dump-autoload && php artisan test --filter CvaHelperTest</automated>
  </verify>
  <done>cva() function is globally available. All 6+ unit tests pass. composer.json has autoload.files entry.</done>
</task>

<task type="auto">
  <name>Task 2: Relocate all SCSS to resources/css/components/ and wire into app.scss</name>
  <files>resources/css/components/**/*.scss, resources/css/app.scss, tests/Feature/ComponentSmokeTest.php</files>
  <action>
1. Create directory structure under `resources/css/components/`:
   - `resources/css/components/primitive/`
   - `resources/css/components/domain/`
   - `resources/css/components/layout/`
   - `resources/css/components/panel/`

2. Copy (not move — Vue components still import their SCSS via `import './foo.scss'`) every SCSS file from `resources/js/components/` to the mirrored path under `resources/css/components/`. For example:
   - `resources/js/components/primitive/button/button.scss` -> `resources/css/components/primitive/button.scss`
   - `resources/js/components/domain/navigation/navigation.scss` -> `resources/css/components/domain/navigation.scss`
   - `resources/js/components/layout/auth/auth.scss` -> `resources/css/components/layout/auth.scss`
   - `resources/js/components/panel/page-title/page-title.scss` -> `resources/css/components/panel/page-title.scss`
   Note: destination files are FLAT (no subfolder per component) — Blade convention.

3. Check each copied SCSS file for `@use` paths that reference `styles/colors` or similar. These should already work because vite.config.js `loadPaths` includes `resources/css`. If any use relative paths like `../../../css/styles/colors`, change to `@use 'styles/colors'`.

4. Update `resources/css/app.scss` to import ALL component SCSS via `@use` directives. Group by category with comments. Order: existing @use rules first, then primitive, domain, layout, panel. Include ALL components (both Blade-bound and Vue-kept — SCSS for Vue components is duplicated intentionally so it flows through both Vite entry points).

   The full list of @use additions (after existing `@use 'compliance'` and `@use 'card-depth'`):
   ```scss
   // Primitive components
   @use 'components/primitive/avatar';
   @use 'components/primitive/badge';
   @use 'components/primitive/bullet-text-input';
   @use 'components/primitive/button';
   @use 'components/primitive/card';
   @use 'components/primitive/date-input';
   @use 'components/primitive/digit';
   @use 'components/primitive/empty-state';
   @use 'components/primitive/gender-select';
   @use 'components/primitive/icon';
   @use 'components/primitive/input';
   @use 'components/primitive/label';
   @use 'components/primitive/loading';
   @use 'components/primitive/mobile-date';
   @use 'components/primitive/mobile-input';
   @use 'components/primitive/mobile-select';
   @use 'components/primitive/modal';
   @use 'components/primitive/qr-code';
   @use 'components/primitive/social-button';
   @use 'components/primitive/step-indicator';
   @use 'components/primitive/toggle';
   @use 'components/primitive/verify-code';

   // Domain components
   @use 'components/domain/account-link';
   @use 'components/domain/account-modal-content';
   @use 'components/domain/edit-profile-modal-content';
   @use 'components/domain/event-card';
   @use 'components/domain/group-card';
   @use 'components/domain/group-home';
   @use 'components/domain/group-home-header';
   @use 'components/domain/group-leader-info';
   @use 'components/domain/group-leader-note';
   @use 'components/domain/group-list-card';
   @use 'components/domain/group-post-card';
   @use 'components/domain/invite-header';
   @use 'components/domain/invite-modal';
   @use 'components/domain/join-code-page';
   @use 'components/domain/lesson-page-header';
   @use 'components/domain/member-card';
   @use 'components/domain/navigation';
   @use 'components/domain/navigation-menu-content';
   @use 'components/domain/organization-card';
   @use 'components/domain/phone-entry';
   @use 'components/domain/profile-form';
   @use 'components/domain/question-button';
   @use 'components/domain/question-modal';
   @use 'components/domain/read-passage-button';
   @use 'components/domain/read-verse-modal';
   @use 'components/domain/scripture-display';
   @use 'components/domain/study-card';
   @use 'components/domain/study-launcher';
   @use 'components/domain/study-schedule-card';
   @use 'components/domain/video-player';

   // Layout components
   @use 'components/layout/auth';
   @use 'components/layout/home';
   @use 'components/layout/modal-provider';

   // Panel components
   @use 'components/panel/confirmation';
   @use 'components/panel/group-info-card';
   @use 'components/panel/keypad';
   @use 'components/panel/page-title';
   @use 'components/panel/study-info-card';
   ```

5. Run `npm run build` to confirm all SCSS compiles without errors.

6. Create `tests/Feature/ComponentSmokeTest.php` — a PHPUnit test that renders a minimal Blade component inline to verify the cva() helper works in Blade context:
   - Use `$this->blade()` to render `<x-primitive.button>Test</x-primitive.button>`
   - Assert the response contains "Button" class and "Test" text
   - This requires the button.blade.php from Plan 06 — so for NOW, create the test file with a simpler test: render a raw Blade view that calls `{{ cva('Button', ['variants' => ['variant' => ['Primary' => 'Button--primary']]], ['variant' => 'Primary']) }}` and assert it contains "Button Button--primary". Create a test view at `resources/views/test/cva-test.blade.php`.

7. Run `php artisan test --filter ComponentSmokeTest` to confirm it passes.
  </action>
  <verify>
    <automated>cd /Users/lukekeith/www/makeready/client && npm run build && php artisan test --filter ComponentSmokeTest</automated>
  </verify>
  <done>All ~60 SCSS files exist at resources/css/components/. app.scss imports them all. npm run build succeeds. Smoke test passes confirming cva() works in Blade context.</done>
</task>

</tasks>

<verification>
- `php artisan test` — all tests pass (CvaHelperTest + ComponentSmokeTest)
- `npm run build` — Vite compiles all SCSS without errors
- `ls resources/css/components/primitive/ | wc -l` — shows 22 SCSS files
- `ls resources/css/components/domain/ | wc -l` — shows 30 SCSS files
- `grep -c "@use 'components/" resources/css/app.scss` — shows ~60 @use lines
</verification>

<success_criteria>
PHP cva() helper globally available and tested. All component SCSS relocated and compiling through Vite. Test scaffold ready for Blade component verification in Plan 06.
</success_criteria>

<output>
After completion, create `.planning/phases/02-component-system/02-05-SUMMARY.md`
</output>
