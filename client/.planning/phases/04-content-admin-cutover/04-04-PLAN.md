---
phase: 04-content-admin-cutover
plan: "04"
type: execute
wave: 3
depends_on: ["04-01", "04-02", "04-03"]
files_modified: []
autonomous: false
requirements: [CONT-01, CONT-02, CONT-03, CONT-04, CONT-05, CONT-06, CONT-07, ADMN-01, INFR-03, INFR-04]

must_haves:
  truths:
    - "The production Railway deployment serves the Laravel app at the live domain"
    - "All pages return correct HTTP status codes on the live URL"
    - "The archive/react-spa branch exists as a rollback target"
    - "The full PHPUnit suite passes before cutover push"
  artifacts: []
  key_links:
    - from: "main branch"
      to: "Railway CI/CD"
      via: "git push triggers deploy"
      pattern: "railway"
---

<objective>
Execute production cutover: verify all Phase 4 work is complete, push to Railway, verify the live deployment, and confirm rollback capability.

Purpose: This is the final gate for the React-to-Laravel migration. Per user decision, the cutover replaces in-place on the same Railway service. The archive/react-spa branch is the rollback path.

Output: Laravel app live on production Railway URL with all pages serving correct responses.
</objective>

<execution_context>
@/Users/lukekeith/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lukekeith/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-content-admin-cutover/04-RESEARCH.md

Prior plan SUMMARYs needed:
- .planning/phases/04-content-admin-cutover/04-01-SUMMARY.md
- .planning/phases/04-content-admin-cutover/04-02-SUMMARY.md
- .planning/phases/04-content-admin-cutover/04-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-cutover verification -- full test suite + build + route audit</name>
  <!-- No files modified: this is a verification-only task that runs commands to confirm all prior plans completed correctly -->
  <files></files>
  <action>
Run the full verification checklist before pushing to production:

1. **Full test suite**: `php artisan test` -- ALL tests must pass (133+ existing + all Phase 4 tests). If any fail, stop and fix before proceeding.

2. **Vite build**: `npm run build` -- must succeed with LessonIsland in bundle, no errors.

3. **Route audit**: `php artisan route:list` -- verify all expected routes exist:
   - `/groups/{groupId}/study/{studyEnrollmentId}` (study home)
   - `/groups/{groupId}/lessons/{lessonScheduleId}/{step?}` (lesson)
   - `/groups/{groupId}/lessons/{lessonScheduleId}/activity/{activityId}/submit` (note save)
   - `/groups/{groupId}/lessons/{lessonScheduleId}/activity/{activityId}/video-progress` (video progress)
   - `/api/bible/{translation}/{book}/{chapter}` (scripture)
   - `/admin` (admin)
   - `/study` (study code)
   - `/public/preview/{token}` (study preview)
   - `/public/preview/{token}/lesson/{lessonId}/{step?}` (lesson preview)

4. **LessonIsland registration**: Grep `app.js` for `LessonIsland` to confirm it is in the component registry.

5. **Error pages exist**: Confirm `resources/views/errors/404.blade.php` and `resources/views/errors/500.blade.php` exist.

6. **Git status clean**: `git status` should show clean working tree on main branch. All Phase 4 work committed.

7. **Archive branch exists**: `git branch -a | grep archive/react-spa` -- confirm rollback branch exists.

If all checks pass, proceed to Task 2. If any fail, document the failure and fix it before continuing.
  </action>
  <verify>
    <automated>cd /Users/lukekeith/www/makeready/client && php artisan test 2>&1 | tail -5 && npm run build 2>&1 | tail -3 && php artisan route:list 2>&1 | grep -c "GET\|POST" && echo "Routes OK"</automated>
  </verify>
  <done>Full test suite green, Vite build succeeds, all routes registered, LessonIsland in app.js, error pages exist, git clean on main, archive branch exists.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Push to Railway and verify live deployment</name>
  <files></files>
  <action>
Human action required: push to Railway and verify the live deployment. Claude cannot push to production or verify live URLs.

Steps for the user:
1. Push main to the Railway remote: `git push` (Railway CI/CD will trigger automatically)
2. Monitor the Railway deployment in the dashboard until it completes
3. Verify the live URL with these checks:
   - `curl -s https://app.makeready.org/ | grep 'MakeReady'` -- should see server-rendered HTML (not blank div)
   - `curl -s -o /dev/null -w "%{http_code}" https://app.makeready.org/privacy` -- should return 200
   - `curl -s -o /dev/null -w "%{http_code}" https://app.makeready.org/this-does-not-exist` -- should return 404
   - Visit https://app.makeready.org/ in a browser -- login flow should work
   - Visit a lesson page as an authenticated member -- LessonIsland should mount and steps should work
4. If anything is broken, rollback by redeploying the archive/react-spa branch from the Railway dashboard

Resume signal: Type "deployed" if live verification passed, or describe issues encountered.
  </action>
  <verify>
    <automated>echo "Manual verification -- user confirms deployment status"</automated>
  </verify>
  <done>Laravel app is live on production Railway URL. All pages return correct HTTP status codes. Rollback path confirmed via archive/react-spa branch.</done>
</task>

</tasks>

<verification>
- Full test suite passes locally before push
- Railway deployment completes successfully
- Live URL returns server-rendered HTML on all pages
- 404 page renders on non-existent routes
- Rollback path confirmed (archive/react-spa branch exists)
</verification>

<success_criteria>
- Laravel app is live on production Railway URL
- All pages return correct HTTP status codes
- No SSR hydration errors in browser console
- Rollback to React SPA is possible via archive/react-spa branch
</success_criteria>

<output>
After completion, create `.planning/phases/04-content-admin-cutover/04-04-SUMMARY.md`
</output>
