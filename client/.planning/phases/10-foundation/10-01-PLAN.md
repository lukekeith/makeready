---
phase: 10-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - resources/js/islands/admin-island/router.ts
  - resources/js/islands/admin-island/sections/members-section.vue
  - resources/js/islands/admin-island/components/admin-sidebar.vue
autonomous: true
requirements: [MLIST-08]

must_haves:
  truths:
    - "Leader clicks Members in the admin sidebar and navigates to /admin/members without redirect"
    - "Navigating directly to /admin/members in the browser loads the page (not catch-all redirect)"
    - "@tanstack/vue-table and @tanstack/vue-virtual are present in package.json dependencies"
  artifacts:
    - path: "resources/js/islands/admin-island/sections/members-section.vue"
      provides: "Placeholder Members section component"
      contains: "Members"
    - path: "resources/js/islands/admin-island/router.ts"
      provides: "Members route registration"
      contains: "/admin/members"
    - path: "resources/js/islands/admin-island/components/admin-sidebar.vue"
      provides: "Members nav item in sidebar"
      contains: "ContactRound"
    - path: "package.json"
      provides: "TanStack dependencies"
      contains: "@tanstack/vue-table"
  key_links:
    - from: "admin-sidebar.vue"
      to: "/admin/members"
      via: "router-link in navItems array"
      pattern: "path.*admin/members"
    - from: "router.ts"
      to: "members-section.vue"
      via: "route component import"
      pattern: "import MembersSection"
---

<objective>
Register the /admin/members route, install TanStack packages, create the MembersSection placeholder, and add the Members nav item to the admin sidebar.

Purpose: Make the Members page reachable from the sidebar and browser URL before any table or data work begins in later phases.
Output: Working /admin/members route with sidebar navigation and TanStack packages ready for Phase 11+.
</objective>

<execution_context>
@/Users/lukekeith/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lukekeith/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-foundation/10-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From resources/js/islands/admin-island/router.ts:
```typescript
import { createRouter, createWebHistory } from 'vue-router'
// Routes array — add /admin/members BEFORE the catch-all at the bottom
// Catch-all is: { path: '/admin/:pathMatch(.*)*', redirect: '/admin' }
```

From resources/js/islands/admin-island/components/admin-sidebar.vue:
```typescript
// navItems array at line 24:
const navItems = [
  { label: 'Dashboard', icon: LayoutDashboard, path: '/admin', key: 'dashboard' },
  { label: 'Groups', icon: Users, path: '/admin/groups', key: 'groups' },
  // INSERT Members HERE — between Groups and Programs
  { label: 'Programs', icon: BookOpen, path: '/admin/programs', key: 'programs' },
  { label: 'Profile', icon: UserRound, path: '/admin/profile', key: 'profile' },
]

// isActive function at line 44 — add members case:
// if (key === 'members') return path.startsWith('/admin/members')

// lucide imports at line 4 — add ContactRound:
import { LayoutDashboard, Users, BookOpen, UserRound, PanelLeftOpen, PanelLeftClose } from 'lucide-vue-next'
```

From resources/js/islands/admin-island/sections/dashboard-section.vue (pattern to follow):
```vue
<template>
  <div class="AdminSection">
    <header class="AdminSection__header">
      <h1 class="AdminSection__title">Dashboard</h1>
    </header>
    <div class="AdminSection__body">
      <!-- content here -->
    </div>
  </div>
</template>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install TanStack packages and create MembersSection + route</name>
  <files>package.json, resources/js/islands/admin-island/sections/members-section.vue, resources/js/islands/admin-island/router.ts</files>
  <action>
1. Install TanStack packages (runtime dependencies):
   ```
   npm install @tanstack/vue-table @tanstack/vue-virtual --save
   ```

2. Create `resources/js/islands/admin-island/sections/members-section.vue` following the dashboard-section pattern:
   - `<script setup lang="ts">` with no imports needed yet
   - Template: `<div class="AdminSection">` wrapping `<header class="AdminSection__header"><h1 class="AdminSection__title">Members</h1></header>` and `<div class="AdminSection__body"></div>`
   - No loading skeleton, no empty state — just the section title per user decision

3. Update `resources/js/islands/admin-island/router.ts`:
   - Add import: `import MembersSection from './sections/members-section.vue'`
   - Add route `{ path: '/admin/members', component: MembersSection }` BEFORE the catch-all `/:pathMatch(.*)*` redirect (insert after the programs/:id route, before the catch-all)
  </action>
  <verify>
    <automated>cd /Users/lukekeith/www/makeready/client && grep -q "@tanstack/vue-table" package.json && grep -q "@tanstack/vue-virtual" package.json && grep -q "admin/members" resources/js/islands/admin-island/router.ts && test -f resources/js/islands/admin-island/sections/members-section.vue && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>TanStack packages in package.json dependencies, members-section.vue exists with AdminSection pattern, /admin/members route registered before catch-all in router.ts</done>
</task>

<task type="auto">
  <name>Task 2: Add Members nav item to admin sidebar</name>
  <files>resources/js/islands/admin-island/components/admin-sidebar.vue</files>
  <action>
Update `resources/js/islands/admin-island/components/admin-sidebar.vue`:

1. Add `ContactRound` to the lucide-vue-next import on line 4:
   ```typescript
   import { LayoutDashboard, Users, ContactRound, BookOpen, UserRound, PanelLeftOpen, PanelLeftClose } from 'lucide-vue-next'
   ```

2. Add Members nav item to the `navItems` array between Groups and Programs (after the Groups entry, before Programs):
   ```typescript
   { label: 'Members', icon: ContactRound, path: '/admin/members', key: 'members' },
   ```

3. Add members case to the `isActive` function (after the groups case):
   ```typescript
   if (key === 'members') return path.startsWith('/admin/members')
   ```
  </action>
  <verify>
    <automated>cd /Users/lukekeith/www/makeready/client && grep -q "ContactRound" resources/js/islands/admin-island/components/admin-sidebar.vue && grep -q "members" resources/js/islands/admin-island/components/admin-sidebar.vue && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Members nav item appears in sidebar between Groups and Programs using ContactRound icon, isActive highlights correctly for /admin/members paths, npm run build succeeds</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. `grep "@tanstack/vue-table" package.json` shows the dependency
3. `grep "@tanstack/vue-virtual" package.json` shows the dependency
4. `grep "admin/members" resources/js/islands/admin-island/router.ts` shows the route
5. `grep "ContactRound" resources/js/islands/admin-island/components/admin-sidebar.vue` shows the icon import
6. `grep "members" resources/js/islands/admin-island/components/admin-sidebar.vue` shows the nav item
</verification>

<success_criteria>
- /admin/members route exists in router.ts before the catch-all redirect
- MembersSection placeholder component renders "Members" title using AdminSection BEM pattern
- Members nav item in sidebar between Groups and Programs with ContactRound icon
- @tanstack/vue-table and @tanstack/vue-virtual in package.json dependencies
- npm run build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/10-foundation/10-01-SUMMARY.md`
</output>
