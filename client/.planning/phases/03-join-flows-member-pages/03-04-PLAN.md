---
phase: 03-join-flows-member-pages
plan: 04
type: execute
wave: 3
depends_on: ["03-01", "03-03"]
files_modified:
  - app/Http/Controllers/HomeController.php
  - app/Http/Controllers/GroupsController.php
  - app/Http/Controllers/GroupHomeController.php
  - app/Http/Controllers/ProfileController.php
  - app/Services/ApiService.php
  - resources/views/pages/home-authenticated.blade.php
  - resources/views/pages/groups.blade.php
  - resources/views/pages/group-home.blade.php
  - resources/views/pages/profile.blade.php
  - tests/Feature/MemberPagesTest.php
autonomous: true
requirements:
  - MEMB-03
  - MEMB-04
  - MEMB-05
  - MEMB-06
  - MEMB-07
must_haves:
  truths:
    - "GET /home renders groups list when authenticated (redirects to / when not)"
    - "GET /home redirects directly to /groups/{id} when member has exactly one group"
    - "GET /groups renders a list of member's groups with group names visible in HTML"
    - "GET /groups/{id} renders group home with group name, studies, events, and posts visible in HTML"
    - "GET /profile renders member profile data (name, phone) visible in HTML"
    - "POST /profile updates member data via API and redirects back to /profile"
    - "POST /profile/avatar uploads file to API via multipart forwarding"
    - "All authenticated pages include Navigation component with correct selected state"
  artifacts:
    - path: "app/Http/Controllers/HomeController.php"
      provides: "Authenticated home with single-group redirect logic"
      exports: ["index"]
    - path: "app/Http/Controllers/GroupsController.php"
      provides: "Groups list controller"
      exports: ["index"]
    - path: "app/Http/Controllers/GroupHomeController.php"
      provides: "Group home with posts, studies, events"
      exports: ["show"]
    - path: "app/Http/Controllers/ProfileController.php"
      provides: "Profile view + edit + avatar upload"
      exports: ["show", "update", "uploadAvatar"]
    - path: "resources/views/pages/home-authenticated.blade.php"
      provides: "Authenticated home page (groups list or redirect)"
      contains: "group-list-card"
    - path: "resources/views/pages/groups.blade.php"
      provides: "Groups list page"
      contains: "group-list-card"
    - path: "resources/views/pages/group-home.blade.php"
      provides: "Group home with posts, studies, events"
      contains: "group-card"
    - path: "resources/views/pages/profile.blade.php"
      provides: "Profile view/edit page"
      contains: "profile-form"
  key_links:
    - from: "app/Http/Controllers/GroupHomeController.php"
      to: "app/Services/ApiService.php"
      via: "multiple API calls (group, posts, enrollment)"
      pattern: "api->get.*groups"
    - from: "resources/views/pages/group-home.blade.php"
      to: "resources/views/components/domain/navigation.blade.php"
      via: "Blade component include"
      pattern: "x-domain.navigation"
    - from: "app/Http/Controllers/ProfileController.php"
      to: "app/Services/ApiService.php"
      via: "PATCH for update, multipart POST for avatar"
      pattern: "api->patch|api->upload"
---

<objective>
Implement all authenticated member pages: home (with single-group redirect), groups list, group home (with studies/events/posts), and profile (view + edit + avatar upload).

Purpose: These are the core authenticated experience pages. Every logged-in member interacts with these daily. All data is loaded server-side via ApiService for full SSR.
Output: 4 controllers (1 updated, 3 new), 4 Blade templates, ApiService upload method, passing tests.
</objective>

<execution_context>
@/Users/lukekeith/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lukekeith/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-join-flows-member-pages/03-CONTEXT.md
@.planning/phases/03-join-flows-member-pages/03-RESEARCH.md
@.planning/phases/03-join-flows-member-pages/03-01-SUMMARY.md
@.planning/phases/03-join-flows-member-pages/03-03-SUMMARY.md

@app/Services/ApiService.php
@app/Http/Controllers/HomeController.php
@app/Http/Middleware/CheckMemberSession.php
@resources/views/layouts/home.blade.php

<interfaces>
<!-- ApiService contract -->
```php
public function get(string $endpoint, Request $request): array  // ['status', 'body', 'setCookies']
public function post(string $endpoint, array $data, Request $request): array
// NEED TO ADD: patch() and upload() methods
```

<!-- CheckMemberSession provides member data -->
```php
$member = $request->attributes->get('member');
// $member contains: id, firstName, lastName, phone, avatarUrl, etc.
```

<!-- Member page API endpoints (from RESEARCH.md) -->
```
GET  /api/groups                                        -> member's groups list
GET  /api/groups/{id}/public                            -> group details
GET  /api/groups/{id}/posts?limit=N                     -> group posts feed
GET  /api/groups/{id}/study-enrollment?memberId={id}    -> current study progress
PATCH /api/members/{id}                                 -> update profile fields
POST  /api/members/{id}/avatar                          -> upload avatar (multipart)
```

<!-- Existing Blade components for member pages -->
```
<x-domain.navigation :selected="'home'" :initials="'JD'" :avatarUrl="null" :homeHref="..." :groupsHref="..." :profileHref="...">
<x-domain.group-list-card :group="$group">         — compact group card for lists
<x-domain.group-card mode="Header" :group="...">   — full group hero card
<x-domain.group-post-card :post="$post">            — feed post card
<x-domain.study-card mode="Progress" :study="...">  — study enrollment card
<x-domain.event-card :event="$event">               — event card
<x-domain.profile-form>                              — profile edit form fields
<x-panel.page-title>                                 — page header with optional back button
<x-primitive.avatar>                                 — avatar image
<x-primitive.empty-state>                            — empty state display
```

<!-- Existing HomeController (needs updating) -->
From app/Http/Controllers/HomeController.php:
```php
// Currently minimal — needs groups list + single-group redirect logic
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ApiService.patch() + ApiService.upload() + HomeController + GroupsController + GroupHomeController</name>
  <files>
    app/Services/ApiService.php
    app/Http/Controllers/HomeController.php
    app/Http/Controllers/GroupsController.php
    app/Http/Controllers/GroupHomeController.php
    resources/views/pages/home-authenticated.blade.php
    resources/views/pages/groups.blade.php
    resources/views/pages/group-home.blade.php
  </files>
  <action>
    **1. Add `patch()` and `upload()` methods to `app/Services/ApiService.php`:**

    **patch():** Same pattern as post() but uses `Http::patch()`. Needed for profile updates.
    ```php
    public function patch(string $endpoint, array $data, Request $request): array
    ```

    **upload():** For multipart file forwarding. Uses `Http::attach()` for file upload:
    ```php
    public function upload(string $endpoint, string $fileKey, \Illuminate\Http\UploadedFile $file, Request $request): array
    {
        $response = Http::withHeaders([
            'Cookie' => $request->header('Cookie', ''),
            'Accept' => 'application/json',
        ])->attach($fileKey, $file->getContent(), $file->getClientOriginalName())
          ->post("{$this->baseUrl}{$endpoint}");

        return [
            'status' => $response->status(),
            'body' => $response->json(),
            'setCookies' => $this->extractSetCookies($response),
        ];
    }
    ```

    **2. Update `app/Http/Controllers/HomeController.php`:**

    Constructor: inject `ApiService $api`.

    **index(Request $request):**
    - `$member = $request->attributes->get('member')`
    - Call `$this->api->get('/api/groups', $request)`
    - If 200: extract groups array (check both `$body['data']` and `$body['groups']` — React source shows inconsistency)
    - **Single-group redirect:** If exactly 1 group, redirect to `route('group.home', ['groupId' => $groups[0]['id']])`
    - Otherwise: render `pages.home-authenticated` with `$member` and `$groupList`
    - Forward Set-Cookie headers
    - If API error: render page with empty `$groupList = []`

    **3. Create `app/Http/Controllers/GroupsController.php`:**

    Constructor: inject `ApiService $api`.

    **index(Request $request):**
    - `$member = $request->attributes->get('member')`
    - Call `$this->api->get('/api/groups', $request)`
    - Extract groups (same dual-key check as HomeController)
    - Render `pages.groups` with `$member`, `$groupList`
    - Forward Set-Cookie headers

    **4. Create `app/Http/Controllers/GroupHomeController.php`:**

    Constructor: inject `ApiService $api`.

    **show(Request $request, string $groupId):**
    - `$member = $request->attributes->get('member')`
    - Make 3 parallel-style API calls (sequential in PHP, but all upfront before rendering):
      - `$group = $this->api->get("/api/groups/{$groupId}/public", $request)`
      - `$posts = $this->api->get("/api/groups/{$groupId}/posts?limit=10", $request)`
      - `$enrollment = $this->api->get("/api/groups/{$groupId}/study-enrollment?memberId={$member['id']}", $request)`
    - Extract data with null-safe defaults (non-200 = null/empty array)
    - Render `pages.group-home` with `$member`, `$groupId`, `$groupData`, `$postsData`, `$enrollmentData`
    - Forward Set-Cookie headers from all 3 responses
    - Pattern from RESEARCH.md example controller code

    **5. Create `resources/views/pages/home-authenticated.blade.php`:**
    - `@extends('layouts.home')`
    - Include `<x-domain.navigation selected="home" :initials="..." :avatarUrl="..." ...>` with member data and route hrefs
    - `<x-panel.page-title>` with "Home" or "My Groups" title
    - Loop `$groupList`: render `<x-domain.group-list-card>` for each group, linking to `route('group.home', ['groupId' => $group['id']])`
    - Empty state: `<x-primitive.empty-state>` when no groups
    - BEM classes: `.HomeAuthenticated`, `.HomeAuthenticated__groups`, etc.

    **6. Create `resources/views/pages/groups.blade.php`:**
    - `@extends('layouts.home')`
    - Navigation with `selected="groups"`
    - `<x-panel.page-title>` with "Groups"
    - Same group card loop as home page but always shows full list (no single-group redirect here)

    **7. Create `resources/views/pages/group-home.blade.php`:**
    - `@extends('layouts.home')`
    - Navigation with `selected="home"`
    - `<x-domain.group-card mode="Header">` with group hero image and back button
    - If `$enrollmentData`: `<x-domain.study-card mode="Progress">` showing current study
    - Posts section: loop `$postsData`, render `<x-domain.group-post-card>` for each
    - Events section if applicable
    - Empty states for each section
    - BEM classes: `.GroupHome`, `.GroupHome__posts`, `.GroupHome__studies`, etc.

    Reference the React archive (`archive/react-spa` branch) group-home page for exact layout, section ordering, and content structure.
  </action>
  <verify>
    <automated>cd /Users/lukekeith/www/makeready/client && php artisan test --filter "MemberPagesTest::test_authenticated_home|MemberPagesTest::test_groups_list|MemberPagesTest::test_group_home" 2>&1 | tail -15</automated>
  </verify>
  <done>
    - ApiService has patch() and upload() methods
    - HomeController redirects to single group or renders groups list
    - GroupsController renders groups list
    - GroupHomeController loads group + posts + enrollment and renders
    - All 3 Blade templates use existing components, include Navigation with correct selected state
    - Related MemberPagesTest tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: ProfileController + profile.blade.php + all remaining tests</name>
  <files>
    app/Http/Controllers/ProfileController.php
    resources/views/pages/profile.blade.php
    tests/Feature/MemberPagesTest.php
  </files>
  <action>
    **1. Create `app/Http/Controllers/ProfileController.php`:**

    Constructor: inject `ApiService $api`.

    **show(Request $request):**
    - `$member = $request->attributes->get('member')` (already set by middleware)
    - Render `pages.profile` with `$member`
    - No additional API call needed — member data comes from CheckMemberSession middleware

    **update(Request $request): RedirectResponse**
    - `$member = $request->attributes->get('member')`
    - Validate request: first_name (required, string, max 50), last_name (required, string, max 50), gender (optional), birthday (optional, date format)
    - Call `$this->api->patch("/api/members/{$member['id']}", $request->only(['first_name', 'last_name', 'gender', 'birthday']), $request)`
    - If success: redirect back to profile with flash success message
    - If API error: redirect back with flash error + old input
    - Forward Set-Cookie headers

    **uploadAvatar(Request $request): JsonResponse**
    - `$member = $request->attributes->get('member')`
    - Validate: 'avatar' required, image, max 5MB
    - Call `$this->api->upload("/api/members/{$member['id']}/avatar", 'avatar', $request->file('avatar'), $request)`
    - Return JSON response (this will be called via AJAX from a small JS handler or form submit)
    - Forward Set-Cookie headers

    **2. Create `resources/views/pages/profile.blade.php`:**
    - `@extends('layouts.home')`
    - Navigation with `selected="profile"`
    - `<x-panel.page-title>` with "Profile" title
    - Avatar display: `<x-primitive.avatar>` with member's avatarUrl and initials
    - Avatar upload: A file input (can be wrapped in a small form with `enctype="multipart/form-data"` posting to `route('profile.avatar')`, or use a tiny inline JS fetch with FormData for AJAX upload)
    - Profile form: `<form method="POST" action="{{ route('profile.update') }}">` with `@csrf`
    - Use `<x-domain.profile-form>` component inside the form, pre-populated with `$member` data
    - Submit button: `<x-primitive.button variant="Primary" mode="Block">Save</x-primitive.button>`
    - Flash message display for success/error (use `@if(session('success'))` / `@if(session('error'))`)
    - BEM classes: `.ProfilePage`, `.ProfilePage__avatar`, `.ProfilePage__form`, etc.

    Reference React archive's profile page for exact layout.

    **3. Complete all remaining tests in `tests/Feature/MemberPagesTest.php`:**
    Replace all remaining `markTestIncomplete()` with real assertions. For authenticated tests, use this pattern to mock the auth middleware:
    ```php
    // Mock the session check that CheckMemberSession makes
    Http::fake([
        '*/api/members/session' => Http::response([
            'authenticated' => true,
            'member' => ['id' => 'member-1', 'firstName' => 'Test', 'lastName' => 'User', 'phone' => '+1234567890']
        ]),
        '*/api/groups' => Http::response(['success' => true, 'data' => [
            ['id' => 'group-1', 'name' => 'Test Group', 'memberCount' => 5],
            ['id' => 'group-2', 'name' => 'Other Group', 'memberCount' => 3],
        ]]),
        // ... other mocks
    ]);
    ```

    Tests to complete:
    - `test_public_home_renders` (MEMB-01): Already done in Plan 03
    - `test_authenticated_home_renders` (MEMB-03): With auth + 2+ groups mock, GET /home -> 200, see group names
    - `test_authenticated_home_single_group_redirect` (MEMB-03): With auth + 1 group, GET /home -> 302 to /groups/{id}
    - `test_groups_list` (MEMB-07): With auth, GET /groups -> 200, see group names
    - `test_group_home_renders` (MEMB-04): With auth + group/posts mocks, GET /groups/group-1 -> 200, see group name
    - `test_profile_view` (MEMB-05): With auth, GET /profile -> 200, see member name
    - `test_profile_update` (MEMB-06): With auth + PATCH mock, POST /profile -> redirect to /profile
    - `test_profile_update_validates` (MEMB-06): POST /profile with missing required fields -> validation error
  </action>
  <verify>
    <automated>cd /Users/lukekeith/www/makeready/client && php artisan test --filter MemberPagesTest 2>&1 | tail -15</automated>
  </verify>
  <done>
    - ProfileController handles view, update, and avatar upload
    - Profile page renders member data from middleware
    - Profile update sends PATCH via ApiService and redirects with flash
    - Avatar upload forwards multipart file to API
    - All MemberPagesTest tests pass
    - All authenticated pages include Navigation with correct selected state
  </done>
</task>

</tasks>

<verification>
- `php artisan test --filter MemberPagesTest` all pass
- `php artisan test` (full suite) passes
- All authenticated pages render with Navigation component
- Profile edit saves and redirects with flash message
- HomeController single-group redirect works
</verification>

<success_criteria>
- All 4 authenticated pages work: home, groups, group home, profile
- HomeController implements single-group redirect logic
- GroupHomeController loads 3 API endpoints upfront
- ProfileController handles view + edit + avatar upload
- ApiService has patch() and upload() methods
- All MemberPagesTest tests pass
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/03-join-flows-member-pages/03-04-SUMMARY.md`
</output>
