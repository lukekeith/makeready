---
phase: 03.1-visual-fidelity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - resources/css/pages/public-home.scss
  - resources/css/pages/home.scss
  - resources/css/pages/group-home.scss
  - resources/css/pages/study-home.scss
  - resources/css/pages/study-join.scss
  - resources/css/pages/study-preview.scss
  - resources/css/pages/member-login.scss
  - resources/css/pages/profile.scss
  - resources/css/pages/public-join.scss
  - resources/css/pages/public-event-join.scss
  - resources/css/pages/landing.scss
  - resources/css/pages/not-found.scss
  - resources/css/pages/privacy.scss
  - resources/css/pages/terms.scss
  - resources/css/app.scss
  - resources/views/components/primitive/button.blade.php
  - tests/Feature/VisualFidelityTest.php
autonomous: true
requirements: [VF-SCSS, VF-COMP]

must_haves:
  truths:
    - "All 14 page SCSS files from the React archive exist in resources/css/pages/"
    - "app.scss imports all 14 page SCSS files via @use rules"
    - "npm run build completes without Sass compilation errors"
    - "Button component always wraps content in Button__content span"
    - "Button Jump variant renders Button__details with Button__label and Button__description"
    - "VisualFidelityTest exists and asserts correct root BEM classes on page responses"
  artifacts:
    - path: "resources/css/pages/"
      provides: "Page-level SCSS directory with 14 files"
      min_lines: 14
    - path: "resources/css/app.scss"
      provides: "Updated with @use 'pages/...' imports"
      contains: "@use 'pages/public-home'"
    - path: "resources/views/components/primitive/button.blade.php"
      provides: "Button with correct inner HTML structure"
      contains: "Button__content"
    - path: "tests/Feature/VisualFidelityTest.php"
      provides: "Root BEM class assertions for all pages"
      contains: "VisualFidelityTest"
  key_links:
    - from: "resources/css/app.scss"
      to: "resources/css/pages/*.scss"
      via: "@use rules"
      pattern: "@use 'pages/"
    - from: "resources/views/components/primitive/button.blade.php"
      to: "resources/css/components/primitive/button.scss"
      via: "BEM class names Button__content, Button__details"
      pattern: "Button__content"
---

<objective>
Lay the SCSS foundation, fix the Button component's inner HTML structure, and create the VisualFidelityTest scaffold.

Purpose: Without page SCSS, no page can be styled. Without the Button fix, all buttons render incorrectly. Without the test scaffold, subsequent plans have no automated verification.

Output: 14 page SCSS files in resources/css/pages/, updated app.scss, corrected Button Blade component, VisualFidelityTest.php
</objective>

<execution_context>
@/Users/lukekeith/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lukekeith/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-visual-fidelity-rewrite-all-blade-components-and-page-templates-to-produce-html-css-identical-to-the-original-react-app/03.1-RESEARCH.md

Key reference commands:
- `git show origin/archive/react-spa:src/pages/<name>/<name>.scss` to read each page SCSS
- `git show origin/archive/react-spa:ui/components/primitive/button/button.tsx` to read React Button TSX
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate all 14 page SCSS files and wire app.scss</name>
  <files>
    resources/css/pages/public-home.scss
    resources/css/pages/home.scss
    resources/css/pages/group-home.scss
    resources/css/pages/study-home.scss
    resources/css/pages/study-join.scss
    resources/css/pages/study-preview.scss
    resources/css/pages/member-login.scss
    resources/css/pages/profile.scss
    resources/css/pages/public-join.scss
    resources/css/pages/public-event-join.scss
    resources/css/pages/landing.scss
    resources/css/pages/not-found.scss
    resources/css/pages/privacy.scss
    resources/css/pages/terms.scss
    resources/css/app.scss
  </files>
  <action>
    1. Create the `resources/css/pages/` directory.

    2. For each of the 14 page SCSS files, read the archive version and write it verbatim to the target path:
       - `git show origin/archive/react-spa:src/pages/public-home/public-home.scss` -> `resources/css/pages/public-home.scss`
       - `git show origin/archive/react-spa:src/pages/home/home.scss` -> `resources/css/pages/home.scss`
       - `git show origin/archive/react-spa:src/pages/group-home/group-home.scss` -> `resources/css/pages/group-home.scss`
       - `git show origin/archive/react-spa:src/pages/study-home/study-home.scss` -> `resources/css/pages/study-home.scss`
       - `git show origin/archive/react-spa:src/pages/study-join/study-join.scss` -> `resources/css/pages/study-join.scss`
       - `git show origin/archive/react-spa:src/pages/study-preview/study-preview.scss` -> `resources/css/pages/study-preview.scss`
       - `git show origin/archive/react-spa:src/pages/member-login/member-login.scss` -> `resources/css/pages/member-login.scss`
       - `git show origin/archive/react-spa:src/pages/profile/profile.scss` -> `resources/css/pages/profile.scss`
       - `git show origin/archive/react-spa:src/pages/public-join/public-join.scss` -> `resources/css/pages/public-join.scss`
       - `git show origin/archive/react-spa:src/pages/public-event-join/public-event-join.scss` -> `resources/css/pages/public-event-join.scss`
       - `git show origin/archive/react-spa:src/pages/landing/landing.scss` -> `resources/css/pages/landing.scss`
       - `git show origin/archive/react-spa:src/pages/not-found/not-found.scss` -> `resources/css/pages/not-found.scss`
       - `git show origin/archive/react-spa:src/pages/privacy/privacy.scss` -> `resources/css/pages/privacy.scss`
       - `git show origin/archive/react-spa:src/pages/terms/terms.scss` -> `resources/css/pages/terms.scss`

       IMPORTANT: Copy each file VERBATIM. Do not edit, rename selectors, or remove any rules. The archive SCSS is the source of truth.

       NOTE: Some SCSS files may contain `@import` or `@use` references to color variables (e.g., `@use '../styles/colors'`). These paths need adjusting for the Laravel directory structure. The existing component SCSS files use `@use 'styles/colors'` (resolved via Vite loadPaths config). Match that same pattern — change any `@use '../styles/colors'` to `@use 'styles/colors'`.

    3. Add page SCSS `@use` entries to `resources/css/app.scss`. Insert them AFTER the panel component @use block (after `@use 'components/panel/study-info-card'`) and BEFORE the `*` reset rule. Format:

       ```scss
       // Page styles
       @use 'pages/public-home';
       @use 'pages/home';
       @use 'pages/group-home';
       @use 'pages/study-home';
       @use 'pages/study-join';
       @use 'pages/study-preview';
       @use 'pages/member-login';
       @use 'pages/profile';
       @use 'pages/public-join';
       @use 'pages/public-event-join';
       @use 'pages/landing';
       @use 'pages/not-found';
       @use 'pages/privacy';
       @use 'pages/terms';
       ```

    4. Run `npm run build` to confirm all SCSS compiles without errors. Fix any import path issues (the most likely issue is Sass @use paths to shared variables — match the pattern used by existing component SCSS files).
  </action>
  <verify>
    <automated>cd /Users/lukekeith/www/makeready/client && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>All 14 page SCSS files exist in resources/css/pages/, app.scss imports them all, and `npm run build` passes without Sass errors</done>
</task>

<task type="auto">
  <name>Task 2: Fix Button component inner HTML structure</name>
  <files>resources/views/components/primitive/button.blade.php</files>
  <action>
    Read the React Button TSX from archive:
    `git show origin/archive/react-spa:ui/components/primitive/button/button.tsx`

    Rewrite `resources/views/components/primitive/button.blade.php` to match the React Button's rendered HTML structure exactly:

    1. ALL content must be wrapped in `<span class="Button__content">` — not just the loading state.

    2. For Jump variants (`Jump`, `JumpPrimary`):
       - Wrap label and description in `<span class="Button__details">`
       - Render `<span class="Button__label">{{ $label }}</span>` inside details
       - Render `<span class="Button__description">{{ $description }}</span>` inside details (if $description provided)
       - Add `description` prop to @props array

    3. For standard (non-Jump) variants:
       - If `$label` is provided: `<span class="Button__label">{{ $label }}</span>` inside Button__content
       - If no label: `{{ $slot }}` inside Button__content

    4. Add icon support with `$leftIcon` and `$rightIcon` slots:
       - `<span class="Button__icon Button__icon--left">{{ $leftIcon }}</span>` before label/details (if provided)
       - `<span class="Button__icon Button__icon--right">{{ $rightIcon }}</span>` after label/details (if provided)

    5. Loading spinner goes OUTSIDE Button__content (after it), matching the React pattern:
       ```
       <span class="Button__content">...label...</span>
       @if($loading)
         <span class="Button__spinner">
           <svg ...spinner SVG...></svg>
         </span>
       @endif
       ```

    6. Keep existing CVA/class computation logic unchanged — only the inner HTML structure changes.

    Reference the corrected template from the RESEARCH.md code example. Cross-check against the React TSX to ensure every inner span matches.
  </action>
  <verify>
    <automated>cd /Users/lukekeith/www/makeready/client && php artisan test --filter ComponentSmokeTest 2>&1 | tail -5</automated>
  </verify>
  <done>Button Blade always wraps content in Button__content, Jump variants render Button__details with label+description, icon slots supported, loading spinner outside content span</done>
</task>

<task type="auto">
  <name>Task 3: Create VisualFidelityTest scaffold</name>
  <files>tests/Feature/VisualFidelityTest.php</files>
  <action>
    Create `tests/Feature/VisualFidelityTest.php` that asserts the correct root BEM class appears in the rendered HTML for each page.

    Follow the existing test patterns in `tests/Feature/SsrHtmlTest.php` and `tests/Feature/MemberPagesTest.php` for how to:
    - Make authenticated requests (use Http::fake() to stub the session/member API)
    - Assert response contains specific HTML class strings

    Test cases to include (use `$response->assertSee('class="ClassName"', false)` or assertSee with the root class name):

    Public pages (no auth needed):
    - GET /privacy -> assertSee 'PrivacyPage' or whatever the React root class is (verify from archive)
    - GET /terms -> assertSee correct root class
    - GET / (public home) -> assertSee 'PublicHomePage'

    Authenticated pages (need Http::fake for session):
    - GET /home -> assertSee 'MemberHomePage'
    - GET /groups -> assertSee correct root class (verify from archive — React may use inline styles)
    - GET /profile -> assertSee 'ProfilePage'

    Pages needing route parameters (stub with Http::fake):
    - GET /group/{code} -> assertSee 'GroupHome' (root class)
    - GET /study/{code} -> assertSee 'StudyHome' (root class)

    NOTE: Read the React page TSX for each page to confirm the exact root class name before writing the assertion. Use `git show origin/archive/react-spa:src/pages/<name>/<name>.page.tsx | head -30` to find className on the root element.

    NOTE: For pages that need group/study data in the API response, stub the Http::fake responses with realistic shapes matching the existing test patterns in MemberPagesTest.php and ContentPagesTest.php.

    Mark tests that cannot be automated (e.g., visual CSS rendering) with comments explaining why.
  </action>
  <verify>
    <automated>cd /Users/lukekeith/www/makeready/client && php artisan test --filter VisualFidelityTest 2>&1 | tail -10</automated>
  </verify>
  <done>VisualFidelityTest.php exists with assertions for root BEM class on at least 6 pages (public home, home, group home, study home, profile, privacy), all tests pass</done>
</task>

</tasks>

<verification>
1. `npm run build` passes (SCSS compilation clean)
2. `php artisan test --filter VisualFidelityTest` passes (root classes correct)
3. `php artisan test` full suite passes (no regressions)
4. `ls resources/css/pages/ | wc -l` returns 14
5. `grep -c "@use 'pages/" resources/css/app.scss` returns 14
</verification>

<success_criteria>
- 14 page SCSS files exist in resources/css/pages/ copied verbatim from archive
- app.scss has 14 @use entries for page styles
- npm run build passes without Sass errors
- Button component always wraps content in Button__content span
- Jump variant Button renders Button__details with label/description
- VisualFidelityTest exists and passes with root BEM class assertions
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-visual-fidelity-rewrite-all-blade-components-and-page-templates-to-produce-html-css-identical-to-the-original-react-app/03.1-01-SUMMARY.md`
</output>
