---
phase: 03.1-visual-fidelity
plan: 02
type: execute
wave: 2
depends_on: [03.1-01]
files_modified:
  - resources/views/pages/public-home.blade.php
  - resources/views/pages/home-authenticated.blade.php
  - resources/views/pages/group-home.blade.php
  - resources/views/pages/study-home.blade.php
  - resources/views/pages/login.blade.php
  - resources/views/pages/profile.blade.php
autonomous: true
requirements: [VF-HTML, VF-PAGE]

must_haves:
  truths:
    - "public-home.blade.php renders with root class PublicHomePage and inner structure matching React TSX"
    - "home-authenticated.blade.php renders with root class MemberHomePage (not HomeAuthenticated)"
    - "group-home.blade.php inner structure matches React TSX (no extra h2 headings)"
    - "study-home.blade.php renders with root class StudyHome (not StudyHomePage) and viewport/scroll-container nesting"
    - "login.blade.php renders with root class MemberLoginPage and correct component composition"
    - "profile.blade.php renders with root class ProfilePage and correct component composition"
  artifacts:
    - path: "resources/views/pages/public-home.blade.php"
      provides: "Public home page matching React PublicHomePage"
      contains: "PublicHomePage"
    - path: "resources/views/pages/home-authenticated.blade.php"
      provides: "Member home page matching React MemberHomePage"
      contains: "MemberHomePage"
    - path: "resources/views/pages/group-home.blade.php"
      provides: "Group home matching React GroupHome structure"
      contains: "GroupHome"
    - path: "resources/views/pages/study-home.blade.php"
      provides: "Study home matching React StudyHome structure"
      contains: "StudyHome"
    - path: "resources/views/pages/login.blade.php"
      provides: "Login page matching React MemberLoginPage"
      contains: "MemberLoginPage"
    - path: "resources/views/pages/profile.blade.php"
      provides: "Profile page matching React ProfilePage"
      contains: "ProfilePage"
  key_links:
    - from: "resources/views/pages/home-authenticated.blade.php"
      to: "resources/css/pages/home.scss"
      via: "BEM class MemberHomePage"
      pattern: "MemberHomePage"
    - from: "resources/views/pages/study-home.blade.php"
      to: "resources/css/pages/study-home.scss"
      via: "BEM class StudyHome"
      pattern: "StudyHome__viewport"
---

<objective>
Rewrite the 6 highest-priority page templates to match their React equivalents exactly. These are the pages with confirmed structural mismatches (wrong root classes, extra wrapper elements, wrong nesting) identified during research.

Purpose: These 6 pages are the most-used member pages. Fixing them restores visual fidelity for the core user experience.

Output: 6 rewritten Blade page templates with HTML structure matching React TSX
</objective>

<execution_context>
@/Users/lukekeith/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lukekeith/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-visual-fidelity-rewrite-all-blade-components-and-page-templates-to-produce-html-css-identical-to-the-original-react-app/03.1-RESEARCH.md

Key reference commands:
- `git show origin/archive/react-spa:src/pages/<name>/<name>.page.tsx` to read each React page TSX
- `git show origin/archive/react-spa:src/pages/<name>/<name>.scss` to cross-check class names

Known mismatches (from RESEARCH.md):
- home-authenticated: root class HomeAuthenticated -> must be MemberHomePage, all children HomeAuthenticated__* -> MemberHomePage__*
- study-home: root class StudyHomePage -> must be StudyHome, needs viewport/scroll-container nesting, remove <x-panel.page-title> that React doesn't render, remove <h2>Lessons</h2>
- group-home: root class matches but inner structure has extra <h2>Current Study</h2> and <h2>Feed</h2> headings that React does not render
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite public-home, home-authenticated, and group-home pages</name>
  <files>
    resources/views/pages/public-home.blade.php
    resources/views/pages/home-authenticated.blade.php
    resources/views/pages/group-home.blade.php
  </files>
  <action>
    For each of these 3 pages, follow this exact process:

    1. Read the React TSX: `git show origin/archive/react-spa:src/pages/<name>/<name>.page.tsx`
    2. Read the current Blade template
    3. Rewrite the Blade to match the React HTML structure exactly

    **public-home.blade.php:**
    - Read `git show origin/archive/react-spa:src/pages/public-home/public-home.page.tsx`
    - Ensure root div has class `PublicHomePage` (verify current — may already match)
    - Ensure all inner BEM children match React exactly (PublicHomePage__header, PublicHomePage__content, etc.)
    - Use the same Blade components that map to React components (x-primitive.button for Button, etc.)
    - Do NOT add elements React does not render (no extra section tags, no extra headings)

    **home-authenticated.blade.php:**
    - Read `git show origin/archive/react-spa:src/pages/home/home.page.tsx`
    - CRITICAL: Change root class from `HomeAuthenticated` to `MemberHomePage`
    - Change ALL child BEM classes from `HomeAuthenticated__*` to `MemberHomePage__*`
    - Match the inner structure: React uses `MemberHomePage__content > MemberHomePage__groups-list` (not `HomeAuthenticated__content > HomeAuthenticated__groups`)
    - Preserve the @foreach loop for group cards but use correct BEM wrapper classes

    **group-home.blade.php:**
    - Read `git show origin/archive/react-spa:src/pages/group-home/group-home.page.tsx`
    - Root class `GroupHome` already matches — verify and keep
    - REMOVE any `<h2>Current Study</h2>` and `<h2>Feed</h2>` heading elements that the React page does not render
    - Match the exact nesting structure from React TSX (GroupHome__viewport, GroupHome__scroll-container, etc.)
    - Ensure component composition matches: if React renders `<GroupCard>` in a specific container div, Blade must use `<x-domain.group-card>` in the same container

    For all 3 pages: keep existing Blade directives (@if, @foreach, @auth) and PHP variables — only change the HTML structure and class names to match React.
  </action>
  <verify>
    <automated>cd /Users/lukekeith/www/makeready/client && php artisan test --filter VisualFidelityTest 2>&1 | tail -10</automated>
  </verify>
  <done>public-home has PublicHomePage root class with correct inner structure, home-authenticated has MemberHomePage root class with MemberHomePage__* children, group-home has no extra h2 headings and matches React nesting</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite study-home, login, and profile pages</name>
  <files>
    resources/views/pages/study-home.blade.php
    resources/views/pages/login.blade.php
    resources/views/pages/profile.blade.php
  </files>
  <action>
    For each of these 3 pages, follow this exact process:

    1. Read the React TSX: `git show origin/archive/react-spa:src/pages/<name>/<name>.page.tsx`
    2. Read the current Blade template
    3. Rewrite the Blade to match the React HTML structure exactly

    **study-home.blade.php:**
    - Read `git show origin/archive/react-spa:src/pages/study-home/study-home.page.tsx`
    - CRITICAL: Change root class from `StudyHomePage` to `StudyHome`
    - Change ALL child BEM classes from `StudyHomePage__*` to `StudyHome__*`
    - Implement the React nesting structure:
      ```
      .StudyHome
        .StudyHome__viewport
          .StudyHome__scroll-container
            .StudyHome__header
              <x-domain.study-card mode="Header">
              <x-domain.study-schedule-card>
              .StudyHome__lessons
                @foreach: <x-domain.study-card mode="LessonList">
            .StudyHome__bottom-spacer
          .StudyHome__navigation
            <x-domain.navigation>
      ```
    - REMOVE any `<x-panel.page-title>` that React does not render
    - REMOVE any `<h2>Lessons</h2>` heading that React does not render
    - REMOVE any `<section>` wrappers that React renders as plain `<div>`

    **login.blade.php:**
    - Read `git show origin/archive/react-spa:src/pages/member-login/member-login.page.tsx`
    - Ensure root class is `MemberLoginPage` (verify current)
    - Match all inner BEM classes (MemberLoginPage__header, MemberLoginPage__content, etc.)
    - React login page composes PhoneEntry component and Button — ensure Blade uses corresponding components
    - Preserve any Vue island references for interactive phone entry

    **profile.blade.php:**
    - Read `git show origin/archive/react-spa:src/pages/profile/profile.page.tsx`
    - Ensure root class is `ProfilePage` (likely already correct)
    - Match the inner structure: React has ProfilePage__viewport, ProfilePage__scroll-container, etc.
    - Ensure Avatar, Input, Button components are composed the same way
    - Remove any extra headings or section wrappers not in React

    For all 3 pages: keep existing Blade directives and PHP variables — only change the HTML structure and class names to match React.
  </action>
  <verify>
    <automated>cd /Users/lukekeith/www/makeready/client && php artisan test --filter VisualFidelityTest 2>&1 | tail -10</automated>
  </verify>
  <done>study-home has StudyHome root class with viewport/scroll-container nesting and no extra headings, login has MemberLoginPage root class with correct composition, profile has ProfilePage root class with correct inner structure</done>
</task>

</tasks>

<verification>
1. `php artisan test --filter VisualFidelityTest` passes — all root BEM classes correct
2. `php artisan test` full suite passes — no regressions
3. `npm run build` passes — SCSS still compiles (class names in HTML now match SCSS selectors)
4. grep confirms: no `HomeAuthenticated` in home-authenticated.blade.php, no `StudyHomePage` in study-home.blade.php
</verification>

<success_criteria>
- home-authenticated.blade.php uses MemberHomePage root class and MemberHomePage__* children
- study-home.blade.php uses StudyHome root class with viewport > scroll-container nesting
- group-home.blade.php has no extra h2 headings, matches React nesting exactly
- public-home, login, and profile pages have correct root classes and inner structure
- All VisualFidelityTest assertions pass
- Full test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-visual-fidelity-rewrite-all-blade-components-and-page-templates-to-produce-html-css-identical-to-the-original-react-app/03.1-02-SUMMARY.md`
</output>
