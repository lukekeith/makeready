---
phase: 03.1-visual-fidelity
plan: 03
type: execute
wave: 2
depends_on: [03.1-01]
files_modified:
  - resources/views/pages/join-group.blade.php
  - resources/views/pages/join-study.blade.php
  - resources/views/pages/join-event.blade.php
  - resources/views/pages/join-code.blade.php
  - resources/views/pages/study-preview.blade.php
  - resources/views/pages/lesson-preview.blade.php
  - resources/views/pages/study-code.blade.php
  - resources/views/pages/groups.blade.php
  - resources/views/pages/admin.blade.php
  - resources/views/pages/lesson.blade.php
autonomous: true
requirements: [VF-HTML, VF-PAGE]

must_haves:
  truths:
    - "Join flow pages render with correct root BEM classes matching React equivalents"
    - "Preview pages render with correct root classes and component composition"
    - "Groups, admin, and utility pages match React HTML structure"
    - "No Blade page has extra section, h2, or wrapper elements that React does not render"
  artifacts:
    - path: "resources/views/pages/join-group.blade.php"
      provides: "Group join page matching React structure"
    - path: "resources/views/pages/join-study.blade.php"
      provides: "Study join page matching React structure"
    - path: "resources/views/pages/join-event.blade.php"
      provides: "Event join page matching React structure"
    - path: "resources/views/pages/study-preview.blade.php"
      provides: "Study preview matching React structure"
    - path: "resources/views/pages/groups.blade.php"
      provides: "Groups list matching React structure"
  key_links:
    - from: "resources/views/pages/join-group.blade.php"
      to: "resources/css/pages/public-join.scss"
      via: "BEM root class matching SCSS selector"
      pattern: "JoinPage|PublicJoin"
    - from: "resources/views/pages/study-preview.blade.php"
      to: "resources/css/pages/study-preview.scss"
      via: "BEM root class"
      pattern: "StudyPreview"
---

<objective>
Rewrite the remaining 10 page templates (join flows, previews, utility pages) to match their React equivalents exactly.

Purpose: Complete visual fidelity across all pages, not just the core 6. Join flows are critical for new member onboarding.

Output: 10 rewritten Blade page templates with HTML structure matching React TSX
</objective>

<execution_context>
@/Users/lukekeith/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lukekeith/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-visual-fidelity-rewrite-all-blade-components-and-page-templates-to-produce-html-css-identical-to-the-original-react-app/03.1-RESEARCH.md

Key reference commands:
- `git show origin/archive/react-spa:src/pages/<name>/<name>.page.tsx` to read each React page TSX
- Some archive pages may be at different paths (join flow may be src/pages/join/join.page.tsx or src/pages/public-join/)

Open questions from research:
- Join flow pages were Blade-native designs (not straight ports) so the delta may be significant
- Admin page may have been styled with inline styles in React (no SCSS file)
- Groups page uses inline styles in React (not CSS classes)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite join flow and join-code pages (4 files)</name>
  <files>
    resources/views/pages/join-group.blade.php
    resources/views/pages/join-study.blade.php
    resources/views/pages/join-event.blade.php
    resources/views/pages/join-code.blade.php
  </files>
  <action>
    For each join flow page, read the React equivalent and rewrite the Blade to match:

    **Step 1 — Discover React join page structure:**
    ```bash
    git ls-tree -r origin/archive/react-spa src/pages/join/ src/pages/public-join/ src/pages/public-event-join/ src/pages/join-code/ src/pages/study-join/
    ```
    Read each React page TSX to understand the join flow structure. The React app may use a single `JoinPage` wrapper component or separate page components per flow.

    **Step 2 — For each Blade join page:**

    **join-group.blade.php:**
    - Read the React equivalent (likely `src/pages/public-join/public-join.page.tsx` or `src/pages/join/join.page.tsx`)
    - Compare the root class, inner BEM structure, and component composition
    - Rewrite to match React: use the same root class name, same inner wrappers, same component rendering order
    - The join-group page is multi-step (code, profile, phone, verify, confirm) — each step's HTML must match what React renders for that step
    - Keep existing @if/$step conditional rendering and Vue island references — only change HTML structure and class names

    **join-study.blade.php:**
    - Read `git show origin/archive/react-spa:src/pages/study-join/study-join.page.tsx` (or equivalent path)
    - Compare and rewrite to match

    **join-event.blade.php:**
    - Read `git show origin/archive/react-spa:src/pages/public-event-join/public-event-join.page.tsx` (or equivalent)
    - Compare and rewrite to match

    **join-code.blade.php:**
    - Read `git show origin/archive/react-spa:src/pages/join-code/join-code.page.tsx`
    - Compare and rewrite to match — this is typically a simple input + button page

    **Key rules for all join pages:**
    - Do NOT add section, h2, or wrapper elements that React does not render
    - Preserve existing @if conditionals for multi-step flows
    - Preserve Vue island mount points (data-vue attributes)
    - Only change class names and HTML nesting to match React
    - If the React page uses inline styles for layout (no BEM class), use the same inline styles in Blade
  </action>
  <verify>
    <automated>cd /Users/lukekeith/www/makeready/client && php artisan test --filter JoinFlowTest 2>&1 | tail -10 && php artisan test --filter EventJoinTest 2>&1 | tail -5 && php artisan test --filter StudyJoinTest 2>&1 | tail -5</automated>
  </verify>
  <done>All 4 join pages have root BEM classes and inner structure matching their React equivalents, all join flow tests still pass</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite remaining pages (previews, groups, admin, lesson, study-code)</name>
  <files>
    resources/views/pages/study-preview.blade.php
    resources/views/pages/lesson-preview.blade.php
    resources/views/pages/study-code.blade.php
    resources/views/pages/groups.blade.php
    resources/views/pages/admin.blade.php
    resources/views/pages/lesson.blade.php
  </files>
  <action>
    For each page, read the React TSX and rewrite the Blade to match:

    **study-preview.blade.php:**
    - Read `git show origin/archive/react-spa:src/pages/study-preview/study-preview.page.tsx`
    - Verify root class, match inner structure exactly

    **lesson-preview.blade.php:**
    - Read `git show origin/archive/react-spa:src/pages/lesson-preview/lesson-preview.page.tsx`
    - Verify root class, match inner structure exactly

    **study-code.blade.php:**
    - Read `git show origin/archive/react-spa:src/pages/study-code/study-code.page.tsx` (if exists; may be at a different path)
    - If no React equivalent exists in archive, this page was Blade-native — verify it follows BEM conventions consistent with other pages

    **groups.blade.php:**
    - Read `git show origin/archive/react-spa:src/pages/groups/groups.page.tsx`
    - NOTE from research: React groups page uses inline styles (not CSS classes) for layout
    - If React uses inline styles, replicate those inline styles in the Blade template
    - Preserve the @foreach loop for group cards

    **admin.blade.php:**
    - Read `git show origin/archive/react-spa:src/pages/admin/admin.page.tsx`
    - NOTE from research: Admin may have inline styles (no SCSS file in archive)
    - Match whatever the React page does — inline styles or BEM classes

    **lesson.blade.php:**
    - Read `git show origin/archive/react-spa:src/pages/lesson/lesson.page.tsx`
    - This page hosts the LessonIsland Vue SPA — the Blade shell structure must match React's wrapper
    - Preserve data-vue mount point and JSON props

    **Key rules:**
    - Read the React TSX FIRST, then compare to current Blade, then rewrite
    - Do NOT add elements React does not render
    - If React uses inline styles for a page, use inline styles in Blade too
    - Preserve all existing PHP/Blade logic — only change HTML structure and class names
  </action>
  <verify>
    <automated>cd /Users/lukekeith/www/makeready/client && php artisan test 2>&1 | tail -10</automated>
  </verify>
  <done>All 6 remaining pages match their React equivalents in HTML structure and class names, full test suite passes</done>
</task>

</tasks>

<verification>
1. `php artisan test` full suite passes — no regressions across all test files
2. `npm run build` passes — SCSS still compiles
3. No Blade page contains extra section/h2 elements that React does not render
</verification>

<success_criteria>
- All 10 remaining pages have HTML structure matching their React TSX equivalents
- Join flow pages preserve multi-step functionality while matching React class names
- Preview pages match React structure
- Groups/admin pages use same styling approach as React (inline styles or BEM as appropriate)
- Full test suite passes with zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-visual-fidelity-rewrite-all-blade-components-and-page-templates-to-produce-html-css-identical-to-the-original-react-app/03.1-03-SUMMARY.md`
</output>
