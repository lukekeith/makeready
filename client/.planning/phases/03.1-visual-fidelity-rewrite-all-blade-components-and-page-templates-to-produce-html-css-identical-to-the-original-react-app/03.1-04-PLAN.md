---
phase: 03.1-visual-fidelity
plan: 04
type: execute
wave: 3
depends_on: [03.1-02, 03.1-03]
files_modified: []
autonomous: false
requirements: [VF-ALL]

must_haves:
  truths:
    - "Every page visually matches the React app when viewed side-by-side"
    - "npm run build passes with zero errors"
    - "php artisan test passes with zero failures"
    - "All responsive breakpoints render correctly"
  artifacts: []
  key_links: []
---

<objective>
Final verification that all pages are visually identical to the React app. Run automated tests, then manual visual comparison.

Purpose: Automated tests verify class names but cannot verify CSS rendering. A human must confirm pixel-level fidelity.

Output: Verification that the visual fidelity gap is closed
</objective>

<execution_context>
@/Users/lukekeith/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lukekeith/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full automated verification suite</name>
  <files></files>
  <action>
    Run all automated checks to confirm everything compiles and passes:

    1. `npm run build` — Vite/Sass compilation passes with zero errors
    2. `php artisan test` — Full PHPUnit suite passes with zero failures
    3. `php artisan test --filter VisualFidelityTest` — All root BEM class assertions pass

    If any test fails, diagnose and fix the issue before proceeding to visual verification.

    Also run a quick structural verification:
    - `grep -r "HomeAuthenticated" resources/views/pages/` — should return nothing (old class fully replaced)
    - `grep -r "StudyHomePage" resources/views/pages/` — should return nothing (old class fully replaced)
    - `ls resources/css/pages/ | wc -l` — should return 14
    - `grep -c "@use 'pages/" resources/css/app.scss` — should return 14
  </action>
  <verify>
    <automated>cd /Users/lukekeith/www/makeready/client && npm run build 2>&1 | tail -3 && php artisan test 2>&1 | tail -5</automated>
  </verify>
  <done>npm run build passes, php artisan test passes with zero failures, no stale class names remain</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual fidelity verification</name>
  <files></files>
  <action>
    All 16 Blade page templates have been rewritten to match React HTML/CSS structure. 14 page SCSS files have been migrated. Button component inner structure has been fixed. Every page should now look visually identical to the original React app.

    Present the following verification steps to the user:

    1. Start the dev server: `php artisan serve` (in one terminal) and `npm run dev` (in another)
    2. Open the Laravel app at http://localhost:8000

    Check these pages side-by-side against the React app (or from memory of the original):

    Public pages (no login needed):
    - http://localhost:8000/ — Public home page: hero section, buttons, layout
    - http://localhost:8000/privacy — Privacy page styling
    - http://localhost:8000/terms — Terms page styling

    Authenticated pages (login first):
    - http://localhost:8000/home — Member home: group cards, layout spacing
    - http://localhost:8000/group/{code} — Group home: study card, events, feed
    - http://localhost:8000/study/{code} — Study home: lessons list, header card
    - http://localhost:8000/profile — Profile page: avatar, form fields, buttons

    Key things to verify:
    - Buttons look correct (label text visible, Jump buttons show description)
    - Page backgrounds and spacing match the dark theme
    - Cards have correct shadows, borders, and padding
    - Navigation bar looks correct
    - Responsive: resize browser to mobile width (375px) and verify layout

    If any page looks wrong, describe the issue.
  </action>
  <verify>User confirms visual fidelity by typing "approved" or describes issues to fix</verify>
  <done>Human confirms pages look visually identical to the React app, or specific issues are identified for a follow-up fix</done>
</task>

</tasks>

<verification>
1. `npm run build` passes
2. `php artisan test` passes with zero failures
3. Human confirms visual fidelity across all pages
</verification>

<success_criteria>
- All automated tests pass
- Human confirms pages look visually identical to the React app
- No visual regressions reported
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-visual-fidelity-rewrite-all-blade-components-and-page-templates-to-produce-html-css-identical-to-the-original-react-app/03.1-04-SUMMARY.md`
</output>
